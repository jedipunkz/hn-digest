---
source: "https://github.com/pssah4/digital-innovation-agents"
hn_url: "https://news.ycombinator.com/item?id=48356590"
title: "Introducing Pulse: A Post-Scrum Manifesto and Agent Workflow for AI-Native Teams"
article_title: "GitHub - pssah4/digital-innovation-agents: Digital Innovation Agents is an AI-augmented workflow that connects business analysis and software engineering in one consistent skill set. · GitHub"
author: "pssah4"
captured_at: "2026-06-02T00:40:24Z"
capture_tool: "hn-digest"
hn_id: 48356590
score: 2
comments: 0
posted_at: "2026-06-01T13:28:00Z"
tags:
  - hacker-news
  - translated
---

# Introducing Pulse: A Post-Scrum Manifesto and Agent Workflow for AI-Native Teams

- HN: [48356590](https://news.ycombinator.com/item?id=48356590)
- Source: [github.com](https://github.com/pssah4/digital-innovation-agents)
- Score: 2
- Comments: 0
- Posted: 2026-06-01T13:28:00Z

## Translation

タイトル: Pulse の紹介: AI ネイティブ チームのためのスクラム後のマニフェストとエージェント ワークフロー
記事のタイトル: GitHub - pssah4/digital-innovation-agents: Digital Innovation Agents は、ビジネス分析とソフトウェア エンジニアリングを 1 つの一貫したスキル セットで結び付ける、AI で強化されたワークフローです。 · GitHub
説明: Digital Innovation Agents は、ビジネス分析とソフトウェア エンジニアリングを 1 つの一貫したスキル セットで結び付ける、AI で強化されたワークフローです。 - pssah4/デジタルイノベーションエージェント

記事本文:
GitHub - pssah4/digital-innovation-agents: Digital Innovation Agents は、ビジネス分析とソフトウェア エンジニアリングを 1 つの一貫したスキル セットで結び付ける、AI で強化されたワークフローです。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Spark インテリジェントなアプリを構築してデプロイする
GitHub モデルのプロンプトの管理と比較
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
pssah4
/
デジ

タルイノベーションエージェント
パブリックテンプレート
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
pssah4/デジタルイノベーションエージェント
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
194 コミット 194 コミット .claude-plugin .claude-plugin .codex .codex .cursor-plugin .cursor-plugin .dia .dia .github/ workflows .github/ workflows .opencode .opencode docs docs フック フック スキル スキル ツール ツール .gitignore .gitignore AGENTS.md AGENTS.md CHANGELOG.md CHANGELOG.md GEMINI.md GEMINI.md ライセンス ライセンス README.md README.md gemini-extension.json gemini-extension.json package-lock.json package-lock.json package.json package.json すべてのファイルを表示 リポジトリ ファイルのナビゲーション
コードにほとんどコストがかからない場合、計画は製品になります。
AI コーディング アシスタントをビジネスに導く V-Model ワークフロー
分析、要件エンジニアリング、アーキテクチャ、コーディング、テスト、
セキュリティ監査。各フェーズ間で品質ゲートによるハンドオフが行われます。
完全なドキュメント: pssah4.github.io/digital-innovation-agents
配送コードは解決された問題です。ほとんどのチームにまだ欠けているのは証拠です
彼らが提供する機能が実際のユーザーにとって重要であるということ。デジタルイノベーション
エージェントは、歴戦のイノベーション手法と最先端の手法を組み合わせます。
コーディング ワークフローを強化することで、AI が間違ったものを迅速に構築することがなくなります。作品
クロード コード 、 カーソル 、 コーデックス 、 OpenCode 、 Gemini CLI 、
および GitHub コパイロット 。
このプロジェクトには、AI コーディング内で実行される 13 のスキルが同梱されています。
アシスタント。 6 つは V-Model フェーズのスキル (ビジネス分析、
要件エンジニアリング、アーキテクチャ、コーディング、テスト、セキュリティ
監査）。 2 つは、非グリーンフィールド プロジェクトのエントリーポイント スキルです
(リバースエンジニアリング、ダイアマイグレーション)。 1 つはオンデマンド ワークフローです
ガイド ( /dia-guide )。 4つは

基礎スキル (プロジェクトの慣例、
一貫性チェック、ヒューマナイザー、dia-bootstrap)。毎
フェーズ スキルは V モデルの一部を所有し、独自の品質のゲートを持ち、
そして、構造化された成果物を次のフェーズに渡します。ガイドは
ユーザーが方向の読み取りを希望するたびに個別に呼び出されます。
あらゆる意思決定は、実際のユーザーの問題から追跡可能です。
要件、アーキテクチャ、コード、テスト、セキュリティ監査。
3 つのエントリ ポイントは、グリーンフィールド、ブラウンフィールド、移行プロジェクトをカバーします。
グリーンフィールド: ビジネス分析は構造化された発見から始まります
（ユーザー、ニーズ、洞察、重要な仮説）そして前進する
Vモデルを通じて。
ブラウンフィールド: /reverse-engineering は V を後方に歩きます
既存のコードベースを作成し、プラン コンテキスト、ADR、arc42 スナップショット、
FEATURE インベントリ、バックログ シード、および証拠に基づく BA ドラフト。
すべてのクレームはファイル パスまたはドキュメント セクションをソースとしています。何も発明されていません。
移行: /dia-migration は古い DIA プロジェクト (v1) または
現在の規約に従った既存の V-Model バリアント: クリーンアップ
ステータスのドリフト、ID スキーマの正規化、分析の平坦化、再生成
バックログを唯一の真実の情報源として。
自動化だけではないイノベーション手法
BA および RE エージェントは、32 のイノベーション手法 (定性的な手法) のカタログを出荷します。
インタビュー、エクストリーム ユーザー、壁に飛ぶ、文化的調査、ペルソナ
統合、ステークホルダーマップ、やるべき仕事、ブレインライティング、TRIZ、ウィザード
オンスのオンス、生前分析、価値提案の定量化など) を整理
ドキュメントのメソッドカードとして。
BA または RE セッション中に、回答が乏しい場合、エージェントは停止します
インタビューを行い、マッチングフィールド法を1ページで提案します。
カード: 何を生み出すか、いつそれを手に入れるか、どのように実行するか、チームと
時間、うまくいかなかったこと、セッションに何を持ち帰るべきか。の
実際の

研究活動は人間対人間であり、実際のユーザーへのインタビュー、
実際の状況で観察し、実際に手に取ってプロトタイプを作成します。エージェントの
仕事はギャップを見つけて適切な方法を選択することであり、既存のものを置き換えることではありません。
仕事。
プラットフォームを選択してください。各インストーラーは同じ 13 のスキルをドロップします。
同じテンプレートと高品質のゲートを選択したツールに入力できます。
/plugin は、VS Code 内ではなく、Claude Code CLI 内に存在します。
JetBrains の拡張機能。クロード --version が返った場合
コマンドが見つかりません。最初に CLI をインストールします。
カール -fsSL https://claude.ai/install.sh | bash # 公式インストーラー
# または: brew install --cask claude-code # macOS Homebrew
# または: npm install -g @anthropic-ai/claude-code # ノードを備えた任意の OS
シェル (source ~/.zshrc または ~/.bashrc ) を再度開き、
プラグイン:
クロード
/plugin マーケットプレイスに https://github.com/pssah4/digital-innovation-agents.git を追加します
/plugin install digital-innovation-agents@pssah4-skills
新しいセッションに「/」と入力すると、オートコンプリートでスキルが表示されます。の
dia-bootstrap スキルはセッション時に自動的にロードされます
簡単なオリエンテーションから始めます。
VS Code、JetBrains、および Cursor 拡張機能はプラグインをインストールできません。
/plugin Marketplace add ... を VS Code 内で実行する Claude Code
拡張子は /plugin はこの環境では利用できませんを返します。
上記のように CLI を介して一度インストールします。スキルは以下に到着します
~/.claude/skills/ と IDE 拡張機能は同じディレクトリからそれらを取得します。
次回のセッション開始時にグローバル ディレクトリに移動します。 WSL のない Windows では、
CLI は実験的なものです。 WSL 経由でインストールするか、手動でコピーします。
手動インストールでは、完全なプラグイン バンドル (スキル、
ツール、フック、スクリプト）を安定したパスの下に置き、スキルをシンボリックリンクします
~/.claude/skills/ に。スキルは次の場所でツールを呼び出します。
${DIA_PLUGIN_ROOT}/tools/... 、つまり DIA_PLUGIN_ROOT がエクスポートされます
インストール場所から。ブロックを再実行します

更新する。それは引っ張る
最新のコミットを実行し、バンドルを適切な場所に再書き込みします。スキル
新しい DIA バージョンでは名前が変更または削除されました (たとえば、
v2 の dia-orchestrator) は明示的に削除されるため、古くなることはありません
スキル フォルダはアップグレード後も残ります。
# 安定したプラグインの場所。必要に応じて、DIA_PLUGIN_ROOT env でオーバーライドします。
DIA_PLUGIN_ROOT= " ${DIA_PLUGIN_ROOT :- $HOME / .local / share / dia-plugin} "
# プラグインバンドルのクローンを作成または更新します
if [ -d " $DIA_PLUGIN_ROOT /.git " ] ;それから
git -C " $DIA_PLUGIN_ROOT " fetch --tags --prune
git -C " $DIA_PLUGIN_ROOT " リセット --ハード オリジン/メイン
それ以外の場合
mkdir -p " $( dirname " $DIA_PLUGIN_ROOT " ) "
rm -rf " $DIA_PLUGIN_ROOT "
git clone https://github.com/pssah4/digital-innovation-agents.git " $DIA_PLUGIN_ROOT "
フィ
mkdir -p ~ /.claude/skills
# 名前が変更または削除された従来の DIA スキルを削除します
dia-orchestrator のレガシー用。する
rm -rf " $HOME /.claude/skills/ $legacy "
完了しました
# 現在の DIA スキルセットをシンボリックリンクします (将来の名前変更を自動的にカバーします)
プロジェクトのコンベンション リバース エンジニアリング ビジネス分析のスキルを養う \
要件エンジニアリング アーキテクチャのコーディング テスト \
セキュリティ監査一貫性チェックヒューマナイザー DIA ガイド \
dia-migration dia-setup dia-bootstrap ;する
rm -rf " $HOME /.claude/skills/ $skill "
ln -sfn " $DIA_PLUGIN_ROOT /skills/ $skill " " $HOME /.claude/skills/ $skill "
完了しました
# 実行時にスキルが tools/ を解決できるように、DIA_PLUGIN_ROOT を永続化します。
シェル_rc= " $HOME /.zshrc "
[ -f " $HOME /.bashrc " ] &&shell_rc= " $HOME /.bashrc "
もし！ grep -q " DIA_PLUGIN_ROOT= " " $shell_rc " 2> /dev/null ;それから
echo "export DIA_PLUGIN_ROOT= \" $DIA_PLUGIN_ROOT \" " >> " $shell_rc "
フィ
DIA_PLUGIN_ROOT をエクスポート
最初のインストール後、DIA_PLUGIN_ROOT が
設定してから claude を開始します。スキルはヘルパー スクリプトを次の場所で解決します。
$DIA_PLUGIN_ROOT/tools/... レガ

ユーザープロジェクト cwd の rdless。
/add-plugin デジタル イノベーション エージェント
または、Cursor プラグインで「digital-innovation-agents」を検索します
市場。
GitHub コパイロット (CLI および VS コード)
GitHub Copilot にはマーケットプレイス コマンドがありません。をコピーしてインストールします
.github/ ディレクトリとヘルパー ツールをプロジェクトに追加します。の
エージェントは flow.py 、anchor.py、移行スクリプト、および
整合性チェックのため、tools/ と scripts/ が利用可能である必要があります
地元で。
ブロックを再実行して更新します。ソースチェックアウトは
最新のコミットと各ターゲット サブフォルダーはコピー前にワイプされるため、
古い Copilot エージェント、チャット モード、またはヘルパー スクリプトは存続しません。
アップグレードします。
# ソースチェックアウトを複製または更新する
if [ -d /tmp/dia/.git ] ;それから
git -C /tmp/dia fetch --tags --prune
git -C /tmp/dia リセット --ハード オリジン/メイン
それ以外の場合
rm -rf /tmp/dia
git clone https://github.com/pssah4/digital-innovation-agents.git /tmp/dia
フィ
mkdir -p .github
# 現在のセットをインストールする前に古い DIA コピーを消去します
エージェントのチャットモードの指示テンプレートのサブ用。する
rm -rf " .github/ $sub "
cp -r " /tmp/dia/.github/ $sub " " .github/ $sub "
完了しました
cp /tmp/dia/.github/copilot-instructions.md .github/copilot-instructions.md
# ヘルパー ツール (flow.py、anchor.py、migration、hooks) をインストールします。
# プロジェクトのルートにあるため、エージェントはそれらを呼び出すことができます
ツールスクリプトフックのサブ用。する
rm -rf " $sub "
cp -r " /tmp/dia/ $sub " " $sub "
完了しました
# スキルは、このレイアウトのプロジェクト ルートに関連して tools/ を解決します。
# DIA_PLUGIN_ROOT がプロジェクト自体を指すようにする
echo ' エクスポート DIA_PLUGIN_ROOT="$(pwd)" ' >> .envrc 2> /dev/null ||本当の
Copilot をインストールすると、プロジェクトにヘルパー ツールが組み込まれます。
Copilot エージェントはグローバルな場所ではなく、
プロジェクトを作業ディレクトリとして使用し、プラグインバンドルのパスを持たない
頼りになる。
副操縦士

チャットは、次のセッションで自動的にエージェントをピックアップします。で
副操縦士のチャット:
@business-analyst チームがより良い振り返りを実行できるようにするツールを構築したいです
@requirements-engineer これが私の BA ドキュメントです。エピックと機能を作成します
@architect 要件の引き継ぎに基づいてアーキテクチャを設計する
@developer 最初の機能を実装します
@debugger テストが失敗しました。エラー ログを分析してください
Copilot エージェントは同じ探索/アイデア/検証を実行します
クロードと同じサイクル、同じテンプレート、同じ品質のゲート
コードスキル。
https://raw.githubusercontent.com/pssah4/digital-innovation-agents/main/.codex/INSTALL.md から手順を取得して実行します。
詳細ドキュメント: .codex/INSTALL.md
https://raw.githubusercontent.com/pssah4/digital-innovation-agents/main/.opencode/INSTALL.md から手順を取得して実行します。
詳細ドキュメント: .opencode/INSTALL.md
gemini 拡張機能のインストール https://github.com/pssah4/digital-innovation-agents
更新するには:
gemini 拡張機能がデジタル イノベーション エージェントを更新する
インストールを確認する
選択したプラットフォームでセッションを開始し、次のいずれかを試してください。
/dia-setup このプロジェクトのワークフローをアクティブ化します
/dia-guide アイデアからセキュリティ監査までの完全なガイド サイクル
/ビジネス分析スター

[切り捨てられた]

## Original Extract

Digital Innovation Agents is an AI-augmented workflow that connects business analysis and software engineering in one consistent skill set. - pssah4/digital-innovation-agents

GitHub - pssah4/digital-innovation-agents: Digital Innovation Agents is an AI-augmented workflow that connects business analysis and software engineering in one consistent skill set. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Spark Build and deploy intelligent apps
GitHub Models Manage and compare prompts
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
pssah4
/
digital-innovation-agents
Public template
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
pssah4/digital-innovation-agents
main Branches Tags Go to file Code Open more actions menu Folders and files
194 Commits 194 Commits .claude-plugin .claude-plugin .codex .codex .cursor-plugin .cursor-plugin .dia .dia .github/ workflows .github/ workflows .opencode .opencode docs docs hooks hooks skills skills tools tools .gitignore .gitignore AGENTS.md AGENTS.md CHANGELOG.md CHANGELOG.md GEMINI.md GEMINI.md LICENSE LICENSE README.md README.md gemini-extension.json gemini-extension.json package-lock.json package-lock.json package.json package.json View all files Repository files navigation
When code costs almost nothing, the plan becomes the product.
A V-Model workflow that walks your AI coding assistant through Business
Analysis, Requirements Engineering, Architecture, Coding, Testing, and
a Security Audit, with quality-gated handoffs between every phase.
Full documentation: pssah4.github.io/digital-innovation-agents
Shipping code is a solved problem. What most teams still lack is evidence
that the features they ship matter to a real user. Digital Innovation
Agents pair a battle-tested innovation methodology with a state-of-the-art
coding workflow, so your AI never builds the wrong thing at speed. Works
across Claude Code , Cursor , Codex , OpenCode , Gemini CLI ,
and GitHub Copilot .
The project ships thirteen skills that run inside your AI coding
assistant. Six are V-Model phase skills (business analysis,
requirements engineering, architecture, coding, testing, security
audit). Two are entry-point skills for non-greenfield projects
(reverse engineering, dia-migration). One is the on-demand workflow
guide ( /dia-guide ). Four are foundation skills (project conventions,
consistency check, humanizer, dia-bootstrap). Every
phase skill owns one part of the V-Model, has its own quality gates,
and hands off a structured artifact to the next phase. The guide is
called separately whenever the user wants an orientation read.
Every decision stays traceable from a real user problem through
requirements, architecture, code, tests, and a security audit.
Three entry points cover greenfield, brownfield, and migration projects:
Greenfield: /business-analysis starts with structured discovery
(users, needs, insights, critical hypotheses) and walks forward
through the V-Model.
Brownfield: /reverse-engineering walks the V backwards over an
existing codebase and produces plan-context, ADRs, an arc42 snapshot,
a FEATURE inventory, a backlog seed, and an evidence-based BA draft.
Every claim is sourced to a file path or doc section. Nothing invented.
Migration: /dia-migration brings an older DIA project (v1) or a
pre-existing V-Model variant up to current conventions: cleans
status drift, normalises ID schemas, flattens analysis/, regenerates
the backlog as single source of truth.
Innovation methodology, not just automation
The BA and RE agents ship a catalog of 32 innovation methods (qualitative
interviews, extreme users, fly on the wall, cultural probes, persona
synthesis, stakeholder maps, jobs to be done, brainwriting, TRIZ, wizard
of oz, pre-mortem, value proposition quantification, and more) organised
as method cards in the docs .
During a BA or RE session, when your answers go thin, the agent stops
the interview and proposes the matching field method with a one-page
card: what it produces, when to reach for it, how to run it, team and
time, things that go wrong, and what to bring back to the session. The
actual research work stays human-to-human: interviews with real users,
observations in the real context, prototypes on real hands. The agent's
job is to spot the gap and pick the right method, not to replace the
work.
Pick your platform. Each installer drops the same thirteen skills with
the same templates and quality gates into your tool of choice.
/plugin lives in the Claude Code CLI , not in the VS Code or
JetBrains extensions. If claude --version returns
command not found , install the CLI first:
curl -fsSL https://claude.ai/install.sh | bash # official installer
# or: brew install --cask claude-code # macOS Homebrew
# or: npm install -g @anthropic-ai/claude-code # any OS with Node
Reopen your shell ( source ~/.zshrc or ~/.bashrc ), then install the
plugin:
claude
/plugin marketplace add https://github.com/pssah4/digital-innovation-agents.git
/plugin install digital-innovation-agents@pssah4-skills
Type / in any new session to see the skills in autocomplete. The
dia-bootstrap skill loads automatically at session
start as a brief orientation.
VS Code, JetBrains, and Cursor extensions cannot install plugins.
Running /plugin marketplace add ... inside the VS Code Claude Code
extension returns /plugin isn't available in this environment .
Install once through the CLI as above. The skills land under
~/.claude/skills/ and the IDE extension picks them up from the same
global directory on the next session start. On Windows without WSL,
the CLI is experimental; install through WSL or copy the manually:
The manual install copies the complete plugin bundle (skills,
tools, hooks, scripts) under a stable path and symlinks the skills
into ~/.claude/skills/ . Skills invoke tooling at
${DIA_PLUGIN_ROOT}/tools/... , so DIA_PLUGIN_ROOT is exported
from the install location. Re-run the block to update; it pulls
the latest commit and rewrites the bundle in place. Skills
renamed or removed in newer DIA versions (for example
dia-orchestrator from v2) are deleted explicitly so no stale
skill folders survive the upgrade.
# Stable plugin location. Override with DIA_PLUGIN_ROOT env if needed.
DIA_PLUGIN_ROOT= " ${DIA_PLUGIN_ROOT :- $HOME / .local / share / dia-plugin} "
# Clone or update the plugin bundle
if [ -d " $DIA_PLUGIN_ROOT /.git " ] ; then
git -C " $DIA_PLUGIN_ROOT " fetch --tags --prune
git -C " $DIA_PLUGIN_ROOT " reset --hard origin/main
else
mkdir -p " $( dirname " $DIA_PLUGIN_ROOT " ) "
rm -rf " $DIA_PLUGIN_ROOT "
git clone https://github.com/pssah4/digital-innovation-agents.git " $DIA_PLUGIN_ROOT "
fi
mkdir -p ~ /.claude/skills
# Remove legacy DIA skills that were renamed or dropped
for legacy in dia-orchestrator ; do
rm -rf " $HOME /.claude/skills/ $legacy "
done
# Symlink the current DIA skill set (covers future renames automatically)
for skill in project-conventions reverse-engineering business-analysis \
requirements-engineering architecture coding testing \
security-audit consistency-check humanizer dia-guide \
dia-migration dia-setup dia-bootstrap ; do
rm -rf " $HOME /.claude/skills/ $skill "
ln -sfn " $DIA_PLUGIN_ROOT /skills/ $skill " " $HOME /.claude/skills/ $skill "
done
# Persist DIA_PLUGIN_ROOT so skills can resolve tools/ at runtime
shell_rc= " $HOME /.zshrc "
[ -f " $HOME /.bashrc " ] && shell_rc= " $HOME /.bashrc "
if ! grep -q " DIA_PLUGIN_ROOT= " " $shell_rc " 2> /dev/null ; then
echo " export DIA_PLUGIN_ROOT= \" $DIA_PLUGIN_ROOT \" " >> " $shell_rc "
fi
export DIA_PLUGIN_ROOT
After the first install, open a new shell so DIA_PLUGIN_ROOT is
set, then start claude . Skills resolve their helper scripts at
$DIA_PLUGIN_ROOT/tools/... regardless of the user-project cwd.
/add-plugin digital-innovation-agents
Or search for "digital-innovation-agents" in the Cursor plugin
marketplace.
GitHub Copilot (CLI and VS Code)
GitHub Copilot has no marketplace command. Install by copying the
.github/ directory plus the helper tools into your project. The
agents call flow.py , anchor.py , the migration scripts, and the
consistency check, so tools/ and scripts/ must be available
locally.
Re-run the block to update; the source checkout is pulled to the
latest commit and each target subfolder is wiped before copy, so
no stale Copilot agents, chat modes, or helper scripts survive an
upgrade.
# Clone or update the source checkout
if [ -d /tmp/dia/.git ] ; then
git -C /tmp/dia fetch --tags --prune
git -C /tmp/dia reset --hard origin/main
else
rm -rf /tmp/dia
git clone https://github.com/pssah4/digital-innovation-agents.git /tmp/dia
fi
mkdir -p .github
# Wipe old DIA copies before installing the current set
for sub in agents chatmodes instructions templates ; do
rm -rf " .github/ $sub "
cp -r " /tmp/dia/.github/ $sub " " .github/ $sub "
done
cp /tmp/dia/.github/copilot-instructions.md .github/copilot-instructions.md
# Install the helper tools (flow.py, anchor.py, migration, hooks)
# at the project root so the agents can invoke them
for sub in tools scripts hooks ; do
rm -rf " $sub "
cp -r " /tmp/dia/ $sub " " $sub "
done
# Skills resolve tools/ relative to the project root in this layout,
# so DIA_PLUGIN_ROOT points at the project itself
echo ' export DIA_PLUGIN_ROOT="$(pwd)" ' >> .envrc 2> /dev/null || true
The Copilot install brings the helper tools into the project rather
than to a global location because Copilot agents run with the
project as their working directory and have no plugin-bundle path
to fall back on.
Copilot Chat picks the agents up automatically on the next session. In
Copilot Chat:
@business-analyst I want to build a tool that helps teams run better retrospectives
@requirements-engineer Here is my BA document, create epics and features
@architect Design the architecture based on the requirements handoff
@developer Implement the first feature
@debugger Tests are failing, analyze the error log
The Copilot agents run the same Exploration / Ideation / Validation
cycle, the same templates, and the same quality gates as the Claude
Code skills.
Fetch and follow instructions from https://raw.githubusercontent.com/pssah4/digital-innovation-agents/main/.codex/INSTALL.md
Detailed docs: .codex/INSTALL.md
Fetch and follow instructions from https://raw.githubusercontent.com/pssah4/digital-innovation-agents/main/.opencode/INSTALL.md
Detailed docs: .opencode/INSTALL.md
gemini extensions install https://github.com/pssah4/digital-innovation-agents
To update:
gemini extensions update digital-innovation-agents
Verify the install
Start a session in your chosen platform and try one of these:
/dia-setup Activate the workflow in this project
/dia-guide Full guided cycle from idea to security audit
/business-analysis Star

[truncated]
