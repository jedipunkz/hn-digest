---
source: "https://github.com/kmcnally5/FROG"
hn_url: "https://news.ycombinator.com/item?id=48374361"
title: "Show HN: Claude wrote me a language, now I don't know what to do with it"
article_title: "GitHub - kmcnally5/FROG: kLex is the FROG language — Functional, Reactive, Opinionated, Governed. Strictly typed, no implicit coercion, no exceptions, no truthiness traps. Go-style error handling, exhaustive enum matching, first-class functions, closures, async/await, channels, and a pipeline operat\n[truncated]"
author: "froglang"
captured_at: "2026-06-03T00:38:24Z"
capture_tool: "hn-digest"
hn_id: 48374361
score: 2
comments: 1
posted_at: "2026-06-02T18:39:27Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Claude wrote me a language, now I don't know what to do with it

- HN: [48374361](https://news.ycombinator.com/item?id=48374361)
- Source: [github.com](https://github.com/kmcnally5/FROG)
- Score: 2
- Comments: 1
- Posted: 2026-06-02T18:39:27Z

## Translation

タイトル: HN を表示: クロードが私に言語を書いてくれましたが、今はそれをどうすればいいのかわかりません
記事のタイトル: GitHub - kmcnally5/FROG: kLex は機能的、反応的、意見があり、統治された FROG 言語です。厳密に型指定されており、暗黙の強制、例外、真実性のトラップはありません。 Go スタイルのエラー処理、徹底的な列挙型マッチング、ファーストクラス関数、クロージャ、async/await、チャネル、パイプライン オペレーション
[切り捨てられた]
説明: kLex は FROG 言語であり、機能的、反応的、意見があり、統治されています。厳密に型指定されており、暗黙の強制、例外、真実性のトラップはありません。 Go スタイルのエラー処理、徹底的な列挙型マッチング、ファーストクラス関数、クロージャー、async/await、チャネル、パイプライン オペレーター。一つの正直な方法
[切り捨てられた]
HN テキスト: クロードと私は、Anthropic のクレジットがすぐに使い果たされてしまうのを防ぐために、FROG を構築し始めました。当時私が正直に必要としていた唯一の理由はそれでしたが、言語が成長し、機能が実際に連携して動作するにつれて、すぐに言語自体が継続する理由になりました。 FROG を使用すると、LLM を使用してツールを短時間で作成でき、FROG の「エージェント フック」を介して LLM がデバッグ プロセスを支援できるようになります。私が正直に言って、最も目を見張るものがあると感じたのは、LLM がどのようにデバッグ プロセスに組み込まれるかということです。それが、作成されたツールと渡されたツールの違いでした。

記事本文:
GitHub - kmcnally5/FROG: kLex は FROG 言語であり、機能的、反応的、意見があり、統治されています。厳密に型指定されており、暗黙の強制、例外、真実性のトラップはありません。 Go スタイルのエラー処理、徹底的な列挙型マッチング、ファーストクラス関数、クロージャー、async/await、チャネル、パイプライン オペレーター。すべてを行うための 1 つの正直な方法。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。

別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
kmcnally5
/
カエル
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
39 コミット 39 コミット .github/ workflows .github/ workflows ast ast cmd cmd docs docs editors editors eval eval 例 例 フォーマッタ フォーマッタ レクサー レクサー パーサー パーサー repl repl Snowball/ froglsp Snowball/ froglsp stdlib stdlib テスト テスト ツール ツール vm vm .SRCINFO .SRCINFO .gitignore .gitignore PKGBUILD PKGBUILD README.md README.md RELEASE_NOTES.md RELEASE_NOTES.md go.mod go.mod go.sum go.sum klex.lock klex.lock main.go main.go すべてのファイルを表示 リポジトリ ファイルのナビゲーション
ネイティブ ツール、ブラウザ アプリケーション、言語間パイプライン、および AI 対応の同時実行ユーティリティ用の高性能インタープリター言語。
kLex は、FROG のリファレンス実装です。FROG は、並列ファイル スキャナー、ネイティブ GUI アプリケーション、ブラウザ ツール、言語間パイプライン、AI で高速化された同時実行ユーティリティなど、特定のクラスのプログラム用に構築されたランタイムです。
汎用言語ではありません。これは、実際のツールを迅速に構築したい人のためのランタイムです。
柱
それが何を意味するか
ネイティブツール
ファイル スキャナ、資格情報ハンター、パイプライン プロセッサ
同時デスクトップユーティリティ
負荷がかかっても応答性を維持する GUI アプリ
ブラウザ/WASM
同じスクリプト、同じ UI ツールキット、書き換えなしでブラウザ内で実行
言語間の架け橋
型付きスキーマとストリーミングを使用して、FROG から Python、Node.js、または任意のサブプロセスを呼び出します
AI / ML プリミティブ
ランタイムに組み込まれた Tensor 演算、埋め込み、およびモデル推論
高スループットのスクリプト作成
コンパイルされたツールチェーンを使用しない並列ワークロード
イミディエイトモードのGUI
開く

デスクトップでは GL、ブラウザでは Canvas2D — 同じウィジェット コード
チャネル指向の同時実行性
チャネルは言語のプリミティブであり、ライブラリのアドオンではありません
Secret Hunter — ネイティブ GUI を備えた資格情報スキャナー
FROG で完全に構築された完全な並列セキュリティ スキャナ。コードベースと git 履歴をスキャンして、漏洩した API キー、パスワード、トークンを探します。リアルタイムの進行状況、重大度の分布、フィルタリングを備えたライブ OpenGL インターフェイスを提供します。
./klex 例/SecretHunter/secretHunterUI.lex
40 人のパラレルワーカー。スキャン中は 60fps でのネイティブ OpenGL GUI。約 900 行の FROG が組み込まれています。
ネイティブ GUI と並列非同期チャネルを備えたスクリプト化されたスキャナー (インタープリタ型言語)。
2 つのゼロインストール ブラウザー プレイグラウンド。どちらも WASM ビルドを利用しています。以下を参照してください。
Text Playground / REPL — テキスト出力 ( print / println ) を含む FROG コードを作成して実行します。複数行入力、永続的なセッション状態、完全な標準ライブラリ。言語機能、アルゴリズム、stdlib の探索に最適です。
グラフィック プレイグラウンド — キャンバスに描画する FROG スケッチを作成します。 window() レンダリング ループ、形状、グラデーション、イメージ、パーティクル、マウス + キーボード入力。スケッチを編集し、 Run 、 Stop を押してループを終了します。
Secret Hunter (ネイティブ OpenGL GUI)
kLex プレイグラウンド (ブラウザ・WASM)
tadPole AI画像ジェネレーター
他と異なる 3 つの点
let jobs = チャネル(200)
結果 = チャネル(200) にします
let ワーカー = async(fn() {
true ながら {
let job, ok = recv(jobs)
if ok == false { ブレーク }
send (結果、ジョブ + "処理済み")
}
})
送信 (ジョブ、「 file.txt 」)
let result、ok = recv(result)
println (結果)
エグゼキュータもイベントループもありません。各 async() は実際の goroutine を生成します。チャネルは型指定され、境界があり、ブロックされます。
2 — ネイティブ OpenGL GUI、内蔵
let font =loadFont( " /System/Library/Fonts/SFNS.ttf " , 18)
window (800, 600, " アプリ " , fn(frame) {
背景がある

d (0.07、0.07、0.10)
塗りつぶし (0.40、0 . 82、1 . 00、1 . 0)
円 (mouseX()、mouseY()、20 . 0)
textFont (フォント、「こんにちは。」、40、40、1 . 2)
})
SDF でレンダリングされた図形とテキスト。 8×MSAA。イミディエイトモードのウィジェットシステム。 go-gl 経由でネイティブ速度で実行されます。
プラットフォームに関する注意: グラフィックスと UI は、macOS、Linux、および Windows で実行されます。 macOS から Windows バイナリをクロスコンパイルするには、 mingw-w64 が必要です。ネイティブ ビルドは、ホスト プラットフォームが提供する C ツールチェーン (macOS の Xcode、Linux の gcc、Windows の MSYS2 または TDM-GCC) を使用します。
3 — 暗黙的な動作は一切行わない
1 == "1" // TypeError — 強制なし
if 1 { } // TypeError — 整数はブール値ではありません
let x = null // 明示的な null、エラーではありません
隠蔽型の強制はありません。暗黙的なスレッド化はありません。魔法はありません。 FROG プログラム内のすべての動作は宣言されています。
git clone https://github.com/kmcnally5/FROG
cd カエル
build -o klex に進みます。
./klex your_program.lex
ビルドされた klex バイナリは、その隣にある stdlib/ を自動検出するため、「stdlib/...」をインポートするスクリプトはどのディレクトリからでも機能します。バイナリと stdlib が一緒にある場合、KLEX_PATH 構成は必要ありません。
build -o klex に進みます。プログラムを実行するために必要なのはこれだけです。インタプリタとすべてのバンドル ツール ( klexfmt 、tapetool 、 kpkg 、 froglsp 、WASM ジェネレーターなど) をワンショットでビルドするには、クロスプラットフォーム ビルダーを使用します。
./tools/build # klex + すべてのツールを実行します → ./bin/
go run ./tools/build --wasm # WASM プレイグラウンド バンドルもビルドします → ./bin/klex.wasm
go run ./tools/build --list # コンパイルせずにビルドされるものを表示
これは、ソース ツリーをスキャンしてツールを検出する小さな Go プログラム (make もシェル スクリプトも不要で、Windows、Linux、macOS で同じように動作します) であるため、決して古くなりません。出力 bin/ ディレクトリはビルド出力として生成され、gitignard されます。その中には何も含まれていません。あなたは酸っぱいものからそれを再構築します

いつでも好きなときに。
// 関数はファーストクラスです
fn add (a, b) { return a + b }
println (add( 3 , 4 )) // 7
// 配列とハッシュ
let Points = [{ " x " : 1 , " y " : 2 }, { " x " : 3 , " y " : 4 }]
println (points[ 0 ][ " x " ]) // 1
// 並列処理 — 非同期タスク間で作業をファンアウトします
fn scan(chunk) { return len (chunk) }
let スライス = [ " アルファ " 、 " ベータ " 、 " ガンマ " ]
n = len(スライス)とします
let タスク = makeArray(n, null)
i = 0 とします
while i < n {
チャンク = スライス[i]にします
task[i] = async ( fn () { return scan (chunk) })
i = i + 1
}
let counts = makeArray(n, null)
j = 0 とします
while j < n {
counts[j] = 待機 (タスク[j])
j = j + 1
}
println (カウント) // [5, 4, 5]
// エラー処理 — Go スタイル、例外なし
fnriskyCall(x) { return x * 2 }
let 結果、err =safe(riskyCall, 21 )
if err != null { println (err) }
println (結果) // 42
タイプ: 整数、浮動小数点、ブール値、文字列、null、配列、ハッシュ、タプル、チャネル、関数、構造体、列挙型、タスク
同時実行: async / await / channel / send / recv / select / atomicIntArray / atomicFloatArray
froglsp は、FROG 用の完全な LSP サーバーであり、言語用にゼロから構築されています。ソースは Snowball/froglsp/ にあります。
VS Code 拡張機能 ( editors/vscode_froglsp/ ) 構成により、サーバーが自動的に接続されます。拡張機能をインストールし、.lex ファイルを開くと、追加の構成を行わなくてもすべての機能がアクティブになります。
kLex にはバイトコード コンパイラと VM が含まれています。コンパイラーは、実行時に AST をノードごとに実行するのではなく、プログラムをコンパクトな命令セットに変換し、VM がタイトなフラット ループでディスパッチします (再帰やノードごとのインタープリターのオーバーヘッドはありません)。
./klex --vm your_program.lex
VM は、計算負荷の高いワークロード (緊密な算術ループ、深い再帰、関数 millio を呼び出すプログラム) で最も顕著な利点をもたらします。

ns回。 I/O、チャネル、またはグラフィックス パイプラインにほとんどの時間を費やすプログラムでは、それらのパスがネイティブ Go で実行されるため、違いは小さくなります。
VM は現在開発中です。言語の大部分がサポートされています。少数の構成要素 ( select 、一部の高度な非同期パターン) はコンパイラーにまだ実装されていません。
kLex は WebAssembly ( GOOS=js GOARCH=wasm ) にコンパイルされます。ネイティブ GPU アクセラレーション スキャナーを駆動するのと同じ言語がブラウザー タブで直接実行され、インストールは必要ありません。
cd example/playground && ./serve.sh # ワンステップで構築と提供
能力
仕組み
ゼロインストール配布
URL を貼り付けて FROG を実行 — パッケージマネージャーも手間もかかりません
完全な UI ツールキット
Canvas2D 経由の 35 以上のウィジェット (ボタン、テーブル、チャート、テキスト入力) — デスクトップと同一のコード
組み込み標準ライブラリ
すべての stdlib モジュールはバイナリに焼き付けられます。 import "stdlib/json.lex" はメモリから解決されます
OPFS の永続性
opfs:// URL スキームはスクリプトに永続的なサンドボックス ストレージを提供します。ページのリロード後も存続します
永続的な REPL + 分離された VM
klex_eval はセッション状態を保持します。 runScript は呼び出しごとに新しい VM 環境を実行します
ワーカーブリッジ
Web ワーカー経由で FROG から任意の JS ライブラリを呼び出します - デスクトップのサブプロセスと同じブリッジ API
JS相互運用性
3 つのエクスポート: klex_eval 、 klex_reset 、 klex_ Depth
ファイルシステム I/O、サブプロセス、メタル/テンソル、およびデータベース ドライバーは、ブラウザー サンドボックスでは使用できません。それ以外はすべてそのまま実行されます。
FROG スクリプトは、Python、Node.js、または任意のサブプロセスをブリッジ プロトコル経由で直接呼び出します。スキーマ イントロスペクション、ストリーミング、バックプレッシャーを備えた、型付けされた JSON-over-stdio です。呼び出し側は次の 3 行です。
ブリッジしましょう、err = BridgeOpen("./my_bridge.py")
let result, err = BridgeCall(ブリッジ, "分析", [データ])
ブリッジ閉じる(ブリッジ)
ブリッジワーカーはタイプ検証を処理します

ション、ストリーミング応答、および自動的にキャンセルされます。ブラウザでは、同じ API が Web ワーカーにマップされます。FROG を離れることなく、kLex から任意の JS ライブラリを呼び出します。
エージェントフック — ランタイムが語り返す
FROG プログラムが観察可能です。すべての重要なランタイム イベントは、LLM を含む外部ツールがリアルタイムで監視できる構造化フック層を通じて起動されます。
すべてのイベントには、cause_by ID が付けられます。つまり、ソースに触れることなく、並行プログラムの完全な因果関係グラフを再構築できます。
./tools/tapetool レコード my_app.lex を実行します。
go run ./tools/tapetool show my_app-20260529.lextape --filter error # 通常は必要なものだけです
LLM 支援のリモート デバッグ
プログラムの実行中にテープがストリーミングされます。 LLM は、人間が UI を操作している間、ライブ FROG アプリを観察できます。ソースの変更やリビルドは必要ありません。
KLEX_PATH=。 go run ./tools/tapetool Record --output /tmp/debug.lextape my_app.lex
tail -f /tmp/debug.lextape | grep --line-buffered ' "kind":"error" '
実際の例: SecretHunter の UI はフレームごとにちらつきました。 1 分間のテープ セッションをキャッチした progressBar は、60 fps で起動する 7 つの引数を期待しており、描画中にレンダリング ループをすべてのフレームで静かに中止します。同じメカニズムにより、別のアプリで 1 秒あたり 2310 エラーの嵐が発生し、外部からは UI が応答しないように見えました。

[切り捨てられた]

## Original Extract

kLex is the FROG language — Functional, Reactive, Opinionated, Governed. Strictly typed, no implicit coercion, no exceptions, no truthiness traps. Go-style error handling, exhaustive enum matching, first-class functions, closures, async/await, channels, and a pipeline operator. One honest way to do
[truncated]

Claude and I started building FROG in an effort to stop burning through Anthropic credit so quickly. That was the only reason I honestly needed at the time, but as the language grew and features actually worked together, the language itself quickly became the reason to continue. FROG enables me to write tooling in little to no time using LLMs and have the LLM assist in the debugging process via FROG's "agentic hooks". What I've honestly found most eye-opening is how LLMs hook into the debugging process! It has been the difference between tools that got written and ones that were passed over.

GitHub - kmcnally5/FROG: kLex is the FROG language — Functional, Reactive, Opinionated, Governed. Strictly typed, no implicit coercion, no exceptions, no truthiness traps. Go-style error handling, exhaustive enum matching, first-class functions, closures, async/await, channels, and a pipeline operator. One honest way to do everything. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
kmcnally5
/
FROG
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
39 Commits 39 Commits .github/ workflows .github/ workflows ast ast cmd cmd docs docs editors editors eval eval examples examples formatter formatter lexer lexer parser parser repl repl snowball/ froglsp snowball/ froglsp stdlib stdlib tests tests tools tools vm vm .SRCINFO .SRCINFO .gitignore .gitignore PKGBUILD PKGBUILD README.md README.md RELEASE_NOTES.md RELEASE_NOTES.md go.mod go.mod go.sum go.sum klex.lock klex.lock main.go main.go View all files Repository files navigation
A high-performance interpreted language for native tooling, browser applications, cross-language pipelines, and AI-ready concurrent utilities.
kLex is the reference implementation of FROG — a runtime built for a specific class of program: parallel file scanners, native GUI applications, browser tools, cross-language pipelines, and AI-accelerated concurrent utilities.
It is not a general-purpose language. It is a runtime for people who want to build real tools fast.
Pillar
What it means
Native tooling
File scanners, credential hunters, pipeline processors
Concurrent desktop utilities
GUI apps that stay responsive under load
Browser / WASM
Same scripts, same UI toolkit, running in-browser with no rewrite
Cross-language bridges
Call Python, Node.js, or any subprocess from FROG with a typed schema and streaming
AI / ML primitives
Tensor ops, embeddings, and model inference baked into the runtime
High-throughput scripting
Parallel workloads without a compiled toolchain
Immediate-mode GUI
OpenGL on desktop, Canvas2D in the browser — same widget code
Channel-oriented concurrency
Channels are a language primitive, not a library add-on
Secret Hunter — credential scanner with native GUI
A full parallel security scanner built entirely in FROG. Scans codebases and git history for leaked API keys, passwords, and tokens. Ships a live OpenGL interface with real-time progress, severity distribution, and filtering.
./klex examples/SecretHunter/secretHunterUI.lex
40 parallel workers. Native OpenGL GUI at 60fps during scan. Built in ~900 lines of FROG.
A scripted scanner with a native GUI and parallel async channels — in an interpreted language.
Two zero-install browser playgrounds, both powered by the WASM build — see below :
Text Playground / REPL — write and run FROG code with text output ( print / println ). Multi-line input, persistent session state, full stdlib. Best for language features, algorithms, and stdlib exploration.
Graphics Playground — write FROG sketches that draw to a canvas : window() render loop, shapes, gradients, images, particles, mouse + keyboard input. Edit your sketch, hit Run , Stop to end the loop.
Secret Hunter (native OpenGL GUI)
kLex Playground (browser · WASM)
tadPole AI image generator
Three things that make it different
let jobs = channel(200)
let results = channel(200)
let worker = async(fn() {
while true {
let job, ok = recv(jobs)
if ok == false { break }
send (results, job + " processed " )
}
})
send (jobs, " file.txt " )
let result, ok = recv(results)
println (result)
No executors, no event loops. Each async() spawns a real goroutine. Channels are typed, bounded, and blocking.
2 — Native OpenGL GUI, built in
let font = loadFont( " /System/Library/Fonts/SFNS.ttf " , 18)
window (800, 600, " App " , fn(frame) {
background (0.07, 0 . 07, 0 . 10)
fill (0.40, 0 . 82, 1 . 00, 1 . 0)
circle (mouseX(), mouseY(), 20 . 0)
textFont (font, " Hello. " , 40, 40, 1 . 2)
})
SDF-rendered shapes and text. 8× MSAA. Immediate-mode widget system. Runs at native speed via go-gl.
Platform note: Graphics and UI run on macOS, Linux, and Windows. Cross-compiling Windows binaries from macOS requires mingw-w64 ; native builds use whatever C toolchain the host platform provides (Xcode on macOS, gcc on Linux, MSYS2 or TDM-GCC on Windows).
3 — No implicit behaviour, ever
1 == "1" // TypeError — no coercion
if 1 { } // TypeError — integer is not a boolean
let x = null // explicit null, not an error
No hidden type coercion. No implicit threading. No magic. Every behaviour in a FROG program is declared.
git clone https://github.com/kmcnally5/FROG
cd FROG
go build -o klex .
./klex your_program.lex
The built klex binary auto-discovers stdlib/ next to itself, so scripts that import "stdlib/..." work from any directory — no KLEX_PATH configuration needed when the binary and stdlib stay together.
go build -o klex . is all you need to run programs. To build the interpreter and every bundled tool ( klexfmt , tapetool , kpkg , froglsp , the WASM generators, …) in one shot, use the cross-platform builder:
go run ./tools/build # klex + all tools → ./bin/
go run ./tools/build --wasm # also build the WASM playground bundle → ./bin/klex.wasm
go run ./tools/build --list # show what would be built, without compiling
It's a small Go program (no make, no shell scripts — works identically on Windows, Linux, and macOS) that discovers the tools by scanning the source tree, so it never goes out of date. The output bin/ directory is generated build output and is gitignored — nothing in it ships; you rebuild it from source whenever you like.
// Functions are first-class
fn add (a, b) { return a + b }
println (add( 3 , 4 )) // 7
// Arrays and hashes
let points = [{ " x " : 1 , " y " : 2 }, { " x " : 3 , " y " : 4 }]
println (points[ 0 ][ " x " ]) // 1
// Parallel processing — fan out work across async tasks
fn scan(chunk) { return len (chunk) }
let slices = [ " alpha " , " beta " , " gamma " ]
let n = len(slices)
let tasks = makeArray(n, null)
let i = 0
while i < n {
let chunk = slices[i]
tasks[i] = async ( fn () { return scan (chunk) })
i = i + 1
}
let counts = makeArray(n, null)
let j = 0
while j < n {
counts[j] = await (tasks[j])
j = j + 1
}
println (counts) // [5, 4, 5]
// Error handling — Go-style, no exceptions
fn riskyCall(x) { return x * 2 }
let result, err = safe(riskyCall, 21 )
if err != null { println (err) }
println (result) // 42
Types: integer, float, boolean, string, null, array, hash, tuple, channel, function, struct, enum, task
Concurrency: async / await / channel / send / recv / select / atomicIntArray / atomicFloatArray
froglsp is a full LSP server for FROG, built from the ground up for the language. Source lives in snowball/froglsp/ .
The VS Code extension ( editors/vscode_froglsp/ ) configuration wires the server up automatically — install the extension, open a .lex file, and all features are active with no additional configuration.
kLex includes a bytecode compiler and VM. Rather than walking the AST node-by-node at runtime, the compiler translates your program to a compact instruction set that the VM dispatches in a tight flat loop — no recursion, no interpreter overhead per node.
./klex --vm your_program.lex
The VM delivers the most noticeable gains on compute-heavy workloads: tight arithmetic loops, deep recursion, and programs that call functions millions of times. Programs that spend most of their time in I/O, channels, or the graphics pipeline see smaller differences since those paths run in native Go regardless.
The VM is under active development. The vast majority of the language is supported; a small number of constructs ( select , some advanced async patterns) are not yet implemented in the compiler.
kLex compiles to WebAssembly ( GOOS=js GOARCH=wasm ). The same language that drives a native GPU-accelerated scanner runs directly in a browser tab, no installation required.
cd examples/playground && ./serve.sh # build + serve in one step
Capability
How it works
Zero-install distribution
Paste a URL, run FROG — no package manager, no friction
Full UI toolkit
35+ widgets (buttons, tables, charts, text input) via Canvas2D — identical code to the desktop
Embedded stdlib
Every stdlib module is baked into the binary; import "stdlib/json.lex" resolves from memory
OPFS persistence
opfs:// URL scheme gives scripts persistent sandboxed storage; survives page reloads
Persistent REPL + isolated VM
klex_eval keeps session state; runScript runs a fresh VM environment per call
Worker bridge
Call any JS library from FROG via Web Workers — same bridge API as desktop subprocesses
JS interop
Three exports: klex_eval , klex_reset , klex_depth
Filesystem I/O, subprocesses, Metal/tensor, and database drivers are unavailable in the browser sandbox. Everything else runs as-is.
FROG scripts call Python, Node.js, or any subprocess directly via the bridge protocol — typed JSON-over-stdio with schema introspection, streaming, and backpressure. The calling side is three lines:
let bridge, err = bridgeOpen("./my_bridge.py")
let result, err = bridgeCall(bridge, "analyse", [data])
bridgeClose(bridge)
The bridge worker handles type validation, streaming responses, and cancellation automatically. In the browser, the same API maps to Web Workers — call any JS library from kLex without leaving FROG.
Agentic Hooks — the Runtime Talks Back
FROG programs are observable. Every significant runtime event fires through a structured hook layer that external tools — including LLMs — can watch in real time.
Every event carries a caused_by ID — the full causal graph of a concurrent program is reconstructable without touching the source.
go run ./tools/tapetool record my_app.lex
go run ./tools/tapetool show my_app-20260529.lextape --filter error # usually all you need
LLM-assisted remote debugging
The tape streams as the program runs. An LLM can observe a live FROG app while a human drives the UI — no source changes, no rebuild:
KLEX_PATH=. go run ./tools/tapetool record --output /tmp/debug.lextape my_app.lex
tail -f /tmp/debug.lextape | grep --line-buffered ' "kind":"error" '
Real example: SecretHunter's UI was flickering on every frame. A one-minute tape session caught progressBar expects 7 arguments firing at 60fps — silently aborting the render loop mid-draw, every frame. The same mechanism caught a 2310-errors-per-second storm in another app that looked like an unresponsive UI from the outsid

[truncated]
