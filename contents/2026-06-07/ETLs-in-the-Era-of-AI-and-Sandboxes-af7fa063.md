---
source: "https://zozo123.github.io/agentic-airbyte/"
hn_url: "https://news.ycombinator.com/item?id=48431874"
title: "ETLs in the Era of AI and Sandboxes"
article_title: "Agentic Airbyte | Visual Execution Tutorial"
author: "lol-lol-lol-2"
captured_at: "2026-06-07T07:31:46Z"
capture_tool: "hn-digest"
hn_id: 48431874
score: 1
comments: 0
posted_at: "2026-06-07T04:43:32Z"
tags:
  - hacker-news
  - translated
---

# ETLs in the Era of AI and Sandboxes

- HN: [48431874](https://news.ycombinator.com/item?id=48431874)
- Source: [zozo123.github.io](https://zozo123.github.io/agentic-airbyte/)
- Score: 1
- Comments: 0
- Posted: 2026-06-07T04:43:32Z

## Translation

タイトル: AI とサンドボックスの時代の ETL
記事タイトル: Agentic Airbyte |ビジュアル実行チュートリアル
説明: Agentic Airbyte のビジュアル チュートリアル: エージェントの計画、Crabbox の実行、Airbyte の移動、および証拠によって次の期限付き修復が決定されます。

記事本文:
エージェントの計画。クラブボックスが実行されます。エアバイトが移動します。証拠が決定します。
ハーネスには、Claude Code、Codex、OpenCode、または別の AI コーディング エージェントを使用できます。
制限されたジョブ仕様を書き込みます。 Crabbox はワーカーをリースします。 Airbyte は列を移動します。証拠により、エージェントは次に何をすべきかがわかります。
代理店、執行、データ、証拠を別のレーンに保管します。
エージェントはプロファイルと実行仕様を選択できます。データ パイプ、シークレット ストア、またはアーティファクト サニタイザーになってはなりません。
目標
政策
AIハーネス
仕様を書き込みます
次の行動を選択します
クラブボックス
リース + 実行
アーティファクトを収集する
プロフィール
スコープ付き環境
労働者
リポジトリ + 環境
エアバイトラン
ソース
API・DB
証拠
ログ / JUnit
メトリクス/構成
ターゲット
倉庫
AI Harness 目標を読み取り、制限された仕様を書き込みます。
Crabbox を呼び出します ->
Crabbox ワーカーをリースし、名前付きプロファイルを注入します。
コマンドを実行します ->
Worker + Airbyte ソースを読み取り、ターゲットを書き込みます。エージェントは行を決して参照しません。
証拠を返す ->
証拠ログ、メトリクス、JUnit、編集された構成。
コントロール = インテント + コマンド
認証情報 = プロファイル -> 環境
データ = ソース -> ターゲット
証拠 = 成果物 -> 決定
1プラン
2 リース
3 注入
4 移動
5 証明する
6 修理
所有者: エージェント
エージェントはインテントをジョブ仕様に圧縮します。
目標、リポジトリの状態、スキーマ、以前の証拠、およびポリシーを読み取ります。制限されたランを放出します。データは移動しません。
依頼から修理までを追跡する 1 つの仕事。
行をクリックします。メインフローは同じ境界にジャンプします。
08:00 前に CRM アカウントをウェアハウスに同期します。
エージェントは参照、プロファイル、検証、再試行、アーティファクトを書き込みます。
Crabbox はウォーム ワーカーを見つけて、実行 ID を返します。
Airbyte は、source.crm.accounts を読み取り、ターゲット行を書き込みます。
ワーカーはカウント、スキーマ ドリフト チェック、JUnit、構成を発行します。
エージェントは、鮮度チェックに失敗した 1 つを検出し、修復ジョブを発行します。
人間やスケジュールが意図を与える。
入力はビジネス目標であり、コネクタ構成ダンプではありません

。エージェントは引き続き実行形状を選択する必要があります。
各ボックスに 1 つの質問があると、すべてが簡単になります。
システムを 4 つの契約として読みます。各ボックスはナロー入力を取得し、1 つの決定を所有し、ナロー出力を出力します。
意図と以前の証拠を次の限定されたジョブ仕様に変換します。
サンドボックスをリースし、コードをハイドレートし、スコープ指定された環境を挿入し、出力をキャプチャします。
ワーカーでコネクタを実行します。ソースとターゲットがモデル コンテキストに入ることはありません。
ログとチェックを終了、再試行、修復、またはアラートに変換します。
エージェントはパイプではなくドライバーです。
リポジトリの状態と証拠を検査し、次の制限付きジョブを書き込むことができます。本番行や未加工の秘密を決して含めないでください。
目標は、参照、プロファイル、再試行ポリシー、検証、アーティファクトになります。
Spec は、永続的な実行 ID を持つサンドボックス コマンドになります。
プロファイル名はワーカー内のみのスコープ付き変数になります。
コネクタは行を直接移動します。プロンプトがデータ プレーンになることはありません。
実行はログ、JUnit、メトリクス、カウント、編集された構成になります。
信号は、終了、再試行、修復、またはアラートになります。
シークレット、行、ログ、および再試行は 1 つのプロンプト ループにまとめられます。
各境界には 1 つの所有者、1 つの入力形状、および 1 つの証拠形状があります。
モデル コンテキストがデータ プレーンになります。
エージェントは見すぎ、所有しすぎ、何が起こったのかを証明できません。デバッグは非構造化トランスクリプトに依存します。
実行可能な形状には 3 つのコントラクトがあります。
エージェントの有用な出力は散文ではありません。それは仕様契約、実行引継ぎ、証拠契約です。
# 目標: CRM アカウントをウェアハウスに安全に同期します。
クラブボックス プールは、example-org/data-movement/main/provider/linux/etl を保証します \
--min-ready 3 \
--作成 -- \
--cache-volume airbyte-etl
mkdir -p .crabbox/generated
猫 > .crabbox/generated/accounts-sync.json
ジョブスペックの構造
参照ソース + ターゲット
プロファイルスコープの環境
証明成果物 + テスト

スペック 契約プラン
エージェントは、参照、プロファイル名、許可リスト、検証、再試行、アーティファクト グロブを書き込みます。エージェントは、生のシークレットやコピーされた行を書き込むことはありません。
Crabbox は、プール ID、コマンド、環境プロファイル、アーティファクト パスを受け取ります。実行 ID と証拠バンドルを返します。
次回の実行の変更: 1 つの制限された入力が失敗した所有者に関連付けられます。検証とアーティファクトのキャプチャは継続されます。
この仕様には、機密ペイロードではなく参照が含まれています。
便利なアーティファクトは、境界のある JSON 形状です。ソース、ターゲット、プロファイル、検証、再試行、編集、アーティファクトのパスに名前を付けます。
まずは飼い主を探しましょう。次に信号を読み取ります。
失敗は謎ではありません。それらは境界破壊です。各クラスでは、最初にどこを見るべきか、何を変更できるかを示します。
リース前 所有者：代理店。信号：仕様差。参照、プロファイル、検証、または再試行ポリシーを修正します。
コマンド前 所有者: Crabbox。シグナル: プールとリースのステータス。プール、イメージ、キャッシュ、またはハイドレーションを修正します。
同期前 所有者: プロフィール。シグナル: 編集された環境レポート。プロファイル マッピングまたは許可された環境を修正します。
同期中 所有者: Airbyte/ソース。信号: コネクタのログ。認証スコープ、スキーマ、API 制限、またはカーソルを修正します。
書き込み後 所有者: ワーカー/ターゲット。シグナル: ターゲット数。再試行する前に冪等性を確認してください。
証明後 オーナー: 次の計画。シグナル: JUnit とメトリクス。修復ジョブまたはアラートを発行します。
実行が始まる前に計画が間違っている。
ジョブが間違ったソース、ターゲット、プロファイル、検証、または再試行ルールを指しています。仕様を修正して再実行してください。
境界が難しいため、ループは単純です。
エージェントの計画。クラブボックスが実行されます。エアバイトが移動します。証拠が戻ってきます。何が変わったかという証拠がある場合にのみ繰り返します。

## Original Extract

A visual tutorial for Agentic Airbyte: agent plans, Crabbox runs, Airbyte moves, and evidence decides the next bounded repair.

Agent plans. Crabbox runs. Airbyte moves. Evidence decides.
The harness can be Claude Code, Codex, OpenCode, or another AI coding agent .
It writes a bounded job spec. Crabbox leases the worker. Airbyte moves rows. Evidence tells the agent what to do next.
Keep agency, execution, data, and evidence in separate lanes.
The agent may choose a profile and a run spec. It must not become the data pipe, secret store, or artifact sanitizer.
Goal
policy
AI harness
writes spec
chooses next action
Crabbox
lease + run
collect artifacts
Profile
scoped env
Worker
repo + env
Airbyte runs
Source
API / DB
Evidence
logs / JUnit
metrics / config
Target
warehouse
AI Harness Reads goal and writes a bounded spec.
calls Crabbox ->
Crabbox Leases a worker and injects the named profile.
runs command ->
Worker + Airbyte Reads source and writes target. Agent never sees rows.
returns evidence ->
Evidence Logs, metrics, JUnit, redacted config.
control = intent + command
credentials = profile -> env
data = source -> target
evidence = artifacts -> decision
1 Plan
2 Lease
3 Inject
4 Move
5 Prove
6 Repair
owner: agent
The agent compresses intent into a job spec.
It reads the goal, repo state, schemas, previous evidence, and policy. It emits a bounded run. It does not move data.
One job, traced from request to repair.
Click a row. The main flow jumps to the same boundary.
Sync CRM accounts into the warehouse before 08:00.
Agent writes refs, profile, validation, retry, artifacts.
Crabbox finds a warm worker and returns a run id.
Airbyte reads source.crm.accounts and writes target rows.
Worker emits counts, schema drift check, JUnit, config.
Agent sees one failing freshness check and emits a repair job.
Human or schedule gives intent.
The input is a business goal, not a connector config dump. The agent still has to choose the run shape.
Everything is easier when each box owns one question.
Read the system as 4 contracts. Each box gets a narrow input, owns one decision, and emits a narrow output.
Turns intent and previous evidence into the next bounded job spec.
Leases the sandbox, hydrates code, injects scoped env, captures outputs.
Runs the connector in the worker. Source and target never enter model context.
Converts logs and checks into finish, retry, repair, or alert.
The agent is the driver, not the pipe.
It can inspect repo state and evidence, then write the next bounded job. It should never carry production rows or raw secrets.
Goal becomes refs, profile, retry policy, validation, artifacts.
Spec becomes a sandboxed command with a durable run id.
Profile name becomes scoped variables inside the worker only.
Connector moves rows directly. The prompt never becomes the data plane.
Execution becomes logs, JUnit, metrics, counts, redacted config.
Signals become finish, retry, repair, or alert.
Secrets, rows, logs, and retries collapse into one prompt loop.
Each boundary has one owner, one input shape, and one evidence shape.
The model context becomes the data plane.
The agent sees too much, owns too much, and cannot prove what happened. Debugging depends on an unstructured transcript.
The runnable shape has 3 contracts.
A useful agent output is not prose. It is a spec contract, an execution handoff, and an evidence contract.
# Goal: sync CRM accounts into the warehouse safely.
crabbox pool ensure example-org/data-movement/main/provider/linux/etl \
--min-ready 3 \
--create -- \
--cache-volume airbyte-etl
mkdir -p .crabbox/generated
cat > .crabbox/generated/accounts-sync.json
job spec anatomy
Refs source + target
Profile scoped env
Proof artifacts + tests
Spec Contract plan
Agent writes: refs, profile name, allowlists, validation, retry, artifact globs. Agent never writes: raw secrets or copied rows.
Crabbox receives: pool id, command, env profile, artifact paths. It returns a run id and evidence bundle.
Next run changes: one bounded input tied to the failing owner. Validation and artifact capture stay on.
The spec contains references, not sensitive payloads.
The useful artifact is the bounded JSON shape. It names source, target, profile, validation, retry, redaction, and artifact paths.
First find the owner. Then read the signal.
Failures are not mysteries. They are boundary breaks. Each class tells you where to look first and what you are allowed to change.
before lease Owner: agent. Signal: spec diff. Fix refs, profile, validation, or retry policy.
before command Owner: Crabbox. Signal: pool and lease status. Fix pool, image, cache, or hydration.
before sync Owner: profile. Signal: redacted env report. Fix profile mapping or allowed env.
during sync Owner: Airbyte/source. Signal: connector log. Fix auth scope, schema, API limit, or cursor.
after write Owner: worker/target. Signal: target counts. Check idempotency before retry.
after proof Owner: next plan. Signal: JUnit and metrics. Emit a repair job or alert.
The plan is wrong before execution starts.
The job points at the wrong source, target, profile, validation, or retry rule. Fix the spec and rerun.
The loop is simple because the boundaries are hard.
Agent plans. Crabbox runs. Airbyte moves. Evidence returns. Repeat only when the evidence says what changed.
