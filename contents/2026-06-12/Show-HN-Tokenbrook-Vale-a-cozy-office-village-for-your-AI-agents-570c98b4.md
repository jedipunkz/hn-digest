---
source: "https://github.com/Jodacola/tokenbrook"
hn_url: "https://news.ycombinator.com/item?id=48505722"
title: "Show HN: Tokenbrook Vale, a cozy office village for your AI agents"
article_title: "GitHub - Jodacola/tokenbrook: A cozy office village for all your AI agents! · GitHub"
author: "jodacola"
captured_at: "2026-06-12T16:06:26Z"
capture_tool: "hn-digest"
hn_id: 48505722
score: 1
comments: 0
posted_at: "2026-06-12T15:54:14Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Tokenbrook Vale, a cozy office village for your AI agents

- HN: [48505722](https://news.ycombinator.com/item?id=48505722)
- Source: [github.com](https://github.com/Jodacola/tokenbrook)
- Score: 1
- Comments: 0
- Posted: 2026-06-12T15:54:14Z

## Translation

タイトル: Show HN: Tokenbrook Vale、AI エージェントのための居心地の良いオフィス ビレッジ
記事のタイトル: GitHub - Jodacola/tokenbrook: すべての AI エージェントのための居心地の良いオフィス ビレッジ! · GitHub
説明: すべての AI エージェントのための居心地の良いオフィス ビレッジ! GitHub でアカウントを作成して、Jodacola/tokenbrook の開発に貢献してください。
HN テキスト: https://demo.tokenbrook.com でもライブで実行されています - リポジトリの指示に従って独自の Claude インスタンスを接続してください。これは私が数週間前からまとめたいと思っていたものです。私のエージェントがクロードの中を走っている様子を視覚的に表現すると面白いだろうと思いました。私はレトロなピクセルアートスタイルのゲームのオタクなので、その道を選びました。また、私たちが住んでいるこのクレイジーで新しい AI の世界に、少しの温かさと楽しさを注入したいと思いました。楽しんでください。

記事本文:
GitHub - Jodacola/tokenbrook: すべての AI エージェントのための居心地の良いオフィス ビレッジ! · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
ホダコーラ
/
トークンブルック
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
主なブランチのタグが取得されました

o ファイル コード [その他のアクション] メニューを開く フォルダーとファイル
2 コミット 2 コミット アセット アセット デプロイ デプロイ スクリプト スクリプト サーバー サーバー Web Web .dockerignore .dockerignore .gitignore .gitignore .python-version .python-version COTRIBUTING.md CONTRIBUTING.md Dockerfile Dockerfile LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md build.py build.py char-builder-data.json char-builder-data.json compose.yaml compose.yaml logo.png logo.png要件.txt要件.txt tokenbrook-demo.gif tokenbrook-demo.gif すべてのファイルを表示 リポジトリ ファイルのナビゲーション
あなた (および他の人) のクロード コード エージェントが活動するライブ ピクセル アート ビレッジ!小さな匿名化 CLI をインストールし、フックを介してクロード コードに接続するだけで、トークンブルック ベールの居心地の良いオフィス パークで、エージェントとサブエージェントが愛らしい小さなピクセル アート キャラクターとして生き返るのを見ることができます。
フックはクロード コードをサポートする目的で構築されていますが、他のエージェント システムにも同様に簡単に統合できるシンプルな API です。楽しむ！
https://demo.tokenbrook.com にアクセスしてください。現在、デモ目的で独自のエージェント セッションを行っています。
次の取り込み URL: https://demo.tokenbrook.com/ingest を使用して、デモ サイトを指すように独自のフックを構成することもできます。
「クイックスタート - 参加者として参加して接続する」を参照してください。
また、私の小さなホームサーバーが倒れても、あまり強く襲わないでください。 😅
このリポジトリは、1 セットのスプライトを共有する 2 つのものです。
ライブ インストール — Claude Code ユーザーは小さなフックをインストールします。彼らの
完全に匿名化されたセッション/サブエージェントのアクティビティは、小規模な Go Web サービスにストリームされます。
セッションごとにトップダウンのピクセル アート オフィスとしてレンダリングされます (キャラクター)
(サブエージェント) 入って、座って仕事をし、そして去って、全員にまたがって暮らす
参加しています。
Tokenbrook 文字ジェネレーター — Python t

レンダリングするツール
レイヤー化されたグレースケール スプライト シートからのランダムな色のキャラクターのバリエーション
(歩くアニメーション GIF + 座る PNG + 連絡先カード) をパックし、
フロントエンドが使用する PixiJS テクスチャ アトラス。
ジェネレーター (build.py) ──> web/assets/ (atlas:characters.png + .json + manifest.json)
│消費される
クロード コードフック ─> tokenbrook-hook ─POST─> tokenbrook-server ─SSE─> ブラウザ (PixiJS)
(ローカルで匿名化) (メモリ内ワールド + SQLite ログ)
仕組み / プライバシー
プライバシーは設計上の主要な制約です。
匿名化は、フック内で完全にマシン上で行われます。
ネットワーク通話。識別子はランダムなキーを持つ不透明な HMAC-SHA256 トークンになる
マシンから離れることのないインストールごとのソルト:
office_id = HMAC(salt, session_id) 、 char_id = HMAC(salt, subagent_id) 、
look = HMAC(salt, Agent_type) 。サーバーと他の視聴者はこれまでにのみ見ることができます
不透明なトークン + タイムスタンプ - セッション ID、エージェント名、パス、プロンプトはありません。
フック ( tokenbrook-hook ) はファイアアンドフォーゲットです: 250 ミリ秒のタイムアウト、すべて
エラーは飲み込まれ、常に 0 で終了するため、速度が低下したりクロードが壊れたりすることはありません
コード。
サーバー ( tokenbrook-server ) は 1 つの静的な Go バイナリです。
イベントを取り込み、単一のライターゴルーチンを介してイベントを適用します。
インメモリワールドでは、再起動のために追加専用の SQLite ログ (WAL) に追加します。
リカバリし、SSE 経由でブラウザにファンアウトします。静電気にも対応します
フロントエンド。
フロントエンド ( web/ ) は CDN からロードされた PixiJS v8 であり、ビルド手順はありません。
クロードコードフック
ワイヤーイベント
次のようにレンダリングされます
セッション開始
オフィス_オープン
新しいオフィスが登場
セッション終了
オフィス_クローズ
オフィスは撤去される
サブエージェント開始
char_enter
キャラクターが入ってきて座る
サブエージェント停止
char_leave
キャラクターが立ち上がって歩き出す
ユーザープロンプト送信
リード_ビジー
オフィスのリーダーは仕事をしています（頭を下げて）
停止
リード

_アイドル
リードはあなたに向き、薄暗くなります
クイックスタート
参加するには、匿名化されたストリーミングを行う小さなフックをマシンにインストールします。
実行中の Tokenbrook Vale サーバーへのセッション アクティビティ。
始める前に必要なもの:
クロード・コード — ということ
視覚化された。
Go 1.25+ — フックバイナリはマシン上のソースからビルドされます
(バージョンを確認してください; https://go.dev/dl/ からインストールしてください)。
Python 3 — JSON 構成ファイル (任意の
最近のバージョン。 macOS およびほとんどの Linux ディストリビューションにプレインストールされています)。
macOS または Linux — インストール スクリプトは bash です。
サーバーの取り込み URL — あなたが参加している値を実行している人から (または
あなた自身のもの。セルフホストを参照してください)。
Go や Python の知識は必要ありません。必要なのはインストールすることだけです。
git clone https://github.com/Jodacola/tokenbrook.git
CD トークンブルック
デプロイ/install-hook.sh --url https://demo.tokenbrook.com/ingest
これにより、ローカルの Go ツールチェーンを使用して tokenbrook-hook が構築され、それがインストールされます。
~/.local/bin/tokenbrook-hook 、 ~/.config/tokenbrook/config.json (
サーバー URL とインストールごとのランダムなソルト)、6 つのフックを
~/.claude/settings.json : SessionStart 、 SessionEnd 、 SubagentStart 、
SubagentStop 、 UserPromptSubmit 、および Stop 。新しいクロード コードを開始する
セッションが完了すると、あなたのオフィスが村に表示されます。
フックは TOKENBROOK_URL 環境オーバーライドを尊重します。それ以外の場合は設定を読み取ります
ファイル。デフォルトの URL は http://localhost:8080/ingest です。
デプロイ/アンインストール-hook.sh
これにより、フックが ~/.claude/settings.json から登録解除され、
バイナリ。設定 (インストールごとのソルトを含む) は保持されるため、再インストールする必要があります
同じ匿名のアイデンティティを維持します。 --purge を追加してそれも削除します。
docker compose up -d # イメージを構築し、http://localhost:8080 で提供します
または作成なし:
docker build -t tokenbrook 。
docker run -p 8080:8080

-v トークンブルック-データ:/データ トークンブルック
裸のローカル開発者:
CDサーバー
go build -o bin/tokenbrook-server ./cmd/tokenbrook-server
CD..
STATIC=web ./server/bin/tokenbrook-server
http://localhost:8080 を開きます。交通量はまだなく、村には誰もいない — 参照
以下のクロード コードを使用しないデモを行うか、
フックは http://localhost:8080/ingest を指します。
実際のデプロイメント (VPS、systemd、Cloudflare トンネルまたは Caddy 経由の TLS) については、を参照してください。
デプロイ/README.md 。
フロントエンド コントロール: スクロール = ズーム (カーソルに向かって) · ドラッグ = パン ·
f = すべてを再フレーム化します。
C. スプライトを再生成する (キャラクター ジェネレーター)
web/assets/ はコミットされているため、Python なしでもクローン作成が機能します。これだけ必要です
スプライトをassets/に変更した場合。
Python 3.11 以降が必要です ( .python-version を参照)。唯一の依存関係は Pillow です。
Python -m venv .venv
.venv/bin/pip install -rrequirements.txt
.venv/bin/python build.py --atlas-only # web/assets/ を再生成します
アトラスは設計上決定的です。 --seed を使用しない場合、アトラスはエクスポートされます。
RNG には常に 0 がシードされるため、再実行するとまったく同じものが再現されます。
文字 (コミットされた web/assets/ は再構築時にチャーンされません)。ロールするには
最大限の楽しみを得るために新鮮なランダムキャストを行い、毎回異なるシードを渡します。
.venv/bin/python build.py --atlas-only --seed $RANDOM
(この方法で動作するのはアトラスだけです。output/ の GIF/カード出力は実際に
--seed で固定しない限り、実行ごとにランダムです)。
build.py は、レイヤー化されたグレースケール シートから文字のバリエーションをレンダリングします。
assets/ 、 char-builder-data.json によって駆動されます。生成されるヘアスタイルごとに
N 個のバリエーション。各バリエーションでは、ランダムな色のテーマ (肌/靴/
パンツ / シャツ / 髪）を歩行時の両方に同様に適用します。
(アニメーション GIF) と座っている (静止 PNG) ポーズなので、同じキャラクターが
どのポーズでも一貫性があります。レイヤーは mu で再着色されます

ゆっくりブレンドする
( out = グレースケール * カラー / 255、アルファは保持されます)。
.venv/bin/python build.py [オプション]
旗
デフォルト
説明
--世代
5
ヘアスタイルごとのバリエーション
--期間
120
フレームあたりのミリ秒数 (アニメーション GIF)
--シード
(ランダム; アトラス: 0 )
RNG シード。アトラスは常にシード (未設定の場合は 0) なので、新しいキャラクターの場合は変更します。
--スケール
1
最近隣の高級要素
--ギャップ
20
カードセル間のピクセルギャップ（スケールなし）
--データ
文字ビルダーデータ.json
構成ファイルへのパス
--assets
資産
ソース スプライトシート ディレクトリ
--アウト
出力
出力ディレクトリ
--アトラス
(オフ)
PixiJS アトラスも出力します
--アトラスのみ
(オフ)
アトラスのみを出力し、GIF/カードをスキップします
--アトラスカウント
10
アトラスに詰め込まれた文字数
--アトラスアウト
ウェブ/アセット
アトラスが書かれている場所
新しいスプライトをライブにする (Docker)
Docker イメージはビルド時に web/ (アトラスを含む) をベイクします。
実行中のコンテナは、イメージを再構築するまで古いスプライトを提供し続けます。
.venv/bin/python build.py --atlas-only --seed $RANDOM # web/assets/ を再生成
docker compose up -d --build # イメージを再構築し、再起動します
Docker のレイヤーはファイル コンテンツのキーをキャッシュするため、 --build は確実に
アトラスを変更しました。キャッシュが古いと思われる場合は、最初からビルドを強制してください
docker compose build --no-cache && docker compose up -d を使用します。イベント履歴
再構築しても安全です。トークンブルック データ ボリュームではなく、tokenbrook-data ボリュームに存在します。
画像。その後も村がブラウザで同じように見える場合、それは
ブラウザキャッシュ: ページをハードリフレッシュします。
サーバー構成（環境変数）
ヴァール
デフォルト
意味
港
8080
リッスンポート
静的
ウェブ
提供する静的アセットのディレクトリ
DB_PATH
トークンブルック.db
SQLite イベント ログ (見つからない場合に作成されます)
TTL_SECONDS
900
オフィスがこれより長くアイドル状態になった場合の利益を得る (クラッシュをカバー)
INGEST_BUFFER
1024
取り込みチャネル容量 (オーバーフロー)

低い = 落ちた)
エンドポイント
エンドポイント
方法
説明
/インジェスト
投稿
JSON イベント ( application/json 、4 KB ボディキャップ) を受け入れます。 202を返します
/イベント
ゲット
SSE ストリーム: 接続時の完全なスナップショット、その後のライブ デルタ。 25 秒ごとに ping
/
ゲット
静的フロントエンド
サーバーは CORS ヘッダーを送信しません。フロントエンドは相対を使用します
EventSource('/events') なので、フロントエンドと API を同じオリジン (または背後) で提供します。
代理人 1 名）。 /ingest は設計上認証されていません - これはオープン アートです
インストール。
localhost:8080 でサーバーが実行されている場合:
scripts/demo.sh [カウント] [遅延]
合成オフィス トラフィックを送信します — デモ オフィスをカウント (デフォルトは 1)、最初は 1 つ
遅延秒ごと (デフォルトは 5)。オフィスが開いたり、登場人物が入ってきたり、座ったり、
そして去ります。
build.py 文字ジェネレーター (Python、Pillow)
char-builder-data.json ジェネレーター構成 (レイヤー、パレット、ヘアスタイル)
アセット/レイヤード グレースケール スプライト シート (ジェネレーター入力)
出力/ジェネレーター出力 (GIF、カード) — サーバーでは使用されません
web/静的フロントエンド (CDN 経由の PixiJS v8、ビルドステップなし)
web/assets/ コミットされたテクスチャ アトラス + マニフェスト (ジェネレーター出力)
サーバー/ Go モジュール `tokenbrook`
server/cmd/tokenbrook-server Web サービス (取り込み、SSE、静的ファイル)
server/cmd/tokenbrook-hook クロード コード フック (ローカル匿名化)
デプロイ/インストール

[切り捨てられた]

## Original Extract

A cozy office village for all your AI agents! Contribute to Jodacola/tokenbrook development by creating an account on GitHub.

Also running live at https://demo.tokenbrook.com - hook up your own Claude instances per instructions in the repo! This is something I've been wanting to throw together for a few weeks; I thought it'd be a fun visualization of my agents running through Claude. I'm a nerd for retro pixel art style games so went that route. I also wanted to inject a little warmth and fun into this crazy new AI world we're living in. Enjoy!

GitHub - Jodacola/tokenbrook: A cozy office village for all your AI agents! · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Jodacola
/
tokenbrook
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
2 Commits 2 Commits assets assets deploy deploy scripts scripts server server web web .dockerignore .dockerignore .gitignore .gitignore .python-version .python-version CONTRIBUTING.md CONTRIBUTING.md Dockerfile Dockerfile LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md build.py build.py char-builder-data.json char-builder-data.json compose.yaml compose.yaml logo.png logo.png requirements.txt requirements.txt tokenbrook-demo.gif tokenbrook-demo.gif View all files Repository files navigation
A live pixel-art village of your (and others') Claude Code agents at work! By simply installing a small anonymizing CLI and connecting it to your Claude Code via hooks, you can see your agents and subagents come to life as adorable little pixel art characters in the cozy office park of Tokenbrook Vale!
The hooks are purpose-built to support Claude Code, but it's a simple API that could be integrated into other agentic systems just as easily. Have fun!
Head on over to https://demo.tokenbrook.com , where I currently have my own agent sessions pushing for demo purposes.
You can even configure your own hook to point at the demo site by using the following ingest URL: https://demo.tokenbrook.com/ingest
See Quickstart - Join as a participant to connect.
Also, don't come at me too hard if my little home server falls over. 😅
This repo is two things that share one set of sprites:
The live installation — Claude Code users install a tiny hook; their
fully-anonymized session/subagent activity streams to a small Go web service
and is rendered as a top-down, pixel-art office per session — characters
(subagents) walk in, sit to work, and leave, live across everyone
participating.
The Tokenbrook character generator — a Python tool that renders
randomly-colored character variations from layered grayscale sprite sheets
(animated walking GIFs + sitting PNGs + contact cards), and packs them into
the PixiJS texture atlas the frontend uses.
generator (build.py) ──> web/assets/ (atlas: characters.png + .json + manifest.json)
│ consumed by
Claude Code hook ─> tokenbrook-hook ─POST─> tokenbrook-server ─SSE─> browser (PixiJS)
(anonymizes locally) (in-mem world + SQLite log)
How it works / Privacy
Privacy is the core design constraint:
Anonymization happens entirely on your machine , in the hook, before any
network call. Identifiers become opaque HMAC-SHA256 tokens keyed by a random
per-install salt that never leaves the machine :
office_id = HMAC(salt, session_id) , char_id = HMAC(salt, subagent_id) ,
look = HMAC(salt, agent_type) . The server and other viewers only ever see
opaque tokens + timestamps — no session ids, agent names, paths, or prompts.
The hook ( tokenbrook-hook ) is fire-and-forget: 250 ms timeout, all
errors swallowed, always exits 0, so it can never slow down or break Claude
Code.
The server ( tokenbrook-server ) is one static Go binary: it validates
and ingests events, applies them via a single writer goroutine to an
in-memory world, appends them to an append-only SQLite log (WAL) for restart
recovery, and fans them out to browsers over SSE. It also serves the static
frontend.
The frontend ( web/ ) is PixiJS v8 loaded from a CDN — no build step.
Claude Code hook
Wire event
Rendered as
SessionStart
office_open
a new office appears
SessionEnd
office_close
the office is removed
SubagentStart
char_enter
a character walks in and sits down
SubagentStop
char_leave
the character stands and walks out
UserPromptSubmit
lead_busy
the office's lead works (heads-down)
Stop
lead_idle
the lead turns toward you, dimmed
Quickstart
You join by installing a tiny hook on your machine that streams anonymized
session activity to a running Tokenbrook Vale server.
What you need before you start:
Claude Code — the thing being
visualized.
Go 1.25+ — the hook binary is built from source on your machine
( go version to check; install from https://go.dev/dl/ ).
Python 3 — used by the install script to edit JSON config files (any
recent version; preinstalled on macOS and most Linux distros).
macOS or Linux — the install script is bash.
The server's ingest URL — from whoever runs the vale you're joining (or
your own; see Self-host ).
No Go or Python knowledge is needed — they're only required to be installed.
git clone https://github.com/Jodacola/tokenbrook.git
cd tokenbrook
deploy/install-hook.sh --url https://demo.tokenbrook.com/ingest
This builds tokenbrook-hook with your local Go toolchain and installs it to
~/.local/bin/tokenbrook-hook , writes ~/.config/tokenbrook/config.json (the
server URL plus a random per-install salt), and registers six hooks in
~/.claude/settings.json : SessionStart , SessionEnd , SubagentStart ,
SubagentStop , UserPromptSubmit , and Stop . Start a new Claude Code
session and your office appears in the village.
The hook honors a TOKENBROOK_URL env override; otherwise it reads the config
file. The default URL is http://localhost:8080/ingest .
deploy/uninstall-hook.sh
This unregisters the hooks from ~/.claude/settings.json and removes the
binary. Your config (including the per-install salt) is kept so a reinstall
keeps the same anonymous identity; add --purge to remove that too.
docker compose up -d # builds the image, serves on http://localhost:8080
Or without compose:
docker build -t tokenbrook .
docker run -p 8080:8080 -v tokenbrook-data:/data tokenbrook
Bare local dev:
cd server
go build -o bin/tokenbrook-server ./cmd/tokenbrook-server
cd ..
STATIC=web ./server/bin/tokenbrook-server
Open http://localhost:8080 . With no traffic yet the village is empty — see
Demo without Claude Code below, or install the
hook pointed at http://localhost:8080/ingest .
For a real deployment (VPS, systemd, TLS via Cloudflare Tunnel or Caddy), see
deploy/README.md .
Frontend controls: scroll = zoom (toward the cursor) · drag = pan ·
f = re-frame all.
C. Regenerate sprites (the character generator)
web/assets/ is committed, so cloning works without Python. You only need this
if you change the sprites in assets/ .
Requires Python 3.11+ (see .python-version ); the only dependency is Pillow.
python -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python build.py --atlas-only # regenerates web/assets/
The atlas is deterministic by design : with no --seed , the atlas export
always seeds the RNG with 0 , so re-running it reproduces the exact same
characters (the committed web/assets/ doesn't churn on rebuilds). To roll a
fresh random cast for maximum enjoyment, pass a different seed each time:
.venv/bin/python build.py --atlas-only --seed $RANDOM
(Only the atlas works this way — the GIF/card outputs in output/ are truly
random on every run unless you pin them with --seed .)
build.py renders character variations from the layered grayscale sheets in
assets/ , driven by char-builder-data.json . For each hair style it generates
N variations; each variation picks one random color theme (skin / shoes /
pants / shirt / hair) and applies it identically to both the walking
(animated GIF) and sitting (still PNG) poses, so the same character is
consistent across poses. Layers are recolored with a multiply blend
( out = grayscale * color / 255 , alpha preserved).
.venv/bin/python build.py [options]
Flag
Default
Description
--generations
5
Variations per hair style
--duration
120
Milliseconds per frame (animated GIFs)
--seed
(random; atlas: 0 )
RNG seed; the atlas always seeds ( 0 if unset), so vary it for new characters
--scale
1
Nearest-neighbor upscale factor
--gap
20
Px gap between card cells (not scaled)
--data
char-builder-data.json
Path to the config file
--assets
assets
Source sprite-sheet directory
--out
output
Output directory
--atlas
(off)
Also emit a PixiJS atlas
--atlas-only
(off)
Emit only the atlas, skip GIFs/cards
--atlas-count
10
Number of characters packed in the atlas
--atlas-out
web/assets
Where the atlas is written
Making new sprites live (Docker)
The Docker image bakes web/ (including the atlas) in at build time, so a
running container keeps serving the old sprites until you rebuild the image:
.venv/bin/python build.py --atlas-only --seed $RANDOM # regenerate web/assets/
docker compose up -d --build # rebuild image, restart
Docker's layer cache keys on file content, so --build reliably picks up the
changed atlas; if you ever suspect a stale cache, force a from-scratch build
with docker compose build --no-cache && docker compose up -d . Event history
is safe across rebuilds — it lives in the tokenbrook-data volume, not the
image. If the village still looks the same in your browser afterward, it's the
browser cache: hard-refresh the page.
Server configuration (environment variables)
Var
Default
Meaning
PORT
8080
listen port
STATIC
web
directory of static assets to serve
DB_PATH
tokenbrook.db
SQLite event log (created if missing)
TTL_SECONDS
900
reap offices idle longer than this (covers crashes)
INGEST_BUFFER
1024
ingest channel capacity (overflow = dropped)
Endpoints
Endpoint
Method
Description
/ingest
POST
accepts a JSON event ( application/json , 4 KB body cap); returns 202
/events
GET
SSE stream: full snapshot on connect, then live deltas; ping every 25 s
/
GET
the static frontend
The server sends no CORS headers; the frontend uses a relative
EventSource('/events') , so serve the frontend and API same-origin (or behind
one proxy). /ingest is unauthenticated by design — this is an open art
installation.
With a server running on localhost:8080 :
scripts/demo.sh [count] [delay]
Sends synthetic office traffic — count demo offices (default 1), starting one
every delay seconds (default 5). Watch offices open, characters walk in, sit,
and leave.
build.py character generator (Python, Pillow)
char-builder-data.json generator config (layers, palettes, hair styles)
assets/ layered grayscale sprite sheets (generator input)
output/ generator output (GIFs, cards) — not used by the server
web/ static frontend (PixiJS v8 via CDN, no build step)
web/assets/ committed texture atlas + manifest (generator output)
server/ Go module `tokenbrook`
server/cmd/tokenbrook-server the web service (ingest, SSE, static files)
server/cmd/tokenbrook-hook the Claude Code hook (local anonymization)
deploy/ install

[truncated]
