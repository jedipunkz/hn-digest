---
source: "https://github.com/fellwork/aihu"
hn_url: "https://news.ycombinator.com/item?id=48572686"
title: "Show HN: Aihu – durable Web Components an AI agent can drive (full WC framework)"
article_title: "GitHub - fellwork/aihu: Durable Web Components your AI agent can read and drive — not disposable UI it generates. Author SFCs; a Rust compiler emits standards-based custom elements with MCP + llms.txt baked in. · GitHub"
author: "srmcguirt"
captured_at: "2026-06-17T18:58:46Z"
capture_tool: "hn-digest"
hn_id: 48572686
score: 1
comments: 0
posted_at: "2026-06-17T16:22:34Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Aihu – durable Web Components an AI agent can drive (full WC framework)

- HN: [48572686](https://news.ycombinator.com/item?id=48572686)
- Source: [github.com](https://github.com/fellwork/aihu)
- Score: 1
- Comments: 0
- Posted: 2026-06-17T16:22:34Z

## Translation

タイトル: Show HN: Aihu – AI エージェントが駆動できる耐久性のある Web コンポーネント (完全な WC フレームワーク)
記事のタイトル: GitHub - Felwork/aihu: AI エージェントが読み取って駆動できる耐久性のある Web コンポーネント — 生成する使い捨ての UI ではありません。 SFC の著者。 Rust コンパイラは、MCP + llms.txt が組み込まれた標準ベースのカスタム要素を生成します。 · GitHub
説明: AI エージェントが読み取って駆動できる耐久性のある Web コンポーネント。AI エージェントが生成する使い捨ての UI ではありません。 SFC の著者。 Rust コンパイラは、MCP + llms.txt が組み込まれた標準ベースのカスタム要素を生成します。 - Felwork/aihu

記事本文:
GitHub - Felwork/aihu: AI エージェントが生成する使い捨ての UI ではなく、AI エージェントが読み取って駆動できる耐久性のある Web コンポーネント。 SFC の著者。 Rust コンパイラは、MCP + llms.txt が組み込まれた標準ベースのカスタム要素を生成します。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
懸命な仕事
/
アイフ
プ

ブリック
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
1,003 コミット 1,003 コミット .changeset .changeset .claude .claude .context/ m2 .context/ m2 .github/ workflows .github/ workflows .husky .husky .moon .moon アプリ アプリ ベンチ ベンチ ブランド ブランド クックブック クックブック ドキュメント ドキュメント エディタ エディタ サンプル サンプル サンプル パッケージ パッケージ スクリプト スクリプト テスト テスト.commitlintrc.json .commitlintrc.json .gitattributes .gitattributes .gitignore .gitignore .mcp.json .mcp.json .prototools .prototools .size-limit.README.md .size-limit.README.md .size-limit.json .size-limit.json CLAUDE.md CLAUDE.md CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md CONTRIBUTING.md Cargo.lock Cargo.lock Cargo.toml Cargo.toml ライセンス ライセンス README.md README.md SECURITY.md SECURITY.md TODOS.md TODOS.md biome.json biome.json bun.lock bun.lock bunfig.toml bunfig.toml conductor.json conductor.json llms-full.txt llms-full.txt llms.txt llms.txt package.json package.json Rust-toolchain.toml Rust-toolchain.toml tsconfig.base.json tsconfig.base.json tsconfig.json tsconfig.json vitest.config.ts vitest.config.ts すべてのファイルを表示 リポジトリ ファイルのナビゲーション
Aihu — 人間の目的のためのエージェントの発見と対話。
Aihu は、AI エージェントが読み取って駆動できる耐久性のある Web コンポーネントを構築します。生成する必要がある使い捨ての UI ではありません。エージェントは、llms.txt + MCP マニフェストを通じて実際のコンポーネントを検査し、ライブの画面上のインスタンスでそのアクションを呼び出します。ユーザーが見ているのはエージェントが操作するものであり、毎ターン再生成される使い捨てのインターフェースではありません。
npx create-aihu my-app --template Agent # AI が駆動できる耐久性のあるコンポーネント - 1 つのコマンドで実行
.aihu 単一ファイル コンポーネンを作成する

nts; Rust コンパイラは、標準ベースの Web コンポーネントと機械可読エージェント マニフェストを生成します。個別の API レイヤーを構築する必要はありません。
生成的な UI と耐久性のあるコンポーネント。現在のほとんどの「エージェント UI」は使い捨てです。モデルは HTML または JSON を出力し、一度レンダリングすると消えます (MCP アプリ、生成された iframe)。 Aihu はその逆です。実際の、検査可能で再利用可能なカスタム要素は、サーバーが認証とポリシーを保持し、エージェントがサーバーを介した機能ブリッジを介して駆動します。 UI を信頼し、スタイルを設定し、再利用する必要がある場合は、耐久性が優れています。
内部では、ルーティング、SSR、認証、データ読み込み、クラウド アダプターが含まれる完全なメタ フレームワークです。ランタイムは 2 kB 未満で、出力はバニラのカスタム要素 (ロックインなし、ハイドレーション ステップなし) で、ランタイムの依存関係はありません。また、リアクティブな更新はターゲット書き込みでバニラ DOM よりも 122 倍高速に実行されます (以下のベンチマーク)。
ステータス: 積極的に開発され、v1.0.x リリースで出荷されています。リアクティブ ランタイム、コンパイラー、ルーター、サーバー、エージェント サーフェス、CLI、スタイリング エンジン、および UI プリミティブはすべて現在動作します。 「プロジェクトのステータス」を参照してください。
# エージェント ショーケース — 人間と AI エージェントが駆動する耐久性のある <タスク リスト>
npx create-aihu my-app --template エージェント
cd my-app && bun install
bun run dev # コンポーネント http://localhost:5108 · エージェント ブリッジ :5208
# …または最小限のアプリ
npx create-aihu 私のアプリ
# …または標準サンプルポートフォリオを並行して実行します
git clone https://github.com/fellwork/aihu
cd aihu && bun インストール
bun run dev:例
SFC ツアーについては、examples/live-counter/ (~40 LOC) を参照するか、サンプル ポートフォリオにジャンプしてください。
コンポーネントは 1 つの .aihu ファイルに記述します。マークアップ、状態、スタイル、および (オプションで) そのエージェント インターフェイスがすべて 1 か所にまとめられています。コンパイラは、これを、Web コンポーネントが実行されるどこでも実行できるプレーンなカスタム要素に変換します。得られるもの:
小さな

リアクティブ コア — 2 KB 未満のシグナル、計算結果、エフェクト。直接 DOM 更新があり、仮想 DOM はありません ( @aihu/signals + @aihu/arbor )。
本物の Rust コンパイラ — プラットフォームごとに事前に構築されており、さらにブラウザ内プレイグラウンド用の WebAssembly ビルドも含まれています。
デフォルトでエージェント呼び出し可能 — コンポーネントの @agent インターフェイスを宣言すると、コンパイラーは、A2A および ACP プロトコルのサポートとともに、一致する AI ツール スキーマ (MCP) を出力します。
完全なアプリ フレームワーク — ファイルベースのルーティング、サーバー側レンダリング、ローダー、Cookie、サーバー アクション ( @aihu/router + @aihu/server )。
バッテリーが含まれており、認証、データ読み込み、コンテキスト、プラグイン システム、およびアクセス可能な UI プリミティブがすべて依存関係がありません。
どこにでも導入可能 - ファーストパーティの Cloudflare アダプターと Vercel アダプター。
実際のツールチェーン - スキャフォールディングとビルド用の CLI ( aihu app / page /Component / plugin / dev / build ) と VS Code 拡張機能。
出力はプレーンなカスタム要素です。コンシューマー境界でロックされるものは何もなく、グローバル ランタイムやハイドレーション ステップもありません。また、すべてのコンポーネントは構造上、AI エージェントから呼び出すことができます。
ほとんどのコンポーネント ライブラリでは、コンポーネントを構築する方法が提供されます。 Aihu はアプリを構築する方法を提供します。ルーティング、サーバー側レンダリング、データ、展開はアドオンではなく、ファーストクラスです。
Aihu は Lit に相当し、Next.js は React (小規模な Web コンポーネント ランタイム上に構築された完全なアプリ フレームワーク) に相当します。 Solid は単一の反応性パッケージです。 Lit はテンプレートと基本クラスです。 Vue には独自のスケジューラと仮想 DOM が同梱されています。 Aihu は信号だけ、ランタイムだけ、またはフレームワーク全体を使用してきれいにレイヤー化しており、すべてのコンポーネントが AI 呼び出し可能なツールでもあり、ファイル形式自体に組み込まれている唯一のものです。
プッシュベースのシグナル、計算済み、およびバッチ書き込みによるエフェクト ( @aihu/signals 、~1.8 kB gz)
DOM の直接更新、仮想化なし

al DOM ( @aihu/arbor 、~2.1 kB gz — ターゲットを絞った更新ではバニラより 122 倍高速)
予測可能なティアダウンを備えた同期マウント
コンパイルされたコンポーネントは標準のカスタム要素として登録されます ( @aihu/runtime )
Rustネイティブコンパイラ — .aihuファイルを読み取り、標準のカスタム要素クラスを生成します
Linux、macOS、Windows、および ARM64 Linux (SHA256 検証済み) 用のビルド済みバイナリ (npm install @aihu/compiler 経由)
ブラウザ内プレイグラウンド用の WebAssembly ビルド (目標: 50 行コンポーネントのコンパイルに 200 ミリ秒未満)
スコープ付きスタイル、スロット、リスト/条件付きレンダリング、タイプチェックされたテンプレート、エラー境界、ホットリロード、アイランド、および完全なハイドレーション
@agent ブロックは、コンポーネントの公開された状態とアクションを宣言します。コンパイラは、Web コンポーネントの隣に一致する MCP ツール スキーマを出力します。
A2A および ACP エージェント プロトコルが含まれています ( @aihu/agent-a2a 、 @aihu/agent-acp )
あらゆるアプリの llms.txt 、MCP サーバー カード、 robots.txt を自動生成します — 手動構成は不要です ( @aihu-plugin/agent-readiness )
ネストされたルートとレイアウトを使用したファイルベースのルーティング ( @aihu/router )
サーバー側のレンダリング、ストリーミング、ローダー、Cookie、およびハイドレーション ( @aihu/server )
リクエストスコープのコンテキストとリアクティブなデータ/ローダー層 ( @aihu/context 、 @aihu-plugin/data )
アクセシブルな UI プリミティブ — ガード、ライブ領域、フォーカス トラップ、リンク、アウトレット
CloudflareおよびVercel用のクラウドアダプター
スキャフォールディングとビルド用の aihu CLI (app / page /Component / plugin / dev / build)
VS Code 拡張機能 — 現在構文ハイライト、全言語サーバーが進行中
bun run dev:examples と並行して実行できるサンプル ポートフォリオ
Bun、Rolldown、Biome、Vitest に基づいて構築
llms.txt — すべてのアプリはすぐに AI ツールで検出可能
MCP — モデル コンテキスト プロトコル互換 (サーバー カード、ツール スキーマ、リソース)
エージェント対応 - すべてのコンポーネント

出荷されるアプリにはエージェントインターフェイスが付いています
アクセシビリティ — WCAG 指向のプリミティブ (ライブ リージョン、フォーカス トラップ、スキップ リンク)
Aihu は現在開発中であり、v1.0.x リリースで出荷されます。リアクティブ ランタイム、コンパイラー、ルーター、サーバー、エージェント サーフェス、CLI、スタイリング エンジン (@aihu/css-engine — スコープ付きのゼロブラウザー バイト出力を備えた Tailwind v4 スタイルのユーティリティ エンジン)、およびアクセス可能な UI プリミティブ (@aihu/primitives — ダイアログ、ツールチップ、ボタンなど) はすべて、現在でも機能します。 v1.0.0 マイルストーン タグは、残り 1 つの部分に対して保持されます。
リッチテキスト/マークダウンのサポート、プラグインとして出荷。
エンジン上に構築されたコピー＆ペースト UI レジストリも進行中です。
パッケージのバージョンは独立しているため (早期アクセス時にはほとんどが 0.x 範囲にあります)、任意の部分を単独で採用できます。 Aihu は実行時に依存関係がありません。ブラウザーに付属するすべてのパッケージには空の依存関係リストがあります。これはリサーチ主導のコードベースです。各レイヤーはコードが到着する前に書かれた仕様によって固定され、パフォーマンスの回帰がマージをブロックします。
文法バージョン間の移行は機械的です。 npx aihu merge <file> を実行すると、コンパイラ エラーが正確な修正箇所を示します。移行のリファレンスについては、docs/cli.md を参照してください。
すべての結果はベンチ/ からのものです。ミタタ+バン1.3.8で計測。 p50 のレイテンシーが表示されます。 Bench/signals/RESULTS.md および Bench/arbor/RESULTS.md の完全なテーブル。
@aihu/signals と SOTA リアクティブ ライブラリの比較
ソース: bench/signals/RESULTS.md 。各競合他社の p50 レイテンシが表示されます。
@aihu/arbor と SOTA DOM バインディング ライブラリの比較
ソース: ベンチ/アーバー/RESULTS.md 。 JSDOM ワークロード、p50 レイテンシ。
自動生成 — bun scripts/sync-readme.ts を実行して更新します。
update-1-of-10k-leaves の 122 倍の勝利は、バニラの element.textContent (子リスト ウォーク) と比較して、arbor の Leaf() の textNode.nodeValue (直接プロパティ セット) へのバインディングから来ています。これは測定技術ではありません

ifact — これは、materialize.ts のバインドターゲットの選択を反映します。
すべての JSDOM ワークロードでの Solid-js および @vue/runtime-dom エラー (クライアントのみの API / SVGElement が定義されていない)。ブラウザネイティブの比較はラウンド N+2 の Playwright ランナーに延期されました。
バンの実行サイズによって強制されるパッケージごとのゲート:
自動生成 — bun scripts/sync-readme.ts を実行して更新します。
パッケージごとの行が契約です。予算化されたものではなく、組み合わせて報告されます。 v1 より前の「合計 3.46 kB 以下」ターゲットは、v1 カットオーバー (計画 7.1) で廃止されました。ハイドレーション、アイランド、エラー境界、および調整をサポートするためにパッケージが拡張されました。 .size-limit.json の各行はバインディング ゲートです。 .size-limit.README.md を参照してください。
公開ステータス: パッケージは 0.x 早期アクセスで個別に公開されます (「プロジェクト ステータス」を参照)。いくつかの内部パッケージは、設計が決まるまで非公開のままです。
ディスク上のすべてのパッケージについては、packages/ を参照してください。階層別:
ブラウザー ランタイム (サイズ、クライアントに出荷): @aihu/signals 、 @aihu/arbor 、 @aihu/runtime 、 @aihu/context 、 @aihu/agent 。
サーバー / エッジ / データ (サイズ): @aihu/router 、 @aihu-plugin/data 、 @aihu/agent-service 、 @aihu/agent-acp 、 @aihu/agent-a2a 。さらに、 @aihu/server (SSR + back-compat ルーター エイリアス)、@aihu-plugin/agent-readiness ( llms.txt 、MCP サーバー カード、ロボット、Vite プラグイン)、@aihu/app (トップレベルの統合)。
クラウドアダプター (ツリー内): @aihu/ada

[切り捨てられた]

## Original Extract

Durable Web Components your AI agent can read and drive — not disposable UI it generates. Author SFCs; a Rust compiler emits standards-based custom elements with MCP + llms.txt baked in. - fellwork/aihu

GitHub - fellwork/aihu: Durable Web Components your AI agent can read and drive — not disposable UI it generates. Author SFCs; a Rust compiler emits standards-based custom elements with MCP + llms.txt baked in. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
fellwork
/
aihu
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
1,003 Commits 1,003 Commits .changeset .changeset .claude .claude .context/ m2 .context/ m2 .github/ workflows .github/ workflows .husky .husky .moon .moon apps apps bench bench brand brand cookbook cookbook docs docs editors editors examples examples packages packages scripts scripts tests tests .commitlintrc.json .commitlintrc.json .gitattributes .gitattributes .gitignore .gitignore .mcp.json .mcp.json .prototools .prototools .size-limit.README.md .size-limit.README.md .size-limit.json .size-limit.json CLAUDE.md CLAUDE.md CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md CONTRIBUTING.md Cargo.lock Cargo.lock Cargo.toml Cargo.toml LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md TODOS.md TODOS.md biome.json biome.json bun.lock bun.lock bunfig.toml bunfig.toml conductor.json conductor.json llms-full.txt llms-full.txt llms.txt llms.txt package.json package.json rust-toolchain.toml rust-toolchain.toml tsconfig.base.json tsconfig.base.json tsconfig.json tsconfig.json vitest.config.ts vitest.config.ts View all files Repository files navigation
Aihu — agentic discovery and interaction, for human purpose.
Aihu builds durable Web Components your AI agent can read and drive — not disposable UI it has to generate. An agent inspects a real component through its llms.txt + MCP manifest and calls its actions on the live, on-screen instance. The thing the user sees is the thing the agent drives, not a throwaway interface regenerated every turn.
npx create-aihu my-app --template agent # a durable component an AI can drive — running in one command
You author .aihu single-file components; a Rust compiler emits standards-based Web Components plus the machine-readable agent manifest — no separate API layer to build.
Generative UI vs. durable components. Most "agent UI" today is disposable: the model emits HTML or JSON that renders once and vanishes (MCP Apps, generated iframes). Aihu is the inverse — real, inspectable, reusable custom elements an agent drives over a server-mediated capability bridge, with the server holding auth and policy. Durable wins when the UI has to be trusted, styled, and reused.
Under the hood it's a complete meta-framework — routing, SSR, auth, data loading, and cloud adapters included. The runtime is sub-2 kB , output is vanilla custom elements (no lock-in, no hydration step), with zero runtime dependencies , and reactive updates run 122× faster than vanilla DOM on targeted writes ( benchmarks below ).
Status: actively developed and shipping in v1.0.x releases — the reactive runtime, compiler, router, server, agent surface, CLI, styling engine, and UI primitives all work today. See Project status .
# The agent showcase — a durable <task-list> a human AND an AI agent drive
npx create-aihu my-app --template agent
cd my-app && bun install
bun run dev # component on http://localhost:5108 · agent bridge on :5208
# …or a minimal app
npx create-aihu my-app
# …or run the canonical examples portfolio in parallel
git clone https://github.com/fellwork/aihu
cd aihu && bun install
bun run dev:examples
For an SFC tour, see examples/live-counter/ (~40 LOC) or jump to the example portfolio .
You write a component in a single .aihu file — markup, state, styles, and (optionally) its agent interface, all in one place. The compiler turns it into a plain custom element that runs anywhere Web Components run. What you get:
A tiny reactive core — signals, computeds, and effects in under 2 kB, with direct DOM updates and no virtual DOM ( @aihu/signals + @aihu/arbor ).
A real Rust compiler — pre-built per platform, plus a WebAssembly build for in-browser playgrounds.
Agent-callable by default — declare a component's @agent interface and the compiler emits a matching AI tool schema (MCP), alongside A2A and ACP protocol support.
A complete app framework — file-based routing, server-side rendering, loaders, cookies, and server actions ( @aihu/router + @aihu/server ).
Batteries included — auth, data loading, context, a plugin system, and accessible UI primitives — all dependency-free.
Deploy anywhere — first-party Cloudflare and Vercel adapters.
A real toolchain — a CLI for scaffolding and builds ( aihu app / page / component / plugin / dev / build ) and a VS Code extension.
The output is plain custom elements : nothing locks you in at the consumer boundary, there's no global runtime and no hydration step — and every component is, by construction, callable by an AI agent.
Most component libraries give you a way to build components . Aihu gives you a way to build apps — routing, server-side rendering, data, and deployment are first-class, not add-ons.
Aihu is to Lit what Next.js is to React: a full app framework built on a small Web Components runtime. Solid is a single reactive package; Lit is templating plus a base class; Vue ships its own scheduler and virtual DOM. Aihu layers cleanly — use just the signals, just the runtime, or the whole framework — and it's the only one where every component is also an AI-callable tool, built into the file format itself.
Push-based signals, computeds, and effects with batched writes ( @aihu/signals , ~1.8 kB gz)
Direct DOM updates, no virtual DOM ( @aihu/arbor , ~2.1 kB gz — 122× faster than vanilla on targeted updates)
Synchronous mount with predictable teardown
Compiled components register as standard custom elements ( @aihu/runtime )
Rust-native compiler — reads .aihu files and emits standard custom-element classes
Pre-built binaries for Linux, macOS, Windows, and ARM64 Linux (SHA256-verified), via npm install @aihu/compiler
WebAssembly build for in-browser playgrounds (target: under 200 ms to compile a 50-line component)
Scoped styles, slots, list/conditional rendering, type-checked templates, error boundaries, hot reload, islands, and full hydration
An @agent block declares a component's exposed state and actions; the compiler emits a matching MCP tool schema next to the Web Component
A2A and ACP agent protocols included ( @aihu/agent-a2a , @aihu/agent-acp )
Auto-generates llms.txt , an MCP Server Card, and robots.txt for any app — no manual config ( @aihu-plugin/agent-readiness )
File-based routing with nested routes and layouts ( @aihu/router )
Server-side rendering, streaming, loaders, cookies, and hydration ( @aihu/server )
Request-scoped context plus a reactive data/loader layer ( @aihu/context , @aihu-plugin/data )
Accessible UI primitives — guards, live regions, focus traps, links, and outlets
Cloud adapters for Cloudflare and Vercel
aihu CLI for scaffolding and builds ( app / page / component / plugin / dev / build )
VS Code extension — syntax highlighting today, full language server in progress
An example portfolio you can run in parallel with bun run dev:examples
Built on Bun, Rolldown, Biome, and Vitest
llms.txt — every app is discoverable by AI tools out of the box
MCP — Model Context Protocol compatible (Server Card, tool schemas, resources)
Agent-ready — every component an app ships has an agent interface
Accessibility — WCAG-oriented primitives (live regions, focus traps, skip links)
Aihu is under active development and ships in v1.0.x releases. The reactive runtime, compiler, router, server, agent surface, CLI, styling engine ( @aihu/css-engine — a Tailwind v4-style utility engine with scoped, zero-browser-byte output), and accessible UI primitives ( @aihu/primitives — dialog, tooltip, button, and more) all work today. The v1.0.0 milestone tag is held for one remaining piece:
Rich-text / markdown support, shipping as a plugin.
A copy-paste UI registry built on the engine is also in progress.
Packages version independently (most are in the 0.x range during early access), so you can adopt any piece on its own. Aihu is dependency-free at runtime — every browser-shipped package has an empty dependencies list. It's a research-driven codebase: each layer is pinned by a written spec before code lands, and performance regressions block merges.
Migrating between grammar versions is mechanical — run npx aihu migrate <file> , and compiler errors point you at the exact fix. See docs/cli.md for the migration reference.
All results from bench/ . Measured with mitata + Bun 1.3.8. p50 latencies shown. Full tables in bench/signals/RESULTS.md and bench/arbor/RESULTS.md .
@aihu/signals vs SOTA reactive libraries
Source: bench/signals/RESULTS.md . p50 latency shown for each competitor.
@aihu/arbor vs SOTA DOM-binding libraries
Source: bench/arbor/RESULTS.md . JSDOM workloads, p50 latency.
Auto-generated — run bun scripts/sync-readme.ts to update.
The update-1-of-10k-leaves 122× win comes from arbor's leaf() binding to textNode.nodeValue (direct property set) vs. vanilla's element.textContent (child-list walk). This is not a measurement artifact — it reflects the bind-target choice in materialize.ts .
solid-js and @vue/runtime-dom ERROR in all JSDOM workloads (client-only API / SVGElement not defined). Browser-native comparison deferred to Round N+2 Playwright runner.
Per-package gates enforced by bun run size :
Auto-generated — run bun scripts/sync-readme.ts to update.
Per-package rows are the contract; combined is reported, not budgeted. The pre-v1 "≤ 3.46 kB combined" target was retired at v1 cutover (Plan 7.1) — packages grew to support hydration, islands, error boundaries, and reconciliation. Each row in .size-limit.json is the binding gate. See .size-limit.README.md .
Publish status: packages publish independently at 0.x early-access (see Project status ). A few internal packages stay private until their designs settle.
See packages/ for all packages on disk. By tier:
Browser runtime (sized, ships to client): @aihu/signals , @aihu/arbor , @aihu/runtime , @aihu/context , @aihu/agent .
Server / edge / data (sized): @aihu/router , @aihu-plugin/data , @aihu/agent-service , @aihu/agent-acp , @aihu/agent-a2a . Plus @aihu/server (SSR + back-compat router alias), @aihu-plugin/agent-readiness ( llms.txt , MCP Server Card, robots, Vite plugin), @aihu/app (top-level integration).
Cloud adapters (in-tree): @aihu/ada

[truncated]
