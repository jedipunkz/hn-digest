---
source: "https://github.com/thegoodguysla/myco-brain"
hn_url: "https://news.ycombinator.com/item?id=48589516"
title: "Show HN: Myco Brain – source-traceable memory for AI agents on your own Postgres"
article_title: "GitHub - thegoodguysla/myco-brain: Self-hosted, source-traceable memory for AI agents: on your own Postgres, keyless to run. Facts earn confidence from independent corroboration and supersede on contradiction, never silently overwritten. MCP-native (Claude, Cursor, Windsurf). Reproducible LongMemEva\n[truncated]"
author: "thegoodguys"
captured_at: "2026-06-18T18:53:42Z"
capture_tool: "hn-digest"
hn_id: 48589516
score: 1
comments: 0
posted_at: "2026-06-18T18:32:13Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Myco Brain – source-traceable memory for AI agents on your own Postgres

- HN: [48589516](https://news.ycombinator.com/item?id=48589516)
- Source: [github.com](https://github.com/thegoodguysla/myco-brain)
- Score: 1
- Comments: 0
- Posted: 2026-06-18T18:32:13Z

## Translation

タイトル: Show HN: Myco Brain – 独自の Postgres 上の AI エージェント用のソース追跡可能なメモリ
記事のタイトル: GitHub - thegoodguysla/myco-brain: AI エージェント用の自己ホスト型、ソース追跡可能なメモリ: 独自の Postgres 上で、キーなしで実行できます。事実は独立した裏付けによって信頼を獲得し、矛盾に取って代わり、黙って上書きされることはありません。 MCPネイティブ（クロード、カーソル、ウィンドサーフィン）。再現可能な LongMemEva
[切り捨てられた]
説明: AI エージェント用の自己ホスト型、ソース追跡可能なメモリ: 独自の Postgres 上で、キーなしで実行できます。事実は独立した裏付けによって信頼を獲得し、矛盾に取って代わり、黙って上書きされることはありません。 MCPネイティブ（クロード、カーソル、ウィンドサーフィン）。リポジトリ内での再現可能な LongMemEval: 73.6% QA、リコール@5 89-92
[切り捨てられた]

記事本文:
GitHub - thegoodguysla/myco-brain: AI エージェント用の自己ホスト型、ソース追跡可能なメモリ: 独自の Postgres 上で、キーなしで実行できます。事実は独立した裏付けによって信頼を獲得し、矛盾に取って代わり、黙って上書きされることはありません。 MCPネイティブ（クロード、カーソル、ウィンドサーフィン）。リポジトリ内での再現可能な LongMemEval: 73.6% QA、リコール@5 89-92%。 Apache-2.0。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてリフレッシュしてください

セッション。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
グッドガイスラ
/
マイコブレイン
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
15 コミット 15 コミット .github .github デモデモ ドキュメントドキュメント evals/longmemeval evals/longmemeval 例 例 install-shim install-shim mcp-server mcp-server supabase/ migration supabase/ migrations .env.example .env.example .gitignore .gitignore CHANGELOG.md CHANGELOG.md CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md COTRIBUTING.md Dockerfile.railway Dockerfile.railway ライセンス ライセンス通知 通知 README.md README.md ROADMAP.md ROADMAP.md SECURITY.md SECURITY.md docker-compose.yml docker-compose.yml Quickstart.sh Quickstart.sh Railway.json Railway.json smithery.yaml smithery.yaml すべてのファイルを表示 リポジトリ ファイルのナビゲーション
AI エージェント用のソース追跡可能な永続メモリ — 独自の Postgres で自己ホストされ、実行に API キーは必要ありません。
出所追跡可能。すべての事実は、その出典となった文書 ( Brain_why ) に遡ります。信頼できる要約はありません。
それを信じてください。独立した裏付けは事実の信頼性を高めます。矛盾がそれを置き換えます。保存され、監査され、決して黙って上書きされることはありません。
キーレス＆ローカルファースト。フルテキスト + セマンティック検索とナレッジ グラフはすべて、ホストされた依存関係なしで実行されます。最も正確なグラフに対してのみ Anthropic キーを追加します。
あなたのものです。 Apache-2.0、プレーン Postgres テーブル、11 個の MCP ツール。クロード、カーソル、ウィンドサーフィン、コンティニュー、ゼッドで動作します。
対象者: 1 つの共有メモリを必要とするエージェントを実行している開発チーム、クライアントごとの厳密な分離が必要なエージェンシー、アシスタントに記憶させたい人

セッション全体 — ChatGPT / Claude 履歴を (データ エクスポートから) インポートすると、初日から AI があなたを認識します。
AI コーディング エージェントを指導するキャリア エンジニアではなく、グロース マーケターが約 3 か月かけて単独で構築しました。作り方↓
エージェント健忘症の通常の修正 - LLM に自身の記憶を維持させる -
重複、幻覚の要約、自信に満ちた答えで埋め尽くされる
追跡できます。 Myco Brain は、次のような逆の契約に基づいて構築されています。
LLM が提案します。決定論的なルールは、何が事実になるかを決定します。あなたが設定した
バー — 確証ゲート型自動プロモーション (デフォルト) から厳密なものまで
すべての事実を人間がレビューします ( BRAIN_REQUIRE_HUMAN_REVIEW=1 )。
クロード、カーソル、ウィンドサーフィン、コンティニュー、ゼッド、カスタム エージェントはすべて 1 つのメモリを共有します
独自の Postgres によってサポートされています。
⭐ 信頼モデルが共鳴する場合、スターは他の人がそれを見つけるのに役立ちます。
# 1. スタックを起動します (Postgres + MCP サーバー + 抽出ワーカー)
git clone https://github.com/thegoodguysla/myco-brain.git && cd myco-brain
ドッカー構成 -d
# 2. エージェントにメモリを与えます - 任意のリポジトリまたはフォルダをポイントします
# (env は必要ありません。ローカルホスト上でクイックスタート スタックが見つかります)
npx -y -p @mycobrain/mcp-server mycobrain-ingest github:your-org/your-repo
# 3. クライアントに接続し (以下のワンライナー)、セッション全体で質問します。
# 「認証について何を決定しましたか? それはどこに文書化されていますか?」
# → 出典を引用してドキュメントから回答しました。
ヒント
API キーは最後までゼロです。全文検索、意味検索（ローカル
埋め込み)、ナレッジ グラフ (ローカル抽出) はすべてホストなしで実行されます。
依存性。最も正確なグラフが必要な場合にのみ、Anthropic キーを追加します。
設計により MCP ネイティブ — エージェントはメモリをどのように使用するかだけでなく、いつ使用するかを知っています。
ほとんどの MCP サーバーはツールを公開しており、モデルがそれらを呼び出すことを望んでいます。 Myco は使用量を出荷します
MCP を介した契約

指示チャネル: 接続した瞬間にエージェントが
タスクの前にコンテキストを取得し、永続的な決定を保存し、ソースを引用することを知っています
Brain_why を使用すると、プロジェクトごとのプロンプトは表示されません。 1 回のコピー＆ペーストでポリシーを調整する
ブロック: エージェントにブロックを上手に使用するよう教えます。
クイックリンク:
10 分間のクイックスタート ·
エージェントにそれを上手に使用するように教えてください ·
トラストエンジン ·
ベンチマーク — 自分で実行する ·
すべての証明を実行する ·
誰のためのものですか ·
環境変数 ·
建築・
ロードマップ ·
クラウドの待機リスト
記憶がより信頼できるようになる（自信が増す）
ほとんどのエージェントのメモリは事実を黙って上書きします。 Myco Brain はそれらを複合化します。
独立した情報源が事実に同意し、信頼性が高まる
(減衰されたノイズの多い-OR — 1 つの文書の 10 個のチャンクは何も裏付けません。
個別のソースはカウントされます)。
単一価値の関係（誰と仕事をするか）に関する自信に満ちた矛盾
for 、何かがある場所）は古い事実に取って代わります。
閉鎖され弱体化 — 維持され、削除されることはありません — 置き換えにより
監査済みの請求台帳に記録されます。
事実について Brain_why に質問すると、その個別のソース数が得られます (
関係、言及されていません）、時間の経過に伴う信頼度の傾向（「0.8 → 0.86」）、
および置き換えられた歴史。
矛盾は目に見えたままになります。あなたの記憶はあなたをガスライティングすることはできません。
対象 → Halcyon Labs 0.55 [スーパーシード — 削除されずに保持されます]
対象 → Driftwood Analytics 0.90 [アクティブ]
クレーム台帳: 古い事実が置き換えられ→ 新しい事実 (監査済み)
証明: npm run test:compounding — ライフサイクル全体がライブに対して実行されます。
数秒でデータベースにアクセスでき、LLM は必要ありません。
スキーマはデータとともに進化します (動的スキーマ)
抽出作業者は、カタログのエンティティの種類と関係の種類を認識します。
まだ持っていないので提案しています ( Brain_stats : "Brain は 3 つの新しい提案を行っています)
データからの型」)。
プロモーションはあなたのものです

デフォルト — またはタイプの自動プロモーションをオプトインします
十分に異なるソース文書にわたって裏付けられている
( BRAIN_SCHEMA_AUTO_PROMOTE=1 )、メンションごとではなくドキュメントごとにカウントされるため、2 つ
文書をやり取りしても合意を形成することはできません。おしゃべりな1人
この文書では何も宣伝することはできません。
プロモートされたタイプのスコープは、それを取得したワークスペース (1 つのクライアントのワークスペース) に留まります。
語彙が他人のカタログに漏れることはありません (「
クライアントごとの分離)。
証明: npm run test:dynamic-schema 、 npm run test:schema-promotion 。
何かがあなたを待っているとき — デフォルトモードの新しいタイプ、またはすべて
厳密モードの場合 — コマンドラインから確認します。
mycobrain レビュー # 保留中のエンティティ、関係、タイプをリストする
mycobrain レビュー承認 < id > # グラフにプロモートします
mycobrain レビュー拒否 < id > # 拒否します (保存および監査され、決して削除されません)
証明: npm run test:review — 承認すると実際にエンティティ / エッジ / が着陸します
正準グラフを入力します。拒否することは決してありません。
個人的な思い出、共有された知識
マルチエージェントチームは真の隔離を実現: 非公開とマークされた文書は読み取り可能
それらを作成したエージェントによってのみ — すべての読み取りツールで強制されます。
ワークスペースの行レベルのセキュリティ。ワークスペースのメモリは共有されたままになります。証明:
npm run test:sharing (2 つのエージェントの可視性マトリックス)。ワークスペースのような
分離されているため、最小権限の Brain_app ロールの下でのみバインドされます。
(セキュリティノート)。
クライアントごとに 1 つの独立したワークスペース — 代理店向けに構築
各クライアントを独自のワークスペースに配置し、代理店全体で 1 つのプレイブックを共有し、
あなたが販売する保証は、Postgres の行レベルのセキュリティ、つまりセッション スコープです。
クライアント A にクライアント B の行を返すことはできません。の
エージェンシー スターター キットはプロビジョニング (1 つのコマンド) して出荷されます
分離を実際にバインドさせる最小権限の DB ロール。証明:
npm run test:agency — C

クライアント A はクライアント B の事実をまったく知りません。
分離は、最小特権のロールの下でのみバインドされます。 RLS は制約を与えません
Postgres スーパーユーザー 、およびゼロ構成クイックスタートのデフォルトの頭脳の役割
スーパーユーザーです (単一ワークスペースのセルフホストには問題ありません。何もする必要はありません)
隔離します）。 1 つのデータベースに複数のクライアントを配置する前に、アプリを次のように実行します。
エージェンシー キットに同梱されている NOSUPERUSER Brain_app ロール。マイコブレインドクター
スーパーユーザー接続にフラグを立てます。マルチテナントの分離により、
Brain_app 、デフォルトのクイックスタート ロールではありません。
RLS は、テナントがどの行を読み取ることができるかを決定します。この設定により、どのテナントが
request は、RLS の前のステップです。標準入出力サーバー上で ID が取得されます
デフォルトではサーバーの環境からのみ: workspace_id 、agent_id 、
またはツール呼び出し引数で指定された api_key は無視されるため、
プロンプトによって挿入されたエージェントは workspace_id: "<someone-else>" を渡して到達できません
別のワークスペース。 (brain_ キーの場合、ID はキー文字列から取得され、
他には何もありません。）
BRAIN_TRUST_REQUEST_IDENTITY=1 を設定するのは、サーバーの前面に
各リクエストを認証し、リクエストをマッピングする実際のマルチテナント ゲートウェイ
テナント自体 - リクエストごとの ID が尊重されます (およびサービス ロール JWT
BRAIN_SERVICE_ROLE_KEY と等しくなければなりません (単に似ているだけではありません)。シングルテナント
自己ホストにはこれは必要ありません。自己ホストの ID はすでに環境から派生しています。
すべてが MCP を話すわけではありません。 Web アプリ、オートメーション、またはパートナー バックエンドの場合、
mycobrain-rest は、脳の前に小さな読み取り専用 API を配置します (ちょうど 2 つ)
ツール、検索とその理由、さらに健康:
mycobrain-rest # → http://127.0.0.1:8787
カール -s ローカルホスト:8787/検索 \
-H " 権限: Bearer Brain_<ws>_<agent>_<secret> " \
-d ' {"query":"価格設定について何を決めましたか?","limit":5} '
スコープ: キーのスコープ範囲

ワークスペースへのクエリ (MCP と同じ RLS)、および
書き込みルートはありません。 MCP と同様に、これは最も低い特権でのみバインドされます。
Brain_app ロール (セキュリティ上の注意) —
REST をデフォルトの Brain スーパーユーザーとして公開しないでください (mycobrain-doctor がフラグを立てます)。
キーの検証: インストールは …_agent_api_key_verification.sql に移行されました
シークレットが登録されたら、各キーの <secret> を Agent_api_keys と照合して検証します。
( Brain_set_agent_api_key_secret(...) 経由で登録/回転)。それまで鍵
無記名トークンとして機能します。 BRAIN_REQUIRE_API_KEY_SECRET=1 を設定して、
REST を公開する前に登録されたシークレット。
バインディング: デフォルトでループバック — BRAIN_REST_HOST=0.0.0.0 を自分の後ろに設定します
TLS/プロキシは、公開する場合にのみ使用し、キーをパスワードのように扱います。
1 つの会話でファクトを保存します。
思い出を残してください。取締役会は毎週水曜日、太平洋時間午前 9 時に開催されます。
新たな会話を始めて、次のように尋ねてください。
取締役会は何時ですか?
期待される結果: 新しいセッションは、チャット履歴に依存する代わりに、保存されたファクトを取得します。
思い出を残してください: Acme の更新電話は 10 月 15 日にジョーダンと行われます。
別のクライアントから読み取ります。
Acme の更新日はいつですか?
期待される結果: 信頼できる情報源は単一のチャット スレッドではなく Postgres であるため、両方のクライアントが同じ共有メモリを読み取ります。
事実について Brain_why に質問し、TR ではなくソース チェーンを取得します。

[切り捨てられた]

## Original Extract

Self-hosted, source-traceable memory for AI agents: on your own Postgres, keyless to run. Facts earn confidence from independent corroboration and supersede on contradiction, never silently overwritten. MCP-native (Claude, Cursor, Windsurf). Reproducible LongMemEval in-repo: 73.6% QA, recall@5 89-92
[truncated]

GitHub - thegoodguysla/myco-brain: Self-hosted, source-traceable memory for AI agents: on your own Postgres, keyless to run. Facts earn confidence from independent corroboration and supersede on contradiction, never silently overwritten. MCP-native (Claude, Cursor, Windsurf). Reproducible LongMemEval in-repo: 73.6% QA, recall@5 89-92%. Apache-2.0. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
thegoodguysla
/
myco-brain
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
15 Commits 15 Commits .github .github demos demos docs docs evals/ longmemeval evals/ longmemeval examples examples install-shim install-shim mcp-server mcp-server supabase/ migrations supabase/ migrations .env.example .env.example .gitignore .gitignore CHANGELOG.md CHANGELOG.md CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md CONTRIBUTING.md Dockerfile.railway Dockerfile.railway LICENSE LICENSE NOTICE NOTICE README.md README.md ROADMAP.md ROADMAP.md SECURITY.md SECURITY.md docker-compose.yml docker-compose.yml quickstart.sh quickstart.sh railway.json railway.json smithery.yaml smithery.yaml View all files Repository files navigation
Persistent, source-traceable memory for AI agents — self-hosted on your own Postgres, with no API keys required to run.
Source-traceable. Every fact traces to the document it came from ( brain_why ) — no trust-me summaries.
Trust that compounds. Independent corroboration raises a fact's confidence; a contradiction supersedes it — kept and audited, never silently overwritten.
Keyless & local-first. Full-text + semantic search and the knowledge graph all run with zero hosted dependency — add an Anthropic key only for the most accurate graph.
Yours. Apache-2.0, plain Postgres tables, 11 MCP tools. Works with Claude, Cursor, Windsurf, Continue, Zed.
Who it's for: dev teams running agents that need one shared memory · agencies needing hard per-client isolation · anyone who wants their assistant to remember across sessions — import your ChatGPT / Claude history (from your data export) and your AI knows you on day one.
Built solo by a growth marketer — not a career engineer — directing AI coding agents over ~3 months. How it was built ↓
The usual fix for agent amnesia — letting an LLM maintain its own memory —
fills it with duplicates, hallucinated summaries, and confident answers nobody
can trace. Myco Brain is built on the opposite contract:
The LLM proposes. Deterministic rules decide what becomes a fact. You set
the bar — from corroboration-gated auto-promotion (the default) to strict
human review of every fact ( BRAIN_REQUIRE_HUMAN_REVIEW=1 ).
Claude, Cursor, Windsurf, Continue, Zed, and custom agents all share one memory
backed by your own Postgres.
⭐ If the trust model resonates, a star helps others find it.
# 1. Boot the stack (Postgres + MCP server + extraction worker)
git clone https://github.com/thegoodguysla/myco-brain.git && cd myco-brain
docker compose up -d
# 2. Give your agent a memory — point it at any repo or folder
# (no env needed: it finds the quickstart stack on localhost)
npx -y -p @mycobrain/mcp-server mycobrain-ingest github:your-org/your-repo
# 3. Connect your client (one-liner below), then ask across sessions:
# "what did we decide about auth, and where is that documented?"
# → answered from your docs, with the source cited.
Tip
Zero API keys, all the way down. Full-text search, semantic search (local
embeddings), and the knowledge graph (local extraction) all run with no hosted
dependency. Add an Anthropic key only if you want the most accurate graph.
MCP-native by design — your agent knows when to use memory, not just how .
Most MCP servers expose tools and hope the model calls them. Myco ships a usage
contract over MCP's instructions channel: the moment it connects, your agent
knows to pull context before a task, save durable decisions, and cite sources
with brain_why — no per-project prompting. Tune the policy in one copy-paste
block: Teach your agent to use it well .
Quick links:
10-minute quickstart ·
Teach your agent to use it well ·
The trust engine ·
Benchmark — run it yourself ·
Run every proof ·
Who it's for ·
Environment variables ·
Architecture ·
Roadmap ·
Cloud waitlist
Memory that gets more trustworthy (compounding confidence)
Most agent memory overwrites facts silently. Myco Brain compounds them:
An independent source agreeing with a fact raises its confidence
(damped noisy-OR — ten chunks of one document corroborate nothing; only
distinct sources count).
A confident contradiction on a single-valued relationship (who you work
for , where something is located ) supersedes the old fact: it's
closed and weakened — kept, never deleted — with the supersession
recorded in an audited claims ledger.
Ask brain_why about any fact and you get its distinct source count (per
relationship, not per mention), its confidence trend over time ("0.8 → 0.86"),
and any superseded history.
Contradictions stay visible. Your memory can't gaslight you.
works for → Halcyon Labs 0.55 [SUPERSEDED — kept, not deleted]
works for → Driftwood Analytics 0.90 [ACTIVE]
claims ledger: old fact superseded_by → new fact (audited)
Proof: npm run test:compounding — the full lifecycle runs against a live
database in seconds, no LLM required.
The schema evolves with your data (dynamic schema)
The extraction worker notices entity kinds and relationship types your catalog
doesn't have yet and proposes them ( brain_stats : "Brain proposed 3 new
types from your data" ).
Promotion is yours by default — or opt into auto-promotion for types
corroborated across enough distinct source documents
( BRAIN_SCHEMA_AUTO_PROMOTE=1 ), counted per document, not per mention, so two
documents passed back and forth can't manufacture consensus. One chatty
document can never promote anything.
A promoted type stays scoped to the workspace that earned it — one client's
vocabulary never leaks into another's catalog (see
per-client isolation ).
Proofs: npm run test:dynamic-schema , npm run test:schema-promotion .
When something is waiting on you — novel types in default mode, or everything
in strict mode — review it from the command line:
mycobrain review # list pending entities, relationships, types
mycobrain review approve < id > # promote it into the graph
mycobrain review reject < id > # reject it (kept and audited, never deleted)
Proof: npm run test:review — approving actually lands the entity / edge /
type in the canonical graph; rejecting never does.
Private memories, shared knowledge
Multi-agent teams get real isolation: documents marked private are readable
only by the agent that created them — enforced in every read tool, on top
of workspace row-level security. Workspace memory stays shared. Proof:
npm run test:sharing (a two-agent visibility matrix). Like workspace
isolation, it binds only under the least-privilege brain_app role
( security note ).
One isolated workspace per client — built for agencies
Put each client in their own workspace, share one agency-wide playbook, and
the guarantee you sell is Postgres row-level security — a session scoped
to Client A cannot return Client B's rows. The
agency starter kit provisions it (one command) and ships
the least-privilege DB role that makes the isolation actually bind. Proof:
npm run test:agency — Client A sees zero of Client B's facts.
Isolation binds only under the least-privilege role. RLS does not constrain
a Postgres superuser , and the zero-config quickstart's default brain role
is a superuser (fine for a single-workspace self-host — there's nothing to
isolate). Before you put more than one client in one database, run the app as
the NOSUPERUSER brain_app role the agency kit ships; mycobrain-doctor
flags a superuser connection. Multi-tenant isolation is a guarantee of
brain_app , not of the default quickstart role.
RLS decides which rows a tenant can read ; this setting decides which tenant a
request is — the step before RLS. On the stdio server, identity is taken
only from the server's environment by default: a workspace_id , agent_id ,
or api_key supplied in tool-call arguments is ignored , so even a
prompt-injected agent can't pass workspace_id: "<someone-else>" to reach
another workspace. (For brain_ keys, identity comes from the key string and
nothing else.)
Set BRAIN_TRUST_REQUEST_IDENTITY=1 only when you front the server with a
real multi-tenant gateway that authenticates each request and maps it to a
tenant itself — then per-request identity is honored (and a service-role JWT
must equal BRAIN_SERVICE_ROLE_KEY , not merely look like one). Single-tenant
self-hosts need none of this — their identity is already environment-derived.
Not everything speaks MCP. For a web app, an automation, or a partner backend,
mycobrain-rest puts a small read-only API in front of the brain — exactly two
tools, search and why , plus health:
mycobrain-rest # → http://127.0.0.1:8787
curl -s localhost:8787/search \
-H " Authorization: Bearer brain_<ws>_<agent>_<secret> " \
-d ' {"query":"what did we decide about pricing?","limit":5} '
Scoping: the key scopes every query to its workspace (same RLS as MCP), and
there are no write routes . Like MCP, this binds only under the least-privilege
brain_app role ( security note ) —
never expose REST as the default brain superuser ( mycobrain-doctor flags it).
Key verification: installs migrated to …_agent_api_key_verification.sql
verify each key's <secret> against agent_api_keys once a secret is registered
(register/rotate via brain_set_agent_api_key_secret(...) ). Until then the key
acts as a bearer token; set BRAIN_REQUIRE_API_KEY_SECRET=1 to require a
registered secret before exposing REST.
Binding: loopback by default — set BRAIN_REST_HOST=0.0.0.0 behind your own
TLS/proxy only when you mean to expose it, and treat the key like a password.
Save a fact in one conversation:
Save a memory: the board meeting is every Wednesday at 9 AM Pacific.
Start a fresh conversation and ask:
What time is the board meeting?
Expected result: the new session retrieves the stored fact instead of relying on chat history.
Save a memory: Acme's renewal call is on October 15 with Jordan.
Read from another client:
What is Acme's renewal date?
Expected result: both clients read the same shared memory because the source of truth is Postgres, not a single chat thread.
Ask brain_why about any fact and get the source chain — not a tr

[truncated]
