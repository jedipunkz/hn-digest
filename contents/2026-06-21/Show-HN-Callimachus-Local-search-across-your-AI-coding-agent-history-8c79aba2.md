---
source: "https://github.com/BetaBots-LLC/callimachus"
hn_url: "https://news.ycombinator.com/item?id=48613813"
title: "Show HN: Callimachus – Local search across your AI coding-agent history"
article_title: "GitHub - BetaBots-LLC/callimachus: One local, searchable index of your AI coding-agent history Claude Code, Codex, Cursor, Gemini & more. Keyword + semantic search, MCP server, CLI & VS Code extension. · GitHub"
author: "arishaller"
captured_at: "2026-06-21T01:03:12Z"
capture_tool: "hn-digest"
hn_id: 48613813
score: 3
comments: 2
posted_at: "2026-06-20T23:00:27Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Callimachus – Local search across your AI coding-agent history

- HN: [48613813](https://news.ycombinator.com/item?id=48613813)
- Source: [github.com](https://github.com/BetaBots-LLC/callimachus)
- Score: 3
- Comments: 2
- Posted: 2026-06-20T23:00:27Z

## Translation

タイトル: HN を表示: Callimachus – AI コーディング エージェント履歴全体のローカル検索
記事のタイトル: GitHub - BetaBots-LLC/callimachus: AI コーディング エージェント履歴の 1 つのローカルで検索可能なインデックス Claude Code、Codex、Cursor、Gemini など。キーワード + セマンティック検索、MCP サーバー、CLI および VS Code 拡張機能。 · GitHub
説明: AI コーディング エージェント履歴の 1 つのローカルで検索可能なインデックス。Claude Code、Codex、Cursor、Gemini など。キーワード + セマンティック検索、MCP サーバー、CLI および VS Code 拡張機能。 - BetaBots-LLC/callimachus

記事本文:
GitHub - BetaBots-LLC/callimachus: AI コーディング エージェント履歴の 1 つのローカルで検索可能なインデックス Claude Code、Codex、Cursor、Gemini など。キーワード + セマンティック検索、MCP サーバー、CLI および VS Code 拡張機能。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
BetaBots-LLC
/
カリマコス
プ

ブリック
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
48 コミット 48 コミット .changeset .changeset .github .github .vscode .vscode アプリ アセット アセット スクリプト スクリプト .dockerignore .dockerignore .gitignore .gitignore COMMERCIAL.md COMMERCIAL.md COTRIBUTING.md COTRIBUTING.md Dockerfile Dockerfile ライセンスライセンス通知通知 README.md README.md RELEASING.md RELEASING.md SECURITY.md SECURITY.md SIGNING.md SIGNING.md biome.json biome.json Glama.json Glama.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yamlturbo.jsonturbo.json すべてのファイルを表示 リポジトリ ファイルのナビゲーション
AI コーディング エージェント スレッドのローカル インデックスと検索 — 11 のツール (Claude Code、Codex、Cursor、Gemini CLI、Qwen Code、Goose、OpenCode、Continue、Cline、Roo Code、Kilo Code) — に加えて、プロバイダーに依存しないチャット、MCP サーバー、CLI、および VS Code / Cursor 拡張機能。すべてがマシン上に残ります。
アレクサンドリア図書館の最初の目録を作成したカリマコスにちなんで名付けられました。
リリースから最新の署名付きビルドを取得します — macOS ( .dmg 、Apple Silicon)、Windows ( .msi )、または Linux ( .AppImage / .deb )。以降、アプリは自動更新されます。自分で構築したいですか? 「開発」を参照してください。
11 のコーディング エージェントからのすべての会話を 1 つのローカル SQLite ストア (Claude Code、Codex、Cursor、Gemini CLI、Qwen Code、Goose、OpenCode、Continue、Cline、Roo Code、および Kilo Code) にインデックス付けします。別のソースの追加は、文書化された小さな契約です。
ハイブリッド ランキングで検索します: キーワード (SQLite FTS5 / BM25) とデバイス上のセマンティック類似性 (sqlite-vec KNN、クラウドなし) を相互ランク融合経由で融合します。ソース、プロジェクト、サブエージェントごとにフィルターします。

スターとタグ。
コード認識の検索 — 検索バーに file:embed/mod.rs (または cal files <path> ) と入力すると、パスに接触したすべてのスレッドが検索されます。インデックス作成時に構築されたファイル メンション インデックスによって裏付けられます。
知識を抽出します — 無料のヒューリスティック TODO 抽出に加え、過去の決定/落とし穴をスレッド間でセマンティックに呼び出すことにより、意思決定、落とし穴、要約をオプトイン LLM で蒸留します。オプションの自動蒸留により、新規/変更されたスレッドがバックグラウンドで排出されるため、メモリが自動的に追加されます。ローカルの Ollama (キーレス) またはクラウド API キーが必要です。
ファクトをキュレーションします。抽出されたファクトを固定、編集、または削除して、編集内容が再抽出されても存続するようにします。さらに、互いに矛盾する決定にフラグを立てる LLM「競合のレビュー」パスも提供します。
プロジェクトごとに記憶 — [プロジェクト] タブでは、LLM ブリーフと管理対象の .callimachus/memory.md とともに、各リポジトリの決定事項、注意点、開いている TODO が永続メモリ (正規のプロジェクト キーによってグループ化されるため、ワークツリー / シンボリックリンク / ~ によって 1 つのリポジトリが分割されません) に集約されます。そのメモリは「CLI で開く」ときに先頭に追加され、任意のエージェントに自動的に挿入できます。更新 AGENTS.md (または cal エージェント ) は、マネージド ブロックをリポジトリの AGENTS.md / CLAUDE.md に書き込み、cal フックはそれをクロード コードの SessionStart フックにフィードします。
あなたの履歴を尋ねます (RAG) — あなた自身のスレッド上で合成され、引用された回答であり、[スレッド N] で使用されたソースへの引用が行われます。 LLM エンジンが必要です (ナレッジ/蒸留が有効)。
コレクションに整理します。スレッドにスターを付けて自由形式のタグを付け、スター付きまたはタグでリストをフィルタリングします。
アプリ内エージェント (Anthropic / OpenAI / Gemini / OpenRouter / Ollama — あなたのキー、あなたの選択) とチャットし、あなた自身の履歴を検索し、あなたの承認を得てシェルコマンドを実行できます。ストリーミング、キャンセル可能、ライブモデルリスト付き。チャットが保存され、検索可能になります

ああ。
ツール間でコンテキストを伝達します — エージェント CLI で任意のスレッドを開き (「クロード / コーデックス / ジェミニで開く…」、パックされたトランスクリプトがシードされます)、ネイティブ CLI でクロード コード / コーデックス スレッドを再開し、コンテキストをコピーするか、スレッドを Obsidian にエクスポートします (オプションで AI によって決定事項 / 注意事項 / TODO が要約されます)。
エージェントに表示 — バンドルされた MCP サーバー ( callimachus-mcp ) は、エージェントがセッション中に呼び出すことができるツールとしてインデックスを公開します。これは双方向です。エージェントはファイルに触れることなく、Callimachus 自身のメモリに書き戻すことができます (TODO を閉じ、決定事項や注意点を記録します)。 /recall スキルは、それをいつ使用するかを教えます。
バックグラウンド ファイル ウォッチャーを通じて最新の状態を維持します。非公開のまま — API キーは OS キーチェーン内に存在し、選択した LLM プロバイダー以外には何も送信されません。
シェル: Tauri 2 (Rust) + React 19 + TypeScript + Vite 8
ストア/検索: バンドルされた SQLite + FTS5 (rusqlite); fastembed によるデバイス上の埋め込み (bge-small-en-v1.5、384-dim)。 sqlite-vec (vec0) を介した SQL の KNN
チャット: genai クレート経由のマルチプロバイダー (Anthropic / OpenAI / Gemini / OpenRouter / Ollama)、Tauri チャネル経由のストリーミング トークン、キャンセル可能、エージェント ツール呼び出し (履歴検索 + 承認済みシェル)
シークレット: クロスプラットフォーム キーリング クレートを介した OS 認証情報ストア — macOS キーチェーン、Windows Credential Manager、Linux Secret Service
サイドカー: callimachus-mcp (MCP サーバー) および cal (CLI) — どちらも同じindex.db に対してデスクトップ コア ライブラリを再利用します。
エディター: cal にシェルアウトする VS Code / Cursor 拡張機能 ( apps/vscode 、マーケットプレイス + Open VSX に公開)
これは Turborepo + pnpm ワークスペースです。
アプリ/
Desktop/ # Tauri 2 デスクトップ アプリ + cal CLI および MCP サーバー (src-tauri)
vscode/ # VS Code拡張子（エディタからの検索履歴）
web/ # マーケティング + ダウンロード サイト (予約済み、まだ構築されていません)
パッケージ

s/ # 共有コード (表示される場合)
.changeset/ # バージョン + 変更ログ管理
scripts/ # バージョン同期、リリースのタグ付け
リリース、バージョン管理、および自動アップデータは RELEASING.md に文書化されています。
pnpmインストール
pnpmdesktop:dev # デスクトップウィンドウを起動します (tauri dev)
# すべてのアプリにわたるリポジトリ ルートから:
pnpm build #turbo: すべてのアプリのフロントエンドをビルドする
pnpm typecheck #turbo: すべてのアプリのタイプチェック
最初の起動: インデックスは空です。設定を開いて (または Reindex を押して) ソースにインデックスを付けてから、セマンティック インデックスを構築してセマンティック検索を有効にします。再インデックスは、ビルド セマンティック インデックスとは別に、ソースごとの進行状況バーを備えたバックグラウンド ジョブとして実行されます。この 2 つは相互に排他的です (一方が一時停止し、もう一方が書き込みロックを保持します)。
cd アプリ/デスクトップ/src-tauri
カーゴテスト # 高速単体テスト
貨物テスト -- --ignored --nocapture # 実データ + モデル + キーチェーンのスモーク テスト
--ignored テストは、このマシン上のライブ データを操作します。各ソースには、実際の履歴を読み取り専用でインデックス付けする real_<source>_index スモーク テスト ( ~/.claude 、 ~/.codex 、 Cursor、 ~/.gemini 、 ~/.qwen 、 Goose、OpenCode、Continue、Cline/Roo/Kilo) に加えて、埋め込みモデルのダウンロード (最初の実行、ネットワークが必要) とキーチェーンのラウンドトリップがあります。
デスクトップ ウィンドウを超えて、エージェント、端末、エディタから同じローカル インデックスにアクセスでき、すべて 1 つのindex.db を読み取ります。
MCP サーバー — エージェントがセッション中に自分の過去の作業を検索できるようにします。 callimachus-mcp はデスクトップ アプリ (PATH 上) に同梱されています。クライアントに登録するだけです。
claude mcp add callimachus -- callimachus-mcp # または任意の MCP クライアント
代わりにチェックアウトから構築しますか?カーゴインストール --path apps/desktop/src-tauri --bin callimachus-mcp 。
ツール (16) — 読み取りと書き込みが可能になりました。読み取り: search_threads 、 search_current_project (実行されるリポジトリに自動スコープ設定)、recent_threads 、get_thread

、 list_tags 、 list_open_todos 、 get_thread_knowledge 、 remember_decions 、 remember_gotchas 、 find_prior_work (「前にこれをやったことがある?」ガード - タスクに似た以前のセッション)、 project_memory (プロジェクトの集約された決定事項 / 落とし穴 / 未解決の TODO)、 ask_history (履歴に対する引用された RAG 回答)、 thread_for_file (どのセッションが触れたか)パス）。 (あなたのコードではなく、Callimachus 自身のメモリに) を書き込みます: complete_todo (開いている TODO を閉じる)、record_decion 、および Record_gotcha (ファクトをプロジェクトのメモリに永続化する)。バンドルされている /recall スキル ( .claude/skills/recall ) は、エージェントに連絡するタイミングを伝えます。
CLI — cal 、パイプに優しい。デスクトップ アプリ (PATH 上) に同梱されます。または、Cargo install --path apps/desktop/src-tauri --bin cal を使用してチェックアウトからビルドします。
cal search "ベクトルインデックス移行" -y # -y = ハイブリッド (セマンティック + キーワード)
最近の計算 -n 10
キャルキャット42 | pbcopy # パックされたトランスクリプト → クリップボード
cal stats # インデックス合計 + ソースごとの内訳
cal export 42 --vault ~ /Obsidian # Obsidian ノートとしてスレッドを書き込みます
「リリースをどのように設定しましたか?」という質問 # あなたの履歴に対する RAG の回答を引用
cal files embed/mod.rs ファイルパスに触れたスレッドの数
cal Memory # このリポジトリの蒸留されたメモリ (決定事項/落とし穴/TODO)
calned 17 # 開いている TODO を完了としてマークします (ID は `cal todos` から)
cal remember deal " use sqlite-vec for KNN " # ファクトをリポジトリのメモリに記録する
cal Agents # リポジトリのメモリを AGENTS.md に書き込みます (任意のエージェントが読み取ります)
calフック # リポジトリのメモリを出力します（Claude CodeのSessionStartフックとして使用）
star 、 tag 、 tags 、 todos 、Knowledge 、 distill 、 Decision 、 Gotchas 、および関連するものも存在します。21 つすべてに対して cal help を実行します。
VS Code / Cursor — この拡張機能は、「Callimachus History」サイドバー、ステータスバーの検索ボタン、および / を検索するコマンドを追加します。

スレッドをサート/コピーします ( cal にシェルアウトします)。 VS Code Marketplace または Open VSX (レジストリ Cursor と VSCodium が使用) からインストールするか、 Releases から .vsix を取得します。 apps/vscode/README.md を参照してください。
クロスプラットフォーム (macOS / Windows / Linux)。残っている唯一の macOS 専用部分は、「Open in CLI」/「Resume」ランチャー (AppleScript 経由でターミナルを駆動します) です。 Windows/Linux では、これらのアクションは「まだサポートされていません」という通知を返しますが、他のすべて (インデックス、検索、チャット、エクスポート、キーチェーン) はどこでも機能します。
Cline / Roo Code / Kilo Code は CLI を持たないエディター拡張機能であるため、インデックスのみです (検索は可能ですが、「再開」による再起動はできません)。
Cursor はスレッドごとのワークスペースを保存しないため、現在 Cursor スレッドにはプロジェクト パスがありません。
クロード コードのサブエージェントのトランスクリプトにはインデックスが付けられますが、デフォルトでは「サブエージェント」トグルの背後に隠されています。
最初のインデックスが大きいと、1 回限りのコストがかかります (ここでのクロード コーパスは、約 25 秒で約 90,000 のメッセージでした)。後続のパスでは、変更されていないファイルがスキップされます。
より多くのソース (Charm Crush、Factory Droid、Copilot CLI) は対象範囲が定められていますが、まだ統合されていません。インデクサー ガイドを参照してください。
問題やPRを歓迎します。 CONTRIBUTING.md では、ローカルのセットアップ、規約、リリース フローについて説明します。別のエージェントのサポートの追加は、文書化された小規模な契約です。通常は 1 つのインデクサー モジュール + 移行 +

[切り捨てられた]

## Original Extract

One local, searchable index of your AI coding-agent history Claude Code, Codex, Cursor, Gemini & more. Keyword + semantic search, MCP server, CLI & VS Code extension. - BetaBots-LLC/callimachus

GitHub - BetaBots-LLC/callimachus: One local, searchable index of your AI coding-agent history Claude Code, Codex, Cursor, Gemini & more. Keyword + semantic search, MCP server, CLI & VS Code extension. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
BetaBots-LLC
/
callimachus
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
48 Commits 48 Commits .changeset .changeset .github .github .vscode .vscode apps apps assets assets scripts scripts .dockerignore .dockerignore .gitignore .gitignore COMMERCIAL.md COMMERCIAL.md CONTRIBUTING.md CONTRIBUTING.md Dockerfile Dockerfile LICENSE LICENSE NOTICE NOTICE README.md README.md RELEASING.md RELEASING.md SECURITY.md SECURITY.md SIGNING.md SIGNING.md biome.json biome.json glama.json glama.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yaml turbo.json turbo.json View all files Repository files navigation
Local index & search for your AI coding-agent threads — across 11 tools (Claude Code, Codex, Cursor, Gemini CLI, Qwen Code, Goose, OpenCode, Continue, Cline, Roo Code, Kilo Code) — plus a provider-agnostic chat, an MCP server, a CLI, and a VS Code / Cursor extension. Everything stays on your machine.
Named for Callimachus , who built the first catalogue of the Library of Alexandria.
Grab the latest signed build from Releases — macOS ( .dmg , Apple Silicon), Windows ( .msi ), or Linux ( .AppImage / .deb ). The app auto-updates from there on. Prefer to build it yourself? See Develop .
Indexes every conversation from 11 coding agents into one local SQLite store — Claude Code, Codex, Cursor, Gemini CLI, Qwen Code, Goose, OpenCode, Continue, Cline, Roo Code, and Kilo Code. Adding another source is a small, documented contract .
Searches them with hybrid ranking: keyword (SQLite FTS5 / BM25) fused with on-device semantic similarity (sqlite-vec KNN, no cloud) via Reciprocal Rank Fusion. Filter by source, project, subagents, starred, and tags.
Finds code-aware — type file:embed/mod.rs in the search bar (or cal files <path> ) to find every thread that touched a path; backed by a file-mention index built at index time.
Distills knowledge — free heuristic TODO extraction, plus opt-in LLM distillation of decisions, gotchas, and summaries, with cross-thread semantic recall of past decisions/gotchas. Optional auto-distillation drains new/changed threads in the background so memory self-populates. Needs local Ollama (keyless) or a cloud API key.
Curates the facts — pin, edit, or delete distilled facts so your edits survive re-distilling, plus an LLM "Review conflicts" pass that flags decisions that contradict each other.
Remembers per project — a Projects tab aggregates each repo's decisions, gotchas, and open TODOs into durable memory (grouped by a canonical project key, so worktrees / symlinks / ~ don't split one repo), with an LLM brief and a managed .callimachus/memory.md . That memory is prepended when you "Open in CLI", and you can inject it into any agent automatically: Update AGENTS.md (or cal agents ) writes a managed block into the repo's AGENTS.md / CLAUDE.md , and cal hook feeds it to a Claude Code SessionStart hook.
Asks your history (RAG) — a synthesized, cited answer over your own threads, with [thread N] citations back to the sources it used. Needs an LLM engine (Knowledge/distillation enabled).
Organizes into collections — star threads and attach free-form tags, then filter the list by starred or by tag.
Chats with an in-app agent (Anthropic / OpenAI / Gemini / OpenRouter / Ollama — your key, your choice) that can search your own history and run shell commands with your approval ; streaming, cancellable, with live model lists. Chats are saved and become searchable too.
Carries context across tools — open any thread in any agent CLI ("Open in Claude / Codex / Gemini …", seeded with the packed transcript), resume a Claude Code / Codex thread in its native CLI, copy context, or export a thread to Obsidian (optionally AI-summarized with decisions / gotchas / TODOs).
Surfaces to your agents — a bundled MCP server ( callimachus-mcp ) exposes the index as tools any agent can call mid-session, and it's two-way: agents can write back into Callimachus's own memory (close TODOs, record decisions/gotchas) without ever touching your files. The /recall skill teaches them when to use it.
Stays current via a background file watcher; stays private — API keys live in the OS keychain, nothing is sent anywhere except the LLM provider you pick.
Shell: Tauri 2 (Rust) + React 19 + TypeScript + Vite 8
Store/search: bundled SQLite + FTS5 ( rusqlite ); on-device embeddings via fastembed (bge-small-en-v1.5, 384-dim); KNN in SQL via sqlite-vec (vec0)
Chat: multi-provider via the genai crate (Anthropic / OpenAI / Gemini / OpenRouter / Ollama), streaming tokens over a Tauri Channel, cancellable, with agent tool-calls (history search + approved shell)
Secrets: OS credential store via the cross-platform keyring crate — macOS Keychain, Windows Credential Manager, Linux Secret Service
Sidecars: callimachus-mcp (MCP server) and cal (CLI) — both reuse the desktop core lib against the same index.db
Editor: a VS Code / Cursor extension ( apps/vscode , published to the Marketplace + Open VSX) that shells out to cal
This is a Turborepo + pnpm workspace.
apps/
desktop/ # the Tauri 2 desktop app + the cal CLI and MCP server (src-tauri)
vscode/ # VS Code extension (search history from the editor)
web/ # marketing + download site (reserved, not built yet)
packages/ # shared code, when it appears
.changeset/ # version + changelog management
scripts/ # version-sync, release tagging
Releases, versioning, and the auto-updater are documented in RELEASING.md .
pnpm install
pnpm desktop:dev # launches the desktop window (tauri dev)
# from the repo root, across all apps:
pnpm build # turbo: build every app's frontend
pnpm typecheck # turbo: typecheck every app
First launch: the index is empty — open Settings (or hit Reindex ) to index your sources, then Build semantic index to enable semantic search. Reindex runs as a background job with a per-source progress bar, separate from Build semantic index — the two are mutually exclusive (one pauses while the other holds the write lock).
cd apps/desktop/src-tauri
cargo test # fast unit tests
cargo test -- --ignored --nocapture # real-data + model + keychain smoke tests
The --ignored tests touch live data on this machine: each source has a real_<source>_index smoke test that indexes your real history read-only ( ~/.claude , ~/.codex , Cursor, ~/.gemini , ~/.qwen , Goose, OpenCode, Continue, Cline/Roo/Kilo), plus the embedding-model download (first run, needs network) and a Keychain round-trip.
Beyond the desktop window, the same local index is reachable from your agents, terminal, and editor — all reading one index.db .
MCP server — let any agent search its own past work mid-session. callimachus-mcp ships with the desktop app (on your PATH); just register it with your client:
claude mcp add callimachus -- callimachus-mcp # or any MCP client
Building from a checkout instead? cargo install --path apps/desktop/src-tauri --bin callimachus-mcp .
Tools (16) — now read and write. Reads: search_threads , search_current_project (auto-scoped to the repo it runs in), recent_threads , get_thread , list_tags , list_open_todos , get_thread_knowledge , recall_decisions , recall_gotchas , find_prior_work (the "have I done this before?" guard — prior sessions similar to a task), project_memory (a project's aggregated decisions / gotchas / open TODOs), ask_history (a cited RAG answer over your history), and threads_for_file (which sessions touched a path). Writes (into Callimachus's own memory, never your code): complete_todo (close an open TODO), record_decision , and record_gotcha (persist a fact into a project's memory). The bundled /recall skill ( .claude/skills/recall ) tells agents when to reach for them.
CLI — cal , pipe-friendly. Ships with the desktop app (on your PATH); or build from a checkout with cargo install --path apps/desktop/src-tauri --bin cal .
cal search " vector index migration " -y # -y = hybrid (semantic + keyword)
cal recent -n 10
cal cat 42 | pbcopy # packed transcript → clipboard
cal stats # index totals + per-source breakdown
cal export 42 --vault ~ /Obsidian # write a thread as an Obsidian note
cal ask " how did we set up releases? " # cited RAG answer over your history
cal files embed/mod.rs # threads that touched a file path
cal memory # this repo's distilled memory (decisions/gotchas/TODOs)
cal done 17 # mark an open TODO done (id from `cal todos`)
cal remember decision " use sqlite-vec for KNN " # record a fact into the repo's memory
cal agents # write the repo's memory into AGENTS.md (any agent reads it)
cal hook # print the repo's memory (use as a Claude Code SessionStart hook)
star , tag , tags , todos , knowledge , distill , decisions , gotchas , and related also exist — run cal help for all 21.
VS Code / Cursor — the extension adds a "Callimachus History" sidebar, a status-bar search button, and commands to search / insert / copy threads (it shells out to cal ). Install from the VS Code Marketplace or Open VSX (the registry Cursor and VSCodium use), or grab the .vsix from Releases . See apps/vscode/README.md .
Cross-platform (macOS / Windows / Linux). The only macOS-only piece left is the "Open in CLI" / "Resume" launchers (they drive Terminal via AppleScript); on Windows/Linux those actions return a "not supported yet" notice — everything else (index, search, chat, export, keychain) works everywhere.
Cline / Roo Code / Kilo Code are editor extensions with no CLI, so they are index-only (searchable, but not relaunchable via "Resume").
Cursor doesn't store a per-thread workspace, so Cursor threads currently have no project path.
Claude Code subagent transcripts are indexed but hidden behind a "subagents" toggle by default.
Large first index is a one-time cost (the Claude corpus here was ~90k messages in ~25s); subsequent passes skip unchanged files.
More sources (Charm Crush, Factory Droid, Copilot CLI) are scoped but not yet integrated — see the indexer guide .
Issues and PRs welcome. CONTRIBUTING.md covers local setup, conventions, and the release flow. Adding support for another agent is a small, documented contract — usually one indexer module + a migration +

[truncated]
