---
source: "https://github.com/ethanhq/cc-fleet"
hn_url: "https://news.ycombinator.com/item?id=48614868"
title: "Show HN: Cc-fleet – run other LLMs as Claude Code workers, your sub drives"
article_title: "GitHub - ethanhq/cc-fleet: 🚢 Run Claude Code's ⚙️ Dynamic Workflows, 👥 Agent Teams & ⚡ Subagents on any third-party model — DeepSeek · GLM · Kimi · Qwen … or your Codex subscription. No Anthropic subscription needed. ｜ 🚢 让 Claude Code 的 ⚙️ Dynamic Workflow、👥 Agent Team、⚡ Subagent 用上任意第三方模型 — DeepSee\n[truncated]"
author: "ethanhq"
captured_at: "2026-06-21T04:36:09Z"
capture_tool: "hn-digest"
hn_id: 48614868
score: 3
comments: 4
posted_at: "2026-06-21T01:40:27Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Cc-fleet – run other LLMs as Claude Code workers, your sub drives

- HN: [48614868](https://news.ycombinator.com/item?id=48614868)
- Source: [github.com](https://github.com/ethanhq/cc-fleet)
- Score: 3
- Comments: 4
- Posted: 2026-06-21T01:40:27Z

## Translation

タイトル: HN を表示: Cc-fleet – 他の LLM をクロード コード ワーカー、サブドライブとして実行
記事のタイトル: GitHub - ethanhq/cc-fleet: 🚢 Claude Code の ⚙️ ダイナミック ワークフロー、👥 エージェント チーム & ⚡ サブエージェントをサードパーティ モデル (DeepSeek、GLM、Kimi、Qwen … または Codex サブスクリプション) で実行します。 Anthropic のサブスクリプションは必要ありません。 ｜ 🚢 让 Claude Code の ⚙️ ダイナミックワークフロー、👥 エージェントチーム、⚡ サブエージェント用上任意第三方モデル — DeepSee
[切り捨てられた]
説明: 🚢 Claude Code の ⚙️ ダイナミック ワークフロー、 👥 エージェント チーム、 ⚡ サブエージェントを、任意のサードパーティ モデル (DeepSeek、GLM、Kimi、Qwen … または Codex サブスクリプション) で実行します。 Anthropic のサブスクリプションは必要ありません。 ｜ 🚢 让 Claude Code の ⚙️ ダイナミック ワークフロー、👥 エージェント チーム、⚡ サブエージェント用上任意第三方モデル — DeepSeek · GLM · Kim · Qwen…… または你的
[切り捨てられた]

記事本文:
GitHub - ethanhq/cc-fleet: 🚢 Claude Code の ⚙️ ダイナミック ワークフロー、 👥 エージェント チーム、 ⚡ サブエージェントを任意のサードパーティ モデル (DeepSeek、GLM、Kimi、Qwen … または Codex サブスクリプション) で実行します。 Anthropic のサブスクリプションは必要ありません。 ｜ 🚢 让 Claude Code の ⚙️ Dynamic Workflow、👥 Agent Team、⚡ Subagent 用上任意第三方モデル — DeepSeek · GLM · Kim · Qwen…… または你的 Codex 订阅, 不要 Claude 订阅 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードt

o セッションをリフレッシュします。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
エタンク
/
CCフリート
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
193 コミット 193 コミット .claude-plugin .claude-plugin .github .github cmd/ cc-fleet cmd/ cc-fleet docs docs フック フック 内部 内部 npm npm release リリース スクリプト スクリプト スキル スキル ツール/ サインチェックサム ツール/ サインチェックサム .gitignore .gitignore .goreleaser.yaml .goreleaser.yaml AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md LICENSE LICENSE Makefile Makefile README.md README.md go.mod go.mod go.sum go.sum install.ps1 install.ps1 install.sh install.sh すべてのファイルを表示 リポジトリ ファイル ナビゲーション
🤖 メイン セッションの認証はそのままで、DeepSeek · GLM · Kim · Qwen … から Codex サブスクリプションまで、あらゆるサードパーティ モデルを Claude Code の ⚙️ ダイナミック ワークフロー、👥 エージェント チーム、および ⚡ サブエージェントに接続します。プロバイダーで完全なクロード コードを実行するためにクロード サブスクリプションは必要ありません 🚀
Claude Code のマルチエージェント オーケストレーション (ダイナミック ワークフロー、エージェント チーム、サブエージェント) は、Anthropic 独自のモデルのみを実行します。 cc-fleet を使用すると、Anthropic または OpenAI 互換の API を備えたあらゆるモデル (Codex サブスクリプションも含む) をワークフロー リーフ、長期存続チームメイト、またはネイティブ クロード エージェントと同じ ID と機能を備えたメイン セッションによってスケジュールされたワンショット サブエージェントとして参加させることができます。
すべてのサードパーティ ワーカーは、LLM バックエンドがプロバイダーにスワップされた実際のクロード プロセスであるため、クロード コードはネイティブ エージェントとまったく同じようにワーカーを駆動します。メインセッション独自の認証 (OAuth

サブスクリプションまたは API キー) は変更されず、プロバイダー キーは env、argv、またはシェル履歴に決して入力されないため、漏洩リスクはゼロです。
開始するための 2 つのステップ: 1 行のインストール、プロバイダーの登録。次に、 /workflow 、 /team 、または /subagent を使用してクロード コードで意図を記述します。または単純な言語でタスクを説明するだけで、残りはクロードが理解します。
クロードのサブスクリプションはありませんか? ccf run <provider> は、そのプロバイダーによって駆動される対話型セッションを開始します。プロバイダーのモデル上で実行されているだけの、ご存知のクロードと同じです。
0. 最初に Claude コードをインストールします — cc-fleet は公式の claude CLI を駆動するため、まだ持っていない場合はインストールします (claude がすでに PATH 上にある場合はスキップしてください)。
カール -fsSL https://claude.ai/install.sh |バッシュ
Windows (PowerShell):
irm https://claude.ai/install.ps1 |アイエックス
1. 1 行スクリプトを使用して cc-fleet をインストールします (推奨) — 1 つのコマンドですべてが実行されます。CLI をダウンロードして検証し、PATH に配置し (ccf エイリアスを使用するため、ccf はそれ以降、ccf を起動します)、マーケットプレイス経由で Claude Code プラグイン (スキル + セッション フック) をインストールします。すぐに使用できるようになります:
カール -fsSL https://raw.githubusercontent.com/ethanhq/cc-fleet/main/install.sh |しー
Windows (PowerShell):
irm https://raw.githubusercontent.com/ethanhq/cc - フリート/メイン/install.ps1 |アイエックス
他のチャネル (npm / go install / Releases / source) と Claude Code プラグインの追加、インストーラーのオーバーライド、要件とメンテナンスは、「インストールとメンテナンス」で公開されています。
ccf # インタラクティブ TUI を開きます
CCF ドクター # ヘルスチェック: 依存関係、プロバイダー、プラグインのステータス
ccf update # インストール チャネルによる自己更新 + プラグインの更新
ccf uninstall --all # バイナリとプラグインも削除します
インストールしたら、ccf を実行してプロバイダーを登録し、委任を開始します。
🔌 プロバイダー管理 — 1 つの API キー、接続済み
ccf は TUI を開きます。

プロバイダーの追加を選択します
Anthropic / OpenAI 互換のベンダーを選択してください
API キーとデフォルトのモデルを入力します。オプションでエフォートレベルとクロード権限モード
保存して移動します。さらにモデルを追加してリスト内で切り替えます。s はデフォルトを設定し、d は削除します。
(オプション) Codex を追加: 既存の OAuth を再利用するか、新たにログインします
🖥️ ccf run — 任意のプロバイダーでクロード コードを実行します
ccf run は、デフォルトのプロバイダーで対話型のクロードを起動します。 ccf run <プロバイダ> が 1 つを選択します
すべてのツール、完全な REPL ; Anthropic サブスクリプションなし、メイン セッションのログインは変更されません
セッション中にいつでも切り替えることができます: モデルの場合は /model、思考努力の場合は /effort、許可モードの場合は Shift+Tab
⚙️ ダイナミック ワークフロー — ネイティブ ワークフローと同じオーケストレーション API
/workflow で開始するか、クロードに「各モジュールをディープシークでマッピングし、glm がモジュールごとに監査チェックリストを作成し、gpt が合成する」と指示するだけです。
クロードは JS スクリプトを作成し、バックグラウンドで実行します。メイン セッションからトークンは発生しません。
ワークフローは終了するまでブロックされます - イベント駆動型、ポーリングなし
TUI ボードには、すべてのリーフとフェーズがライブで表示されます。x を押して保持し、r を押して単一のリーフまたはフェーズ全体を再実行します。
👥 エージェント チーム — tmux ペインでのネイティブ マルチエージェント コラボレーション
/team でキックオフするか、Claude に「GLM とディープシークのチームメイトをスポーンして、その強さを比較してください」とだけ伝えてください。
各チームメイトは、サイドの tmux ペインでライブで動作する実際のクロード プロセスです。プロバイダーを 1 つのチームに混在させ、ターンを超えて手作業でフォローアップします。
TUI ボードには、各チームメイトの完全な受信トレイとステータスが表示されます。 h はペインを非表示 / s はペインを表示します — フォアグラウンドで分割またはバックグラウンドで実行します
⚡ サブエージェント — 最も軽い単発委任
/subagent で開始するか、Claude に「kimi、qwen、および glm をこれら 3 つのファイルに対して並行してファンアウトします」と指示するだけです。
クロードはモデルを実行し、結果を収集します。

同期 — 好きなだけ並行してディスパッチします
Slim-ro 読み取り専用モード: コードに触れることなく、プロバイダーがリポジトリを安全に分析できるようにします。
TUI ボードには、各ジョブのプロンプト、回答、支出が表示されます
📊 TUI ボード – フリート全体を 1 つの画面に表示
CCF が起動したら、Tab キーを押してエージェント ボードを表示します。すべてのワークフロー/チーム/サブエージェントがプロジェクト → セッションごとにレイアウトされます。
どれかを開いて詳細を確認します: ワークフローの実行→フェーズ→リーフの進行状況ツリー、チームのチームメイトの受信箱、プロンプト、回答、支出のそれぞれの詳細
ボード内での動作: x / r で停止または再実行、p でクリーンアップに対してピン留め、c で終了をクリア、d で削除、h / s でペインを非表示または表示
終了したチームは記録に残ります。 UI はシステムのライト/ダーク テーマに従います
cc-fleet の機能は 2 つのグループに分類されます。
3 つの委任レーン (ワークフロー / エージェント チーム / サブエージェント) — クロードによって自動的にスケジュールされます。希望を言うと、スキルがレーンとモデルを選択します。手動で選択する必要はありません。
プロバイダーと ccf run — ユーザーが構成して直接使用するツール。
エージェントボード: フェーズ → リーフツリー
1 つのリーフにドリルダウン: 完全なプロンプトと合成出力
オーケストレーション API : マルチフェーズ オーケストレーションは、Claude Code のネイティブ ワークフロー ツールと同一の API を備えた JavaScript ファイル内に存在します。つまり、agent() がノードを開始し、Parallel() がファンアウトし、pipeline() がフローをチェーンします。 1 つの違いは、agent() はプロバイダー オプションを使用して各ノードのモデルを割り当てるため、1 回の実行内で異なるベンダーが混在して並行して実行されることです。
const メタ = {
名前: "API 監査" 、
説明 : "エンドポイントをマップし、監査チェックリストの草案を作成する" ,
フェーズ: [ { タイトル : "マップ" } 、 { タイトル : "ビルド" } 、 { タイトル : "ジャッジ" } ] 、
} ;
フェーズ (「マップ」) ;
const マップ = ( 並列待機 (
[ "認証" 、 "請求" 、 "ユーザー" ] 。マップ ( ( m ) =>
( ) => エージェント ( "エクスポートされたリストをリストする

モジュール内のエンドポイント " + m , { プロバイダー : "ディープシーク" } ) )
) ) 。フィルター (ブール値) ;
フェーズ (「ビルド」) ;
const checklists = await パイプライン ( マップ ,
( endpoints , _ , i ) => エージェント ( "監査チェックリストの草案を作成します:\n" + endpoints ,
{ プロバイダー : "glm" 、ラベル : "build:" + i } ) ;
フェーズ (「裁判官」) ;
const verdict = await Agent ( "最も強いものを選んでその理由を述べてください:\n" + checklists . join ( "\n---\n" ) ,
{ プロバイダー : "クロード" 、モデル : "opus" 、ラベル : "judge" } ) ;
return {チェックリスト、判定} ;
実行と管理: 開始されると、実行全体がバックグラウンド エンジンで実行され、小さなコマンド セットによって管理されます。実行はコンテンツ ハッシュによって記録され、予算は USD またはトークンでの支出の上限を設定します。
RUN= $( ccf workflow run Audit.js ) # バックグラウンドで開始し、実行 ID を出力します
ccf ワークフロー wait " $RUN " --timeout 完了するまで 10m # ブロック、イベント駆動型
ccf ワークフロー stop " $RUN " --leaf build:1 # 単一のリーフを保持します (実行は継続します)
ccf ワークフロー再起動 " $RUN " --leaf build:1 # 再開します
ccf ワークフロー run Audit.js --resume " $RUN " # ジャーナルを再生し、終了した葉はキャッシュにヒットします
保持と再起動: ccf workflow stop --leaf / --phase は実行に失敗しません。指定されたノードが一時停止されるだけです。再起動して再度実行するまで、実行は継続し、他のノードは続行します。現在実行中のすべてのノードが一時停止されると、実行全体が停止状態になり、続行する前に介入する必要があります。
ポーリングせずに待機する: ccf ワークフローは実行が終了するまでブロックし、その後終了します。バックグラウンドにドロップし、終了時に戻ってきます。繰り返しのチェックはありません。終了コードは結果を示します: 0 成功、1 失敗、3 駐車して待機中、124 待機タイムアウト (実行は継続中)、130 中断。
Budget-adaptive : Budget.spent() / Budget.remaining() はスクリプト内でライブで読み取り可能です (USD)

またはトークン)、ワークフローは別のバッチをディスパッチするかどうかを自分で決定できます。
独自のサブスクリプションの混合: プロバイダーが割り当てられたノード: 「claude」は独自の Claude ログインで実行されます。上記のジャッジは、合成と仕上げのノードに適したプロバイダー キーではなく、サブスクリプションを使用します。
4 人のチームメイトが tmux ペインで並んで作業する
チームメイトの概要/メッセージ/ボード上の出力
前提条件: エージェント チームは事前にセットアップが必要な唯一のレーンであり、tmux に依存しているため、まだ Windows をサポートしていません。使用する前の 2 つの条件:
tmux セッション内 ( tmux new-session -s work ) で、チームメイト ペインが横に表示されるようにします。
Claude Code のエージェント チームを有効にする: 初めて ccf を実行すると、これがオンになっていないことが検出され、書き込むよう提案されます。または、自分で ~/.claude/settings.json に追加します。
{ "環境" : { "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" : " 1 " } }
彼らがどのように協力するか: チームメイトはそれぞれ真のクロードプロセスです。クロードはネイティブ TeamCreate でチームを構築し、ネイティブ SendMessage で作業を割り当てます。チームメイトはターンをまたいで生存し続けるため、タスクを追加し続けることができます。 1 つのチームが複数のプロバイダーを同時に使用し、その後 1 人のチームメイトに結果を収集して比較してもらうことができます。
権限の継承 : 各チームメイトは、メイン セッションの権限の状態 (計画 / acceptEdits / デフォルト) を継承します。それが検出できない場合は、最も安全なデフォルトに戻り、

[切り捨てられた]

## Original Extract

🚢 Run Claude Code's ⚙️ Dynamic Workflows, 👥 Agent Teams & ⚡ Subagents on any third-party model — DeepSeek · GLM · Kimi · Qwen … or your Codex subscription. No Anthropic subscription needed. ｜ 🚢 让 Claude Code 的 ⚙️ Dynamic Workflow、👥 Agent Team、⚡ Subagent 用上任意第三方模型 — DeepSeek · GLM · Kimi · Qwen…… 或你的
[truncated]

GitHub - ethanhq/cc-fleet: 🚢 Run Claude Code's ⚙️ Dynamic Workflows, 👥 Agent Teams & ⚡ Subagents on any third-party model — DeepSeek · GLM · Kimi · Qwen … or your Codex subscription. No Anthropic subscription needed. ｜ 🚢 让 Claude Code 的 ⚙️ Dynamic Workflow、👥 Agent Team、⚡ Subagent 用上任意第三方模型 — DeepSeek · GLM · Kimi · Qwen…… 或你的 Codex 订阅,无需 Claude 订阅 · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
ethanhq
/
cc-fleet
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
193 Commits 193 Commits .claude-plugin .claude-plugin .github .github cmd/ cc-fleet cmd/ cc-fleet docs docs hooks hooks internal internal npm npm release release scripts scripts skills skills tools/ sign-checksums tools/ sign-checksums .gitignore .gitignore .goreleaser.yaml .goreleaser.yaml AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md LICENSE LICENSE Makefile Makefile README.md README.md go.mod go.mod go.sum go.sum install.ps1 install.ps1 install.sh install.sh View all files Repository files navigation
🤖 Plug any third-party model into Claude Code's ⚙️ Dynamic Workflows, 👥 Agent Teams, and ⚡ Subagents — from DeepSeek · GLM · Kimi · Qwen … to your Codex subscription, with your main session's auth untouched; no Claude subscription needed to run a full Claude Code on any provider 🚀
Claude Code's multi-agent orchestration — Dynamic Workflows, Agent Teams, Subagents — only runs Anthropic's own models. cc-fleet lets any model with an Anthropic- or OpenAI-compatible API, even your Codex subscription, join as a workflow leaf, a long-lived teammate, or a one-shot subagent — scheduled by your main session, with the same identity and capabilities as a native Claude agent .
Every third-party worker is a real claude process with its LLM backend swapped to the provider, so Claude Code drives it exactly like a native agent. Your main session's own auth (OAuth subscription or API key) is untouched, and provider keys never enter env, argv, or shell history — zero leak risk .
Two steps to get going : one-line install, register a provider. Then state your intent in Claude Code with /workflow , /team , or /subagent — or just describe the task in plain language, and Claude figures out the rest.
No Claude subscription? ccf run <provider> starts an interactive session driven by that provider — the same claude you know , just running on the provider's model.
0. Install Claude Code first — cc-fleet drives the official claude CLI, so install it if you don't have it yet (skip if claude is already on your PATH):
curl -fsSL https://claude.ai/install.sh | bash
Windows (PowerShell):
irm https: // claude.ai / install.ps1 | iex
1. Install cc-fleet with the one-line script (recommended) — one command does it all: downloads and verifies the CLI, puts it on your PATH (with a ccf alias, so ccf launches it from then on), and installs the Claude Code plugin (skill + session hook) via the marketplace. Ready to use right after:
curl -fsSL https://raw.githubusercontent.com/ethanhq/cc-fleet/main/install.sh | sh
Windows (PowerShell):
irm https: // raw.githubusercontent.com / ethanhq / cc - fleet / main / install.ps1 | iex
Other channels (npm / go install / Releases / source) and adding the Claude Code plugin, installer overrides, and requirements & maintenance live in Install & maintenance .
ccf # open the interactive TUI
ccf doctor # health check: dependencies, providers, plugin status
ccf update # self-update by install channel + refresh the plugin
ccf uninstall --all # remove the binary and plugin too
Once installed, run ccf to register a provider and start delegating.
🔌 Provider management — one API key, connected
ccf opens the TUI ; choose Add provider
Pick any Anthropic / OpenAI-compatible vendor
Enter the API key and default model ; optionally an effort level and a Claude permission mode
Save and go ; add more models and toggle them in the list, s sets the default, d deletes
(Optional) Add Codex : reuse an existing OAuth, or log in fresh
🖥️ ccf run — run Claude Code on any provider
ccf run launches an interactive claude on your default provider ; ccf run <provider> picks one
All tools, the full REPL ; no Anthropic subscription, your main session's login untouched
Switch anytime mid-session : /model for the model, /effort for thinking effort, Shift+Tab for permission mode
⚙️ Dynamic Workflows — the same orchestration API as native workflows
/workflow to kick it off, or just tell Claude : "map each module with deepseek, glm drafts an audit checklist per module, gpt synthesizes"
Claude writes the JS script and runs it in the background — no tokens off your main session
workflow wait blocks until it finishes — event-driven, no polling
The TUI board shows every leaf and phase live — x to hold / r to rerun a single leaf or a whole phase
👥 Agent Teams — native multi-agent collaboration in tmux panes
/team to kick it off, or just tell Claude : "spawn a glm and a deepseek teammate, then compare their strengths"
Each teammate is a real claude process working live in a side tmux pane — mix providers in one team, hand follow-ups across turns
The TUI board shows each teammate's full inbox and status ; h hides / s shows a pane — split in the foreground or run in the background
⚡ Subagents — the lightest one-shot delegation
/subagent to kick it off, or just tell Claude : "fan out kimi, qwen, and glm over these three files in parallel"
Claude runs the models and collects results synchronously — dispatch as many in parallel as you like
slim-ro read-only mode : let a provider analyze your repo safely, without touching code
The TUI board shows each job's prompt, answer, and spend
📊 The TUI board — your whole fleet on one screen
After ccf launches, press Tab for the Agents Board — every Workflow / Team / Subagent laid out by project → session
Open any one for detail : a Workflow's run → phase → leaf progress tree, a Team's teammate inbox, each drilling into prompt, answer, and spend
Act inside the board : x / r to stop or rerun, p to pin against cleanup, c to clear finished, d to delete, h / s to hide or show a pane
Finished teams stay on record ; the UI follows your system light / dark theme
cc-fleet's capabilities fall into two groups:
Three delegation lanes (Workflow / Agent Team / Subagent) — scheduled automatically by Claude: say what you want and the skills pick the lane and the model for you, no manual choice.
Provider and ccf run — tools you configure and use directly.
Agents Board: the phase → leaf tree
drill into one leaf: full prompt and synthesized output
Orchestration API : multi-phase orchestration lives in a JavaScript file, with an API identical to Claude Code's native Workflow tool — agent() starts a node, parallel() fans out, pipeline() chains a flow. The one difference is that agent() takes a provider option to assign each node's model, so different vendors mix and run in parallel within a single run:
const meta = {
name : "api audit" ,
description : "map endpoints, then draft audit checklists" ,
phases : [ { title : "map" } , { title : "build" } , { title : "judge" } ] ,
} ;
phase ( "map" ) ;
const maps = ( await parallel (
[ "auth" , "billing" , "users" ] . map ( ( m ) =>
( ) => agent ( "List the exported endpoints in module " + m , { provider : "deepseek" } ) )
) ) . filter ( Boolean ) ;
phase ( "build" ) ;
const checklists = await pipeline ( maps ,
( endpoints , _ , i ) => agent ( "Draft an audit checklist:\n" + endpoints ,
{ provider : "glm" , label : "build:" + i } ) ) ;
phase ( "judge" ) ;
const verdict = await agent ( "Pick the strongest one and say why:\n" + checklists . join ( "\n---\n" ) ,
{ provider : "claude" , model : "opus" , label : "judge" } ) ;
return { checklists , verdict } ;
Running and managing : once kicked off, the whole run executes in a background engine, managed by a small command set. Runs are journaled by content hash, and budgets cap spend in USD or tokens:
RUN= $( ccf workflow run audit.js ) # starts in the background, prints the run id
ccf workflow wait " $RUN " --timeout 10m # blocks until done, event-driven
ccf workflow stop " $RUN " --leaf build:1 # hold a single leaf (run keeps going)
ccf workflow restart " $RUN " --leaf build:1 # resume it
ccf workflow run audit.js --resume " $RUN " # replay the journal, finished leaves hit cache
Holding and restarting : ccf workflow stop --leaf / --phase doesn't fail the run — it just pauses the named node: the run keeps going and other nodes carry on, until you restart it to run again. If every node currently running gets paused, the whole run settles into a parked state that needs you to step in before it continues.
Wait without polling : ccf workflow wait blocks until the run ends, then exits — drop it in the background and come back when it exits, no repeated checking. The exit code states the outcome: 0 succeeded, 1 failed, 3 parked and waiting, 124 wait timed out (run still going), 130 interrupted.
Budget-adaptive : budget.spent() / budget.remaining() are readable live inside the script (USD or tokens), so a workflow can decide for itself whether to dispatch another batch.
Mixing in your own subscription : a node assigned provider: "claude" runs on your own Claude login — the judge above uses your subscription, not a provider key, which suits a synthesis-and-finish node.
four teammates working side by side in tmux panes
a teammate's overview / messages / output on the board
Prerequisites : Agent Team is the only lane that needs setup up front, and because it relies on tmux it doesn't support Windows yet . Two conditions before you use it:
Be inside a tmux session ( tmux new-session -s work ) so teammate panes can show up alongside you;
Enable Claude Code's agent-teams : the first time you run ccf it detects this isn't on and offers to write it in for you — or add it once yourself to ~/.claude/settings.json :
{ "env" : { "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" : " 1 " } }
How they collaborate : each teammate is a real claude process; Claude builds the team with native TeamCreate and assigns work with native SendMessage , and teammates stay alive across turns so you can keep adding tasks. One team can use several providers at once, then have one teammate gather and compare the results.
Permission inheritance : each teammate inherits your main session's permission posture (plan / acceptEdits / default). If that can't be detected, it falls back to the safest default and wo

[truncated]
