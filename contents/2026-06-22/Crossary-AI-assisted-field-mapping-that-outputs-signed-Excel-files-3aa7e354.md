---
source: "https://www.crossary.com"
hn_url: "https://news.ycombinator.com/item?id=48624501"
title: "Crossary – AI-assisted field mapping that outputs signed Excel files"
article_title: "Crossary — AI Source-to-Target Data Mapping"
author: "migueljpalmeida"
captured_at: "2026-06-22T01:48:55Z"
capture_tool: "hn-digest"
hn_id: 48624501
score: 2
comments: 0
posted_at: "2026-06-22T01:25:56Z"
tags:
  - hacker-news
  - translated
---

# Crossary – AI-assisted field mapping that outputs signed Excel files

- HN: [48624501](https://news.ycombinator.com/item?id=48624501)
- Source: [www.crossary.com](https://www.crossary.com)
- Score: 2
- Comments: 0
- Posted: 2026-06-22T01:25:56Z

## Translation

タイトル: Crossary – 署名付き Excel ファイルを出力する AI 支援フィールド マッピング
記事のタイトル: Crossary — AI ソースからターゲットへのデータ マッピング
説明: Crossary は、すべてのソースからターゲットへのマッピングを、そのベースとなっている逐語的なソース引用と自己評価による信頼度を使用して提案します。また、何も当てはまらない場合は推測する代わりに棄権します。 Excel、JSON、XML、PDF、CSV。ラウンドトリップするレビュー済みワークブックをエクスポートします。

記事本文:
Crossary — AI ソースからターゲットへのデータ マッピング Crossary 仕組み 仕組み 仕組みを確認する 信頼価格 ログイン 無料で始める 無料で始める ソース → ターゲット フィールドのマッピング、確認済み 間違ったマッピングは、正直なギャップよりも悪いです。
Crossary は、乱雑なソースとターゲットの仕様を、レビュー済みの実装準備ができたマッピング ワークブックに変換します。すべての行は正確なソース引用に裏付けられ、不確実な部分にはフラグが付けられます。データではなく仕様をマッピングします。サインオフするまでは何も進みません。
レビューされた行 3 の統合クレジットを参照してください。無料、カードなし、マッピングは共有モデルをトレーニングしません。
他の人が実装するマッピング シートを手動で作成する場合、これはあなたのものになります。
厳密な 5 段階のパイプライン。
それから往復です。
5 つのステップで署名付きワークブックにアップロードし、メモを失うことなく再度アップロードします。
ソースとターゲットの仕様を入力します。ファイルを完全に読み取れない場合は、ファイルがどれだけ減少したかを正確に示します。
xlsx · pdf · csv · json · xml · xsd · sql · yaml
フィールド インベントリは両側 (すべてのパス、すべての列) から抽出されるため、実際の表面に対してマッピングします。
ターゲットごとに、提案された情報源、種類、逐語的な証拠、推論、信頼度。自信がないときは棄権する。
構造的および基数のブロッカーの決定論的チェック。 AI ゼロ、支出ゼロ。
署名された .xlsx は、チームがどこでも開くことができ、戻ってきたときにきれいに調整されます。
各行からわかる 6 つのこと。
信仰に基づくものは何もありませんでした。
提案されたマッピングを開くと、AI がそれを提案した理由と、それがどれほど確実であるかがわかります。自信ではなく証拠が製品です。
推測できない正直なギャップ —
そしてそれが成立する往復。
エンドツーエンドの実際のサンプル: AI が魅力的な推測を撤回して代わりに質問し、編集内容が Excel を往復しても保存されます。
確信がないときは、

そうそう。
マッピング メモリ マッピングを承認するたびに、次のマッピングが高速になります。
エクスポートをサインオフすると、承認されたフィールド ペアがプライベートのワークスペース スコープのライブラリになります。次回の実行では、AI が棄権したターゲットのみのギャップを埋めます。これは、まだレビューする提案として挿入され、自動適用されることはありません。
スコープは自分のワークスペースです。別のワークスペースにまたがることはありません。
決定のみを記録し、証拠、価値観、顧客データは記録しません。
共有モデルをトレーニングすることはありません。
ここでは信頼は求められていません。信頼はコードで強制され、書き留められます。
取り込み中にソースの約 10% がドロップされた場合、アプリ内およびエクスポート カバー シート上でフラグが付けられたままになります。部分的な読み取りは完了として報告されません。
検証は決定的であり、構造とカーディナリティのみをチェックします。意味上の正確性を主張するものではありません。UI はまさにそれを示しています。
再インポートすると、一致した内容が適用され、変更された内容がスキップされ、すべての査読者のメモが追跡された質問に変わります。何も黙って上書きされることはありません。
すべての AI 応答は、データにアクセスする前に厳密なスキーマに照らしてチェックされます。不正な結果は破棄され、永続化されません。
ソース仕様とターゲット仕様からの相互マップ (スキーマ、辞書、ガイド)。データではなく仕様に基づいて動作するように構築されているため、ほとんどの場合、製造記録や PII をアップロードする必要はありません。
無料で始めましょう。 AI の実行に対して支払います。レビューに対しては決して支払いません。
レビュー、検証、エクスポート、往復の再インポートは AI を呼び出さないため、どのプランでも常に無料です。
初めての実際の統合のために。
無料のレビュー、検証、エクスポート
3 つのワークスペース · 最大 3 人のメンバー
無制限のワークスペース · 最大 10 人のメンバー
足りなくなったら？既存の作業がブロックされることはありません。レビュー、検証、エクスポート、再インポートは無料のままです。未使用のクレジットは繰り越され、いつでも追加購入したり、アップグレードしたりできます

アデ。新しい AI のみ実行が一時停止されます。
サインアップする前に、正直に答えます。
およそ 1 つの標準統合。精度が高く、異常に大規模または頻繁に再実行されるジョブでは使用量が多くなり、各統合にかかるコストがわかります。
レビュー、検証、エクスポート、および往復の再インポート。彼らはAIを呼び出さないので、クレジットを使用することはありません。
すべてのプランで同じコア マッピング品質が得られます。 Higher Accuracy (オプションのより重いパス) は有料プランにあります。マッピングメモリはすべてのプランに含まれています。
既存の作業がブロックされることはありません。レビュー、検証、エクスポート、再インポートは無料のままです。未使用のクレジットは繰り越され、いつでも追加購入したり、アップグレードしたりできます。新しい AI のみ実行が一時停止されます。
希望するプランの [購読] をクリックすると、安全なチェックアウトが行われ、プランはすぐに有効になります。
いいえ。マッピング メモリのスコープはワークスペース内であり、ワークスペースを越えることはありません。共有モデルはマッピングから学習しません。
最初の統合をマッピングする
次の10分以内に。
ソース仕様とターゲット仕様をアップロードします。レビューされ、証拠に裏付けられたワークブックを取り戻します。すべての正直なギャップにフラグが付けられます。
3 つの統合クレジットは無料、カードなし、ロックインなし。
Crossary フィールド マッピングは、その動作を示し、推測ではなくギャップにフラグを立てます。
往復エクスポートは署名されたワークブックであり、ロックされたアプリ ビューではありません。
それを開発者またはクライアントに渡します。 Excel、Google Sheets、または Numbers で編集し、決定事項を変更し、メモを追加してから、再インポートします。 Crossary は一致したものを適用し、下に移動した行を上書きするのではなくスキップし、すべてのメモを追跡された質問に変換します。
署名付き .xlsx をエクスポート — すべての行に安定した ID がスタンプされます。
Excel、Google Sheets、Numbers のどこでも編集できます。Crossary へのログインは必要ありません。
再インポート 一致したものを適用し、移動したものをスキップし、メモを失うことはありません。
名前とセマンティクスは一致します。唯一の変更は文字列→daです

キャストしてください。ソース文字列が整形式の ISO-8601 であると想定しており、レビュー用にフラグが立てられています。
以前に承認したペアから再利用されます。これは、AI が回避するギャップを埋め、依然として承認を待ちます。
すべてをエクスポートしたり、レコードごとに削除したりできます。いつでもセルフサービスで実行できます。 GDPR の移植性と消去はサポート チケットではなく組み込まれています。
仕様はジョブを実行するためにのみ AI に送信されます。不正行為チェックのために一時的に保持された後、削除され、共有モデルのトレーニングには決して使用されません。
副処理者の完全なリストは公開されており、変更される前に通知されます。データがどこに行くかについては何も秘密ではありません。
プライバシーとデータに関する全規約を読む→

## Original Extract

Crossary proposes every source-to-target mapping with the verbatim source quote it's based on and a self-rated confidence — and abstains instead of guessing when nothing fits. Excel, JSON, XML, PDF, CSV. Export a reviewed workbook that round-trips.

Crossary — AI Source-to-Target Data Mapping cross ary How it works See it work Trust Pricing Log in Start free Start free Source → target field mapping, reviewed A wrong mapping is worse than an honest gap.
Crossary turns messy source and target specs into a reviewed, implementation-ready mapping workbook — every row backed by its exact source quote, every uncertain one flagged for you. It maps the spec, not the data. Nothing moves until you sign off.
See a reviewed row 3 integration credits free · no card · your mappings never train a shared model.
If you hand-build the mapping sheet someone else implements — this is for you.
A strict five-stage pipeline.
Then a round-trip.
Upload to signed workbook in five steps — and back again, without losing a note.
Drop in your source + target specs. If a file can't be fully read, it tells you exactly how much it dropped.
xlsx · pdf · csv · json · xml · xsd · sql · yaml
A field inventory is extracted from both sides — every path, every column — so you map against the real surface.
For each target: a proposed source, type, verbatim evidence, reasoning, and confidence. Abstains when unsure.
A deterministic check for structural & cardinality blockers. Zero AI, zero spend.
A signed .xlsx your team can open anywhere — that reconciles cleanly when it comes back.
Six things every row tells you.
Nothing taken on faith.
Open any proposed mapping and you see exactly why the AI suggested it — and how sure it says it is. The evidence, not the confidence, is the product.
The honest gap it won't guess —
and the round-trip that holds.
The real sample, end to end: where the AI withdraws a tempting guess and asks instead, and where your edits survive a trip through Excel and back.
When it isn't sure, it says so.
Mapping memory Every mapping you approve makes the next one faster.
Sign off an export and your approved field-pairs become a private, workspace-scoped library. On the next run it gap-fills only the targets the AI abstained on — inserted as suggestions you still review, never auto-applied.
Scoped to your workspace — it never crosses to another.
Captures the decision only — no evidence, values, or client data.
It never trains a shared model.
Trust isn't asked for here — it's enforced in code, and written down.
If more than ~10% of a source is dropped during ingestion, it stays flagged — in the app and on the export cover sheet. A partial read is never reported as complete.
Validation is deterministic and checks structure and cardinality only. It does not claim semantic correctness — and the UI says exactly that.
On re-import it applies what matched, skips what changed underneath you, and turns every reviewer note into a tracked question. Nothing is silently overwritten.
Every AI response is checked against a strict schema before it can touch your data. A malformed result is thrown away, never persisted.
Crossary maps from your source and target specs — schemas, dictionaries, guides. It's built to work from the spec, not your data, so in most cases there's no need to upload production records or PII.
Start free. Pay for AI runs, never for reviewing.
Reviewing, validating, exporting, and round-trip re-import don't call the AI — so they're always free, on every plan.
For your first real integration.
Free review, validate & export
3 workspaces · up to 3 members
Unlimited workspaces · up to 10 members
Run out? Existing work is never blocked — review, validate, export & re-import stay free. Unused credits roll over, you can buy more anytime, or upgrade. Only new AI runs pause.
The honest answers, before you sign up.
Roughly one standard integration. Higher Accuracy and unusually large or heavily re-run jobs use more — and you can see what each integration cost.
Reviewing, validating, exporting, and round-trip re-import. They don't call the AI, so they never use a credit.
Every plan gets the same core mapping quality. Higher Accuracy — an optional, heavier pass — is on paid plans; Mapping Memory is included on every plan.
Existing work is never blocked — review, validate, export and re-import stay free. Unused credits roll over, you can buy more anytime, or upgrade. Only new AI runs pause.
Click subscribe on the plan you want — secure checkout, and your plan activates immediately.
No. Mapping Memory is scoped to your workspace and never crosses it; no shared model learns from your mappings.
Map your first integration
in the next ten minutes.
Upload a source and a target spec. Get back a reviewed, evidence-backed workbook — with every honest gap flagged for you.
3 integration credits free · no card · no lock-in.
cross ary Field mapping that shows its work — and flags the gaps instead of guessing.
the round-trip Export is a signed workbook, not a locked app view.
Hand it to a developer or a client. Edit it in Excel, Google Sheets, or Numbers — change decisions, add notes — then re-import. Crossary applies what matched, skips rows that moved underneath you instead of overwriting them, and turns every note into a tracked question.
Export A signed .xlsx — every row stamped with a stable id.
Edit anywhere In Excel, Google Sheets, or Numbers — no Crossary login required.
Re-import Applies what matched, skips what moved, never loses a note.
Names and semantics align; the only change is a string→date cast. Assumes the source string is well-formed ISO-8601 — flagged for your review.
Reused from a pair you approved before — it fills a gap the AI would otherwise abstain on, and still waits for your sign-off.
Export everything, or delete it per record — any time, self-serve. GDPR portability and erasure are built in, not a support ticket.
Specs go to the AI only to run your job — held briefly for abuse checks, then deleted, and never used to train a shared model.
The full list of subprocessors is published, and we give notice before it changes. Nothing about where your data goes is a secret.
Read the full privacy & data terms →
