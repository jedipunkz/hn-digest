---
source: "https://github.com/Pupok462/open-geo"
hn_url: "https://news.ycombinator.com/item?id=48644674"
title: "Show HN: Open-geo – Skill for Claude Code which shows your AI Visibility"
article_title: "GitHub - Pupok462/open-geo: open-geo — GEO (Generative Engine Optimization) visibility tracker: measure how a brand surfaces in AI answers, with a FastAPI/React dashboard and dark-themed PDF reports. · GitHub"
author: "pupok46"
captured_at: "2026-06-23T13:51:07Z"
capture_tool: "hn-digest"
hn_id: 48644674
score: 1
comments: 0
posted_at: "2026-06-23T13:29:40Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Open-geo – Skill for Claude Code which shows your AI Visibility

- HN: [48644674](https://news.ycombinator.com/item?id=48644674)
- Source: [github.com](https://github.com/Pupok462/open-geo)
- Score: 1
- Comments: 0
- Posted: 2026-06-23T13:29:40Z

## Translation

タイトル: Show HN: Open-geo – AI の可視性を示すクロード コードのスキル
記事のタイトル: GitHub - Pupok462/open-geo: open-geo — GEO (Generative Engine Optimization) 可視性トラッカー: FastAPI/React ダッシュボードとダークテーマの PDF レポートを使用して、AI の回答でブランドがどのように現れるかを測定します。 · GitHub
説明: open-geo — GEO (Generative Engine Optimization) 可視性トラッカー: FastAPI/React ダッシュボードとダークテーマの PDF レポートを使用して、AI の回答でブランドがどのように表面化するかを測定します。 - Pupok462/オープンジオ

記事本文:
GitHub - Pupok462/open-geo: open-geo — GEO (Generative Engine Optimization) 可視性トラッカー: FastAPI/React ダッシュボードとダークテーマの PDF レポートを使用して、AI の回答でブランドがどのように浮上するかを測定します。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
プポク462
/
オープンジオ
公共
いいえ

通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
8 コミット 8 コミット .claude-plugin .claude-plugin .claude .claude .github/ workflows .github/ workflows アセット アセット ダッシュボード ダッシュボード データ データ エンジン エンジン サンプル サンプル フォント フォント i18n i18n パイプライン パイプライン レポート レポート レポート スクリプト スクリプト テスト テスト .gitignore .gitignore ライセンス ライセンスREADME.ar.md README.ar.md README.md README.md README.ru.md README.ru.md README.zh.md README.zh.md conftest.py conftest.py pyproject.toml pyproject.toml要件.txt要件.txt すべてのファイルを表示 リポジトリ ファイルのナビゲーション
英語 · Русский · 中国語 · العربية
open-geo — クロード コード用の GEO 可視性トラッカー
open geo は、すべての主要なエンジンにわたって、AI 回答内でブランドがどの程度認知されているかを測定します。
検索は「10 個の青いリンク」から生成された答えに移行しています: ChatGPT、Perplexity、Gemini、
クロード、Google AI 概要、Yandex、DeepSeek。それぞれの答えはいくつかの情報源に基づいています。
AI の可視性もその 1 つです。 open-geo は、実際のエンジンを通じてクエリを実行します。
ログインしたブラウザで、ドメインがソースに含まれるかどうかを記録します。
引用 、テキストへの組み込み、およびブランドがその際にどのように語られるか。
API ではなく人間のように答えを読み取ります。キャプチャは Chrome のクロードを通じて実行されます。
実際のログインしたブラウザ — レンダリングされた AI の回答 (ソース パネルとインライン ブラウザ) が表示されます。
引用チップ）、ドメインを正規化し、クエリごとに 1 つの検証済みレコードを出力します。脆くない
表面を削るエンジンは決して安定を保つとは約束されていません。
バニティスコアではなく、可視性ファネルです。ファネルとしてネストされた 6 つの指標 — 回答 →
酸っぱい

ces → 引用 — さらに、定性的なセンチメントの読み取りとトップドメインのリーダーボード (あなたの
ブランドは、回答内の他のすべてのドメインに対してランク付けされます）。複合インデックスも作成もありません
シェアオブボイスインデックス。すべての数値は、 Pipeline/INTERFACES.md で監査できます。
ローカルファーストのマルチブランド時系列。ローカル SQLite (WAL) データベースにランドをキャプチャするため、
ブランドごと、エンジンごとの履歴とランオーバーランデルタを構築します。成果物はテーマ別の PDF です。
4 言語スイッチャーを備えた FastAPI + React ダッシュボード。データがマシンから離れることはありません。
GEO / SEO コンサルタント — ブランドの AI に対する答えを実際に古いまま読んでピッチに参加します
「AI 検索は重要です、信じてください」ではなく、可視性を重視します。
ブランドの社内成長 / SEO — AI 回答における独自ドメインの存在を長期にわたって追跡します。
クエリ レンズ (一般/ブランド/比較) によって分割し、週ごとの変動を把握します。
創設者と開発者はすでに Claude Code を使用しています。これは単なるスキルです。/open-geo で CSV と
ドメイン、ダッシュボードを取得します。 SaaS もアップロードもアカウントもありません。
AI 回答のキャプチャ — クエリのリストは、実際にログインした環境でエンジンを通じて実行されます。
ブラウザでターゲット ドメインがどのように表示されるかが記録され、クエリごとに 1 つの検証済みレコードが記録されます。
6 つの指標 + 定性的センチメント — 可視性ファネル (回答 → ソース → 引用):
報道範囲、可視率、出典と引用の平均最適位置に加えて、
出典→引用の変換 (相対引用) と、それぞれの変換方法に関する短いフリーテキストのメモ
答えはブランドを扱います。ダッシュボードと PDF には、レンズごとの定性的なセンチメントも表示されます
クエリごとのメモから合成された概要 (「メトリクス」を参照)。
トップドメイン（競合他社）のリーダーボード — あなたの情報から一般化された平均掲載順位の指標
回答内のすべてのドメインに対するブランドを、出現頻度によってランク付けします (

その平均
出典/引用位置)。正直な「あなたの回答スペースを共有する人」 - ブランドのライバルと
出版社も同様に、並べ替え可能なダッシュボード パネルと PDF セクションとしてあなたのブランドが強調表示されます。いいえ
追加のキャプチャ: すでに収集したデータから計算されるため、過去の実行にも機能します。
SQLite マルチブランド時系列 - すべての実行は data/aeo.db (SQLite、WAL) に保存されます。
そのため、ブランドとエンジンごとに履歴が蓄積され、ランオーバーランデルタが得られます。
4 つの言語スイッチャーを備えたダッシュボード — 英語、Русский、中文、العربية (RTL 対応) —
FastAPI 読み取り専用 API + ライト/ダーク テーマとメトリクスごとのツールチップを備えた Vite/React フロントエンド。
PDF レポート — 自己完結型のテーマ別 A4 レポート (ReportLab + matplotlib)、ヘッドレスなし
Chrome とシステム ライブラリは必要ありません。
open-geo はクロード コードのスキルです。これは、大量のファイルからではなく、クロードとのチャットから実行されます。
シェルコマンド。全体のセットアップは次のとおりです。クローンを作成し、クロードにインストールするように依頼し、それをコマンドとして使用します。
リポジトリのクローンを作成します (または、Claude に URL を指定するだけです)。
クロードにセットアップを依頼してください。そのフォルダー内の Claude Code セッションで、次のように言います。
open-geo をセットアップし ( scripts/setup.sh を実行)、Google で example.com (ブランド「Example」) を追跡します。
Examples/questions.csv を使用します。
Claude はインストールとキャプチャを実行し、ダッシュボードのリンクと概要を出力します。
または、インストール後にコマンドとして直接実行します。
/open-geo Examples/questions.csv google example.com --brand " Example " --n-worker 3 --output 両方
Examples/questions.csv はプレースホルダーです — 架空のブランドの質問セットです。
最初の実行はすぐに機能します。実際に読む場合は、独自のクエリを置き換えてください。質問セットは次のとおりです。
中心となるインプット — 何を測定するかを決定し、レポートの質は質問に応じて決まります。
あなたは尋ねます。形式とその選択方法:

どのような入力が必要ですか? 。
スケジュールに従って追跡します。クロード コードの /loop でコマンドをラップして、
間隔をあけてドリフトを観察します — 例:毎週の読書:
/loop 1w /open-geo Examples/questions.csv google example.com --brand " Example " --output 両方
Claude があなたの代わりにできないことの 1 つは、Claude-in-Chrome 拡張機能を接続し、ログを記録することです。
追跡したい市場にブラウザでアクセスします。そのログイン セッションがキャプチャの原動力となります。
すべては 1 つのオペレーター コマンド (/open-geo スキル) によって実行されます。あなたは触れません
Python: クロードは、キャプチャ→メトリクス→成果物を調整し、ダッシュボードや PDF を渡します。
/open-geo <questions.csv> <エンジン> <ドメイン> --brand "<名前>" --n-worker <N> \
[--出力ダッシュボード|pdf|両方] [--今日の期間|すべて] [--lang en|ru|zh|ar]
引数
意味
<質問.csv>
列 query,lens を含む CSV、ここで、lens ∈ general |ブランド |比較。準備ができたサンプル: example/questions.csv 。
<エンジン>
どの AI エンジンを追跡するか (例: google )。同じスロットは、 Engines/ の下にキャプチャ プレイブックを持つ任意のエンジンを受け取ります。
<ドメイン>
ターゲット ドメイン (任意のスペル: https://www.example.com 、 example.com - 自動的に正規化されます)。
--ブランド「<名前>」
人間のブランド名 (レポート/ダッシュボードのタイトルと概要で使用されます)。
--n-worker <N>
並行して実行されるキャプチャ ワーカーの数 - 実行の同時実行数。
--出力
ダッシュボード (デフォルト) | PDF |両方 。
--ピリオド
all (デフォルト — 完全なブランド + エンジン履歴、デルタを有効にする) |今日（今回の実行のみ）。
--lang
成果物の UI 言語 — en (デフォルト) |る | zh | ar 。
エンドツーエンドでの動作: 実行を作成 → 並列キャプチャ ワーカー間でクエリを分割します。
(それぞれがログインしている Chrome のエンジンを駆動し、クエリごとに 1 つの検証済みレコードを返します) →
それらを一元的に取り込んでスコアリング → ダッシュボードを出力し、

/または PDF → から短い概要を印刷します
クロスレンズ全列。 /loop で再実行して、時間の経過に伴うドリフトを追跡します。
トラッカー全体は /open-geo コマンドによって調整されます。
Capture Playbook — エンジンごとの Playbook (engines/<engine>.md) は次によって駆動されます。
Claude-in-Chrome は、ログイン済みの Chrome に表示されます。レンダリングされた AI の回答を次のように読み取ります。
LLM は、ソース パネルとインライン引用チップを拡張し、ドメインを正規化し、出力します。
クエリごとに 1 つの QueryCapture オブジェクト。
QueryCapture — 検証されたキャプチャ コントラクト (Pydantic v2; の権威ある仕様)
パイプライン/INTERFACES.md )。
インジェスト / スコア — ワーカーはキャプチャのみです。各ワーカーはビルドして自己検証します。
レコード (読み取り専用) を作成し、オーケストレーターに返します。オーケストレーター (スキル)
すべての DB 書き込みを所有します。ワーカーが返すときに各チャンクを増分的に取り込みます。
実行中にクラッシュしても、キャプチャした作業が失われることはありません - 実行を終了し、レンズごとのメトリクスを計算します
さらにすべての行。
ダッシュボード / PDF — オーケストレーターは最後に、保存されているファイルから成果物を出力します。
メトリクスと短い概要 (ダッシュボード サーバーは、すべてのキャプチャが入った後にのみ起動されます)。
パイプラインはエンジンに依存しません。エンジンはエンドツーエンドのオープン ID (コントラクト、DB、CLI、
ダッシュボード、レポート)、新しいエンジンのサポートは主に新しい Engines/<engine>.md プレイブックです —
Engines/README.md を参照してください。
分かりやすく言うと漏斗です。 4 つのカウントは各ステップで絞り込まれます。
クエリ — 入力した質問 (CSV)。
AI の概要 — エンジンが実際に AI 回答を生成したクエリ (実際には AI 回答は生成されません)
常に - 欠席は失敗ではなく有効なデータです)。
ソースでは、そのうち、ソースの中にあなたのドメインが含まれているクエリが答えとなります。
描きました。
引用 — そのうち、回答内でドメインが実際にリンク/引用されているクエリ
テキスト。
それぞれ

h ステップはその前のステップのサブセットであるため、カウントはネストされます。
n_cited ≤ n_in_sources ≤ n_overviews ≤ n_queries 。 (引用は情報源のサブセットです。
モデルは取得した内容のみを引用できます。) 可視性の分母は、回答が存在するクエリです。
— あなたは、回答が実際にレンダリングされた場所にのみ表示されます。すべてはレンズごとに計算されます
( 一般 / ブランド / 比較 ) に加えて、すべての行を集計します。
6 つの指標は、ファネルに沿った単なる比率と位置です。
overview_coverage — AI の回答を生成したクエリの割合
( n_overviews / n_queries )。
Visibility_in_sources — 回答クエリのうち、ドメインがアクセスされたシェア
依存するソース ( n_in_sources / n_overviews )。
Visibility_in_quotes — 回答クエリのうち、ドメインが引用されているシェア
答え (n_cited / n_overviews)。
avg_source_position — ソース間でのドメインの平均最高 ( min ) ランク
出現する場所をクエリします (出現しない場合は低い方が良いです)。
avg_quote_position — クエリ全体における引用間の平均最良 ( min ) ランク。
それは引用されています (低いほど良いです; — 引用されない場合)。
相対引用 — 出典→引用変換 : クエリが存在した場所
ソースに取得され、モデルが実際にあなたを引用したシェア ( n_cited / n_in_sources ;
高いほど優れており、[0, 1] に制限されます)。
感情

[切り捨てられた]

## Original Extract

open-geo — GEO (Generative Engine Optimization) visibility tracker: measure how a brand surfaces in AI answers, with a FastAPI/React dashboard and dark-themed PDF reports. - Pupok462/open-geo

GitHub - Pupok462/open-geo: open-geo — GEO (Generative Engine Optimization) visibility tracker: measure how a brand surfaces in AI answers, with a FastAPI/React dashboard and dark-themed PDF reports. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Pupok462
/
open-geo
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
8 Commits 8 Commits .claude-plugin .claude-plugin .claude .claude .github/ workflows .github/ workflows assets assets dashboard dashboard data data engines engines examples examples fonts fonts i18n i18n pipeline pipeline report report reports reports scripts scripts tests tests .gitignore .gitignore LICENSE LICENSE README.ar.md README.ar.md README.md README.md README.ru.md README.ru.md README.zh.md README.zh.md conftest.py conftest.py pyproject.toml pyproject.toml requirements.txt requirements.txt View all files Repository files navigation
English · Русский · 中文 · العربية
open-geo — GEO Visibility Tracker for Claude Code
open-geo measures how visible your brand is inside AI answers — across every major engine.
Search is shifting from "ten blue links" to a generated answer: ChatGPT, Perplexity, Gemini,
Claude, Google AI Overview, Yandex, DeepSeek. Each answer leans on a handful of sources — and
being one of them is visibility in AI. open-geo runs your queries through an engine in a real,
logged-in browser and records whether your domain makes it into the sources , into the
citations , into the text — and how the brand is spoken about when it does.
It reads the answer like a human, not an API. Capture runs through Claude-in-Chrome in a
real, logged-in browser — it sees the rendered AI answer (the sources panel and the inline
citation chips), normalizes domains, and emits one validated record per query. No brittle
scraping of a surface no engine ever promised to keep stable.
A visibility funnel, not a vanity score. Six metrics that nest as a funnel — answer →
sources → citations — plus a qualitative sentiment read and a top-domains leaderboard (your
brand ranked against every other domain in the answers). No composite index, no made-up
share-of-voice index . Every number is auditable to pipeline/INTERFACES.md .
Local-first, multi-brand time-series. Captures land in a local SQLite (WAL) database, so you
build per-brand, per-engine history and run-over-run deltas. Deliverables are a themed PDF and
a FastAPI + React dashboard with a four-language switcher. Your data never leaves your machine.
GEO / SEO consultants — walk into a pitch with a real, dated read of a brand's AI-answer
visibility instead of "AI search matters, trust me."
In-house growth / SEO at a brand — track your own domain's presence in AI answers over time,
split by query lens (general / branded / comparative), and catch week-over-week drift.
Founders & devs already in Claude Code — it's just a skill: point /open-geo at a CSV and a
domain, get a dashboard. No SaaS, no upload, no account.
Capture of AI answers — a list of queries is run through an engine in a real, logged-in
browser, and how the target domain shows up is recorded, one validated record per query.
Six metrics + qualitative sentiment — a visibility funnel (answer → sources → citations):
coverage, a visibility rate and an average best position for sources and for citations, plus
the source→citation conversion ( relative_citation ) and a short free-text note on how each
answer treats the brand. The dashboard and PDF also show a per-lens qualitative sentiment
summary synthesized from those per-query notes (see Metrics ).
A top-domains (competitor) leaderboard — the average-position metric generalized from your
brand to every domain in the answers, ranked by how often it appears (with its average
source/citation position). The honest "who shares your answer space" — brand rivals and
publishers alike, your brand highlighted — as a sortable dashboard panel and a PDF section. No
extra capture: it's computed from the data you already collected, so it works on past runs too.
SQLite multi-brand time-series — every run is stored in data/aeo.db (SQLite, WAL),
so you accumulate history per brand + engine and get run-over-run deltas.
A dashboard with a four-language switcher — English, Русский, 中文, العربية (RTL-aware) —
FastAPI read-only API + a Vite/React frontend with light/dark themes and per-metric tooltips.
A PDF report — a self-contained themed A4 report (ReportLab + matplotlib), no headless
Chrome and no system libraries required.
open-geo is a Claude Code skill — you drive it from a chat with Claude, not from a pile of
shell commands. The whole setup is: clone, ask Claude to install it, then use it as a command.
Clone the repo (or just point Claude at the URL):
Ask Claude to set it up. In a Claude Code session in that folder, say something like:
Set up open-geo (run scripts/setup.sh ), then track example.com (brand "Example") on google
using examples/questions.csv .
Claude runs the install and the capture for you — and prints a dashboard link and a summary.
Or run it directly as a command once installed:
/open-geo examples/questions.csv google example.com --brand " Example " --n-worker 3 --output both
examples/questions.csv is a placeholder — a fictional brand's question set, there so the
first run works out of the box. For a real read, swap in your own queries: the question set is
the core input — it decides what gets measured, and the report is only as good as the questions
you ask. Format and how to choose them: What input do I need? .
Track it on a schedule. Wrap the command in Claude Code's /loop to re-capture on an
interval and watch the drift — e.g. a weekly read:
/loop 1w /open-geo examples/questions.csv google example.com --brand " Example " --output both
The one thing Claude can't do for you: connect the Claude-in-Chrome extension and log the
browser in to the market you want to track. That logged-in session is what capture drives.
Everything runs through one operator command — the /open-geo skill. You don't touch
Python: Claude orchestrates capture → metrics → deliverables and hands you a dashboard and/or a PDF.
/open-geo <questions.csv> <engine> <domain> --brand "<name>" --n-worker <N> \
[--output dashboard|pdf|both] [--period today|all] [--lang en|ru|zh|ar]
argument
meaning
<questions.csv>
CSV with columns query,lens , where lens ∈ general | branded | comparative . Ready sample: examples/questions.csv .
<engine>
which AI engine to track (e.g. google ). The same slot takes any engine that has a capture playbook under engines/ .
<domain>
the target domain (any spelling: https://www.example.com , example.com — normalized automatically).
--brand "<name>"
human brand name (used in report/dashboard titles and the summary).
--n-worker <N>
number of capture workers run in parallel — the run's concurrency.
--output
dashboard (default) | pdf | both .
--period
all (default — full brand+engine history, enables deltas) | today (this run only).
--lang
UI language of the deliverables — en (default) | ru | zh | ar .
What it does, end to end: creates a run → splits the queries across parallel capture workers
(each drives the engine in your logged-in Chrome and returns one validated record per query) →
ingests and scores them centrally → emits the dashboard and/or PDF → prints a short summary from
the cross-lens all row. Re-run on a /loop to track drift over time.
The whole tracker is orchestrated by the /open-geo command:
Capture playbook — a per-engine playbook ( engines/<engine>.md ) is driven by
Claude-in-Chrome in a visible, logged-in Chrome. It reads the rendered AI answer as an
LLM does, expands the sources panel and the inline citation chips, normalizes domains, and emits
one QueryCapture object per query .
QueryCapture — the validated capture contract (Pydantic v2; authoritative spec in
pipeline/INTERFACES.md ).
ingest / score — the workers are capture-only : each builds and self-validates its
records (read-only) and returns them to the orchestrator. The orchestrator (the skill)
owns every DB write: it ingests each chunk as its worker returns — incrementally, so a
crash mid-run never loses captured work — finalizes the run, then computes metrics per lens
plus an all row.
dashboard / PDF — the orchestrator emits the deliverable(s) last , from the stored
metrics, plus a short summary (the dashboard server is started only after all captures are in).
The pipeline is engine-agnostic : engine is an open id end to end (contract, DB, CLI,
dashboard, report), and supporting a new engine is mainly a new engines/<engine>.md playbook —
see engines/README.md .
The funnel, in plain words. The four counts narrow down at each step:
Queries — the questions you feed in (your CSV).
AI Overview — the queries where the engine actually generated an AI answer (it doesn't
always — and an absence is valid data, not a failure).
In sources — of those, the queries where your domain was among the sources the answer
drew on.
Cited — of those, the queries where your domain was actually linked/cited in the answer
text.
Each step is a subset of the one before it, so the counts nest:
n_cited ≤ n_in_sources ≤ n_overviews ≤ n_queries . (Citations are a subset of sources because the
model can only cite what it retrieved.) The denominator for visibility is answer-present queries
— you can only be visible where an answer actually rendered. Everything is computed per lens
( general / branded / comparative ) plus an aggregate all row.
The six metrics are just ratios and positions along that funnel:
overview_coverage — share of queries that produced an AI answer at all
( n_overviews / n_queries ).
visibility_in_sources — of answer queries, the share where your domain made it into the
relied-on sources ( n_in_sources / n_overviews ).
visibility_in_citations — of answer queries, the share where your domain is cited in
the answer ( n_cited / n_overviews ).
avg_source_position — average best ( min ) rank of your domain among sources, over the
queries where it appears ( lower is better ; — if it never appears).
avg_citation_position — average best ( min ) rank among citations, over the queries where
it is cited ( lower is better ; — if never cited).
relative_citation — the source→citation conversion : of the queries where you were
retrieved into sources, the share where the model actually cited you ( n_cited / n_in_sources ;
higher is better , bounded to [0, 1] ).
sentimen

[truncated]
