---
source: "https://github.com/Karthick-Ramachandran/persist-os"
hn_url: "https://news.ycombinator.com/item?id=48650348"
title: "Show HN: Persist OS – Durable decisions for AI code"
article_title: "GitHub - Karthick-Ramachandran/persist-os: 🧠 Local-first CLI that gives your repo durable, AI-ready memory — features, ADRs, modules, architecture, standards & tests — and a 🩺 deterministic doctor that validates health, evidence & drift. Git tracks what changed; Persist OS tracks why. · GitHub"
author: "karthickrmchn"
captured_at: "2026-06-23T20:42:26Z"
capture_tool: "hn-digest"
hn_id: 48650348
score: 2
comments: 0
posted_at: "2026-06-23T19:48:10Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Persist OS – Durable decisions for AI code

- HN: [48650348](https://news.ycombinator.com/item?id=48650348)
- Source: [github.com](https://github.com/Karthick-Ramachandran/persist-os)
- Score: 2
- Comments: 0
- Posted: 2026-06-23T19:48:10Z

## Translation

タイトル: Show HN: Persist OS – AI コードの永続的な決定
記事のタイトル: GitHub - Karthick-Ramachandran/persist-os: 🧠 リポジトリに耐久性があり、AI 対応のメモリ (機能、ADR、モジュール、アーキテクチャ、標準、テスト) を提供するローカルファースト CLI、および健全性、証拠、ドリフトを検証する 🩺 決定論的ドクター。 Git は何が変更されたかを追跡します。永続的な OS は理由を追跡します。 · GitHub
説明: 🧠 ローカルファースト CLI は、リポジトリに耐久性のある AI 対応メモリ (機能、ADR、モジュール、アーキテクチャ、標準、テスト) と、健全性、証拠、ドリフトを検証する 🩺 決定論的ドクターを提供します。 Git は何が変更されたかを追跡します。永続的な OS は理由を追跡します。 - Karthick-Ramachandran/persist-os
HN テキスト: 過去数か月間、私は非常に速く構築して出荷してきました。私が友人のために作成した MVP の一部は完全にバイブコーディングされており、デモを見せるために 1 日以内に出荷されました。 AI ツールを使って作業するのは素晴らしい経験でしたが、いくつかの基本事項が守られていること、何をなぜ行うかの決定が書き留められていること、明らかな事柄が省略されていないこと、そして拡張できることを確認したかったのです。それが私がpersist-osを構築した理由です。ローカルで実行される CLI です。ネットワークは必要ありません。アーキテクチャの決定と標準をリポジトリ内のプレーン ファイルに記録し、「永続ドクター」がそれらをチェックします。矛盾するものや欠落しているものがある場合、またはタスクがテストなしで完了した場合にフラグを立てます。ルールはリポジトリ ファイル内に存在するため、新しいセッションを開始するかセッションが圧縮されるかは関係なく、標準とルールは存続します。 MITライセンス取得済み。試してみる方法は次のとおりです: npxpersist-os@latestinit

記事本文:
GitHub - Karthick-Ramachandran/persist-os: 🧠 リポジトリに耐久性があり、AI 対応のメモリ (機能、ADR、モジュール、アーキテクチャ、標準、テスト) を提供するローカル ファースト CLI、および健全性、証拠、ドリフトを検証する 🩺 決定論的ドクター。 Git は何が変更されたかを追跡します。永続的な OS は理由を追跡します。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブでアカウントを切り替えたか、勝利しました

ダウ。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
カーシック・ラマチャンドラン
/
永続OS
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
Karthick-Ramachandran/persist-os
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
80 コミット 80 コミット .agents/ スキル .agents/ スキル .claude/ スキル .claude/ スキル .github .github .pagecast .pagecast .persist .persist docs docs 例 例 スクリプト スクリプト サイト サイト src src テスト テスト .gitignore .gitignore .prettierignore .prettierignore .prettierrc.json .prettierrc.json AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md COTRIBUTING.md COTRIBUTING.md ライセンス ライセンス 哲学.md PHILOSOPHY.md README.md README.md SECURITY.md SECURITY.md eslint.config.js eslint.config.js llms.txt llms.txt package.json package.json pnpm-lock.yaml pnpm-lock.yaml tsconfig.json tsconfig.json tsup.config.ts tsup.config.ts vitest.config.ts vitest.config.ts すべてのファイルを表示 リポジトリ ファイルのナビゲーション
リポジトリ用の耐久性のある AI 対応エンジニアリング メモリ — そして決定論的なドクター
健康であることを証明しています。
Persist OS は、リポジトリを信頼できる情報源に変えるローカルファーストの CLI です。
そのままの形で建てられました。製品の意図、アーキテクチャなど、構造化されたレビュー可能な記憶を作成します。
意思決定、モジュールの所有権、テストとセキュリティの期待、AI エージェントのルール、そして
は、persist Doctor を使用してその記憶を検証します。アーキテクチャ中立。ネットワークもテレメトリもなし
コード生成。
ウェブサイト · インストール · クイックスタート ·
コマンド · 医師がチェックする内容 · プリセット ·
これを構築した理由 · 貢献
AI はコードを速く書くことができますが、そのコンテキストは一時的なものです。AI は決定を忘れ、会話をコンパクトにし、
そして以前の意図から逸脱します。 Git は何を記録しますか

変わりました。 OS がその理由を形式で記録し続ける
人間とエージェントは、作業の前後に再読み込みして検証できます。
私たちは何を構築しているのでしょうか?なぜこれを決定したのでしょうか?
流れてはいけないものは何ですか？この作業が完了したことを証明する証拠は何ですか?
これらの質問がチャット ウィンドウではなくリポジトリ内にある場合、リポジトリは回答できます。
彼ら。
ベクトルメモリエンジンではありません。 supermemory や mem0 などのツールは、次のように情報を取得します。
埋め込み; Persist OS は決定自体を、ベクトルではなくレビュー可能なファイルとして書き込みます。
一貫性が保たれていることを確認します。彼らは競合するものではなく、補完的なものです。 →
これを構築した理由
会話を超えて残る記憶。意思決定、制約、所有権は以下にコミットされます。
リポジトリは、エージェントのコンテキスト ウィンドウに閉じ込められません。
単なるドキュメントではなく、ゲートです。永続ドクターは決定的で終了コードを返すため、「これは
仕事は実際に完了し、一貫性のあるものになっているでしょうか?」フックまたは CI で実行できるチェックになります。
決断は安全に変わります。決定が変更されると、永続的な adr がそれを記録します (古い
ADR は置き換えられたものとしてマークされ、新しいものはリンクバックされます)、そしてドクターはまだ古いものを引用している記憶にフラグを立てます
1 つ目 — そのため、痕跡は静かに矛盾するのではなく、監査可能な状態に保たれます。生成されたエージェント ルール
CLI コマンドもインラインで実行されるため、AI ツール自体が CLI コマンドを使用します。
コンテキストの腐敗と戦います。常に読み込まれているメモリがテキストの壁に膨れ上がった場合、医師が警告します。
メモリが変更されてからかなり経ってから変更された src/ コードをまだ指している場合、メモリはそのままになります。
無駄のない、最新のマップであり、古いダンプではありません。
再発明ではなく再利用。生成された CONVENTIONS.md (正規の再利用可能な語彙) と
LESSONS.md (永続的な落とし穴) がすべてのエージェント セッションにロードされ、エージェントはそれらを保持するように指示されます。
現在のコンポーネントとパターンを再発明するのではなく再利用し、停止します。
繰り返しm

あなたがすでに解決した問題。バイブコーディングされたプロジェクトでも機能します。
記憶を維持するのはあなたではありません。
アーキテクチャに中立な設計。 OS の記録を永続化し、決定を保護します。それは決してありません
フレームワーク、データベース、またはパターンを黙って選択します。
地元第一主義でプライベート。ネットワーク呼び出し、テレメトリ、AI API 呼び出し、リモート テンプレートはありません。
それは完全にあなたのマシン上で実行されます。
デフォルトでは安全です。非破壊書き込み、パストラバーサルおよびシンボリックリンク保護、および拒否
明示的な意図なしに既存のインストールを上書きすること。
インストールせずに実行してください。これを試す最も簡単な方法は次のとおりです。
npxpersist-os@最新初期化
すべてのコマンドは同じように機能します: npxpersist-os <command> (例: npxpersist-os Doctor )。
npm install -gpersist-os
永続化 --ヘルプ
(Node.js >= 20 が必要です。
npmjs.com/package/persist-os 。)
# 1. リポジトリ メモリを作成する (アーキテクチャに依存しない、または意見パックを選択する)
初期化を維持する
persist init --preset kotlin-android # オプション、スタックの決定を提案します
#2. 作業中に意図と決定を把握する
永続化機能の作成チェックアウト
永続的な adr 支払いプロバイダーの作成
persist adr acceptpayment-provider # プロポーザルを受け入れられたメモリにプロモートします
# 3. MCP サーバーのコンテキストを永続メモリに取り込む (オフライン)
持続 mcp figma を追加
# 4. メモリが正常で完全であることを検証する
医者を続ける
すべてのコマンドがユーザーをガイドし、作成したファイルの名前、ファイルの場所、次に何をするかを示します。
使用する AI ツールのみのファイルを生成します:persist init --ai-tools claude,cursor (デフォルト: all
クロード、コーデックス、カーソルの。 AGENTS.md は常に書き込まれます)。
persist init は、追跡されたプリコミットフックとプリプッシュフックを .persist/hooks/ に生成します。
persist Doctor と設定したゲートを実行します。プリプッシュフックは最終回帰ゲートです
コードが手元から離れる前に、

マシン (--no-verify で行われたコミット、またはフックが実行される前に行われたコミットをキャッチします)
アクティブです）。クローンごとに 1 回有効にする — Persist OS はコマンドを提案しますが、それを実行することはありません。
git config core.hooksPath .persist/hooks
コマンド
コマンド
目的
初期化を維持する
中立的なリポジトリ メモリ (およびコミット前フック) を作成します。
persist init --preset <id>
オピニオンパックを追加: 豊富なガイダンスと提案された ADR。
persist init --ai-tools <リスト>
使用する AI ツール (クロード、コーデックス、カーソル、汎用) のみのファイルを生成します。
持続的に採用する
既存のリポジトリを検査し、レビュー可能なメモリを提案します。
プリセットリストを保持する
組み込みのプリセットを一覧表示します。
永続化機能作成 <名前>
足場特徴の記憶 (PRD、受け入れ、テスト、レビュー)。
persist adr create <タイトル>
提案されたアーキテクチャ決定記録を作成します。
persist adr accept <名前>
提案された ADR を信頼できる情報源に宣伝します。
persist adr を置き換える <古い> <新しい>
変更された決定を記録します。古い ADR を新しく承認された ADR に置き換えるマークを付けます。
永続モジュール作成 <名前>
足場モジュールのメモリ (所有権、境界、テスト)。
永続スキル作成 <名前>
ポータブル AI エージェント スキル (クロード + エージェント スキル) を生成します。
永続スキルリスト
組み込みのエージェント スキル カタログを一覧表示します。
persist mcp add <サーバー>
MCP サーバー用にオフラインで提案されたメモリを生成します。
医者を続ける
メモリの健全性、証拠、ドリフトを検証します。
persist Guard --source <ディレクトリ>
ステージングされたソースがテストなしで変更されると失敗します (強制するためにゲートを追加します)。
医師がチェックする内容
Persist Doctor は、Persist OS をテンプレート以上のものにする部分です。すべてのチェックは、
決定的、ローカル、読み取り専用。
終了コード: 0 = 正常 1 = 警告のみ 2 = エラー
終了コードが返されるため、Doctor は完了ループに直接入ります。
pnpm テスト:実行 && pnpm typecheck && 永続ドクター
生成されたものを介してローカルで使用します

pre-commit フック、または CI のステップとして永続ドクターを追加します。
プリセットは意見パックです。彼らは、スタックの実際の提案されたガイダンスと提案された ADR を出荷します。
意思決定の分岐 — そして、彼らはあなたの選択を黙って受け入れることはできません (スキーマは提案されたものを強制します)
すべてのプリセット決定のステータス)。
プリセットの追加は少額の貢献です。 CONTRIBUTING.md を参照してください。
メモリへの書き込みは、エージェントがメモリを読み取る場合にのみ役立ちます。そのため、各ツールを独自のパラメータで永続的に初期化します。
ネイティブメカニズム:
すべてのツールにわたる移植性の保証は、AGENTS.md と生成されたエージェント スキルです。
( .agents/skills/ )。セッションごとの動的な ADR/モジュール マップは、Claude Code のボーナスです。カーソルルール
および AGENTS.md は、他の場所でも同じルールを適用します。
AGENTS.md は、短い命令型の Rules ブロックで始まります (最初にメモリを読み取り、
慣習に従い、レッスンを記録し、受け入れられた ADR と矛盾しないようにし、「完了」する前にpersistent Doctorを実行します) —
そして、CONVENTIONS.md と LESSONS.md 自体を最新の状態に保つようにエージェントに指示します。
誰も手書きしなくても役に立ちます。人間はプル内のエージェントの編集を確認するだけです
リクエスト。
Persist OS は、明示的な設定を使用して、 docs/ および .persist/config.json の下にメモリ構造を作成します。
真実の情報源の順序:
1. 承認された ADR とリポジトリの決定 6. モジュールのドキュメント
2. アーキテクチャ ドキュメント 7. 機能計画
3. 技術基準 8. タスクファイル
4. 現在の PRD と受け入れられた変更 9. 外部コンテキスト
5. セキュリティとテストに関するドキュメント 10. チャット履歴
外部コンテキストまたはチャット履歴がリポジトリ メモリと競合する場合は、リポジトリ メモリが優先されます。
実行時にネットワーク呼び出しを行う。
MCP サーバーに接続するか、AI API を呼び出します。
実稼働アプリケーションコードを生成します。
依存関係をリポジトリにインストールします。
デフォルトでは既存のファイルを上書きします。
コミットされたサンプル出力には、各プリセットが生成する正確なメモリが表示されます。
例/ge

nerated-generic/examples/generated-kotlin-android/
例/generated-nextjs/ 例/generated-python-fastapi/
例/generated-ios-swift/ 例/generated-flutter/
例/generated-laravel-react/ 例/generated-laravel-vue/
例/生成された-laravel-api/
開発
pnpmインストール
pnpm リント
pnpm 形式:チェック
pnpm テスト:実行
pnpm タイプチェック
pnpmビルド
pnpm パック:チェック
上のゲートを通過し、作業が完了したと主張する前に医師を持続させてください。参照
完全なワークフローとプリセットの追加方法については CONTRIBUTING.md を参照してください。
セキュリティ モデルの SECURITY.md。
ランディング ページは次のように展開されます。
ページキャスト — ありがとうございます。
🧠 ローカルファースト CLI は、リポジトリに耐久性のある AI 対応メモリ (機能、ADR、モジュール、アーキテクチャ、標準、テスト) と、健全性、証拠、ドリフトを検証する 🩺 決定論的ドクターを提供します。 Git は何が変更されたかを追跡します。永続的な OS は理由を追跡します。
読み込み中にエラーが発生しました。このページをリロードしてください。
0
フォーク
レポートリポジトリ
リリース
6
v0.6.1 — 適切な規模の規律
最新の
2026 年 6 月 23 日
+ 5 リリース
パッケージ
0
読み込み中にエラーが発生しました。このページをリロードしてください。
読み込み中にエラーが発生しました。このページをリロードしてください。
© 2026 GitHub, Inc.
フッターナビゲーション
私の個人情報を共有しないでください

## Original Extract

🧠 Local-first CLI that gives your repo durable, AI-ready memory — features, ADRs, modules, architecture, standards & tests — and a 🩺 deterministic doctor that validates health, evidence & drift. Git tracks what changed; Persist OS tracks why. - Karthick-Ramachandran/persist-os

In the past few months, I’ve been building and shipping things extremely fast. Some of the MVPs that I built for my friends were completely vibe-coded and shipped within a day to show a demo. While it was a great experience working with AI tools, I wanted to make sure some basics were followed, decisions of what and why written down, obvious things not skipped and that it could scale. That’s why I built persist-os. It’s a CLI that runs locally. No network required. It records the architecture decisions and standards in plain files in the repo and 'persist doctor' checks them - it flags if anything contradicts or is missing, or if the task is done with no tests. Because the rules live in your repo files, it doesn't matter if you start a new session or the session is compacted - the standards and rules survive. MIT licensed. Here’s how you can try it: npx persist-os@latest init

GitHub - Karthick-Ramachandran/persist-os: 🧠 Local-first CLI that gives your repo durable, AI-ready memory — features, ADRs, modules, architecture, standards & tests — and a 🩺 deterministic doctor that validates health, evidence & drift. Git tracks what changed; Persist OS tracks why. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Karthick-Ramachandran
/
persist-os
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
Karthick-Ramachandran/persist-os
main Branches Tags Go to file Code Open more actions menu Folders and files
80 Commits 80 Commits .agents/ skills .agents/ skills .claude/ skills .claude/ skills .github .github .pagecast .pagecast .persist .persist docs docs examples examples scripts scripts site site src src tests tests .gitignore .gitignore .prettierignore .prettierignore .prettierrc.json .prettierrc.json AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md CONTRIBUTING.md CONTRIBUTING.md LICENSE LICENSE PHILOSOPHY.md PHILOSOPHY.md README.md README.md SECURITY.md SECURITY.md eslint.config.js eslint.config.js llms.txt llms.txt package.json package.json pnpm-lock.yaml pnpm-lock.yaml tsconfig.json tsconfig.json tsup.config.ts tsup.config.ts vitest.config.ts vitest.config.ts View all files Repository files navigation
Durable, AI-ready engineering memory for your repository — and a deterministic doctor that
proves it stays healthy.
Persist OS is a local-first CLI that turns a repository into the source of truth for why it is
built the way it is. It creates structured, reviewable memory — product intent, architecture
decisions, module ownership, testing and security expectations, AI agent rules — and then
validates that memory with persist doctor . Architecture-neutral. No network, no telemetry, no
code generation.
Website · Install · Quickstart ·
Commands · What Doctor Checks · Presets ·
Why I built this · Contributing
AI can write code fast, but its context is temporary — it forgets decisions, compacts conversations,
and drifts from earlier intent. Git records what changed. Persist OS records why , in a form
humans and agents can re-read and validate before and after work.
What are we building? Why did we decide this?
What must not drift? What evidence proves this work is complete?
When these questions live in the repository instead of a chat window, the repository can answer
them.
Not a vector memory engine. Tools like supermemory or mem0 retrieve information with
embeddings; Persist OS writes the decisions themselves — reviewable files, not vectors — and
checks they stay consistent. They're complementary, not competitors. →
Why I built this
Memory that outlives the conversation. Decisions, constraints, and ownership are committed to
the repo, not trapped in an agent's context window.
A gate, not just docs. persist doctor is deterministic and returns an exit code, so "is this
work actually finished and consistent?" becomes a check you can run in a hook or CI.
Decisions change safely. When a decision changes, persist adr supersede records it (the old
ADR is marked superseded, the new one links back) and Doctor flags any memory still citing the old
one — so the trail stays auditable instead of silently contradicted. The generated agent rules
even carry the CLI commands inline, so your AI tool uses them itself.
Fights context rot. Doctor warns when the always-loaded memory bloats into a wall of text, or
when it still points at src/ code that changed long after the memory did — so memory stays a
lean, current map, not a stale dump.
Reuse over reinvent. Generated CONVENTIONS.md (your canonical, reusable vocabulary) and
LESSONS.md (durable pitfalls) load into every agent session, and the agent is told to keep them
current itself — so it reuses your components and patterns instead of reinventing them, and stops
repeating mistakes you already solved. It works even on vibe-coded projects, because the agent
maintains the memory, not you.
Architecture-neutral by design. Persist OS records and protects your decisions. It never
silently picks a framework, database, or pattern for you.
Local-first and private. No network calls, no telemetry, no AI API calls, no remote templates.
It runs entirely on your machine.
Safe by default. Non-destructive writes, path-traversal and symlink protection, and a refusal
to overwrite an existing installation without explicit intent.
Run it without installing — the quickest way to try it:
npx persist-os@latest init
Every command works the same way: npx persist-os <command> (e.g. npx persist-os doctor ).
npm install -g persist-os
persist --help
(Requires Node.js >= 20. Published at
npmjs.com/package/persist-os .)
# 1. Create repository memory (architecture-neutral, or pick an opinion pack)
persist init
persist init --preset kotlin-android # optional, proposes stack decisions
# 2. Capture intent and decisions as you work
persist feature create checkout
persist adr create payment-provider
persist adr accept payment-provider # promote a proposal to accepted memory
# 3. Bring an MCP server's context into durable memory (offline)
persist mcp add figma
# 4. Validate the memory is healthy and complete
persist doctor
Every command guides you — it names the file it created, where it is, and what to do next.
Generate files only for the AI tools you use: persist init --ai-tools claude,cursor (default: all
of claude , codex , cursor ; AGENTS.md is always written).
persist init also generates tracked pre-commit and pre-push hooks in .persist/hooks/ that
run persist doctor plus any gates you configure. The pre-push hook is the final regression gate
before code leaves your machine (it catches commits made with --no-verify or before the hook was
active). Enable them once per clone — Persist OS proposes the command but never runs it for you:
git config core.hooksPath .persist/hooks
Commands
Command
Purpose
persist init
Create neutral repository memory (and a pre-commit hook).
persist init --preset <id>
Add an opinion pack: rich guidance and proposed ADRs.
persist init --ai-tools <list>
Generate files only for the AI tools you use (claude, codex, cursor, generic).
persist adopt
Inspect an existing repo and propose reviewable memory.
persist preset list
List built-in presets.
persist feature create <name>
Scaffold feature memory (PRD, acceptance, tests, review).
persist adr create <title>
Create a proposed architecture decision record.
persist adr accept <name>
Promote a proposed ADR to accepted source-of-truth.
persist adr supersede <old> <new>
Record a changed decision: mark the old ADR superseded by a new accepted ADR.
persist module create <name>
Scaffold module memory (ownership, boundaries, tests).
persist skill create <name>
Generate a portable AI agent skill (Claude + Agent Skills).
persist skill list
List the built-in agent skill catalog.
persist mcp add <server>
Generate offline, proposed memory for an MCP server.
persist doctor
Validate memory health, evidence, and drift.
persist guard --source <dirs>
Fail when staged source changed without tests (add to your gates to enforce).
What Doctor Checks
persist doctor is the part that makes Persist OS more than a template. Every check is
deterministic, local, and read-only.
Exit codes: 0 = healthy 1 = warnings only 2 = errors
Because it returns an exit code, Doctor drops straight into the completion loop:
pnpm test:run && pnpm typecheck && persist doctor
Use it locally via the generated pre-commit hook, or add persist doctor as a step in CI.
Presets are opinion packs. They ship proposed guidance and proposed ADRs for a stack's real
decision forks — and they can never silently accept a choice for you (the schema enforces Proposed
status on every preset decision).
Adding a preset is a small contribution — see CONTRIBUTING.md .
Writing memory only helps if the agent reads it, so persist init wires each tool with its own
native mechanism:
The portable guarantee across every tool is AGENTS.md plus the generated Agent Skills
( .agents/skills/ ). The dynamic per-session ADR/module map is a Claude Code bonus; the Cursor rule
and AGENTS.md carry the same rules everywhere else.
AGENTS.md leads with a short, imperative Rules block (read memory first, reuse the
conventions, record lessons, don't contradict accepted ADRs, run persist doctor before "done") —
and instructs the agent to keep CONVENTIONS.md and LESSONS.md current itself, so that memory
stays useful without anyone hand-writing it. The human just reviews the agent's edits in the pull
request.
Persist OS creates a memory structure under docs/ and .persist/config.json , with an explicit
source-of-truth order:
1. Accepted ADRs and repository decisions 6. Module docs
2. Architecture docs 7. Feature plans
3. Engineering standards 8. Task files
4. Current PRD and accepted changes 9. External context
5. Security and testing docs 10. Chat history
If external context or chat history conflicts with repository memory, repository memory wins .
make network calls at runtime;
connect to MCP servers or call AI APIs;
generate production application code;
install dependencies into your repository;
overwrite existing files by default.
Committed sample outputs show the exact memory each preset generates:
examples/generated-generic/ examples/generated-kotlin-android/
examples/generated-nextjs/ examples/generated-python-fastapi/
examples/generated-ios-swift/ examples/generated-flutter/
examples/generated-laravel-react/ examples/generated-laravel-vue/
examples/generated-laravel-api/
Development
pnpm install
pnpm lint
pnpm format:check
pnpm test:run
pnpm typecheck
pnpm build
pnpm pack:check
Run the gates above and persist doctor before claiming work is complete. See
CONTRIBUTING.md for the full workflow and how to add a preset, and
SECURITY.md for the security model.
The landing page is deployed with
Pagecast — thank you.
🧠 Local-first CLI that gives your repo durable, AI-ready memory — features, ADRs, modules, architecture, standards & tests — and a 🩺 deterministic doctor that validates health, evidence & drift. Git tracks what changed; Persist OS tracks why.
There was an error while loading. Please reload this page .
0
forks
Report repository
Releases
6
v0.6.1 — Right-sized discipline
Latest
Jun 23, 2026
+ 5 releases
Packages
0
There was an error while loading. Please reload this page .
There was an error while loading. Please reload this page .
© 2026 GitHub, Inc.
Footer navigation
Do not share my personal information
