---
source: "https://trypolygraph.com/"
hn_url: "https://news.ycombinator.com/item?id=48678471"
title: "Show HN: Polygraph – Let AI agents see cross repo and maintain session memory"
article_title: "Polygraph"
author: "jeffbcross"
captured_at: "2026-06-25T20:38:42Z"
capture_tool: "hn-digest"
hn_id: 48678471
score: 5
comments: 0
posted_at: "2026-06-25T19:58:56Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Polygraph – Let AI agents see cross repo and maintain session memory

- HN: [48678471](https://news.ycombinator.com/item?id=48678471)
- Source: [trypolygraph.com](https://trypolygraph.com/)
- Score: 5
- Comments: 0
- Posted: 2026-06-25T19:58:56Z

## Translation

タイトル: HN の表示: Polygraph – AI エージェントにクロス リポジトリを参照させ、セッション メモリを維持させます
記事タイトル: ポリグラフ
説明: エージェントに必要な機能を提供するメタハーネス

記事本文:
ドキュメント ログイン サインアップ ドキュメント ログイン はじめに ドキュメント ログイン サインアップ リポジトリ Deps Lang Stars すべてを表示し、何も忘れません
すべてを見て、何も忘れない
すべてを見て、何も忘れない
エージェントに欠けているものを提供するメタハーネス: すべてのリポジトリ境界にわたる可視性、およびすべてのセッションで存続するメモリ。
エージェントに欠けているものを提供するメタハーネス: すべてのリポジトリ境界にわたる可視性、およびすべてのセッションで存続するメモリ。
エージェントに欠けているものを提供するメタハーネス: すべてのリポジトリ境界にわたる可視性、およびすべてのセッションで存続するメモリ。
Polygraph は、アクセスできるすべてのリポジトリ (プライベートおよび OSS) にインデックスを付け、それらがパッケージや API を介してどのように関係しているかを示すグラフを作成します。エージェントはこれを使用して、どこを見ればよいかを指示されることなく、リポジトリ間の作業を計画します。コードベースを合成モノリポジトリに変換します。
ポリグラフはエージェントではありません。彼らがより野心的なタスクに取り組むことを可能にするのはメタハーネスです。
ポリグラフはエージェントではありません。それは、彼らがより野心的なタスクに取り組むことを可能にするメタハーネスです。
ポリグラフはエージェントではありません。それは、彼らがより野心的なタスクに取り組むことを可能にするメタハーネスです。
任意のエージェント間でセッションを引き継ぎます。リポジトリ、ブランチ、PR、ログが付属しているため、誰も何も再説明しません。
任意のエージェント間でセッションを引き継ぎます。リポジトリ、ブランチ、PR、ログが付属しているため、誰も何も再説明しません。
過去のセッションを参照します。 Polygraph はその説明、PR、トレースを戻すので、履歴を調べなくてもバグを修正できます。
Polygraph は既存のスタックにプラグインします。リポジトリと CI を一度接続すれば、残りはエージェントに処理させます。
Polygraph は既存のスタックにプラグインします。リポジトリと CI を一度接続すれば、残りはエージェントに処理させます。
Polygraph は既存のスタックにプラグインします。リポジトリを接続し、

CI を 1 回実行すれば、残りはエージェントに処理させます。
完全な製品、コストゼロ。価格変更の 30 日前までに通知してください。
完全な製品、コストゼロ。価格変更の 30 日前までに通知してください。
エージェントには星が見えます。ポリグラフは星座全体を見ます。
エージェントには星が見えます。ポリグラフは星座全体を見ます。
エージェントには星が見えます。ポリグラフは星座全体を見ます。

## Original Extract

The meta-harness that gives agents what they

Docs Log in Sign up Docs Log in Getting started Docs Log in Sign up Repository Deps Lang Stars See everything, forget nothing
See everything, forget nothing
See everything, forget nothing
The meta-harness that gives agents what they're missing: visibility across every repo boundary, and memory that survives every session.
The meta-harness that gives agents what they’re missing: visibility across every repo boundary, and memory that survives every session.
The meta-harness that gives agents what they’re missing: visibility across every repo boundary, and memory that survives every session.
Polygraph indexes every repo you have access to (private and OSS) and builds a graph of how they relate via packages and APIs. Your agent uses this to plan cross-repo work without being told where to look; turning your codebase into a synthetic monorepo.
Polygraph isn't an agent. It's the meta-harness that lets them take on more ambitious tasks.
Polygraph isn’t an agent. It’s the meta-harness that lets them take on more ambitious tasks.
Polygraph isn’t an agent. It’s the meta-harness that lets them take on more ambitious tasks.
Hand off a session across any agent. Repos, branches, PRs, and logs come with it, so nobody re-explains anything.
Hand off a session across any agent. Repos, branches, PRs, and logs come with it, so nobody re-explains anything.
Reference any past session. Polygraph brings back its description, PRs, and traces, so you fix bugs without digging through history.
Polygraph plugs into your existing stack. Connect your repos and CI once, and let the agent handle the rest.
Polygraph plugs into your existing stack. Connect your repos and CI once, and let the agent handle the rest.
Polygraph plugs into your existing stack. Connect your repos and CI once, and let the agent handle the rest.
Full product, zero cost. 30 day notice before any pricing changes.
Full product, zero cost. 30 day notice before any pricing changes.
Your agent sees stars. Polygraph sees the whole constellation.
Your agent sees stars. Polygraph sees the whole constellation.
Your agent sees stars. Polygraph sees the whole constellation.
