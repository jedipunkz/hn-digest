---
source: "https://github.com/vicmaster/framesmith"
hn_url: "https://news.ycombinator.com/item?id=48690983"
title: "Framesmith 1.7 – a quality gate that tells an AI agent when a UI is done"
article_title: "GitHub - vicmaster/framesmith: Open-source MCP server that gives AI assistants a visual design canvas, rendering HTML/CSS scene graphs to PNG via headless Chromium. · GitHub"
author: "vicvelazquez"
captured_at: "2026-06-26T20:33:25Z"
capture_tool: "hn-digest"
hn_id: 48690983
score: 2
comments: 0
posted_at: "2026-06-26T19:35:36Z"
tags:
  - hacker-news
  - translated
---

# Framesmith 1.7 – a quality gate that tells an AI agent when a UI is done

- HN: [48690983](https://news.ycombinator.com/item?id=48690983)
- Source: [github.com](https://github.com/vicmaster/framesmith)
- Score: 2
- Comments: 0
- Posted: 2026-06-26T19:35:36Z

## Translation

タイトル: Framesmith 1.7 – UI の完了を AI エージェントに伝える品質ゲート
記事タイトル: GitHub - vicmaster/framesmith: AI アシスタントにビジュアル デザイン キャンバスを提供し、ヘッドレス Chromium 経由で HTML/CSS シーン グラフを PNG にレンダリングするオープンソース MCP サーバー。 · GitHub
説明: AI アシスタントにビジュアル デザイン キャンバスを提供し、ヘッドレス Chromium 経由で HTML/CSS シーン グラフを PNG にレンダリングするオープンソース MCP サーバー。 - ヴィクマスター/フレームスミス

記事本文:
GitHub - vicmaster/framesmith: AI アシスタントにビジュアル デザイン キャンバスを提供し、ヘッドレス Chromium 経由で HTML/CSS シーン グラフを PNG にレンダリングするオープンソース MCP サーバー。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
ヴィクマスター
/
額縁屋
公共
通知
サインインする必要があります

通知設定を変更するには
追加のナビゲーション オプション
コード
マスター ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
262 コミット 262 コミット .framesmith .framesmith ベンチマーク ベンチマーク docs docs scripts scripts src src .gitignore .gitignore ライセンス ライセンス README.md README.md VISION.md VISION.md package-lock.json package-lock.json package.json package.json test-animations.ts test-animations.ts test-apply- Structure.ts test-apply-structor.ts test-archive-view.ts test-archive-view.ts test-auto-relative.ts test-auto-relative.ts test-autofix.ts test-autofix.ts test-backdrop-filter.ts test-backdrop-filter.ts test-batch-design-idmap.ts test-batch-design-idmap.ts test-build-log.ts test-build-log.ts test-cliche.ts test-cliche.ts test-component- Structures.ts test-component-structions.ts test-correctness-fixes.ts test-correctness-fixes.ts test-critique.ts test-critique.ts test-custom-fonts.ts test-custom-fonts.ts test-default-font.ts test-default-font.ts test-design-md-tolerant.ts test-design-md-tolerant.ts test-design-md.ts test-design-md.ts test-design-system-inheritance.ts test-design-system-inheritance.ts test-discoverability.ts test-discoverability.ts test-diversification.ts test-diversification.ts test-empty-thumbnail.ts test-empty-thumbnail.ts test-env.ts test-env.ts test-evaluate.ts test-evaluate.ts test-fluid-widths.ts test-fluid-widths.ts test-fonts-by-name.ts test-fonts-by-name.ts test-gradient-shadow-move-fixes.ts test-gradient-shadow-move-fixes.ts test-guidelines.ts test-guidelines.ts test-import-core.ts test-import-core.ts test-import-html.ts test-import-html.ts test-import- Structure.ts test-import-structor.ts test-import-url.ts test-import-url.ts test-init.ts test-init.ts test-llm-judge.ts test-llm-judge.ts test-material-icons.ts test-m

aterial-icons.ts test-ops-parser.ts test-ops-parser.ts test-patterns.ts test-patterns.ts test-phase2.ts test-phase2.ts test-phase3.ts test-phase3.ts test-primitives.ts test-primitives.ts test-repo-assets.ts test-repo-assets.ts test-repo-canvases.ts test-repo-canvases.ts test-repo-external-change.ts test-repo-external-change.ts test-sensitive-
[切り捨てられた]
AI コーディング エージェントにビジュアル キャンバスを提供するオープンソース MCP サーバー。フレームワーク コードを作成する前に、UI をスケッチし、ブラウザで確認し、設計に同意します。
内容：ビューア・インストール・ツール・利用例・ワークフロー・開発
上: Framesmith ビューア。サイドバーにワークスペースとプロジェクトが表示され、右側にライブサムネイルとしてキャンバスが表示されます。 AI エージェントは MCP ツールを介してキャンバスを作成します。 Figma ファイルのように参照します。
MCP クライアント → 標準出力 → フレームスミス サーバー
↓
シーングラフ (インメモリ JSON ツリー)
↓
HTML/CSS レンダラー (インライン スタイル)
↓
Puppeteer (ヘッドレス クロム → PNG)
ビューア
npx -p Framesmith Framesmith-viewer を実行して、スタンドアロン ブラウザ ビューア (デフォルト ポート 3001) を起動します。キャンバスを開いて、複数のブレークポイントで確認したり、並べて比較したり、基になる JSON を検査したり、アーカイブ/削除したりできます。
上: 詳細ビューの単一のキャンバス。上部のツールバーには、ブレークポイント プレビュー モード、並列レンダリングの比較、最大幅のフィット、生のシーン グラフの JSON、およびライフサイクル アクションが表示されます。
高品質パネル。キャンバスの詳細ビューの右側には、読み取り専用の品質インスペクターが表示されます。ヒューリスティックな Canvas_evaluate スコア (0 ～ 100)、カテゴリごとのバー、および問題リスト — それぞれの常套句がそのカテゴリで表示され、バッジ、重大度、提案が表示されます。 Canvas_autofix で解決できる問題には自動修正可能なタグが付けられており、問題をクリックすると、ライブ p でそのノードが強調表示されます。

レビュー。すべてのギャラリー カードには色分けされたスコア バッジも表示されるため、弱いキャンバスが一目で目立ちます。スコアは、エージェントが MCP で表示するものと一致します (同じ高速モード評価、キャンバスのプリセットによってジャンルが緩和されます)。表示のみを目的として計算され、書き戻されることはありません。
デザイン系パネル。 2 番目のインスペクター タブには、ワークスペース ▸ プロジェクト ▸ キャンバス継承チェーン全体を通じて解決された、キャンバスの効果的なデザイン トークン (色見本、タイプ スケール、間隔、半径) が表示されます。各セクションには、その主要なソース レイヤーが記載されており、それをオーバーライドするトークンにはタグ ( Canvas / project ) が付けられているため、特定のキャンバスがカスタマイズされたものと継承されたものを一目で確認できます。
ビューアは純粋に読み取り専用です。すべてのキャンバスは、AI アシスタントからの MCP ツール呼び出しを通じて作成されます。ファイルは ~/.framesmith/canvases/ に保存されるため、ビューアはセッションをまたいでファイルを表示し続けます。
クローンやビルドは必要ありません。npx 経由で Framesmith を MCP クライアントに登録します (Node 20+ が必要)。
クロード mcp フレームスミスを追加 -- npx -y フレームスミス
コーデックス
[ mcp_servers .フレームスミス ]
コマンド = " npx "
args = [ " -y " , " フレームスミス " ]
カーソル
~/.cursor/mcp.json (またはプロジェクトごとの .cursor/mcp.json ) に追加します。
{
"mcpサーバー": {
"フレームスミス" : {
"コマンド" : " npx " ,
"args" : [ " -y " , " フレームスミス " ]
}
}
}
ウィンドサーフィン
~/.codeium/windsurf/mcp_config.json に追加します。
{
"mcpサーバー": {
"フレームスミス" : {
"コマンド" : " npx " ,
"args" : [ " -y " , " フレームスミス " ]
}
}
}
VS コード + MCP 拡張機能
.vscode/mcp.json (プロジェクト スコープ) またはグローバル MCP 設定に追加します。
{
「サーバー」: {
"フレームスミス" : {
"コマンド" : " npx " ,
"args" : [ " -y " , " フレームスミス " ]
}
}
}
その他の MCP 互換クライアント
Framesmith は標準の stdio MCP を話します。クライアントが期待する構成形状を使用して、クライアントに npx -y Framesmith を指定します。
オプション:

MCP サーバー環境で FRAMESMITH_VIEWER_URL=http://localhost:3001 を設定して、有効期間の長いスタンドアロン ビューア プロセスに固定します。「 ビューアの実行 」を参照してください。
ソースからビルドする (開発用)
git clone https://github.com/vicmaster/framesmith.git
CDフレームスミス
npmインストール
npm ビルドを実行する
# 次に、クライアントに次の場所を指定します。node /path/to/framesmith/dist/index.js
ツール
ワンコールオンボーディング — 各セッションの最初のコールが推奨されており、繰り返し実行しても安全です (冪等)。現在のリポジトリがまだバインドされていない場合はバインドし (キャンバスは .framesmith/ の下にチェックインされた JSON になります)、コンベンション プロジェクトが存在することを確認して、作業を開始するために必要なライブ状態を返します。
バインドされたワークスペース + プロジェクト ID (re-keys ID を repo-* にバインド - init が返すものを使用)、ディスク上のレイアウト、ワークスペース層のトークン数、ワークフロー チートシート、現在の注意点、framesmith://guidelines URI、およびビューア URL を返します。デザイン トークンはシードされません。workspace_set_design_system を使用してワークスペース層でデザイン トークンを設定します。デフォルトの Foundations プロジェクトは、ワークスペース トークン (デザイン システムが実際に存在する場所) を視覚化する単なるキャンバスです。
新しいキャンバスを作成します。 projectId が省略された場合、個人ワークスペースの組み込みの無題プロジェクトに配置されます。
応答には、ターゲット プロジェクトの多様化シグナルも含まれます。つまり、最近構築された構造 (新しいものから順) と、少なくとも 1 つの分類軸で異なるため、連続するキャンバスが同じレイアウトに収束しないようにするためのヒントです。これは勧告であり、決してブロックするものではありません。
キャンバスをリストします。デフォルトではアーカイブされたキャンバスを除外します。
[{ id, name, createdAt, lastModified, projectId, archived }] を返します。
Canvas_move / Canvas_archive / Canvas_unarchive / Canvas_delete
キャンバスのライフサイクル。 Canvas_move は、キャンバスを別のプロジェクトに再割り当てします。キャンバスアーチ

ive は論理的な削除フラグを設定します (キャンバスはディスク上に残り、デフォルトの Canvas_list からは非表示になります)。 Canvas_unarchive はそれをクリアします。 Canvas_delete は、キャンバスとそのファイルを永久に削除します (元に戻すことはできません)。
ライブ ビューアの URL とキャンバスごとの URL を取得します。これらをユーザーと共有すると、ユーザーはブラウザーでデザインを開くことができます。パラメータはありません。
{
"url" : " http://localhost:3001 " ,
"ギャラリー" : " http://localhost:3001 " 、
"キャンバス" : [
{ "name" : " ログイン " 、 "viewer" : " http://localhost:3001/canvas/abc123 " }
】
}
Canvas_create はすでに応答でキャンバスごとのビューア URL を返しています。ギャラリーの URL が必要な場合、または 1 回の呼び出しで既存のキャンバスの URL をすべて列挙する場合は、viewer_url に到達します。
workspace_create / workspace_list / workspace_rename / workspace_delete
最上位コンテナの CRUD。組み込みの個人ワークスペースは削除できず、ワークスペースにまだプロジェクトが含まれている場合 (最初にプロジェクトを移動または削除する)、workspace_delete は拒否します。
project_create / project_list / project_rename / project_delete
ワークスペース内の中間レベルのコンテナ CRUD。組み込みの無題プロジェクトは削除できません。 project_delete は、プロジェクトにキャンバスがまだ含まれている場合は拒否します (アーカイブされたキャンバスもカウントされます。最初に移動または削除してください)。
ワークスペースを現在のプロジェクト ディレクトリにバインドして、そのキャンバスがオープン JSON (グローバル ~/.framesmith ストアではなく、コードと一緒にチェックインされる .framesmith/ ディレクトリ) としてリポジトリ内に存在するようにします。リポジトリごとに 1 回実行します。
.framesmith/workspace.json (バインディングとデザイン システム。したがって、新しいクローンはトークンを同じように解決します) と、キャンバスごとに 1 つのスラッグ名ファイルを保持するプロジェクトごとに 1 つのサブディレクトリを作成します。
.フレームスミス/
workspace.json # ワークスペース + プロジェクト[] + デザイン システム
デザインシステム/
デザイントークン.json
うい/
ブルームランディング.json
ログインフォーム.json
ワークスペースのプロジェクトを移行します

ects + キャンバスにアクセスし、リポジトリを残りのセッションの信頼できるソースにします。キャンバスはリポバインドまたはグローバルのいずれかであり、両方になることはありません。その後、サーバーは起動時に (作業ディレクトリからの起動時に) .framesmith/ を自動検出します。 .framesmith/ をコミットすると、デザインがコードとともに移動し、レビューできれいに差分が得られます。
バインドはリポジトリを ~/.framesmith/registry.json に記録するため、スタンドアロン ビューアはバインドされたリポジトリをグローバル ワークスペースと並んで 1 つのギャラリーに表示します (起動時およびレジストリが変更されるたびに読み取り専用ミラーを再構築します)。
シーングラフ上で操作を実行します。操作は行で区切られた文字列です。
# ドキュメントルートにフレームを挿入します
header=I("ドキュメント", { タイプ: "フレーム"、レイアウト: "水平"、フィル: "#1a1a2e"、パディング: 24、ギャップ: 16、幅: 1440、高さ: 80 })
# ヘッダーにテキストを挿入します
I(ヘッダー、{ タイプ: "テキスト"、コンテンツ: "マイアプリ"、fontSize: 24、fontWeight: 700、色: "#ffffff" })
# ノードを更新する
U("ノードID", { 塗りつぶし: "#e94560" })
# ノードを削除する
D("ノードID")
# ノードを新しい親にコピーします
copy=C("sourceId", "parentId", { fill: "#0f3460" })
# ノードを移動する
M("ノードID", "新しい親ID", 0)
# ノードを完全に置き換える
R("nodeId", { タイプ: "テキスト", コンテンツ: "置換" })
{ ok、nodeIds、results } を返します。 nodeIds は各バインドされた変数をマップします

[切り捨てられた]

## Original Extract

Open-source MCP server that gives AI assistants a visual design canvas, rendering HTML/CSS scene graphs to PNG via headless Chromium. - vicmaster/framesmith

GitHub - vicmaster/framesmith: Open-source MCP server that gives AI assistants a visual design canvas, rendering HTML/CSS scene graphs to PNG via headless Chromium. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
vicmaster
/
framesmith
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
master Branches Tags Go to file Code Open more actions menu Folders and files
262 Commits 262 Commits .framesmith .framesmith benchmark benchmark docs docs scripts scripts src src .gitignore .gitignore LICENSE LICENSE README.md README.md VISION.md VISION.md package-lock.json package-lock.json package.json package.json test-animations.ts test-animations.ts test-apply-structure.ts test-apply-structure.ts test-archive-view.ts test-archive-view.ts test-auto-relative.ts test-auto-relative.ts test-autofix.ts test-autofix.ts test-backdrop-filter.ts test-backdrop-filter.ts test-batch-design-idmap.ts test-batch-design-idmap.ts test-build-log.ts test-build-log.ts test-cliche.ts test-cliche.ts test-component-structures.ts test-component-structures.ts test-correctness-fixes.ts test-correctness-fixes.ts test-critique.ts test-critique.ts test-custom-fonts.ts test-custom-fonts.ts test-default-font.ts test-default-font.ts test-design-md-tolerant.ts test-design-md-tolerant.ts test-design-md.ts test-design-md.ts test-design-system-inheritance.ts test-design-system-inheritance.ts test-discoverability.ts test-discoverability.ts test-diversification.ts test-diversification.ts test-empty-thumbnail.ts test-empty-thumbnail.ts test-env.ts test-env.ts test-evaluate.ts test-evaluate.ts test-fluid-widths.ts test-fluid-widths.ts test-fonts-by-name.ts test-fonts-by-name.ts test-gradient-shadow-move-fixes.ts test-gradient-shadow-move-fixes.ts test-guidelines.ts test-guidelines.ts test-import-core.ts test-import-core.ts test-import-html.ts test-import-html.ts test-import-structure.ts test-import-structure.ts test-import-url.ts test-import-url.ts test-init.ts test-init.ts test-llm-judge.ts test-llm-judge.ts test-material-icons.ts test-material-icons.ts test-ops-parser.ts test-ops-parser.ts test-patterns.ts test-patterns.ts test-phase2.ts test-phase2.ts test-phase3.ts test-phase3.ts test-primitives.ts test-primitives.ts test-repo-assets.ts test-repo-assets.ts test-repo-canvases.ts test-repo-canvases.ts test-repo-external-change.ts test-repo-external-change.ts test-responsive-
[truncated]
An open-source MCP server that gives your AI coding agent a visual canvas. Sketch the UI, review it in a browser, agree on the design — before any framework code gets written.
Contents: Viewer · Installation · Tools · Usage Example · Workflow · Development
Above: the framesmith viewer. Workspaces and projects in the sidebar, canvases as live thumbnails on the right. AI agents create canvases via MCP tools; you browse them like Figma files.
MCP Client → stdio → framesmith server
↓
Scene Graph (in-memory JSON tree)
↓
HTML/CSS Renderer (inline styles)
↓
Puppeteer (headless Chromium → PNG)
Viewer
Run npx -p framesmith framesmith-viewer to start the standalone browser viewer (default port 3001). Open any canvas to review it at multiple breakpoints, compare them side-by-side, inspect the underlying JSON, or archive / delete.
Above: a single canvas in the detail view. The toolbar across the top exposes the breakpoint preview modes, Compare for side-by-side rendering, Fit for max-width, JSON for the raw scene graph, and lifecycle actions.
Quality panel. The canvas detail view shows a read-only quality inspector on the right: the heuristic canvas_evaluate score (0–100), per-category bars, and the issue list — each cliché tell with its category · tell badge, severity, and suggestion. Issues that canvas_autofix can resolve carry an auto-fixable tag, and clicking any issue highlights its node in the live preview. Every gallery card also shows a color-coded score badge so weak canvases stand out at a glance. The score matches what your agent sees over MCP (same fast-mode evaluation, genre-relaxed by the canvas's preset) — it's computed for display only and never written back.
Design-system panel. A second inspector tab shows the canvas's effective design tokens — color swatches, type scale, spacing, and radius — resolved through the full workspace ▸ project ▸ canvas inheritance chain. Each section notes its dominant source layer, and any token that overrides it is tagged ( canvas / project ) so you can see at a glance what a given canvas customized versus inherited.
The viewer is purely read-only — every canvas is authored through MCP tool calls from your AI assistant. Files persist to ~/.framesmith/canvases/ so the viewer keeps showing them across sessions.
No clone or build needed — register framesmith with your MCP client via npx (requires Node 20+).
claude mcp add framesmith -- npx -y framesmith
Codex
[ mcp_servers . framesmith ]
command = " npx "
args = [ " -y " , " framesmith " ]
Cursor
Add to ~/.cursor/mcp.json (or per-project .cursor/mcp.json ):
{
"mcpServers" : {
"framesmith" : {
"command" : " npx " ,
"args" : [ " -y " , " framesmith " ]
}
}
}
Windsurf
Add to ~/.codeium/windsurf/mcp_config.json :
{
"mcpServers" : {
"framesmith" : {
"command" : " npx " ,
"args" : [ " -y " , " framesmith " ]
}
}
}
VS Code + MCP extension
Add to .vscode/mcp.json (project-scoped) or your global MCP settings:
{
"servers" : {
"framesmith" : {
"command" : " npx " ,
"args" : [ " -y " , " framesmith " ]
}
}
}
Any other MCP-compatible client
framesmith speaks standard stdio MCP. Point your client at npx -y framesmith using whatever config shape your client expects.
Optional: set FRAMESMITH_VIEWER_URL=http://localhost:3001 in the MCP server env to pin it to a long-lived standalone viewer process — see Running the viewer .
Build from source (for development)
git clone https://github.com/vicmaster/framesmith.git
cd framesmith
npm install
npm run build
# then point your client at: node /path/to/framesmith/dist/index.js
Tools
One-call onboarding — the recommended first call each session , and safe to run repeatedly (idempotent). Binds the current repo if it isn't already (canvases become checked-in JSON under .framesmith/ ), ensures the convention projects exist, and returns the live state you need to start working.
Returns the bound workspace + project IDs (binding re-keys IDs to repo-* — use the ones init returns), the on-disk layout, the workspace-layer token count, a workflow cheatsheet, the current gotchas, the framesmith://guidelines URI, and the viewer URL. It does not seed design tokens — set those at the workspace layer with workspace_set_design_system . The default Foundations project is just a canvas that visualizes the workspace tokens (which is where the design system actually lives).
Create a new canvas. If projectId is omitted, it lands in the built-in Untitled project of the Personal workspace.
The response also carries a diversification signal for the target project: the recently-built structures (newest first) and a hint to differ on at least one taxonomy axis, so successive canvases don't converge on the same layout. It's advisory — never blocking.
List canvases. Excludes archived canvases by default.
Returns [{ id, name, createdAt, lastModified, projectId, archived }] .
canvas_move / canvas_archive / canvas_unarchive / canvas_delete
Canvas lifecycle. canvas_move reassigns a canvas to a different project. canvas_archive sets a soft-delete flag (canvas stays on disk, hidden from default canvas_list ); canvas_unarchive clears it. canvas_delete removes the canvas and its file permanently — irreversible.
Get the URL of the live viewer plus per-canvas URLs. Share these with the user so they can open the design in their browser. No params.
{
"url" : " http://localhost:3001 " ,
"gallery" : " http://localhost:3001 " ,
"canvases" : [
{ "name" : " Login " , "viewer" : " http://localhost:3001/canvas/abc123 " }
]
}
canvas_create already returns the per-canvas viewer URL in its response; reach for viewer_url when you want the gallery URL or to enumerate every existing canvas's URL in one call.
workspace_create / workspace_list / workspace_rename / workspace_delete
Top-level container CRUD. The built-in Personal workspace cannot be deleted, and workspace_delete refuses if the workspace still contains projects (move or delete them first).
project_create / project_list / project_rename / project_delete
Mid-level container CRUD inside a workspace. The built-in Untitled project cannot be deleted. project_delete refuses if the project still contains any canvases (archived ones still count — move or delete them first).
Bind a workspace to the current project directory so its canvases live in the repo as open JSON — a .framesmith/ directory checked in alongside the code, instead of the global ~/.framesmith store. Run it once per repo.
It creates .framesmith/workspace.json (the binding plus the design system, so a fresh clone resolves tokens identically) and one subdirectory per project holding one slug-named file per canvas:
.framesmith/
workspace.json # workspace + projects[] + design system
design-system/
design-tokens.json
ui/
bloom-landing.json
login-form.json
It migrates the workspace's projects + canvases in and makes the repo the source of truth for the rest of the session. A canvas is either repo-bound or global, never both. Afterwards the server auto-detects .framesmith/ on startup (walking up from its working directory). Commit .framesmith/ so designs travel with the code and diff cleanly in review.
The bind also records the repo in ~/.framesmith/registry.json , so the standalone viewer shows bound repos alongside your global workspaces in one gallery (it rebuilds that read-only mirror on launch and whenever the registry changes).
Execute operations on the scene graph. Operations are line-separated strings:
# Insert a frame into the document root
header=I("document", { type: "frame", layout: "horizontal", fill: "#1a1a2e", padding: 24, gap: 16, width: 1440, height: 80 })
# Insert text into the header
I(header, { type: "text", content: "My App", fontSize: 24, fontWeight: 700, color: "#ffffff" })
# Update a node
U("nodeId", { fill: "#e94560" })
# Delete a node
D("nodeId")
# Copy a node to a new parent
copy=C("sourceId", "parentId", { fill: "#0f3460" })
# Move a node
M("nodeId", "newParentId", 0)
# Replace a node entirely
R("nodeId", { type: "text", content: "Replaced" })
Returns { ok, nodeIds, results } . nodeIds maps each bound varia

[truncated]
