---
source: "https://www.agent-kits.com/kitforge"
hn_url: "https://news.ycombinator.com/item?id=48723068"
title: "KitForge–generate an AI agent manifest; scaffold with enforced approval gates"
article_title: "KitForge — enforcing agent scaffold + safety framework — AgentKits"
author: "stoicstoic"
captured_at: "2026-06-29T18:38:12Z"
capture_tool: "hn-digest"
hn_id: 48723068
score: 1
comments: 0
posted_at: "2026-06-29T18:20:41Z"
tags:
  - hacker-news
  - translated
---

# KitForge–generate an AI agent manifest; scaffold with enforced approval gates

- HN: [48723068](https://news.ycombinator.com/item?id=48723068)
- Source: [www.agent-kits.com](https://www.agent-kits.com/kitforge)
- Score: 1
- Comments: 0
- Posted: 2026-06-29T18:20:41Z

## Translation

タイトル: KitForge – AI エージェント マニフェストを生成します。強制承認ゲートを備えた足場
記事のタイトル: KitForge — エージェントの足場 + 安全フレームワークの強制 — AgentKits
説明: AgentKits の KitForge は、ブロックする権限バジェット、改ざん時に失敗する HMAC チェーン監査ログ、停止する人間参加型ゲートなどのガードレールを実際に適用する LangGraph エージェント スキャフォールドを生成します。無料、Python、MIT。

記事本文:
KitForge — エージェント スキャフォールド + 安全フレームワークの強制 — AgentKits コンテンツにスキップ AgentKits ツールの参照 ブログ 保存された検索について ⌘ K ツール · オープン ソース
検証済みのブループリントを LangGraph スタイルのエージェントに変換する決定論的な足場。ガードレールが実際に強制する安全層を備えています。 Python、MIT、アカウントなし。生成されるものにはすべて「Generated by www.agent-kits.com」という属性が付けられます。
なぜこれが存在するのか。ほとんどの「エージェントの安全性」フレームワークでは、コードが強制することのないガードレール、つまり true を返す監査検証、ブロックされない予算カウンターについて説明しています。 KitForge は逆の方法で構築されています。コントロールはモデルの周りの決定論的なゲートであり、リポジトリには実際の違反を阻止することを証明するテストが同梱されています。 python -m pytest testing/ を実行し、改ざんされた監査ログが検証に失敗し、承認が拒否されてアクションが停止するのを確認します。
ツール呼び出し、トークン、および壁時計のカウンターは、使い果たされると減ったり上がったりします。エージェントのループは停止します。警告はログに記録されず、続行されます。
改ざん時に失敗した監査証跡
追加専用の HMAC チェーン ログ。 verify() はチェーンを再計算し、エントリが変更、並べ替え、または削除された場合は false を返します。何もしないわけではありません。
拒否またはタイムアウトした承認が発生すると、保護されたアクションは実行されません。承認は構造的に必要であり、丁寧に要求されるものではありません。
サーキットブレーカー + 出力検証
ツールが繰り返し故障するとブレーカーが開き、それ以上の呼び出しを拒否します。ツールの出力は、エージェントの状態に影響する前に、宣言されたスキーマに対して検証されます。
ここでエージェントをスケッチします。有効な blueprint.json を取得してローカルに保存し、KitForge にフィードします。また、コンプライアンス スキャナーでワンクリックで設計をチェックできます。コード自体は、Python CLI によってマシン上で生成されます。これにより、実行されるブループリントが構築されます。
名前と

構築するツールが少なくとも 1 つ必要です。
kitforge.zip を解凍し、kitforge を cd します。
pip install pydantic # 唯一のランタイム dep
python -m pytest testing/ -q # 13 の強制テストに合格
# デモエージェントを生成する (四半期収益アナライザー)
python -m kitforge デモ -o ./my-agent
cd my-agent && エクスポート KITFORGE_AUDIT_KEY=$(openssl rand -hex 32)
python main.py # ループを実行します。正直なスコープを承認するまで、HITL ゲートは「公開」を停止します
Python + LangGraph パターンのみ。生成されるグラフ ランナーは意図的に最小限になっています。 langgraph.StateGraph を運用環境用に交換します。安全配線は同一です。
生成されるツールは[MOCK]スタブです。これらを実際のシステム (および実際の承認者 - Slack、電子メール、ウェブ) に接続するのがラストワンマイルであり、それはあなたのものです。
フレームワークは、同梱されている制御を強制します。これは、特定の展開のセキュリティ レビューに代わるものではありません。
AgentKits ガバナンス ツールの一部。 「コンプライアンス スキャナーとリスク評価」も参照してください。
本番環境に対応した AI エージェント ブループリント — 実際のエージェントを構築するためのアーキテクチャ、プロンプト、ツール、ワークフロー。ログインがありません。

## Original Extract

KitForge by AgentKits generates LangGraph agent scaffolds whose guardrails actually enforce: authority budgets that block, an HMAC-chained audit log that fails on tamper, and human-in-the-loop gates that halt. Free, Python, MIT.

KitForge — enforcing agent scaffold + safety framework — AgentKits Skip to content AgentKits Browse Tools Blog About Saved Search ⌘ K Tools · open source
A deterministic scaffold that turns a validated blueprint into a LangGraph-style agent — with a safety layer whose guardrails actually enforce . Python, MIT, no account. Everything it generates carries a “Generated by www.agent-kits.com ” attribution.
Why this exists. Most “agent safety” frameworks describe guardrails the code never enforces — an audit verify that returns true, budget counters that never block. KitForge is built the opposite way: the controls are deterministic gates around the model, and the repo ships the tests that prove they stop a real violation. Run python -m pytest tests/ and watch a tampered audit log fail verification and a rejected approval halt the action.
Tool-call, token, and wall-clock counters decrement and raise when exhausted. The agent loop stops — it does not log a warning and continue.
Audit trail that fails on tamper
An append-only, HMAC-chained log. verify() recomputes the chain and returns false if any entry was altered, reordered, or deleted. Not a no-op.
A rejected or timed-out approval raises and the guarded action never runs. Approval is required structurally, not requested politely.
Circuit breaker + output validation
A breaker opens after repeated tool failures and refuses further calls; tool output is validated against its declared schema before it touches agent state.
Sketch an agent here. You get a valid blueprint.json to save and feed to KitForge locally — and a one-click check of the design in the Compliance Scanner. The code itself is generated on your machine by the Python CLI; this builds the blueprint it runs on.
Add a name and at least one tool to build.
unzip kitforge.zip && cd kitforge
pip install pydantic # the only runtime dep
python -m pytest tests/ -q # 13 enforcement tests pass
# generate the demo agent (Quarterly Earnings Analyzer)
python -m kitforge demo -o ./my-agent
cd my-agent && export KITFORGE_AUDIT_KEY=$(openssl rand -hex 32)
python main.py # runs the loop; HITL gate stops 'publish' until you approve Honest scope
Python + LangGraph patterns only. The generated graph runner is intentionally minimal; swap in langgraph.StateGraph for production — the safety wiring is identical.
Generated tools are [MOCK] stubs. Wiring them to your real systems (and your real approver — Slack, email, web) is the last mile, and it's yours.
The framework enforces the controls it ships; it is not a substitute for a security review of your specific deployment.
Part of the AgentKits governance tools. See also the Compliance Scanner and Risk Assessment .
Production-ready AI Agent Blueprints — architecture, prompts, tools, and workflows to build real agents. No login.
