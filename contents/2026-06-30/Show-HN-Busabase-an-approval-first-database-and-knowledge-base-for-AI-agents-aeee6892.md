---
source: "https://github.com/busabase/busabase"
hn_url: "https://news.ycombinator.com/item?id=48734021"
title: "Show HN: Busabase – an approval-first database and knowledge base for AI agents"
article_title: "GitHub - busabase/busabase: ⚡ The approval-first, open-source database & knowledge base for AI agents — review & approve every change before it's trusted. Local-first, self-hostable Airtable/Notion alternative. · GitHub"
author: "chepy"
captured_at: "2026-06-30T15:49:33Z"
capture_tool: "hn-digest"
hn_id: 48734021
score: 1
comments: 0
posted_at: "2026-06-30T15:21:50Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Busabase – an approval-first database and knowledge base for AI agents

- HN: [48734021](https://news.ycombinator.com/item?id=48734021)
- Source: [github.com](https://github.com/busabase/busabase)
- Score: 1
- Comments: 0
- Posted: 2026-06-30T15:21:50Z

## Translation

タイトル: Show HN: Busabase – AI エージェント向けの承認優先データベースおよびナレッジ ベース
記事のタイトル: GitHub - Busabase/busabase: ⚡ AI エージェント向けの承認優先のオープンソース データベースおよびナレッジ ベース — 信頼される前に、すべての変更を確認して承認します。ローカルファーストで自己ホスト可能な Airtable/Notion の代替品。 · GitHub
説明: ⚡ AI エージェント向けの承認優先のオープンソース データベースとナレッジ ベース — 信頼される前に、すべての変更をレビューして承認します。ローカルファーストで自己ホスト可能な Airtable/Notion の代替品。 - ブサベース/ブサベース

記事本文:
GitHub - Busabase/busabase: ⚡ AI エージェント向けの承認優先のオープンソース データベースおよびナレッジ ベース — 信頼される前にすべての変更をレビューおよび承認します。ローカルファーストで自己ホスト可能な Airtable/Notion の代替品。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
ああ、ああ！
ありました

ロード中にエラーが発生しました。このページをリロードしてください。
ブサベース
/
ブサベース
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
28 コミット 28 コミット .github/ workflows .github/ workflows apps apps パッケージ パッケージ .gitignore .gitignore ライセンス ライセンス README.md README.md biome.json biome.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yaml 表示すべてのファイル リポジトリ ファイルのナビゲーション
AI によって生成されたコンテンツ、ビジネス データ、データセット、マルチモーダルな知識を対象としたローカル ファーストのレビュー データベース。
AI は無限のデータを生成できます。Busabase は、信頼できるデータをレビュー、承認、マージする場所です。
クイックスタート ·
スクリーンショット ·
使用例 ·
API・
比較する ·
ロードマップ
Busabase は、1 つの単純な問題に対応する無料のオープンソース ( MIT ) アプリです。
AI は無限のコンテンツとデータを生成できますが、何が信頼できるかを誰かが判断する必要があります。
Busabase は、その承認プロセスにホーム、つまり承認優先のデータベースと AI エージェントの知識ベースを提供します。これは、プライベート CMS、プロジェクト データベース、および組み込みの変更リクエスト、操作、コメント、監査証跡、アプリと AI エージェント用のシンプルな API を備えた構造化された信頼できる情報源です。
無料＆オープンソース。地元第一主義。まずはレビュー。エージェント対応。
SaaS、アカウント、ベンダーを必要とせず、自分で実行します。データがマシンから流出する必要はありません。
好きな方法を選択してください。どの方法でも同じレビュー優先データベースが提供されます。
⚡ 今すぐ実行 — コマンド 1 つ、セットアップ不要
npxバスベースサーバー
http://localhost:15419/dashboard/inbox を開きます。これがセットアップ全体です。完全なローカルです。
実行するデータベースも構成するものも何もないインスタンス。 Busabase シードの例
ベース、レコード、および Ch

最初のリクエストでリクエストを行うため、レビューを確認できます
すぐにワークフローを実行できます。
npm i -g Busabase # 一度インストールすれば、次だけインストールします: Busabase Server
npx Busabase-cli --help # 独自の API クライアント (任意の Busabase サーバーと通信)
🐳 ドッカー
docker run --rm -p 15419:15419 Busabase/busabase
http://localhost:15419/dashboard/inbox を開きます。すべてをローカルに保存します - いいえ
外部サービス。イメージは Docker Hub ( Busabase/busabase ) および GHCR に公開されます
( ghcr.io/busabase/busabase )。
ネイティブ アプリをお好みですか? macOS、Windows、Linux 用の Busabase を次の場所からダウンロードします。
Busabase.com/download 。完全ネイティブかつ完全オフライン —
すべてのデータはコンピュータ上に残り、オンラインになることはありません。
pnpmインストール
cp apps/busabase/.env.example apps/busabase/.env
pnpm --filter Busabase dev
http://localhost:15419/dashboard/inbox を開きます。ローカルスタートチェックが最初に実行されます。
依存関係、 PG_DATABASE_URL 、または STORAGE_URL が欠落しているため、セットアップ メッセージが表示されて失敗します。
空白のダッシュボードの代わりに。デフォルトの .env.example では、.data/busabase で PGlite を使用します。
および .data/busabase-storage の下のローカル ファイル ストレージ。
変更リクエストをレビューするための受信箱
レコードレベルの履歴と監査証跡
.data/busabase でのローカル PGlite 永続化
アプリ、ワークフロー、AI エージェント用の REST API エンドポイント
Busabase には組み込みモデルがありません。独自のエージェント (クロード コード、カーソル、コーデックス、
Gemini CLI、OpenClaw、Hermes…) を使用します。プロンプトを 1 つ貼り付けると、それ自体がオンボードされ、提案されます。
あなたが承認した変更リクエストとして変更されます。
Busabase Agent Skill を読んで従ってください。これが唯一の真実の情報源です。
http://localhost:15419/SETUP_SKILL.md
オンボーディングに従って私を設定し、私の承認なしに ChangeRequest をマージしないでください。英語で返信してください。
→ Bring Your Own Agent — 完全なフロー、その後
npx スキルは Busabase を追加/スキルは永続的なものをインストールします

貼り直す必要がないようにします。
npx Busabase サーバーとデスクトップ アプリは 1 つのローカル データ ルートを共有するため、
どの方法で起動しても同じデータベース:
~/.busabase/data/
§── pgdata/ # 組み込み PGlite データベース
└── ストレージ/アップロードされた添付ファイルの数
BUSABASE_DATA_DIR (または Busabase server --db <dir> ) を使用して別の場所を指定するか、または
外部 Postgres / S3 を使用するには、PG_DATABASE_URL / STORAGE_URL を直接設定します。
Docker はコンテナー内の /data に書き込みます — 同じホストフォルダーをバインドマウントして共有します
CLI とデスクトップ アプリを備えたその 1 つのデータベース:
docker run --rm -p 15419:15419 -v ~ /.busabase/data:/data Busabase/busabase
(pnpm dev を使用してソースから実行すると、代わりにリポジトリの .env 、つまり .data/busabase が使用されます。)
PGlite データベースを保持できるプロセスは一度に 1 つだけなので、一度に 1 つのランチャーを実行します。
保留中の変更リクエスト、レビュー担当者のステータス、および承認アクションが含まれる受信箱
フィールドの差分やレビュー担当者のアクションを含む、マージ前にエージェントが提案した変更
フィールド、コメント、レビュー履歴、系統を含むレコード詳細ページ
構造化レコードと豊富なフィールドを示すベーステーブル
ベース内のレコード — 型付きフィールド、豊富な値、承認ステータスが一目で分かる
ベース間のシード済レコード間の関係を示すグラフ・ビュー
モバイルで
Busabase モバイル アプリで、同じ受信トレイ、提案プレビュー、信頼できるレコードを使用して、携帯電話からエージェントの変更リクエストを確認して承認します。
ほとんどのデータベースはデータの保存に優れています。ほとんどの CMS ツールはコンテンツの公開に優れています。ほとんどのコード プラットフォームはファイルのレビューに優れています。
Busabase は、AI を多用するチームが現在必要としている中間層向けです。
デフォルトでは承認が優先され、エージェントに優しい設計になっており、ローカルで実行できるほど小さいです。
エージェントが作成できるすべてのベース - 最初にレビューされます。 Busabase 上で人々が構築するもののいくつか:
→ 全て見る

16 の使用例 (スクリーンショット付き)
Busabase は、データ ワークフローのイベント ソースになることができます。
レビュー中に、人間は変更リクエストをマージする前に、ACP 互換エージェントに変更リクエストを改善するよう依頼できます。
ポリシー、品質、一貫性をチェックする
マージ後、承認されたデータは下流の自動化をトリガーできます。
ETL またはデータセットのエクスポートを開始する
外部 ACP エージェントを呼び出してワークフローを続行します
そのため、Busabase は単なるデータを保存する場所ではありません。これは、人間、アプリケーション、エージェント間の制御されたハンドオフ ポイントになります。
現地エージェントがナレッジベースを運営します
Busabase は、自分のコンピュータ上ですでに実行されているエージェントによって駆動されるように構築されています。
API はローカルで信頼できるものであるため、コーディングおよび自動化エージェント ( OpenClaw、Codex、Claude Code、Hermes 、および同様のローカル スキル) を Busabase インスタンスで直接指定できます。承認されたナレッジを読み取り、それに対してスキルを実行し、変更を変更リクエストとして提案することができます。
Busabase を使用してローカル エージェントができること:
承認された個人的な知識を根拠のあるコンテキストとして読み取る
ベースをクエリまたは要約するローカル スキルを実行する
新しいレコードまたは編集をレビュー可能な変更リクエストとして提案する
制御されない書き込みアクセスを行わずにデータを強化、クリーンアップ、またはラベル付けする
何かが信頼される前に人間の承認を待ちます
ローカルエージェントは承認されたナレッジを読み取ります ->
変更リクエストを提案します ->
自分のマシンでレビューします ->
承認 -> ローカルの信頼できる情報源にマージ
これにより、ループが完全にハードウェア上に保持されます。エージェントは、実際に構造化されたメモリを使用して作業を行うことができ、あなたは信頼できるものに対する権限を保持します。エージェントが機能するために個人データがコンピュータから流出する必要はありません。
→ ステップバイステップ: 独自のエージェントの導入 — オンボードにプロンプ​​トを 1 つ貼り付けてから、npx スキルで Busabase/skills を追加し、エージェントがセッションごとに使用する永続的なスキルをインストールします。

イオン。
OpenClaw がローカル コンピュータ上のエージェントの革命であるなら、BusaBase はローカル コンピュータ上のデータベースとナレッジ ベースの革命です。
Busabase は単に「この行の最新の値は何ですか?」と尋ねるのではありません。
これは作成、更新、削除、またはバリアント操作ですか?
AI エージェントは受け入れられる前に何を作成しましたか?
エージェントの出力をレビューしたのは誰ですか?
人間がエージェントに修正を依頼したのでしょうか？
提案は統合されましたか、それとも拒否されましたか?
マージ後にどのような自動化が実行されましたか?
後でその決定を追跡することはできますか?
そのため、Busabase は AI エージェントの作業に特に役立ちます。エージェントは、草案、ラベル、概要、調整、運用更新を作成できますが、Busabase は、それらの出力が信頼できるデータになる前に、人間にプレビュー レイヤーを提供します。
Busabase は使い慣れたツールと重複していますが、AI エージェントがデータを書き込み、それが信頼される前に人間がそれを承認するという別のジョブに最適化されています。
これらのほとんどは、ライターが信頼できる人間 (または自分が書いたスクリプト) であることを前提としています。 Busabase は、書き込み者が AI エージェントであることが多く、すべてのエージェントの書き込みが自動的に信頼されるべきではないと想定しています。したがって、エージェント駆動データベースに必要なものが追加されます。
プロポーザルレイヤー - エージェントは行を直接編集する代わりに、変更リクエストを送信します。
マージ前のプレビュー — エージェントが生成した内容をフィールドごとに正確に確認します。
修正ループ — 提案が受け入れられる前に、エージェントに提案を修正するよう依頼します。
監査証跡 - すべての読み取り、書き込み、レビュー、マージ、削除が追跡可能です。
ローカルで信頼できる API — 人間のスプレッドシート ユーザーだけでなく、自分のマシン上のエージェント向けにも構築されています。
Airtable / APITable: 人々が編集できるデータベース。
Busabase: エージェントが提案し、人々が承認するためのデータベース。
クラウドファーストではなくローカルファースト。デフォルトでは、データはマシンまたはプライベート ネットワーク上に残ります。リモートまたはエージェントアクセスが必要ですか? eへのトンネルを開いてください

すべてを中央のクラウド データベースにコピーするのではなく、選択したエンドポイントを公開します。自己ホスト型、無料、オープンソース。
複数のオペレーションを含む変更リクエスト
作成、更新、削除、およびバリアント操作
レコード変更の履歴をコミットする
レコードおよびレビューオブジェクトに対するコメント
読み取りおよび書き込みの監査イベント
マークダウン、HTML、リンク、ファイル、リレーションフィールド、およびリッチフィールドタイプ
検索可能なインデックス付きフィールド値
アプリ、ワークフロー、AI エージェント用の REST API
エージェントが提案した変更に対する人間参加型のコラボレーション
マージ前の AI エージェント出力プレビュー
承認された運用記録の信頼できる唯一の情報源
承認されたデータ変更後に自動化がトリガーされる
レビュー中およびマージ後の ACP エージェント フック
Busabase は、ダッシュボード クライアント、アプリ、AI エージェント用のシンプルなローカル REST API を公開します。
この API は、オープンソース バージョンで信頼できるローカル ネットワークまたはプライベート ネットワークで使用することを目的としています。
これはコア エージェント ループです。スキーマを読み、提案を送信し、UI でレビューしてから、
マージ後の正規レコード。
# 1. ブログ投稿ベースを見つけます。
BLOG_BASE_ID= $(curl -s http://localhost:15419/api/v1/bases \
| jq -r ' .[] | select(.slug == "ブログ") | .id ')
# 2. エージェントに新しいレコードを提案させます。これにより、正規データではなく変更リクエストが作成されます。
CHANGE_REQUEST_ID= $(curl -s -X POST \
「 http://

[切り捨てられた]

## Original Extract

⚡ The approval-first, open-source database & knowledge base for AI agents — review & approve every change before it's trusted. Local-first, self-hostable Airtable/Notion alternative. - busabase/busabase

GitHub - busabase/busabase: ⚡ The approval-first, open-source database & knowledge base for AI agents — review & approve every change before it's trusted. Local-first, self-hostable Airtable/Notion alternative. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Uh oh!
There was an error while loading. Please reload this page .
busabase
/
busabase
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
28 Commits 28 Commits .github/ workflows .github/ workflows apps apps packages packages .gitignore .gitignore LICENSE LICENSE README.md README.md biome.json biome.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yaml View all files Repository files navigation
Local-first review database for AI-generated content, business data, datasets & multimodal knowledge.
AI can generate endless data — Busabase is where you review, approve, and merge what's good enough to trust.
Quick Start ·
Screenshots ·
Use Cases ·
API ·
Compare ·
Roadmap
Busabase is a free and open-source ( MIT ) app for one simple problem:
AI can generate endless content and data, but someone still needs to decide what is good enough to trust.
Busabase gives that approval process a home — an approval-first database and knowledge base for AI agents. It is a private CMS, project database, and structured source of truth with built-in Change Requests, Operations, comments, audit trails, and a simple API for apps and AI agents.
Free & open source. Local-first. Review-first. Agent-ready.
Run it yourself — no SaaS, no account, no vendor. Your data never has to leave your machine.
Pick whichever way you like — all of them give you the same review-first database.
⚡ Run it now — one command, zero setup
npx busabase server
Open http://localhost:15419/dashboard/inbox . That's the whole setup: a full local
instance with no database to run and nothing to configure. Busabase seeds example
Bases, records, and Change Requests on first request, so you can inspect the review
workflow immediately.
npm i -g busabase # install once, then just: busabase server
npx busabase-cli --help # the API client on its own (talks to any busabase server)
🐳 Docker
docker run --rm -p 15419:15419 busabase/busabase
Open http://localhost:15419/dashboard/inbox . Stores everything locally — no
external services. Images are published to Docker Hub ( busabase/busabase ) and GHCR
( ghcr.io/busabase/busabase ).
Prefer a native app? Download Busabase for macOS, Windows, and Linux at
busabase.com/download . Fully native and fully offline —
all your data stays on your computer, never online.
pnpm install
cp apps/busabase/.env.example apps/busabase/.env
pnpm --filter busabase dev
Open http://localhost:15419/dashboard/inbox . A local-start check runs first: if
dependencies, PG_DATABASE_URL , or STORAGE_URL are missing, it fails with a setup message
instead of a blank dashboard. The default .env.example uses PGlite under .data/busabase
and local file storage under .data/busabase-storage .
an Inbox for reviewing Change Requests
record-level history and audit trails
local PGlite persistence under .data/busabase
REST API endpoints for apps, workflows, and AI Agents
Busabase has no built-in model — you point your own agent (Claude Code, Cursor, Codex,
Gemini CLI, OpenClaw, Hermes…) at it. Paste one prompt and it onboards itself, then proposes
changes as Change Requests you approve.
Read and follow the Busabase Agent Skill — it is the single source of truth:
http://localhost:15419/SETUP_SKILL.md
Follow its onboarding to set me up, and never merge a ChangeRequest without my approval. Reply to me in English.
→ Bring Your Own Agent — the full flow, then
npx skills add busabase/skills installs a permanent skill so you never re-paste it.
npx busabase server and the desktop app share one local data root so you see the
same database whichever way you launch:
~/.busabase/data/
├── pgdata/ # embedded PGlite database
└── storage/ # uploaded attachments
Point them somewhere else with BUSABASE_DATA_DIR (or busabase server --db <dir> ), or
set PG_DATABASE_URL / STORAGE_URL directly to use external Postgres / S3.
Docker writes to /data inside the container — bind-mount the same host folder to share
that one database with the CLI and desktop app:
docker run --rm -p 15419:15419 -v ~ /.busabase/data:/data busabase/busabase
(Running from source with pnpm dev instead uses the repo's .env , i.e. .data/busabase .)
Only one process can hold the PGlite database at a time, so run one launcher at a time.
Inbox with pending Change Requests, reviewer status, and approval actions
Agent-proposed changes before merge, including field diffs and reviewer actions
Record detail page with fields, comments, review history, and lineage
Base table showing structured records and rich fields
Records inside a Base — typed fields, rich values, and approval status at a glance
Graph view showing relationships between seeded records across Bases
On mobile
Review and approve agent Change Requests from your phone — the same inbox, proposal preview, and trusted records, in the Busabase mobile app .
Most databases are good at storing data. Most CMS tools are good at publishing content. Most code platforms are good at reviewing files.
Busabase is for the middle layer that AI-heavy teams now need:
It is approval-first by default, agent-friendly by design, and still small enough to run locally.
Any base an agent can write — reviewed first. A few of the things people build on Busabase:
→ See all 16 use cases, with screenshots
Busabase can become an event source for data workflows.
During review, a human can ask an ACP-compatible agent to improve the Change Request before it is merged:
check policy, quality, or consistency
After merge, approved data can trigger downstream automation:
kick off an ETL or dataset export
call an external ACP Agent to continue the workflow
That makes Busabase more than a place to store data. It becomes a controlled handoff point between humans, applications, and agents.
Local Agents Operate Your Knowledge Base
Busabase is built to be driven by the agents already running on your own computer.
Because the API is local and trusted, you can point coding and automation agents — OpenClaw, Codex, Claude Code, Hermes , and similar local skills — directly at your Busabase instance. They can read approved knowledge, run skills against it, and propose changes back as Change Requests.
What a local agent can do with Busabase:
read your private, approved knowledge as grounded context
run a local skill that queries or summarizes your bases
propose new records or edits as reviewable Change Requests
enrich, clean, or label data without uncontrolled write access
wait for human approval before anything becomes trusted
Local agent reads approved knowledge ->
proposes a Change Request ->
you review on your own machine ->
approve -> merged into your local source of truth
This keeps the loop entirely on your hardware. The agent gets a real, structured memory to work against, and you keep authority over what becomes trusted — no private data has to leave your computer for any of it to work.
→ Step-by-step: Bring Your Own Agent — paste one prompt to onboard, then npx skills add busabase/skills to install a permanent skill your agent uses every session.
If OpenClaw is the revolution for agents on your local computer, then BusaBase is the revolution for the database and knowledge base on your local computer.
Busabase is not just asking "what is the latest value in this row?"
Is this a create, update, delete, or variant operation?
What did the AI Agent produce before it was accepted?
Who reviewed the agent output?
Did a human ask the agent to revise?
Was the proposal merged or rejected?
What automation ran after merge?
Can we trace the decision later?
That makes Busabase especially useful for AI Agent work. Agents can produce drafts, labels, summaries, reconciliations, or operational updates, but Busabase gives humans a preview layer before those outputs become trusted data.
Busabase overlaps with familiar tools, but it's optimized for a different job: AI agents writing data, and humans approving it before it's trusted.
Most of these assume the writer is a trusted human (or a script you wrote). Busabase assumes the writer is often an AI agent — and that not every agent write should be trusted automatically. So it adds what an agent-driven database needs:
A proposal layer — agents submit Change Requests instead of editing rows directly.
A preview before merge — see exactly what the agent produced, field by field.
A revise loop — ask the agent to fix the proposal before it's accepted.
An audit trail — every read, write, review, merge, and delete is traceable.
A local, trusted API — built for agents on your own machine, not just human spreadsheet users.
Airtable / APITable: a database for people to edit.
Busabase: a database for agents to propose and people to approve.
Local-first, not cloud-first. Your data stays on your machine or private network by default. Need remote or agent access? Open a tunnel to expose selected endpoints instead of copying everything into a central cloud database. Self-hostable, free, and open-source.
Change Requests with multiple Operations
Create, update, delete, and variant operations
Commit history for record changes
Comments on records and review objects
Audit events for reads and writes
Markdown, HTML, links, files, relation fields, and rich field types
Search-ready indexed field values
REST API for apps, workflows, and AI agents
Human-in-the-loop collaboration for agent-proposed changes
AI Agent output preview before merge
Single source of truth for approved operational records
Automation triggers after approved data changes
ACP Agent hooks during review and after merge
Busabase exposes a simple local REST API for dashboard clients, apps, and AI agents.
The API is meant for trusted local or private-network usage in the open-source version.
This is the core agent loop: read the schema, submit a proposal, review it in the UI, then read the
canonical record after merge.
# 1. Find the Blog Posts base.
BLOG_BASE_ID= $( curl -s http://localhost:15419/api/v1/bases \
| jq -r ' .[] | select(.slug == "blog") | .id ' )
# 2. Let an agent propose a new record. This creates a Change Request, not canonical data.
CHANGE_REQUEST_ID= $( curl -s -X POST \
" http:/

[truncated]
