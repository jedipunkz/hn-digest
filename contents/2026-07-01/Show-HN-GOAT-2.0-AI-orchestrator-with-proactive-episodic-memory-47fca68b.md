---
source: "https://github.com/takashikiari/GOAT2-General-Orchestrated-Agent-Topology"
hn_url: "https://news.ycombinator.com/item?id=48751769"
title: "Show HN: GOAT 2.0 – AI orchestrator with proactive episodic memory"
article_title: "GitHub - takashikiari/GOAT2-General-Orchestrated-Agent-Topology: GOAT-General-Orchestration-Agent-Topology Multi-agent AI orchestration with async DAG execution, 3-tier memory (Redis/ChromaDB/Letta), behavioral learning, and independent validation. Telegram interface. Built modular with central conf\n[truncated]"
author: "takashikiari"
captured_at: "2026-07-01T19:32:34Z"
capture_tool: "hn-digest"
hn_id: 48751769
score: 1
comments: 0
posted_at: "2026-07-01T19:12:55Z"
tags:
  - hacker-news
  - translated
---

# Show HN: GOAT 2.0 – AI orchestrator with proactive episodic memory

- HN: [48751769](https://news.ycombinator.com/item?id=48751769)
- Source: [github.com](https://github.com/takashikiari/GOAT2-General-Orchestrated-Agent-Topology)
- Score: 1
- Comments: 0
- Posted: 2026-07-01T19:12:55Z

## Translation

タイトル: Show HN: GOAT 2.0 – プロアクティブなエピソード記憶を備えた AI オーケストレーター
記事タイトル: GitHub - takasekiari/GOAT2-General-Orchestrated-Agent-Topology: GOAT-General-Orchestration-Agent-Topology 非同期 DAG 実行、3 層メモリ (Redis/ChromaDB/Letta)、行動学習、および独立した検証を備えたマルチエージェント AI オーケストレーション。テレグラムインターフェイス。中央の設定を使用してモジュール式に構築
[切り捨てられた]
説明: GOAT-General-Orchestration-Agent-Topology 非同期 DAG 実行、3 層メモリ (Redis/ChromaDB/Letta)、行動学習、および独立した検証を備えたマルチエージェント AI オーケストレーション。テレグラムインターフェイス。中央の構成レジストリを使用してモジュール式に構築されています。 - takasakiari/GOAT2-General-Orchestrated-Agent-Topo
[切り捨てられた]

記事本文:
GitHub - takasekiari/GOAT2-General-Orchestrated-Agent-Topology: GOAT-General-Orchestration-Agent-Topology 非同期 DAG 実行、3 層メモリ (Redis/ChromaDB/Letta)、行動学習、および独立した検証を備えたマルチエージェント AI オーケストレーション。テレグラムインターフェイス。中央の構成レジストリを使用してモジュール式に構築されています。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
アカウントを切り替えました

別のタブまたはウィンドウ。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
タカシキアリ
/
GOAT2-一般-オーケストレーション-エージェント-トポロジ
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
takasakiari/GOAT2-General-Orchestrated-Agent-Topology
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
265 コミット 265 コミット .claude/ skill/ run-goat2 .claude/ skill/ run-goat2 .superpowers/ sdd .superpowers/ sdd __pycache__ __pycache__ エージェント エージェント chromadb_data chromadb_data config config docs/ superpowers docs/ superpowers mcp_server mcp_server メモリ メモリ オーケストレータ オーケストレータ プラグインプラグイン レジストリ レジストリ telegram_interface telegram_interface テスト テスト テスト テスト ツール utils utils .gitignore .gitignore ライセンス ライセンス README.md README.md SETUP.md SETUP.md conftest.py conftest.py 要件-dev.txt 要件-dev.txt 要件.txt 要件.txt すべてのファイルを表示 リポジトリ ファイルのナビゲーション
GOAT 2.0 は、プロアクティブな階層化を中心に構築された Telegram ベースの AI エージェントです。
記憶システム。標準の RAG セットアップとの主な違い: メモリの取得
クエリとは関係なく、毎ターン、LLM が応答する前に実行されます。
内容。たった 1 語のあいまいなメッセージでも、過去の意味検索が引き起こされる
セッションを実行し、構造的に関連するものはすべてプロンプトに挿入します。
モデルは「これを覚えていますか?」と尋ねる必要はありません。すでに取得されているため、
起こった。
ターンごとのドライバーは Orchestrator.run ( Orchestrator/orchestrator.py ) です。それ
1 つのファサード (memory/layers.py 内の MemoryLayers) を通じてメモリと対話し、
物理バックエンドを直接インポートすることはありません。
受け身ではなく、積極的に。プリフェッチ デーモン ( Orchestrator._prefetch_daemon )
すべてのターンの最初のステップとして開始します (asyncio.

create_task at
Orchestrator.py:247 ) で、L0/L1/L2 フェッチと並行して実行されます。検索
生成に先立ちます。モデルがギャップを認識することによってトリガーされるわけではありません。
3 つの独立した物理バックエンド。 Redis (動作中)、ChromaDB (エピソード)、
と Letta (永続) はそれぞれ遅延接続し、独立して失敗します - Letta
停止により L1 ファクトが空になり、ターンが継続します (memory/layers.py:80)。
静的なしきい値はありません。 L3 関連性フィルターはスコアに対する比率です
構造によりスケール不変の分布 ( MemoryLayers._gap_filter 、
レイヤー.py:353 )。コンテキスト バジェットは 2 つの実際の信号からターンごとに適応します
(memory/aits.py)。 L3 の最小トークン スライスは構造によって保証されており、
調整されていません (memory/context_budget.py)。
完全な忠実度。何も圧縮または抽出されません。毎ターンは
逐語的にアーカイブされているため ( _archive_turn 、 Orchestrator.py:173 )、全文
過去のやり取りは単一のセマンティック クエリから取得可能です。要約はありません
モデルと元の単語の間に位置します。
バックエンド
クラス
ストレージ
キー/場所
働く
WorkingMemory (memory/working/working.py)
レディス
goat2:working:{chat_id} (メッセージ)、キャッシュ:{chat_id}:{key} (L2.5)
エピソード的な
EpisodicMemory (memory/episodic/episodic.py)
ChromaDB PersistentClient
EPISODIC_STORAGE_PATH ( ./chroma_data ) のローカル コレクション、名前はepisodic_memory
永久的
PermanentMemory (memory/permanent/permanent.py)
Letta HTTP API
エージェント goat-permanent のコアメモリ ファクト ブロック
最初の使用では、これら 3 つすべてが遅延接続します。 Redisクライアントが組み込まれています
WorkingMemory._get_client ; EpisodicMemory._get_collection の ChromaDB ;レッタ
PermanentMemory._get_http の httpx.AsyncClient 経由。 ChromaDBの同期APIは
asyncio.to_thread でブリッジされます。エピソード コレクションには hnsw:space がありません
オーバーライドするため、検索結果で L2 の 2 乗距離が返されます

lt[「スコア」]
(低い = 近い)。
レイヤー
内容
文脈の中で
支援者
L0 — アイデンティティ
[identity]の基本ペルソナbase_prompt
いつも
設定 (memory.toml)
L1 — 事実
Letta ファクト ブロック内の厳選されたキー→値ファクト
いつも
永久的
L2 — 作業中
現在のチャットの完全な会話履歴
常に (上限付き)
ワーキング / Redis
L2.5 — セッションキャッシュ
L3 検索結果 + ツール出力の TTL キャッシュ
利用可能な場合
ワーキング / Redis
L3 — エピソード
意味論的な長期記憶。関連する場合に挿入される
条件付き
エピソード / ChromaDB
マッパー (memory/layers.py)
MemoryLayers は、オーケストレーターとボットが接触する唯一のメモリ インターフェイスです。それ
5 つの論理層を 3 つの物理層にマッピングし、型指定されたデータを公開します。
メソッド: Assembly_context 、 get_working_context 、 save_working_context 、
search_episodic 、 search_episodic_with_cache 、 store_episodic 、
find_by_keys 、bump_access 、promote_fact 、および完全な L2.5 キャッシュ API
( get_cache 、 set_cache 、invalidate_cache 、clear_cache 、cache_exists )。
Orchestrator.py も telegram_interface/bot.py もインポートしません
WorkingMemory 、 EpisodicMemory 、または PermanentMemory 。
サービスレジストリ (registry/registry.py)
ServiceRegistry は、すべてのサービスの有効期間を所有する遅延 DI コンテナーです。
LLM クライアント、 MemoryLayers 、 MemoryAnalytics 、および
プラグインマネージャー。これは呼び出し元によって渡されるクラス インスタンスであり、
モジュールレベルのシングルトン (ゼロシングルトンルール)。各バックエンドは上に構築されています
初めてのプロパティアクセス。
これが核となる差別化要因です。これは、LLM 呼び出しの前に、毎ターン実行されます。
1. L0/L1/L2 と並行して最初に開始
run() は asyncio.create_task(self._prefetch_daemon(chat_id,tent)) で開きます
( Orchestrator.py:247 )。その直後、さらに 2 つのタスクが開始されます —
layers.get_identity_and_facts() (L0+L1) およびlayers.get_working_context()

(L2) Orchestrator.py:250-251 — したがって、デーモンの L3 検索は層と重複します。
順次ではなく、実際の同時実行でフェッチします。 AITS 分類 (CPU、
instant) は、このオーバーラップ中に実行されます。
2. 3 つのメカニズム、独立して評価、ゲートなし
デーモンはクエリを 3 つの方法で分類し、スコアを付けるすべてのメカニズムを実行します。
ゼロより上。プリフェッチが実際に実行されるかどうかについての信頼ゲートはありません。
タイムアウトが唯一の障害です。分類は次のような場所に存在します
メモリ/query_classifier.py:
一時的なメカニズムでは、キーワード リストではなく、文法的な日付パーサーが使用されます。
「completed-past」ルール (temporal_parser.py:59、before < now) は、次のことを意味します。
azi /today は何も生成しません (その日の範囲は今夜で終わります) 一方、ieri /yesterday
または、絶対的な過去の日付からその日の範囲が得られるため、azi が使用されます
non-temporally は誤ってトリガーしません。 STRICT_PARSING は曖昧さを拒否します
月のみのトークン (RO mai = 副詞「まだ」対月「5 月」)。特定のキー
メカニズムは構造形式のみに一致します - 自然を超える正規表現はありません
言語。
メカニズムは次の方法で同時に実行されます。
asyncio.gather(*タスク、return_Exceptions=True) ( Orchestrator.py:434 );ある
発生する 1 つのメカニズムはスキップされますが、残りのメカニズムは引き続き寄与します。
merge_results (memory/result_merger.py:71) は 3 つの結果全体で重複を排除します。
message_id でグループ化し、各結果にスコアを付けて、最良の順に並べ替えます。ブレンドは
構成の重み ( [プリフェッチ] ) によって修正されます。
ブレンド = 類似度 * 0.6 + 最新性 * 0.3 + アクセス数 * 0.1
類似度 = 1 / (1 + 距離) — クロマ二乗 L2 距離 → 0–1;正確な
構造的一致では、距離 0 → 類似度 1.0 が得られます。
reency — recency_window_days (30 日) にわたるストレージ タイムスタンプの経過時間、1.0 のみ
保存 → ウィンドウを超えて 0.0。
access_count — 取得人気度、access_count_ref (10) で上限、
bug_access による取得時にバンプされます。
E

マージされた結果には、blended_score フィールドが含まれます。リストは次のようにトリミングされます
PREFETCH_MAX_RESULTS (15)。アクセスされたレコードには、access_count / が含まれます。
last_accessed_ts は fire-and-forget ( Orchestrator.py:459 ) をバンプしました。
4. 制限された待機、正常な劣化
デーモンは以下で待機しています
asyncio.wait_for(prefetch_task, timeout=PREFETCH_TIMEOUT) (0.5 秒、
Orchestrator.py:276 )。タイムアウトまたは例外が発生すると、タスクはキャンセルされ、
ターンは L3 なしで継続します ( Orchestrator.py:281-287 ) - オンデマンド
search_memory ツールはフォールバックです。実行が成功すると返されます
(結果、cache_hit、cache_key) 。
5. インジェクション — 会話履歴ではなく、システム プロンプト
結果は Assembly_context に渡され、L3 トークンに適合されます。
スライスして [Context recuperat din istoric] ブロックとして出力します
(layers.py:290)。デーモンが結果を事前にスコア付けしているため、assemble_context
Blended_score フィールドを検出し、
ギャップフィルター (layers.py:281-285)。ブロックは system_content に結合されます
( Orchestrator.py:305 ) — システム プロンプトの一部。会話履歴
API に送信されるのは [{system}, {user}] ( Orchestrator.py:314 ) のみです。 L3 決して
偽の前ターンとして表示されます。
6. 非デーモン結果の関連性 — ギャップフィルター
Assembly_context が Blended_score のない生の結果を受け取った場合 (
オンデマンドの search_memory パス、または直接テスト）、フォールバックします。
MemoryLayers._gap_filter (layers.py:353):
スコア = [ r [ "スコア" ] for r in results ] # L2 距離の 2 乗、昇順
ギャップ = [ スコア [ i + 1 ] - 範囲 ( len ( スコア ) - 1 ) 内の i のスコア [ i ]]
最大 (ギャップ) / 平均 (ギャップ) >= L3_GAP_SIGNIFICANCE の場合: # デフォルト 3.0
最大のギャップより前にすべてを維持する
それ以外の場合:
何も注入しない
結果が 3 件未満の場合は、代わりに 1.5 の絶対上限が適用されます (2 乗)

-L2
≈ ユニットノルム MiniLM 埋め込みの下で「ほぼ直交」)。比率の基準は、
スケール不変: アーカイブが成長するにつれて、本物のクエリ クラスターが比率を生成します。
再キャリブレーションなしで 10。 12 個のラベル付きクエリから 3.0 で調整
(2026-06-29): 無関係な比率 2.33 ～ 2.76 は拒否され、本物の比率 3.13 ～ 5.13
合格しました。
AITS — アダプティブ インテント トークン スケーリング (memory/aits.py)
毎ターン、ユーザーの 2 つのシグナルから動的なトークン バジェットを計算します。
メッセージ:
予算 = BUDGET_BASE
+ 信頼度 × BUDGET_CONFIDENCE_MULTIPLIER
+ 複雑さ × BUDGET_COMPLEXITY_MAX_BONUS
(上限はBUDGET_HARD_CAP)
信頼性 (0–1): クエリの単語トークンに対するセット メンバーシップ (分割
空白、句読点の除去 - 正規表現なし) 2 つのリストに対する - 高
(疑問/分析の手がかり: what 、 how 、 Why 、cum 、 dece 、 când 、…)
とmedium（助動詞）。空のクエリ → 0.2;ハイキュー → 0.8 ～ 1.0 でスケール
キュー数;中程度のキュー → 0.5;その他のステートメント → 0.5。ありません
信頼度の低いリスト - 挨拶と短いターンのデフォルトは 0.2 ではなく 0.5 です。
複雑さ (0–1): (len(query) / 200) × 0.7 + Connector_bonus × 0.3、上限付き
1.0で。 Connector_bonus は、マルチパート コネクタ ( 、 、 、 、
ři 、 sau 、 プラス 、 ; 、、、）。
デフォルトのノブ ( config/memory.toml [aits] ): ベース 2000、乗数 4000、
c

[切り捨てられた]

## Original Extract

GOAT-General-Orchestration-Agent-Topology Multi-agent AI orchestration with async DAG execution, 3-tier memory (Redis/ChromaDB/Letta), behavioral learning, and independent validation. Telegram interface. Built modular with central config registry. - takashikiari/GOAT2-General-Orchestrated-Agent-Topo
[truncated]

GitHub - takashikiari/GOAT2-General-Orchestrated-Agent-Topology: GOAT-General-Orchestration-Agent-Topology Multi-agent AI orchestration with async DAG execution, 3-tier memory (Redis/ChromaDB/Letta), behavioral learning, and independent validation. Telegram interface. Built modular with central config registry. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
takashikiari
/
GOAT2-General-Orchestrated-Agent-Topology
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
takashikiari/GOAT2-General-Orchestrated-Agent-Topology
main Branches Tags Go to file Code Open more actions menu Folders and files
265 Commits 265 Commits .claude/ skills/ run-goat2 .claude/ skills/ run-goat2 .superpowers/ sdd .superpowers/ sdd __pycache__ __pycache__ agents agents chromadb_data chromadb_data config config docs/ superpowers docs/ superpowers mcp_server mcp_server memory memory orchestrator orchestrator plugins plugins registry registry telegram_interface telegram_interface test test tests tests tools tools utils utils .gitignore .gitignore LICENSE LICENSE README.md README.md SETUP.md SETUP.md conftest.py conftest.py requirements-dev.txt requirements-dev.txt requirements.txt requirements.txt View all files Repository files navigation
GOAT 2.0 is a Telegram-based AI agent built around a proactive layered
memory system. The core distinction from a standard RAG setup: memory retrieval
runs before the LLM responds, on every turn, independent of the query's
content. A one-word ambiguous message still triggers a semantic search of past
sessions and injects whatever is structurally relevant into the prompt — the
model never has to ask "do I remember this?" because retrieval already
happened.
The per-turn driver is Orchestrator.run ( orchestrator/orchestrator.py ). It
talks to memory through one façade — MemoryLayers in memory/layers.py — and
never imports a physical backend directly.
Proactive, not reactive. The prefetch daemon ( Orchestrator._prefetch_daemon )
starts as the first step of every turn ( asyncio.create_task at
orchestrator.py:247 ) and runs in parallel with the L0/L1/L2 fetch. Retrieval
precedes generation; it is not triggered by the model noticing a gap.
Three independent physical backends. Redis (working), ChromaDB (episodic),
and Letta (permanent) each connect lazily and fail independently — a Letta
outage empties L1 facts and the turn continues ( memory/layers.py:80 ).
No static thresholds. The L3 relevance filter is a ratio over the score
distribution, scale-invariant by construction ( MemoryLayers._gap_filter ,
layers.py:353 ). The context budget adapts per turn from two real signals
( memory/aits.py ). L3's minimum token slice is guaranteed by construction ,
not tuned ( memory/context_budget.py ).
Complete fidelity. Nothing is compressed or extracted. Every turn is
archived verbatim ( _archive_turn , orchestrator.py:173 ), so the full text of
any past exchange is retrievable from a single semantic query — no summary
stands between the model and the original words.
Backend
Class
Storage
Key / location
Working
WorkingMemory ( memory/working/working.py )
Redis
goat2:working:{chat_id} (messages), cache:{chat_id}:{key} (L2.5)
Episodic
EpisodicMemory ( memory/episodic/episodic.py )
ChromaDB PersistentClient
local collection at EPISODIC_STORAGE_PATH ( ./chroma_data ), name episodic_memory
Permanent
PermanentMemory ( memory/permanent/permanent.py )
Letta HTTP API
core-memory facts block on agent goat-permanent
All three connect lazily on first use. The Redis client is built in
WorkingMemory._get_client ; ChromaDB in EpisodicMemory._get_collection ; Letta
via httpx.AsyncClient in PermanentMemory._get_http . ChromaDB's sync API is
bridged with asyncio.to_thread . The episodic collection has no hnsw:space
override, so search returns squared-L2 distances in result["score"]
(lower = closer).
Layer
Content
In context
Backed by
L0 — Identity
Base persona from [identity] base_prompt
Always
config ( memory.toml )
L1 — Facts
Curated key→value facts in Letta facts block
Always
Permanent
L2 — Working
Full conversation history for the current chat
Always (capped)
Working / Redis
L2.5 — Session Cache
TTL cache for L3 search results + tool outputs
When available
Working / Redis
L3 — Episodic
Semantic long-term memory; injected when relevant
Conditional
Episodic / ChromaDB
The mapper ( memory/layers.py )
MemoryLayers is the only memory interface the orchestrator and bot touch. It
maps the five logical layers onto the three physical tiers and exposes typed
methods: assemble_context , get_working_context , save_working_context ,
search_episodic , search_episodic_with_cache , store_episodic ,
find_by_keys , bump_access , promote_fact , and the full L2.5 cache API
( get_cache , set_cache , invalidate_cache , clear_cache , cache_exists ).
Neither orchestrator.py nor telegram_interface/bot.py imports
WorkingMemory , EpisodicMemory , or PermanentMemory .
Service registry ( registry/registry.py )
ServiceRegistry is a lazy DI container that owns every service lifetime —
LLM client, the three tiers, MemoryLayers , MemoryAnalytics , and
PluginManager . It is a class instance passed by the caller, not a
module-level singleton (the zero-singleton rule). Each backend is built on
first property access.
This is the core differentiator. It runs on every turn, before the LLM call.
1. Started first, in parallel with L0/L1/L2
run() opens with asyncio.create_task(self._prefetch_daemon(chat_id, intent))
( orchestrator.py:247 ). Immediately after, it starts two more tasks —
layers.get_identity_and_facts() (L0+L1) and layers.get_working_context()
(L2) at orchestrator.py:250-251 — so the daemon's L3 search overlaps the tier
fetches with real concurrency, not sequentially. The AITS classify (CPU,
instant) runs during this overlap.
2. Three mechanisms, evaluated independently, no gate
The daemon classifies the query three ways and runs every mechanism that scores
above zero. There is no confidence gate on whether prefetch runs at all —
the timeout is the only blocker. Classification lives in
memory/query_classifier.py :
The temporal mechanism uses a grammatical date parser, not a keyword list.
The "completed-past" rule ( temporal_parser.py:59 , before < now ) means
azi /today yields None (its day-range ends tonight) while ieri /yesterday
or an absolute past date yields that day's range — so azi used
non-temporally does not false-trigger it. STRICT_PARSING rejects ambiguous
month-only tokens (RO mai = adverb "still" vs month "May"). The specific-key
mechanism matches only structural forms — there is no regex over natural
language.
The mechanisms run concurrently via
asyncio.gather(*tasks, return_exceptions=True) ( orchestrator.py:434 ); a
single mechanism that raises is skipped, the rest still contribute.
merge_results ( memory/result_merger.py:71 ) dedupes across the three result
groups by message_id , scores each result, and sorts best-first. The blend is
fixed by config weights ( [prefetch] ):
blended = similarity * 0.6 + recency * 0.3 + access_count * 0.1
similarity = 1 / (1 + distance) — Chroma squared-L2 distance → 0–1; exact
structural matches carry distance 0 → similarity 1.0.
recency — storage-timestamp age over recency_window_days (30 d), 1.0 just
stored → 0.0 past the window.
access_count — retrieval popularity, capped at access_count_ref (10),
bumped on retrieval by bump_access .
Each merged result carries a blended_score field. The list is trimmed to
PREFETCH_MAX_RESULTS (15). Accessed records have their access_count /
last_accessed_ts bumped fire-and-forget ( orchestrator.py:459 ).
4. Bounded wait, graceful degradation
The daemon is awaited under
asyncio.wait_for(prefetch_task, timeout=PREFETCH_TIMEOUT) (0.5 s,
orchestrator.py:276 ). On timeout or any exception the task is cancelled and
the turn continues without L3 ( orchestrator.py:281-287 ) — the on-demand
search_memory tool is the fallback. A successful run returns
(results, cache_hit, cache_key) .
5. Injection — system prompt, not conversation history
Results are passed to assemble_context , which fits them into the L3 token
slice and emits them as a [Context recuperat din istoric] block
( layers.py:290 ). Because the daemon pre-scored the results, assemble_context
detects the blended_score field and sorts best-first instead of running the
gap filter ( layers.py:281-285 ). The block is joined into system_content
( orchestrator.py:305 ) — part of the system prompt. The conversation history
sent to the API is only [{system}, {user}] ( orchestrator.py:314 ); L3 never
appears as fake prior turns.
6. Relevance for non-daemon results — the gap filter
When assemble_context receives raw results with no blended_score (the
on-demand search_memory path, or direct tests), it falls back to
MemoryLayers._gap_filter ( layers.py:353 ):
scores = [ r [ "score" ] for r in results ] # squared-L2 distances, ascending
gaps = [ scores [ i + 1 ] - scores [ i ] for i in range ( len ( scores ) - 1 )]
if max ( gaps ) / mean ( gaps ) >= L3_GAP_SIGNIFICANCE : # default 3.0
keep everything before the largest gap
else :
inject nothing
With fewer than 3 results, an absolute ceiling of 1.5 applies instead (squared-L2
≈ "nearly orthogonal" under unit-norm MiniLM embeddings). The ratio criterion is
scale-invariant: as the archive grows, genuine query clusters produce ratios
10 with no recalibration. Calibrated at 3.0 from 12 labeled queries
(2026-06-29): unrelated ratios 2.33–2.76 rejected, genuine ratios 3.13–5.13
passed.
AITS — Adaptive Intent Token Scaling ( memory/aits.py )
Every turn computes a dynamic token budget from two signals in the user
message:
budget = BUDGET_BASE
+ confidence × BUDGET_CONFIDENCE_MULTIPLIER
+ complexity × BUDGET_COMPLEXITY_MAX_BONUS
(capped at BUDGET_HARD_CAP)
Confidence (0–1): set-membership over the query's word tokens (split on
whitespace, stripped of punctuation — no regex) against two lists — high
(interrogative/analytical cues: what , how , why , cum , dece , când , …)
and medium (auxiliary verbs). Empty query → 0.2; high cues → 0.8–1.0 scaled by
cue count; medium cues → 0.5; any other statement → 0.5. There is no
low-confidence list — greetings and short turns default to 0.5, not 0.2.
Complexity (0–1): (len(query) / 200) × 0.7 + connector_bonus × 0.3 , capped
at 1.0. connector_bonus fires on multi-part connectors ( and , or ,
și , sau , plus , ; , , ).
Default knobs ( config/memory.toml [aits] ): base 2000, multiplier 4000,
c

[truncated]
