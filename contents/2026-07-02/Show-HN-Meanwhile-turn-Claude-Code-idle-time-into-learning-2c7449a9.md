---
source: "https://github.com/riccardomusumeci11/meanwhile"
hn_url: "https://news.ycombinator.com/item?id=48767874"
title: "Show HN: Meanwhile – turn Claude Code idle time into learning"
article_title: "GitHub - riccardomusumeci11/meanwhile: Anti-brainrot side pane for Claude Code: a news + learning digest that splits in while CC works and hides while it waits. Daily AI news (summarized, no API key) + spaced-repetition concepts. tmux · Python stdlib only. · GitHub"
author: "RiccardoMus"
captured_at: "2026-07-02T21:59:48Z"
capture_tool: "hn-digest"
hn_id: 48767874
score: 1
comments: 0
posted_at: "2026-07-02T21:54:45Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Meanwhile – turn Claude Code idle time into learning

- HN: [48767874](https://news.ycombinator.com/item?id=48767874)
- Source: [github.com](https://github.com/riccardomusumeci11/meanwhile)
- Score: 1
- Comments: 0
- Posted: 2026-07-02T21:54:45Z

## Translation

タイトル: HN を表示: 一方 – クロード コードのアイドル時間を学習に変える
記事のタイトル: GitHub - riccardomusumeci11/meanwhile: クロード コードの頭脳腐れ対策サイド ペイン: CC の動作中に分割され、待機中に非表示になるニュース + 学習ダイジェスト。毎日の AI ニュース (要約、API キーなし) + 間隔をあけた繰り返しのコンセプト。 tmux · Python stdlib のみ。 · GitHub
説明: クロード コードの頭脳腐敗防止サイド ペイン: CC が動作している間は分割され、待機中は非表示になるニュースと学習のダイジェスト。毎日の AI ニュース (要約、API キーなし) + 間隔をあけた繰り返しのコンセプト。 tmux · Python stdlib のみ。 - riccardomusumeci11/その間

記事本文:
GitHub - riccardomusumeci11/meanwhile: クロード コードの頭脳腐れ対策サイド ペイン: CC の動作中に分割され、待機中に非表示になるニュースと学習のダイジェスト。毎日の AI ニュース (要約、API キーなし) + 間隔をあけた繰り返しのコンセプト。 tmux · Python stdlib のみ。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
D

アラートを逃す
{{ メッセージ }}
リッカルドムスメシ11
/
その間
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
9 コミット 9 コミット .github .github アセット アセット コマンド コマンド データ データ dist dist .gitignore .gitignore CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md CONTRIBUTING.md ライセンス ライセンス通知 NOTICE README.md README.md SECURITY.md SECURITY.md close_panel.sh close_panel.sh install install install.sh install.sh news news news-tmux news-tmux news.py news.py open_panel.sh open_panel.sh pane_hide.sh pane_hide.sh pane_show.sh pane_show.sh すべてのファイルを表示 リポジトリ ファイルのナビゲーション
一方 — クロード コードの頭脳腐敗防止サイド ペイン
Claude Code が動作中にビジー状態になると、その横にニュースと学習ペインが分割されます
—そしてあなたを必要とする瞬間に消えます。別のタブで腐るのではなく、
右を見て 2 ～ 3 秒で学習または最新情報を維持: 今日の AI が起動し、
密度の高い、明確ではないエンジニアリングコンセプトを間隔をあけた繰り返しスケジュールで実行します。
API キーもアカウントもコストもかかりません。ダイジェストは 1 日に 1 回作成され、要約されます
プロジェクトによって実行され、クライアントによって自動的にプルされるため、インストールは一度だけで済みます。
もう一度触れてください。
┌───────────────────┬─────────────────┐
│ クロード・コード │ ▸ ニュース 3/150 │
│ │ ───────────────── │
│ > 認証モジュールをリファクタリングする │ OpenAI が Daybreak を起動 │
│ ● 作業中…auth.py を編集中 │ あらゆる組織を保護 │
│ ランニング

テスト │ ↻ まとめ · OpenAI · ↗ リンク │
│ │ │
│ │ ▸ コンセプト パイソン │ GIL は │
│ │ ブロッキング I/O を中心にリリース… │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┴─────────────────┘
CC動作中→右側にニュース・CC待機中→全角
クイックスタート
tmux ( brew install tmux ) と
クロードコード CLI。
1 行 — ~/meanwhile にクローンを作成し、すべてを設定します。
カール -fsSL https://raw.githubusercontent.com/riccardomusumeci11/meanwhile/main/install.sh |バッシュ
各ステップを確認したいですか?代わりに手動で行ってください。
git clone https://github.com/riccardomusumeci11/meanwhile.git
その間CD
./install --quickstart # tmux 分割フック + `claude` ラッパーをワンショットで実行
次に、新しいターミナルを開いて次を実行します。
クロード
クロード コードは tmux 内で開始されます。動作中はダイジェストペインが右側に表示されます
待っている間に閉じます (全角 CC)。それでおしまい。
./install --uninstall でいつでもすべてをきれいに元に戻します。
キーは必要なく、錠剤は自動的に更新されます。高価な部分 —
フィードの取得、クラスタリング、ランキング、および要約 - CI で 1 日に 1 回実行されます。
メンテナの Gemini キーを使用します。結果は小さな JSON として公開されます。
クライアントがプルするもの:
メンテナーの CI (毎日) ──ビルド + サマリー──▶ dist/news_cache.json (`dist` ブランチ)
│ クライアントは 24 時間以上経過するとプルします
あなたのマシン: ./news ──◀── 中央フィードをプルします ────┘
└─ コンセプトをインターリーブします (間隔をあけた繰り返し、ローカル) ─ ペイン
オフラインでも安全: ネットワークなし → 最後のコピーが保持され、クラッシュすることはありません。
コンセプトはローカルです。中央のフィードはニュースのみです。常緑樹

n のコンセプトと
間隔をあけた繰り返しスケジュールはマシン上に存在します。
代わりにセルフホスト: ./news --self-refresh は生のデータからダイジェストを構築します
独自の GEMINI_API_KEY を使用してフィードします (環境からの読み取り専用)。
そして、ペインは Claude Code のライフサイクルに接続されています: tmux フックはニュース ペインを表示します
CC が動作し始めたとき (プロンプトまたはツールの使用)、CC を実行した瞬間に非表示にします。
承認を求めたり、あなたを待ったりするので、あなたがいない間にニュースがそこにあります
アイドル状態で、順番が来ると去っていきます。
ニュース — AI ニュース。一次情報源のみからの見出し（研究室の公式ブログ、
評判の良い AI 報道機関、個々の技術的な声、arXiv)、報道機関全体に集中
(1 つのストーリー → すべてのソース + リンクをリストした 1 つの錠剤)、客観的なシグナルによってランク付けされ、
そして、中立的な 3 ～ 5 文の要約に書き直され、次のように明確にラベル付けされています。
AI によって生成され (エラーが含まれる場合があります)、常に属性が付けられ、リンクされます。
コンセプト — 永遠のコンセプト。密度の高い、自明ではないレッスン (KV キャッシュ、GIL、
MVCC、フォールス シェアリング、RoPE…) が間隔をあけた繰り返しスケジュールで再浮上したため、
実際にくっつく。ニュースは消費して忘れてしまいます。あなたが繰り返し、維持するコンセプト。
スタックの GitHub リリース用の CHANGELOG/RELEASE ピル タイプも存在しますが、
デフォルトではオフです。data/feeds.txt の変更ログ: 行のコメントを解除して、
それを有効にします。
デザインバー — 1 秒あたりの最大信号。すべての錠剤は吸収可能でなければなりません
2 ～ 3 秒で目尻を調整、自己完結型 (クリックスルーは不要)、
無償で放棄可能。これは Brainrot の逆です。つまり、
タスクを離れることなく摂取できる最も密度の高い有用なもの。
あなたが厳選したものはすべて、手動で編集可能なプレーンなテキストです - 文字が読めない場合でも
猫、ここには属さないよ。
ソースを追加します — data/feeds.txt に 1 行に 1 つずつ追加します。
https://example.com/feed.xml # 任意の RSS / Atom フィード
arxiv:cs

.CL # arXiv カテゴリ
変更ログ:https://github.com/fastapi/fastapi/releases.atom # GitHub リリース フィード
hn:"言語モデル" # ハッカーニュースクエリ
読む権限のある一次情報源のみを追加してください。無効なフィードまたは解析できないフィードは、
自動的にスキップされました。
コンセプトを追加します — data/concepts.txt に 1 行に 1 つ、CATEGORY │ 単一の密な文を英語で追加します。歯のテスト: それぞれが自明ではないことを生徒に教えなければなりません
経験豊富なエンジニア — メカニズム、数値、またはトレードオフ。
別のフィードをポイントします — NEWS_FEED_URL を自分の公開済みフィードに設定します
news_cache.json (セルフホスティングを参照)。
./news # インタラクティブ ビューア (ペインが実行するものでもあります)
./news --recap # 今週のトップアイテムを出力して終了します (非対話型)
./news --self-refresh # 独自の GEMINI_API_KEY を使用してローカルでダイジェストを構築します
./news --interval 60 # 基本回転秒数 (錠剤の長さに合わせてドウェルスケール)
キー
アクション
n / → / スペース
次の錠剤
p / ←
以前の錠剤
↑ / ↓ ( k / j )
複数のソースからなる長い丸薬をスクロールする
ああ
ブラウザで一番上のソースリンクを開きます
s
現在の錠剤を data/saved.txt に保存します
q
終了（ターミナルはきれいに復元されました）
tmux 分割で、Ctrl-b → を押してフォーカスをペインに切り替えます (また、Ctrl-b を押して戻ります)。
Ctrl-b ← )。ペインは再起動してもその錠剤を記憶しており、その順序は
ランキング (最新性 × マルチソース カバレッジ × 具体的なリリース × ソースの権威)、
滞留時間は長さに応じてスケールされるため、密度の高い錠剤がスクロールして消えてしまうことはありません。
これは、一次情報源のヘッドライン アグリゲーターです。項目ごとに表示されるのは、
タイトル、ソース、日付、シンジケート配信された短い抜粋 (またはその LLM 書き換え)
抜粋)、リンク — 記事本文やページをスクレイピングすることはありません。毎
項目は帰属され、発行者にリンクされます。概要のみが導出されます
発行者自身の RSS 抽出から。詳細については「通知」を参照してください。
削除/削除。もし

あなたは出版社または権利所有者であり、情報源が必要です
除外されました、問題をオープンしてください
(ラベル削除)。フィードは data/feeds.txt および中央フィードから削除されます
すぐに再構築されます - 質問はありません。
免責事項。 While は独立した非営利のオープンソース プロジェクトです。
これは、Anthropic、OpenAI、と提携、承認、後援されていません。
Google、またはそのリンク先のソース。クロードおよびクロード コードは、
人間的;他のすべての製品名、ロゴ、およびブランドは、それらの企業の所有物です。
それぞれの所有者を識別（名指し）する目的でのみ使用されます。
クロード コード CLI、 tmux 、および Python 3 — 以上です。
Python 依存関係なし: 標準ライブラリのみ ( pip install なし)。の
オプションのセルフホスティング サマリーでは、提供された無料の Gemini キーを使用します。
独自の毎日のフィード (独自のキー、独自のフォーク) を実行します。
リポジトリをフォークし、リポジトリ Secret GEMINI_API_KEY を追加します
(無料キー)。
付属のアクション .github/workflows/digest.yml は、毎日 news_cache.json を構築します。
(そしてオンデマンドで) それを dist ブランチに強制的にプッシュします。
NEWS_FEED_URL を使用してクライアントにそのアドレスを指定します (または news.py の REMOTE_URL を編集します)。
Summarizer フォールバック (オプション、すべて無料)。概要はプロバイダーを通じて実行されます
チェーン — Gemini → Groq → Cerebras — したがって、レート制限や機能停止は発生しません。
カードを要約しないままにしておきます。これらのいずれかをシークレット (または環境変数) として追加します。
--self-refresh ) であり、それらは順番に使用されます。なしを設定すると、丸薬は生の RSS を保持します
抜粋：
個別のウィンドウ、ステータスライン、およびアラカルトのインストール モード
./install --quickstart が推奨パスです。構成する個々のモード
(およびいくつかの代替案) も利用可能です。それぞれは次のようにマージされます。
~/.claude/settings.json にはタイムスタンプ付きのバックアップがあり、他のすべてのキーが保持されます。
無効な JSON への書き込みを拒否します。 --uninstall は、それらのいずれかを元に戻します。
./i

nstall --tmux # 分割フックのみ (シェル ラッパーなし)
./install --cc-aware # 状態フック: CC が待機している間、パネルは非表示になります (任意のレイアウト)
./install --statusline # クロード コードのステータス行内にキャッシュ専用の 1 行
./install --lifecycle # macOS: 別のターミナル ウィンドウを自動的に開く/閉じる
./install --panel # スクリプトを実行可能にする + `ccnews` tmux ランチャー (~/.claude なし)
./install --uninstall # 上記のいずれかを元に戻します (フック、スクリプト、シェル ラッパー、状態)
macOS Terminal.app (ペインを分割できない) では、--lifecycle は別の、
AppleScript 経由でターミナル ウィンドウを並べて表示し、セッション終了時にウィンドウを閉じます。最初の走行
自動化の許可を求めます (システム設定 → プライバシーとセキュリティ →
オートメーション→ターミナル）。ステータスライン ビューはキャッシュのみであり、決してアクセスされません。
ネットワークに接続されており、タイムゲート制御されているため、再実行時にちらつくことはありません。
PR は歓迎します。特に新しいプライマリ フィードとハードルの高いコンセプトを歓迎します。参照
貢献.md 。参加すると、次のことに同意したことになります。
行動規範。
./install --uninstall # フック、シェル ラッパー、および実行時状態を削除します
その間、rm -rf # フォルダーを削除します。他には何もインストールされていません
ライセンス
マサチューセッツ工科大学集約されたコンテンツは発行者の所有物のままです — を参照してください。
注意。
Claude Code の頭脳腐敗防止サイド ペイン: CC が動作している間は分割され、待機中は非表示になるニュースと学習のダイジェスト。毎日の AI ニュース (要約、API キーなし) + 間隔をあけた繰り返しのコンセプト。 tmux · Python stdlib のみ。
読み込み中にエラーが発生しました。このページをリロードしてください。
0
フォーク
レポートリポジトリ
リリース
読み込み中にエラーが発生しました。このページをリロードしてください。
読み込み中にエラーが発生しました。このページをリロードしてください。
© 2026 GitHub, Inc.
フッターナビゲーション
私の個人情報を共有しないでください

## Original Extract

Anti-brainrot side pane for Claude Code: a news + learning digest that splits in while CC works and hides while it waits. Daily AI news (summarized, no API key) + spaced-repetition concepts. tmux · Python stdlib only. - riccardomusumeci11/meanwhile

GitHub - riccardomusumeci11/meanwhile: Anti-brainrot side pane for Claude Code: a news + learning digest that splits in while CC works and hides while it waits. Daily AI news (summarized, no API key) + spaced-repetition concepts. tmux · Python stdlib only. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
riccardomusumeci11
/
meanwhile
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
9 Commits 9 Commits .github .github assets assets commands commands data data dist dist .gitignore .gitignore CODE_OF_CONDUCT.md CODE_OF_CONDUCT.md CONTRIBUTING.md CONTRIBUTING.md LICENSE LICENSE NOTICE NOTICE README.md README.md SECURITY.md SECURITY.md close_panel.sh close_panel.sh install install install.sh install.sh news news news-tmux news-tmux news.py news.py open_panel.sh open_panel.sh panel_hide.sh panel_hide.sh panel_show.sh panel_show.sh View all files Repository files navigation
Meanwhile — the anti-brainrot side pane for Claude Code
When Claude Code is busy working , a news + learning pane splits in beside it
— and disappears the moment it needs you. Instead of rotting in another tab, you
glance right and learn or stay current in 2–3 seconds: today's AI launches, and
dense, non-obvious engineering concepts on a spaced-repetition schedule.
No API key, no account, no cost. The digest is built and summarized once a day
by the project and pulled automatically by your client — you install once and never
touch it again.
┌───────────────────────────────────┬─────────────────────────────────┐
│ Claude Code │ ▸ NEWS 3/150 │
│ │ ─────────────────────────────── │
│ > refactor the auth module │ OpenAI launches Daybreak to │
│ ● Working… editing auth.py │ secure every organization │
│ running tests │ ↻ summarized · OpenAI · ↗ link │
│ │ │
│ │ ▸ CONCEPT PYTHON │ the GIL is │
│ │ released around blocking I/O… │
└───────────────────────────────────┴─────────────────────────────────┘
while CC works → news on the right · CC waits for you → full-width
Quickstart
Requires tmux ( brew install tmux ) and the
Claude Code CLI.
One line — clones to ~/meanwhile and sets everything up:
curl -fsSL https://raw.githubusercontent.com/riccardomusumeci11/meanwhile/main/install.sh | bash
Prefer to see each step? Do it by hand instead.
git clone https://github.com/riccardomusumeci11/meanwhile.git
cd meanwhile
./install --quickstart # tmux split hooks + a `claude` wrapper, in one shot
Then open a new terminal and just run:
claude
Claude Code starts inside tmux; the digest pane appears on the right while it works
and closes (CC full-width) while it waits for you. That's it.
Undo everything cleanly at any time: ./install --uninstall .
You never need a key, and the pills update on their own. The expensive part —
fetching feeds, clustering, ranking, and summarizing — runs once a day in CI
with the maintainer's Gemini key; the result is published as a small JSON that your
client pulls:
maintainer's CI (daily) ──build + summarize──▶ dist/news_cache.json (the `dist` branch)
│ client pulls when >24h old
your machine: ./news ──◀── pulls central feed ────────┘
└─ interleaves YOUR concepts (spaced repetition, local) ─▶ the pane
Offline-safe: no network → it keeps the last copy and never crashes.
Concepts are local: the central feed is news only; the evergreen concepts and
your spaced-repetition schedule live on your machine.
Self-host instead: ./news --self-refresh builds the digest from the raw
feeds with your own GEMINI_API_KEY (read only from the environment).
And the pane is wired to Claude Code's lifecycle: tmux hooks show the news pane
when CC starts working (a prompt or any tool use) and hide it the moment CC
asks for approval or waits for you — so the news is there while you'd otherwise be
idle, and gone when it's your turn.
NEWS — AI news. Headlines from primary sources only (official lab blogs,
reputable AI press, individual technical voices, arXiv), clustered across outlets
(one story → one pill listing every source + link), ranked by objective signals,
and rewritten into neutral 3-to-5-sentence summaries — clearly labeled as
AI-generated (and may contain errors), always attributed and linked.
CONCEPT — evergreen concepts. Dense, non-obvious lessons (KV cache, the GIL,
MVCC, false sharing, RoPE…) resurfaced on a spaced-repetition schedule so they
actually stick. News you consume and forget; concepts you repeat and keep.
A CHANGELOG/RELEASE pill type for your stack's GitHub releases also exists but
is off by default — uncomment the changelog: lines in data/feeds.txt to
enable it.
Design bar — maximum signal per second. Every pill must be absorbable with the
corner of your eye in 2–3 seconds, self-contained (no click-through needed), and
abandonable at no cost. It's the opposite of brainrot: it fills the gap with the
densest useful thing you can take in without leaving your task.
Everything you curate is plain, hand-editable text — if you can't read it with
cat , it doesn't belong here.
Add a source — one per line in data/feeds.txt :
https://example.com/feed.xml # any RSS / Atom feed
arxiv:cs.CL # an arXiv category
changelog:https://github.com/fastapi/fastapi/releases.atom # a GitHub release feed
hn:"language model" # a Hacker News query
Add only primary sources you're authorized to read; dead/unparseable feeds are
skipped automatically.
Add a concept — one per line in data/concepts.txt , CATEGORY │ a single dense sentence , in English. The teeth test: each must teach something non-obvious to an
experienced engineer — a mechanism, a number, or a tradeoff.
Point at a different feed — set NEWS_FEED_URL to your own published
news_cache.json (see Self-hosting ).
./news # the interactive viewer (also what the pane runs)
./news --recap # print the week's top items and exit (non-interactive)
./news --self-refresh # build the digest locally with your own GEMINI_API_KEY
./news --interval 60 # base rotation seconds (dwell scales with pill length)
Key
Action
n / → / space
next pill
p / ←
previous pill
↑ / ↓ ( k / j )
scroll a long, multi-source pill
o
open the top source link in your browser
s
save the current pill to data/saved.txt
q
quit (terminal restored cleanly)
In the tmux split, switch focus to the pane with Ctrl-b → (and back with
Ctrl-b ← ). The pane remembers its pill across restarts, the order is the
ranking (recency × multi-source coverage × concrete-launch × source authority),
and dwell time scales with length so dense pills don't scroll away.
It is a headline aggregator of primary sources . Per item it shows only the
title, source, date, a short syndicated extract (or an LLM rewrite of that
extract), and the link — never the article body , never scraped pages. Every
item is attributed and links back to the publisher; summaries are derived solely
from the publisher's own RSS extract. Full details in NOTICE .
Removal / takedown. If you're a publisher or rights holder and want your source
excluded, open an issue
(label takedown ). The feed is removed from data/feeds.txt and the central feed
rebuilt promptly — no questions asked.
Disclaimer. Meanwhile is an independent, non-commercial, open-source project.
It is not affiliated with, endorsed by, or sponsored by Anthropic, OpenAI,
Google, or any source it links to. Claude and Claude Code are trademarks of
Anthropic; all other product names, logos, and brands are the property of their
respective owners and are used for identification (nominative) purposes only.
Claude Code CLI, tmux , and Python 3 — that's it.
Zero Python dependencies: standard library only (no pip install ). The
optional self-hosting summaries use a free Gemini key you provide.
Run your own daily feed (your own key, your own fork):
Fork the repo and add a repository Secret GEMINI_API_KEY
( free key ).
The included Action .github/workflows/digest.yml builds news_cache.json daily
(and on demand) and force-pushes it to the dist branch.
Point clients at it with NEWS_FEED_URL (or edit REMOTE_URL in news.py ).
Summarizer fallbacks (optional, all free). Summaries run through a provider
chain — Gemini → Groq → Cerebras — so a rate limit or outage on one doesn't
leave cards un-summarized. Add any of these as Secrets (or env vars for
--self-refresh ) and they're used in order; set none and pills keep their raw RSS
extract:
Separate window, statusline, and à-la-carte install modes
./install --quickstart is the recommended path. The individual modes it composes
(and a couple of alternatives) are also available — each merges into
~/.claude/settings.json with a timestamped backup, preserves every other key, and
refuses to write on invalid JSON; --uninstall reverses any of them:
./install --tmux # the split hooks only (no shell wrapper)
./install --cc-aware # state hooks: the panel hides while CC waits (any layout)
./install --statusline # one cache-only line inside Claude Code's status line
./install --lifecycle # macOS: auto-open/close a separate Terminal window
./install --panel # make scripts executable + a `ccnews` tmux launcher (no ~/.claude)
./install --uninstall # undo any of the above (hooks, scripts, shell wrapper, state)
On macOS Terminal.app (which can't split panes), --lifecycle opens a separate,
tiled Terminal window via AppleScript and closes it on session end; the first run
asks for the Automation permission ( System Settings → Privacy & Security →
Automation → Terminal ). The statusline view is cache-only, never touches the
network, and is time-gated so it never flickers when re-run.
PRs welcome — especially new primary feeds and high-bar concepts . See
CONTRIBUTING.md . By participating you agree to the
Code of Conduct .
./install --uninstall # removes hooks, the shell wrapper, and runtime state
rm -rf meanwhile # then delete the folder; nothing is installed elsewhere
License
MIT . Aggregated content remains the property of its publishers — see
NOTICE .
Anti-brainrot side pane for Claude Code: a news + learning digest that splits in while CC works and hides while it waits. Daily AI news (summarized, no API key) + spaced-repetition concepts. tmux · Python stdlib only.
There was an error while loading. Please reload this page .
0
forks
Report repository
Releases
There was an error while loading. Please reload this page .
There was an error while loading. Please reload this page .
© 2026 GitHub, Inc.
Footer navigation
Do not share my personal information
