---
source: "https://artifipedia.com/map"
hn_url: "https://news.ycombinator.com/item?id=48930160"
title: "An Interactive Map of AI"
article_title: "The map of AI — every concept, connected | Artifipedia"
author: "ceprompts"
captured_at: "2026-07-16T04:52:15Z"
capture_tool: "hn-digest"
hn_id: 48930160
score: 14
comments: 8
posted_at: "2026-07-16T03:53:55Z"
tags:
  - hacker-news
  - translated
---

# An Interactive Map of AI

- HN: [48930160](https://news.ycombinator.com/item?id=48930160)
- Source: [artifipedia.com](https://artifipedia.com/map)
- Score: 14
- Comments: 8
- Posted: 2026-07-16T03:53:55Z

## Translation

タイトル: AI のインタラクティブ マップ
記事のタイトル: AI の地図 — すべての概念が接続されている |アーティフィペディア
説明: 56 の AI 概念とそれらの接続方法のインタラクティブなマップ。ドラッグして探索し、あらゆるアイデアがどこに当てはまるかを確認します。

記事本文:
コンテンツにスキップ
アーティフィペディア
ディープラーニング 言語とLLM AIエージェント 生成AI 機械学習 コンピュータビジョン 安全性と倫理基盤 ツールとエコシステム 用語集 マップ決定 タイムライン ブログ検索
用語集マップのブログ
フィールド
ナビゲート
Artifipedia 上のすべてのコンセプト、およびそれらの間のすべてのリンク。ノードをドラッグし、マウスを移動して接続をトレースし、クリックして読み取ります。
どのサイトでも無料でご利用いただけます。これを表示したい場所に貼り付けます。百科事典が成長しても最新の状態が維持されます。
それぞれのサークルがコンセプトです。各行は、この 2 つが真に関連していることを意味します。つまり、一方が他方を説明する、一方が他方から構築される、または、後者なしでは前者を理解できないということです。円のサイズは接続の数です。色はフィールドです。
マップには 56 のコンセプトと 150 のリンクがあり、それぞれ平均 5.4 の接続があります。密度は 9.7% です。ほとんどの可能性のあるペアはリンクされていません。これが重要です。すべてがすべてにつながっている地図は何も教えてくれません。
Large Language Model (LLM) は最も関連性の高い概念であり、21 個のリンクがあります。それは編集者の選択ではなく、グラフから外れています。最も関連性が高い 5 つは、大規模言語モデル (LLM) (21)、ニューラル ネットワーク (12)、埋め込み (11)、機械学習 (11)、深層学習 (9) です。何もないところから始める場合は、そこから始めてください。これらは他の概念に必要な概念です。
150 のリンクのうち 78 はフィールド間を横断します。それらは興味深いものです。それ自体にのみ接続するフィールドはサイロです。このマップをまとめる概念は、大規模言語モデル (LLM) 、埋め込み、ニューラル ネットワーク、深層学習です。 AI を一度に 1 つのトピックずつ学習することができないのはこのためです。
周辺部は現実であり、見落としではありません。 OCR (光学式文字認識)、物体検出、インテリジェンスはエッジ近くにあり、接続はほとんどありません。それは現場を反映しています。いくつかのアイデアは自己流です。

含まれており、他に何もせずにそれらを学ぶことができます。そうでないふりをした地図はお世辞であって、正確ではありません。
正直に言うと、コンセプトに従って手作業で行っています。各エントリは接続先を宣言し、グラフはそれらの宣言から組み立てられます。これには既知の結果があります。リンクはローカルでは認識されますが、グローバルでは不均一になります。初期に書かれた基本的なコンセプトは、より多くの地図を視野に入れて書かれた新しいコンセプトよりもリンクが少ない場合があります。
これらは、テキストの類似性、共起、またはモデルから生成されたものではありません。その場合、相互に説明するものではなく、単に一緒に表示されるものを結び付けることになるため、より密で滑らかなグラフが生成されますが、正直さは失われます。
リンクが間違っているように見える場合は、それが間違っている可能性があります。教えてください — それは私が訂正します。
どのサイトにも無料で埋め込むことができます。上のボタンをクリックすると iframe が提供されます。コンセプトが追加されても最新の状態が維持されます。基礎となるデータもオープンです。content.json にはあらゆる概念と関係が含まれており、llms-full.txt はプレーン テキストとしてのコーパス全体です。
以下の各コンセプトは分野ごとにグループ化されています。地図はインデックスです。これらがエントリーです。
モデル自体はセッションが終了した瞬間にすべてを忘れてしまうため、AI に会話全体を記憶する方法を提供します。
AGI (汎用人工知能)
領域をまたがる人間レベルの広範な能力を備えた仮説的なシステム。それが到着したかどうかについて人々が議論できるほど未定義です。
一度答えて停止するのではなく、決定、行動、反応という独自のステップを踏んで目標を追求するソフトウェア。
AI システムを人間の意図どおりに実際に実行させるという問題、つまり、誤って指定した目標だけでなく、私たちが望む目標を確実に追求するという問題です。
インテリジェンスが必要と思われることを機械に実行させる分野 - この定義は、時代が変わるたびに変化しています。

彼は機械が成功する。
各単語を解釈する際に他のどの単語が重要かを AI に判断させるメカニズム、これがトランスフォーマーの背後にある中心的なアイデアです。
どの重みがどの程度ミスを引き起こしたかを解明するアルゴリズム、これがニューラルネットワークがそもそも学習できる理由です。
AI システムが、通常、トレーニング データに存在するバイアスを吸収することによって、不公平または差別的な結果を生み出す問題。
モデルに答える前に段階的に推論させることで、難しい問題に対するパフォーマンスが劇的に向上します。
互いに似ているものをグループ化すること、そして、存在するかどうかに関係なく、アルゴリズムは常にグループを返すという事実。
CNN (畳み込みニューラル ネットワーク)
画像上で小さなフィルターをスライドさせて局所的なパターンを見つけるネットワーク、つまりコンピューター ビジョンを機能させるアーキテクチャです。
AI が一度に検討できるテキストの最大量、つまりトークンで測定される短期作業記憶です。
多くの層を持つニューラル ネットワークを使用した機械学習。これは、最近のほぼすべての AI の進歩の背後にあるアプローチです。
ほとんどの AI 画像ツールの仕組み — ランダムなノイズから始まり、プロンプトに従って段階的にノイズを除去し、画像が表示されるまで続きます。
単語 (または画像、またはその他のもの) を数字のリストに変換し、類似した意味が互いに近くなるように配置します。
モデルが機能していることを示すこと、そしてほとんどの方法では決定ではなく説明が説明されるという不快な事実。
生データをモデルが実際に使用できるものに再形成しますが、依然として精度の大部分はディープ ラーニングの外部から得られます。
独自のサンプルでモデルのトレーニングを継続し、その動作を変更します。これは、応答時に提供されるのではなく、モデルに組み込まれます。
GAN (敵対的生成ネットワーク)
2 つのネットワークが互いに訓練され、1 つは偽装、もう 1 つは検出されます。

l 偽物は通ります。技術の普及は大幅に置き換えられました。
ディープ ラーニングを可能にしたチップ。何千もの小さなコアが同時に同じ計算を実行します。これはまさにニューラル ネットワークに必要なものです。
エラー サーフェスを下り坂で、一度に 1 つずつ小さなステップで歩きます。モデルの重みが実際にどのように更新されるか。
モデルが何を受け取り、発言し、何を行うことが許可されているかを決定するモデルに関するチェック、つまりデモが事件になるのを防ぐ部分です。
AI が流暢で自信に満ちた何かを生成した場合、それは単なる誤りであり、流暢さと正確さは同じではありません。
AI に画像を見てそれが何であるかを言わせることは、コンピューター ビジョンの基本的なタスクです。
ボックスを描画するのではなく、すべてのピクセルにラベルを付けることは、正確な形状が重要な場合に必要なことです。
リクエストに応じてモデルをレンタルする - 実際にほぼすべての人が AI を使用していることと、それに伴う依存関係。
「人工知能」の下にある言葉は、常に使用され、誰も定義せず、この分野の最大の議論が解決しない理由です。
モデルが拒否するように訓練されたことをモデルにやらせること、そしてそれが機能し続ける構造的な理由。
AI は、膨大な量のテキストを学習して次の文章を予測します。これは、ChatGPT や Claude などのチャットボットの背後にあるテクノロジーです。
モデルがどの程度間違っているかを示す数値、つまりモデルがどのようなものになろうとしているかの定義です。
コンピューターにルールを明示的にプログラムするのではなく、データからパターンを学習させてタスクを改善させます。
複数の AI エージェントが 1 つの問題に協力し、それぞれに役割があります。デモでは強力ですが、本番環境では不器用です。
複数の種類の入力 (テキストと画像、場合によってはオーディオとビデオ) を単一の共有表現で処理するモデル。
例からパターンを学習する、単純に接続されたユニットのシステム

— ディープラーニングと最新の AI の基盤。
画像内のオブジェクトがどこにあるか、そしてそれらが何であるかを検索します。各オブジェクトの周囲にラベル付きのボックスを描画します。
OCR (光学式文字認識)
テキストの画像をテキストに変換する - クリーンなドキュメントについては解決しましたが、その他のすべてについては依然として本当に困難です。
自分で重みをダウンロードして実行できるモデル — オープンソースと呼ばれることが多いですが、通常は完全ではありません。
モデルが一般的なパターンを学習するのではなく、トレーニング データを記憶する場合、トレーニングでは優れているように見えますが、新しいデータでは失敗します。
個人データが AI システムに入り、そこから戻ってくること、そしてトレーニングされたモデルのトレーニングを解除するのが非常に難しいという事実。
AI モデルから最良かつ最も信頼性の高い出力を得る命令を作成する技術。
モデルの数値を精度を下げて保存することで、より少ないメモリに収まり、より高速に実行できます。通常、品質面でのコストは驚くほど低くなります。
誰かが無料で攻撃する前に、意図的に自分のシステムを攻撃する。
報酬を通じた試行錯誤による学習 — ペットを訓練する方法をソフトウェアに適用します。
検索拡張生成 (RAG)
AI が記憶した内容だけに依存するのではなく、応答時に検索することで特定の文書セットから回答できるようにします。
RLHF (ヒューマンフィードバックからの強化学習)
正解ではなく人間の好みに基づいてモデルをトレーニングする - テキスト予測子をアシスタントに変えるステップ。
AI にラベル付きの例 (入力と正しい答えのペア) を示して教えることで、新しい入力に対する答えを予測できるようになります。
会話が始まる前にモデルが受け取る常設の指示。影響力があり、ユーザーには見えず、セキュリティ境界ではありません。
AI の出力がどの程度ランダムまたは予測可能であるかを制御する 1 つの設定。集中している場合は低く、集中している場合は高くなります。

反応的な。
AI が読み書きする小さなテキスト。通常は単語全体ではなく、単語の一部です。
記憶からすべてに答えようとするのではなく、モデルに実際のソフトウェア (検索、計算機、データベース) を呼び出させます。
モデルが決して見ることのないデータを保持することで、モデルが何かを学習したのか、単に記憶したのかを知ることができます。
モデルの構築とモデルの使用 — コスト、ハードウェア、制約が異なる 2 つのまったく異なるアクティビティ。
乱数からではなく、すでに学習した一般的なモデルから始めることで、なぜ小規模なチームが本物の AI を構築できるのか。
ほぼすべての最新の AI モデルの背後にあるニューラル ネットワーク アーキテクチャは、注意を中心に構築されており、すべての単語を一度に他の単語と比較して比較できます。
誰もラベル付けされていないデータの構造を見つけることは、便利ですが過小評価されていますが、正しいかどうかを知るのははるかに困難です。
埋め込みを保存し、最も類似したものを迅速に見つけるために構築されたデータベース。意味ベースの検索の背後にある検索エンジンです。
無料の AI 知識百科事典。平易な言葉から研究の最前線まで、あらゆる概念が 5 つの深さで説明されています。
検索拡張生成 (RAG)

## Original Extract

An interactive map of 56 AI concepts and how they connect. Drag, explore, and see where every idea fits.

Skip to content
Artifipedia
Deep Learning Language & LLMs AI Agents Generative AI Machine Learning Computer Vision Safety & Ethics Foundations Tools & Ecosystem Glossary Map Decide Timeline Blog Search
Glossary Map Blog
Fields
Navigate
Every concept on Artifipedia, and every link between them. Drag a node, hover to trace its connections, click to read.
Free to use on any site. Paste this where you want it to appear — it stays current as the encyclopedia grows.
Each circle is a concept. Each line means the two are genuinely related — one explains the other, one is built from the other, or you cannot understand the first without the second. Circle size is the number of connections. Colour is the field.
The map has 56 concepts and 150 links, an average of 5.4 connections each. Density is 9.7% — most possible pairs are not linked, which is the point. A map where everything connects to everything tells you nothing.
Large Language Model (LLM) is the most connected concept , with 21 links. That is not an editorial choice — it falls out of the graph. The five most connected are Large Language Model (LLM) (21), Neural Network (12), Embeddings (11), Machine Learning (11), Deep Learning (9). If you are starting from nothing, start there: they are the concepts other concepts need.
78 of the 150 links cross between fields. Those are the interesting ones. A field that only connects to itself is a silo; the concepts holding this map together are Large Language Model (LLM) , Embeddings , Neural Network , Deep Learning . They are why you cannot learn AI one topic at a time.
The periphery is real, not an oversight. OCR (Optical Character Recognition), Object Detection, Intelligence sit near the edge with few connections. That reflects the field: some ideas are self-contained and you can learn them without much else. A map that pretended otherwise would be flattering, not accurate.
Honestly: by hand, per concept. Each entry declares what it connects to, and the graph is assembled from those declarations. That has a known consequence — the links are locally sensible and globally uneven. A foundational concept written early may have fewer links than a newer one written with more of the map in view.
They are not generated from text similarity, co-occurrence, or a model. That would produce a denser, smoother graph and a less honest one, because it would link things that merely appear together rather than things that explain each other.
If a link looks wrong to you, it might be. Tell me — that is a correction I will make.
It is free to embed on any site — the button above gives you an iframe. It stays current as concepts are added. The underlying data is open too: content.json has every concept and relation, and llms-full.txt is the whole corpus as plain text.
Every concept below, grouped by field. The map is the index; these are the entries.
Giving an AI a way to remember across conversations, since the model itself forgets everything the moment a session ends.
AGI (Artificial General Intelligence)
A hypothetical system with broad human-level capability across domains — undefined enough that people can argue about whether it's arrived.
Software that pursues a goal by taking its own steps — deciding, acting, and reacting — instead of answering once and stopping.
The problem of making AI systems actually do what people intend — reliably pursuing the goals we want, not just the ones we accidentally specified.
The field of making machines do things that seem to require intelligence — a definition that has moved every time the machines succeed.
The mechanism that lets an AI decide which other words matter when interpreting each word — the core idea behind transformers.
The algorithm that works out which weights caused a mistake and by how much — the reason neural networks can learn at all.
The problem of AI systems producing unfair or discriminatory outcomes — usually by absorbing biases present in their training data.
Getting a model to reason step by step before answering — which dramatically improves its performance on hard problems.
Grouping things that resemble each other — and the fact that the algorithm always returns groups, whether or not any exist.
CNN (Convolutional Neural Network)
A network that slides small filters across an image to find local patterns — the architecture that made computer vision work.
The maximum amount of text an AI can consider at once — its short-term working memory, measured in tokens.
Machine learning using neural networks with many layers — the approach behind nearly every recent AI breakthrough.
How most AI image tools work — starting from random noise and removing it step by step, guided by a prompt, until a picture appears.
Turning words (or images, or anything) into lists of numbers, arranged so that similar meanings end up close together.
Getting a model to show its working — and the uncomfortable fact that most methods explain the explanation, not the decision.
Reshaping raw data into things a model can actually use — still where most of the accuracy comes from outside deep learning.
Continuing a model's training on your own examples so its behavior changes — baked into the model, not supplied at answer time.
GAN (Generative Adversarial Network)
Two networks trained against each other — one faking, one detecting — until the fakes pass. The technique diffusion largely replaced.
The chip that made deep learning possible — thousands of small cores doing the same maths at once, which is exactly what neural networks need.
Walking downhill on the error surface, one small step at a time — how a model's weights actually get updated.
The checks around a model that decide what it's allowed to receive, say, and do — the part that stops a demo becoming an incident.
When an AI produces something fluent and confident that is simply false — fluency is not the same as accuracy.
Getting an AI to look at an image and say what it is — the foundational task of computer vision.
Labelling every pixel rather than drawing a box — what you need when the exact shape matters.
Renting a model by the request — how nearly everyone actually uses AI, and the dependency that comes with it.
The word underneath "artificial intelligence" — used constantly, defined by nobody, and the reason the field's biggest arguments never resolve.
Getting a model to do what it was trained to refuse — and the structural reason it keeps working.
An AI trained on enormous amounts of text to predict the next piece of writing — the technology behind chatbots like ChatGPT and Claude.
The number that says how wrong the model is — and therefore the definition of what it's trying to become.
Getting computers to learn patterns from data and improve at a task, instead of being explicitly programmed with rules.
Several AI agents working together on one problem, each with a role — powerful in demos, awkward in production.
Models that handle more than one kind of input — text and images, sometimes audio and video — in a single shared representation.
A system of simple connected units that learns patterns from examples — the foundation underneath deep learning and modern AI.
Finding where objects are in an image and what they are — drawing a labelled box around each one.
OCR (Optical Character Recognition)
Turning pictures of text into text — solved for clean documents, still genuinely hard for everything else.
Models whose weights you can download and run yourself — often called open source, usually not quite.
When a model memorizes its training data instead of learning the general pattern — so it looks great in training but fails on new data.
Personal data going into AI systems, coming back out of them, and the fact that a trained model is very hard to un-train.
The craft of writing instructions that get the best, most reliable output from an AI model.
Storing a model's numbers with less precision so it fits in less memory and runs faster — usually at a surprisingly small cost in quality.
Attacking your own system on purpose, before someone else does it for free.
Learning by trial and error through rewards — the way you'd train a pet, applied to software.
Retrieval-Augmented Generation (RAG)
Letting an AI answer from a specific set of documents by looking them up as it responds — instead of relying only on what it memorized.
RLHF (Reinforcement Learning from Human Feedback)
Training a model on human preferences rather than correct answers — the step that turned text predictors into assistants.
Teaching an AI by showing it labelled examples — inputs paired with the correct answers — so it can predict answers for new inputs.
The standing instructions a model gets before the conversation starts — influential, invisible to users, and not a security boundary.
A single setting that controls how random or predictable an AI's output is — low for focused, high for creative.
The small piece of text an AI reads and writes — usually a chunk of a word, not a whole word.
Letting a model call real software — a search, a calculator, your database — instead of trying to answer everything from memory.
Holding back data the model never sees, so you can find out whether it learned anything or just memorised.
Building the model versus using it — two completely different activities with different costs, hardware, and constraints.
Starting from a model that already learned something general, instead of from random numbers — why small teams can build real AI.
The neural-network architecture behind almost every modern AI model — built around attention, which lets it weigh every word against every other, all at once.
Finding structure in data nobody labelled — useful, underrated, and much harder to know if you got right.
A database built to store embeddings and find the most similar ones fast — the search engine behind meaning-based retrieval.
The free AI knowledge encyclopedia. Every concept explained at five depths — from plain words to the research frontier.
Retrieval-Augmented Generation (RAG)
