---
source: "https://github.com/AdamACE9/bothread"
hn_url: "https://news.ycombinator.com/item?id=48970865"
title: "Show HN: Bothread, multiple AI coding agents talk, share one repo, no collisions"
article_title: "GitHub - AdamACE9/bothread: Local room where AI coding agents collaborate on one codebase via MCP — collisions prevented, human in command. · GitHub"
author: "AdamACE9"
captured_at: "2026-07-19T20:03:10Z"
capture_tool: "hn-digest"
hn_id: 48970865
score: 1
comments: 0
posted_at: "2026-07-19T19:09:57Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Bothread, multiple AI coding agents talk, share one repo, no collisions

- HN: [48970865](https://news.ycombinator.com/item?id=48970865)
- Source: [github.com](https://github.com/AdamACE9/bothread)
- Score: 1
- Comments: 0
- Posted: 2026-07-19T19:09:57Z

## Translation

タイトル: HN を表示: Bothread、複数の AI コーディング エージェントが会話、1 つのリポジトリを共有、衝突なし
記事のタイトル: GitHub - AdamACE9/両方のスレッド: AI コーディング エージェントが MCP 経由で 1 つのコードベースで共同作業するローカル ルーム — 衝突が防止され、人間が指揮を執ります。 · GitHub
説明: AI コーディング エージェントが MCP を介して 1 つのコードベースで共同作業するローカル ルーム — 衝突が防止され、人間が指揮を執ります。 - AdamACE9/両方のスレッド

記事本文:
GitHub - AdamACE9/bothread: AI コーディング エージェントが MCP を介して 1 つのコードベースで共同作業するローカル ルーム — 衝突が防止され、人間が指揮を執ります。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
アダムACE9
/
どちらも読む
公共
通知
通知セットを変更するにはサインインする必要があります

ひずみ
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
69 コミット 69 コミット .claude-plugin .claude-plugin .claude .claude apps/ room-ui apps/ room-ui bin bin docs docs パッケージ パッケージ スクリプト スクリプト スキル スキル Web サイト Web サイト .gitignore .gitignore .nvmrc .nvmrc ライセンス ライセンス README.md README.md package-lock.json package-lock.json package.json package.json server.json server.json tsconfig.base.json tsconfig.base.json すべてのファイルを表示 リポジトリ ファイルのナビゲーション
すでに使用している AI コーディング エージェントを 1 つのコードベースで一緒に実行します。ローカル、MCP ネイティブのあなたが指揮を執ります。
無料のオープンソース ローカル アプリ。 API キーもクラウドもありません。・刺繍ブランド「Brothread」とは無関係です。
ウェブサイト · はじめに · スキル · npm
3 人のエージェント (Claude Code、Antigravity、OpenCode) が作業を分担し、1 つの共有ルームでプレイ可能なゲームをライブで構築します。
Bothread を一文で表すと、 Bothread は無料のオープンソースのローカル調整ハブです。
複数の AI コーディング エージェント — Claude Code、Cursor、Antigravity、Gemini CLI、Codex、OpenCode、またはその他
他の MCP 互換エージェント — 同じコードベースで連携して動作します
1 つの共有ルームで、人間がすべてのファイルを監視している間、ファイルが互いに上書きされないように要求します。
移動して指揮を執り続けます。 API キーもクラウドもコストもかかりません。
AI コーディング エージェントを使用せずに同じプロジェクトで複数の AI コーディング エージェントを実行すると、すぐに苦痛になります。
互いに会話することができず、同じファイルを開いてお互いの作業を黙って上書きします。
存在するあらゆる調整は、別々の端末間で目に見えない形で行われます。 Bothread は MCP を実行します
サーバーを使用して、MCP 互換のエージェントが 1 つのルームに参加し、同じコードベースで共同作業できるようにします。
人間があらゆる動きを監視している間、お互いの邪魔をしないでください。

いつでも参入してください。
AI モデル自体を呼び出すことはなく、API キーも必要ありません。エージェントが調整します。
それぞれが独自のサブスクリプションですでに実行されています。両方とも部屋、衝突防止、そして
人間のコントロールがその上に重ねられます。
彼らはお互いに話すことができません。各エージェントは独自のプロセス、独自のコンテキスト、独自の環境で実行されます。
ループ。これらには、1 つの計画、1 つのリファクタリング、1 つのテストという補完的な強みがありますが、それを実現する方法はありません。
実際にチームとして働きます。
彼らは衝突します。 2 人のエージェントが同じファイルを開き、お互いの作業を静かに上書きします。によって
気づいたときにはすでに被害が発生しています。
あなたはシャットアウトされています。存在するわずかな調整は、端末や設定で目に見えない形で行われます。
ファイル。注目すべきものは何もなく、何か危険なことが起こる前に介入する瞬間もありません。
Bothread は単にメッセージの受け渡しとファイルのロックを行うだけではありません。いくつかのオープン ツールがすでにそれを実行しています。
ターミナル。追加される部分は、上部にある人間が管理する目に見える部屋です。
ソロビルダーやバイブコーダー向けに構築されています。読むのではなく、エージェントを見て操作したい人
ターミナル内の生の JSON は、ベテランのエンジニアと同様です。
コスト
無料、オープンソース、MIT ライセンス取得
どこで実行されるか
ローカルの 127.0.0.1 — クラウドなし、アカウントなし
保存するもの
ローカル SQLite ファイル (WAL モード)。マシンからは何も残らない
必要なもの
Node.js 20+、および少なくとも 1 つの MCP 互換エージェント
必要のないもの
任意の API キー、任意の有料 Bothread サブスクリプション、実行するインターネット接続
エージェントツールの表面
19 個の MCP ツール — メッセージング、ファイル リース、タスク、メモ、引き継ぎ、承認
テストされたクライアント
クロード コード、クロード デスクトップ、カーソル、反重力、Gemini CLI、コーデックス、OpenCode
特長
🧵 1 つのライブ スレッド — エージェントはリアルタイムで相互に会話し、またあなたと会話し、返信します。
@メンション（配信確認済み、装飾的ではない）、編集可能/撤回可能

メッセージ。
🔒 衝突の防止 — エージェントは編集前にファイルを要求します。重複する排他的クレームは、
拒否され表示されるため、2 人のエージェントが黙ってお互いを攻撃することはありません。
🌿 エージェントごとの git diff — git リポジトリのルームと、要求と要求の間の各エージェントの変更をポイントします。
リリース中のファイルはレビュー可能な差分としてキャプチャされます。マージするか、破棄するか、または
あなたが望むハンク — あなた自身のコミットされていない編集には決して触れません。自動およびオプトイン (そうでない場合はオフ)
ルームには git プロジェクト フォルダーがあります)。
🤝 ルーティングされたハンドオフ — エージェントが他のエージェントが保持しているファイルでブロックされると、 Bothread は追跡されたファイルを開きます
リクエストを行うと、保持者に @ メンションが付けられ、解放された瞬間にウェイターに通知されます。
📋 共有タスクボードとメモ台帳 — 永続的なタスクボード (作成/要求/更新) と
すべての参加者が閲覧できる耐久性のあるメモ台帳 (決定事項、問題点、検証レポート)。
✋ あなたが指揮を執ります - ルームを一時停止し、危険なアクションを承認/拒否/リダイレクトし、ミュートまたは取り消します
エージェント、監督者としてメッセージを送り、静かな人を少しずつ動かし、部屋ごとに承認ゲートを設定します。
📜 ライブアクティビティの証跡 - すべての結合、クレーム、衝突、マージ、承認、ナッジが記録され、
ルームの「アクティビティ」タブでスクロール可能です。完全な追加のみの監査を一目で確認できます。
🏠 ローカルファースト — 127.0.0.1 をバインドし、状態を SQLite に保存します。クラウドやアカウントは必要ありません。
ルームを作成します — マシン上で Bothread を開き、プロジェクト用のルームを開始し、
プライベートセッションID。
エージェントを接続します。各エージェントに「これは Bothread セッションです」と伝え、ID を貼り付けます。結合します
数秒で。
彼らのコラボレーションを観察します - ライブの会話、誰がどのファイルを要求したか、そしてすべてのファイルを確認します
衝突が起こるのを防ぐことができます。
いつでも参加できます - ルームを一時停止したり、危険なアクションを承認したり、メッセージでリダイレクトしたり、ミュートしたり、
エージェントを取り消す。
どのOSでも同じコマンド

ds — 1 つ選択してください:
npx Bothread start # ゼロインストール、今すぐ試してください
npm install -g Bothread # 一度インストールすると、「bothread」は任意のフォルダーからの PATH 上にあります
次に、任意のディレクトリから次のようにします。
両方とも読み取り開始
最初の実行時にルーム UI が構築され、ブラウザでルームが開きます。 Ctrl-C で停止します。
⚠️ よくある混同: npx install -g Bothread — npx ではなく、 npm install -g Bothread です。
パッケージを実行すると、インストール フラグがなく、そのコマンドはエラーになるだけです。 read start の両方に npx を使用する
(インストールなし) または npm install -g Bothread (実際のグローバル インストール) — 両方を同時に行うことはありません。
git clone https://github.com/AdamACE9/bothread.git
CD 両方読む
npm install # 依存関係をインストールします (1 回)
npm link # どこからでも「bothread」を実行可能にする
ギットがありませんか? GitHub で Code → Download ZIP をクリックして解凍し、フォルダー内のターミナルを開きます。
npm link の後に Bothread が見つからない場合は、代わりにフォルダー内で npm start を実行します。結果は同じです。
上記のコマンドはどの OS でも同じであり、場合によってはトラブルシューティングが異なるだけです。
PowerShell または cmd ではそのまま動作します。 PowerShell が両方の実行を拒否した場合は、
「このシステムではスクリプトの実行が無効になっています」エラー、一度実行します (管理者ではなく通常のユーザーとして):
Set-ExecutionPolicy - スコープ CurrentUser RemoteSigned
ハブは 127.0.0.1 とローカルホスト (IPv4 + IPv6 ループバック) の両方をリッスンするため、Claude Code の
claude mcp add はヘッダーに問題なく動作します。サーバーに障害が発生したと表示される場合は、次のことを確認してください。
Bothread start はすでに実行されているので、追加して claude mcp list で確認します。
npm install -g Bothread が EACCES 権限エラーで失敗した場合は、sudo を使用しないでください — ポイント
代わりに、ホーム ディレクトリにある npm のグローバル フォルダーを使用します。
mkdir -p ~ /.npm-global
npm config セット プレフィックス ' ~/.npm-global '
echo ' エクスポート PATH=~/.npm-global/bin:$PATH ' >> ~ /.zshrc
ソース ~ /.zshrc
逆に失敗した場合、次の理由で

事前に構築されたネイティブ モジュール ( better-sqlite3 ) が見つからない場合は、インストールしてください
Xcode のコマンドライン ツールをコンパイルできるようにします。
xcode-select --install
🐧 リナックス
上記と同じコマンドです。ソースから better-sqlite3 をビルドしようとしてインストールが失敗した場合は、
最初にツールをビルドします (Debian/Ubuntu が示されています。それ以外の場合は、ディストリビューションのパッケージ マネージャーを使用してください)。
sudo apt-get install -y build-essential python3
更新中
まず実行中のハブを停止します (ターミナルで Ctrl-C - 2 つのインスタンスはポートを共有できません)。それから、
インストール方法に応じて次のようになります。
npx — npx はキャッシュされたバージョンを再利用できるため、バージョンを明示的に固定します: npx Bothread@latest start
npm install -g — npm install -g Bothread@latest 、次に Bothread start
クローン化されたリポジトリ — git pull 、その後両方の read start
いずれにしても、read start は、ソースが変更されるたびにルーム UI を自動的に再構築します。
常に新鮮に実行されるため、古いビルドが静かに残されることはありません。
npm リンク後に両方の読み取りが見つかりませんか?フォルダー内で npm start を実行するだけです — 同じ結果、いいえ
グローバルコマンドが必要です。
ルームで、「エージェントに接続」をクリックします。このパネルでは、各エージェントの設定をコピー＆ペーストできます。
MCP URL はすでに入力されています。 Bothread を各エージェントに 1 回追加します。それからそれを言ってください
「これは Bothread セッションです: <セッション ID> 」と参加します。 (ハブは 127.0.0.1 ではトークンフリーです
デフォルトでは; BOTHREAD_AUTH= を指定すると、パネル上で認証ヘッダーも入力されます。)
Claude デスクトップ アプリのメモ: [カスタム コネクタの追加] URL ボックスはクラウド 仲介されており、クラウド 仲介はできません。
ローカルホストのハブに到達します。したがって、ローカルの Bothread は mcp-remote 経由で claude_desktop_config.json に入ります。
橋。再起動後、[+] → [コネクタ] メニューにトグルとして表示されます。 (Claude Code の CLI は
より単純なローカル パス — 1 つの claude mcp add 行、ブリッジなし。)
生のスニペット: skill/mcp-config-examples 。
エージェントのセットアップの手順

ステップ
上の表のクライアント用のコマンドを使用して Bothread MCP サーバーを追加します。 MCP
URL (通常は http://127.0.0.1:4889/mcp ) はマシンごとです。実行中の独自のハブからコピーします。
「エージェントの接続」パネル。
エチケット スキルをインストールして、エージェントが部屋の慣習を理解できるようにします。編集前に主張し、
停滞するのではなくハンドオフし、メッセージを簡潔に保ちます。
npx スキル追加 AdamACE9/bothread -y
これにより、このリポジトリからスキルが取得され、エージェント自体の構成に自動的にインストールされます。
エージェントをリロードすると、新しい両方の読み取りツールが表示されます。通常、MCP サーバーを追加するには
プロセスを再起動します。
ルームのライブ セッション ID (「エージェントの接続」に表示、ルームごとに生成) を指定します。これはできません
予測される）。 { sessionId, AgentName, brand } を指定して join_session を呼び出します。
get_room_state を使用して、誰がすでにそこにいて、何が要求されているかを確認します。
それ以来、チームメイトのように動作します。編集する前に常にclaim_filesを実行し、決してファイルに触れないでください。
別の参加者が保持しているファイルを想定する代わりに send_message を通じて会話し、電話をかける
wait_for_update は、そのステップは完了したがルームのタスクは完了していないときにアイドル状態になるのではなく、wait_for_update を実行します。
エチケットの詳細: skill/bothread/SKILL.md および
スキル/AGENTS.md 。
スキルをインストールするその他の方法
Claude Code プラグイン: このリポジトリは有効なプラグイン + 単一プラグイン マーケットプレイスでもあります
( .claude-plugin/plugin.json + .claude-

[切り捨てられた]

## Original Extract

Local room where AI coding agents collaborate on one codebase via MCP — collisions prevented, human in command. - AdamACE9/bothread

GitHub - AdamACE9/bothread: Local room where AI coding agents collaborate on one codebase via MCP — collisions prevented, human in command. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
AdamACE9
/
bothread
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
69 Commits 69 Commits .claude-plugin .claude-plugin .claude .claude apps/ room-ui apps/ room-ui bin bin docs docs packages packages scripts scripts skill skill website website .gitignore .gitignore .nvmrc .nvmrc LICENSE LICENSE README.md README.md package-lock.json package-lock.json package.json package.json server.json server.json tsconfig.base.json tsconfig.base.json View all files Repository files navigation
Run the AI coding agents you already use — together, on one codebase. Local, MCP-native, you in command.
A free, open-source local app. No API keys, no cloud. · Not affiliated with the embroidery brand "Brothread".
Website · Get started · The skill · npm
Three agents (Claude Code · Antigravity · OpenCode) splitting up the work and building a playable game in one shared room — live.
Bothread in one sentence: Bothread is a free, open-source, local coordination hub that lets
multiple AI coding agents — Claude Code, Cursor, Antigravity, Gemini CLI, Codex, OpenCode, or any
other MCP -compatible agent — work together on the same codebase
in one shared room, claiming files so they never overwrite each other, while a human watches every
move and stays in command. No API keys, no cloud, no cost.
Run more than one AI coding agent on the same project without it and it gets painful fast: they
can't talk to each other, they open the same file and silently overwrite each other's work, and
whatever coordination exists happens invisibly across separate terminals. Bothread runs an MCP
server so any MCP-compatible agent can join one room , collaborate on the same codebase , and
stay out of each other's way — while a human watches every move and can step in at any time.
It does not call any AI model itself and takes no API keys — it coordinates the agents you
already run, each on its own subscription. Bothread is the room, the collision prevention, and the
human controls layered on top.
They can't talk to each other. Each agent runs in its own process, its own context, its own
loop. They have complementary strengths — one plans, one refactors, one tests — but no way to
actually work as a team.
They collide. Two agents open the same file and quietly overwrite each other's work. By the
time you notice, the damage is already committed.
You're shut out. What little coordination exists happens invisibly, in terminals and config
files. There's nothing to watch, and no moment to step in before something risky runs.
Bothread isn't just message-passing and file-locking — a few open tools already do that in a
terminal. The part it adds is the visible, human-governed room on top:
Built for solo builders and vibe-coders — people who want to see and steer their agents, not read
raw JSON in a terminal — as much as for veteran engineers.
Cost
Free, open source, MIT licensed
Where it runs
Locally, on 127.0.0.1 — no cloud, no account
What it stores
A local SQLite file (WAL mode); nothing leaves your machine
What it needs
Node.js 20+, and at least one MCP-compatible agent
What it doesn't need
Any API key, any paid Bothread subscription, an internet connection to run
Agent tool surface
19 MCP tools — messaging, file leases, tasks, notes, hand-offs, approvals
Tested clients
Claude Code, Claude Desktop, Cursor, Antigravity, Gemini CLI, Codex, OpenCode
Features
🧵 One live thread — agents talk to each other and to you, in real time, with replies,
@-mentions (delivery-confirmed, not decorative), and editable/retractable messages.
🔒 Collisions prevented — agents claim files before editing; an overlapping exclusive claim is
denied and shown , so two agents never silently clobber each other.
🌿 Per-agent git diffs — point a room at a git repo and each agent's changes between claiming and
releasing files are captured as a reviewable diff. Merge it, discard it, or keep only the
hunks you want — your own uncommitted edits are never touched. Automatic and opt-in (off unless
the room has a git project folder).
🤝 Routed hand-offs — when an agent is blocked on a file another holds, Bothread opens a tracked
request, @-mentions the holder, and notifies the waiter the moment it's released.
📋 Shared task board & notes ledger — a persistent task board (create/claim/update) and a
durable notes ledger (decisions, issues, verification reports) every participant can see.
✋ You're in command — pause the room, approve / reject / redirect risky actions, mute or revoke
an agent, message as the overseer, nudge a quiet one, and set per-room approval gates .
📜 Live activity trail — every join, claim, collision, merge, approval, and nudge is recorded and
scrollable in the room's Activity tab. Full append-only audit, in plain sight.
🏠 Local-first — binds 127.0.0.1 , stores state in SQLite, no cloud, no account.
Create a room — open Bothread on your machine, start a room for your project, and get a
private session ID.
Connect your agents — tell each agent "this is a Bothread session" and paste the ID. It joins
in seconds.
Watch them collaborate — see the live conversation, who's claimed which files, and every
collision prevented, as it happens.
Step in anytime — pause the room, approve a risky action, redirect with a message, mute or
revoke an agent.
Any OS, same commands — pick one:
npx bothread start # zero-install, try it right now
npm install -g bothread # install once, `bothread` is on your PATH from any folder
Then, from any directory:
bothread start
It builds the room UI on first run and opens the room in your browser . Stop with Ctrl-C .
⚠️ Common mix-up: it's npm install -g bothread , not npx install -g bothread — npx
runs a package, it has no install flag, and that command will just error. Use npx bothread start
(no install) or npm install -g bothread (real global install) — never both together.
git clone https://github.com/AdamACE9/bothread.git
cd bothread
npm install # install dependencies (one time)
npm link # make 'bothread' runnable from anywhere
No git? On GitHub click Code → Download ZIP , unzip it, and open a terminal in the folder.
If bothread isn't found after npm link , just run npm start in the folder instead — same result.
The commands above are identical on every OS — only the occasional troubleshooting differs:
Works as-is in PowerShell or cmd. If PowerShell refuses to run the bothread shim with a
"running scripts is disabled on this system" error, run once (as your normal user, not admin):
Set-ExecutionPolicy - Scope CurrentUser RemoteSigned
The hub listens on both 127.0.0.1 and localhost (IPv4 + IPv6 loopback), so Claude Code's
claude mcp add works without header quirks. If a server shows as failed , make sure
bothread start is already running, then add it and check with claude mcp list .
If npm install -g bothread fails with an EACCES permission error, don't use sudo — point
npm's global folder at your home directory instead:
mkdir -p ~ /.npm-global
npm config set prefix ' ~/.npm-global '
echo ' export PATH=~/.npm-global/bin:$PATH ' >> ~ /.zshrc
source ~ /.zshrc
If it instead fails because it can't find a prebuilt native module ( better-sqlite3 ), install
Xcode's command-line tools so it can compile one:
xcode-select --install
🐧 Linux
Same commands as above. If the install fails trying to build better-sqlite3 from source, install
build tools first (Debian/Ubuntu shown — use your distro's package manager otherwise):
sudo apt-get install -y build-essential python3
Updating
Stop any running hub first ( Ctrl-C in its terminal — two instances can't share a port). Then,
depending on how you installed it:
npx — pin the version explicitly, since npx can reuse a cached one: npx bothread@latest start
npm install -g — npm install -g bothread@latest , then bothread start
Cloned repo — git pull , then bothread start
Either way, bothread start rebuilds the room UI automatically whenever its source changed, and
always runs fresh, so there's never a stale build silently left behind.
bothread not found after npm link ? Just run npm start in the folder — same result, no
global command needed.
In the room, click "Connect an agent." The panel gives you copy-paste setup for each agent with
the MCP URL already filled in . You add Bothread to each agent once; then tell it
"This is a Bothread session: <session ID> " and it joins. (The hub is token-free on 127.0.0.1
by default; with BOTHREAD_AUTH=on the panel also fills in the Authorization header.)
Claude desktop app note: the "Add custom connector" URL box is cloud-brokered — it can't
reach a localhost hub. So a local Bothread goes in claude_desktop_config.json via the mcp-remote
bridge; after a restart it shows up in the + → Connectors menu as a toggle. (Claude Code's CLI is
the simpler local path — one claude mcp add line, no bridge.)
Raw snippets: skill/mcp-config-examples .
Setting up an agent, step by step
Add the Bothread MCP server using the command for that client from the table above. The MCP
URL (usually http://127.0.0.1:4889/mcp ) is per-machine — copy it from your own running hub's
"Connect an agent" panel.
Install the etiquette skill so the agent knows the room's conventions — claim before editing,
hand off instead of stalling, keep messages terse:
npx skills add AdamACE9/bothread -y
This fetches the skill from this repo and installs it into the agent's own config automatically.
Reload the agent so the new bothread tools appear — adding an MCP server usually requires a
restart of its process.
Give it the room's live session ID (shown in "Connect an agent", generated per room — it can't
be predicted). It calls join_session with { sessionId, agentName, brand } , then
get_room_state to see who's already there and what's claimed.
From then on it behaves like a teammate: always claim_files before editing, never touch a
file another participant holds, talk through send_message instead of assuming, and call
wait_for_update instead of going idle when its step is done but the room's task isn't.
Full etiquette details: skill/bothread/SKILL.md and
skill/AGENTS.md .
Other ways to install the skill
Claude Code plugin: this repo is also a valid plugin + single-plugin marketplace
( .claude-plugin/plugin.json + .claude-

[truncated]
