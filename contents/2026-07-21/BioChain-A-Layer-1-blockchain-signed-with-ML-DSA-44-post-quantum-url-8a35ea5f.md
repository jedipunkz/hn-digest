---
source: "https://github.com/biochain-code/biochain"
hn_url: "https://news.ycombinator.com/item?id=48999464"
title: "BioChain: A Layer-1 blockchain signed with ML-DSA-44 (post-quantum) url"
article_title: "GitHub - biochain-code/biochain: Post-quantum Layer-1 blockchain (ML-DSA-44/Dilithium3), organic node emergence, honest tokenomics · GitHub"
author: "biochain"
captured_at: "2026-07-21T23:48:59Z"
capture_tool: "hn-digest"
hn_id: 48999464
score: 2
comments: 0
posted_at: "2026-07-21T22:55:17Z"
tags:
  - hacker-news
  - translated
---

# BioChain: A Layer-1 blockchain signed with ML-DSA-44 (post-quantum) url

- HN: [48999464](https://news.ycombinator.com/item?id=48999464)
- Source: [github.com](https://github.com/biochain-code/biochain)
- Score: 2
- Comments: 0
- Posted: 2026-07-21T22:55:17Z

## Translation

タイトル: BioChain: ML-DSA-44 (ポスト量子) URL で署名されたレイヤー 1 ブロックチェーン
記事タイトル: GitHub - biochain-code/biochain: ポスト量子レイヤー 1 ブロックチェーン (ML-DSA-44/Dilithium3)、有機ノードの出現、正直なトークンノミクス · GitHub
説明: ポスト量子レイヤー 1 ブロックチェーン (ML-DSA-44/ダイリチウム 3)、有機ノードの出現、正直なトークンノミクス - バイオチェーン コード/バイオチェーン

記事本文:
GitHub - バイオチェーン コード/バイオチェーン: ポスト量子レイヤー 1 ブロックチェーン (ML-DSA-44/ダイリチウム 3)、有機ノードの出現、正直なトークンノミクス · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
バイオチェーンコード
/
バイオチェーン
公共
通知
あなたは署名しているに違いありません

通知設定を変更するには編集してください
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
24 コミット 24 コミット バックエンド バックエンド ドキュメント ドキュメント ウォレット ウォレット .gitignore .gitignore README.md README.md github_Discussion_intro.md github_Discussion_intro.md すべてのファイルを表示 リポジトリ ファイルのナビゲーション
自律的適応型緊急コンセンサスネットワーク
ノードが登録またはデプロイされていないポスト量子レイヤー 1 ブロックチェーン - ノードは参加者のアクティビティから有機的に出現します。 21 個のインパルス (トランザクション) を送信するアドレスは、自動的にライブ ネットワーク ノードになります。継続的な活動がなければ、そのエネルギーは減衰し、消滅します。新たな衝動がそれを蘇らせる。
ステータス: 初期段階、動作中、実稼働環境で 2 つの独立したノードにデプロイされています。まだ公表されていません。このリポジトリへのアクセスは、プロジェクトの分散化と法的審査が完了している間、直接招待によって行われます。以下の「アクセス」を参照してください。
ジェネシスからのポストクォンタム。すべての署名は ML-DSA-44 (CRYSTALS-Dilithium3、NIST FIPS 204) を使用します。ECDSA チェーンに後付けされるものではなく、ブロック 0 から構築されます。
有機ノードの出現。登録のためのステーキングはなく、起動時に許可されたバリデーターも設定されません。アクティビティ自体がノードを作成します。
整数のお金、例外はありません。すべての貨幣価値はサトシスケールの整数です。供給不変条件 (ウォレット + プール + ロック + ステーク == 21,000,000 BIO) は、許容誤差ではなく、正確な同等性でチェックされます。たとえ 1 つのサトシでもドリフトがある場合は、丸め誤差ではなくバグです。
カストディフリーのアトミックスワップ。 BIO と任意の外部アセット間の HTLC ベースのスワップ ( SWAP_OFFER / SWAP_LOCK / SWAP_CLAIM / SWAP_REFUND )。 want_asset はフリー テキストとして使用されます。プロトコルを単一の外部チェーンに結び付けるハードコーディングされたホワイトリストはありません。
州のチェックポイント。新しいノードは代わりにハッシュ検証されたスナップショットを採用できます

ジェネシスからの完全なチェーンを再生し、ハッシュの不一致がある場合は完全な再生に自動的にフォールバックします。
liboqs (C) 署名バックエンド。実際の運用ハードウェアで測定した場合、純粋な Python リファレンス実装よりも 228 倍高速な ML-DSA-44 検証 — 1 秒あたり最大 44,365 回の検証。
単なるテストスイートではない現実世界の検証
これはホワイトペーパーのみのデザインではありません。現在のリリースの時点では:
194 テストの回帰スイートは、デバイス上および運用環境において、本物の ML-DSA-44 暗号化 (モック化されていない) で合格します。コンセンサス、5 つの資金保持バケットすべてにわたる供給不変条件、ウォレット登録許可、ノード検出/自動プロモーション ロジック、シビル耐性ノードの出現、暗号化ピアの自己認識、開発者とサーバー オペレーターの許可プールをカバーします。
独立してホストされている 2 つの本番ノード (異なる国、異なるデータセンター) は、実際のネットワーク分割後の自動フォーク解決を含むライブピア同期を実行します。オペレーターの介入なしに、有効なチェーンが長い方が勝ちます。
導入されると、自動ノード検出。まったく新しいノードは、それ自体をアナウンスして ( POST /peer/announce 、ビットコインの addr メッセージ / イーサリアムの FINDNODE に一致)、可視になることができます。これだけでは信頼は得られません。信頼できるピアに実際に昇格するには、他の候補者と同様に、通常のゴシップを通じて設定された既存の信頼の厳密な大多数からの独立した確認が必要です。新しいサーバーは、指定された既存のノードからすぐにチェーンをプルできます。少なくとも 1 つの既存の信頼できるノードのオペレーターは、逆方向に新しい URL を手動で 1 回追加します。そこから、うわさ話や大多数の宣伝によって、その信頼がさらに自動的に広がります。
シビル耐性ノードの出現。ライブの投票ノードになるには、常に 21 個のインパルスが必要です。私

さらに、アドレスの最初のアクティビティと 21 番目のインパルスの間には少なくとも 7 実日数が必要になり、大量のノードの作成が単なるスクリプトと予備の変更ではなく、リアルタイムでコストがかかるものに変わります。
開発者補助金。 254,500 の BIO は現実世界のビルダー (ウォレット、エクスプローラー、SDK、統合) に資金を提供し、ガバナンス投票によってのみリリースされ、助成金ごとに 5,000 BIO が上限となります。
サーバー運営者の許可。 254,500 の BIO は独立したノード オペレーターに資金を提供し、ガバナンス投票によってのみ解放され、助成金あたり 2,000 BIO に制限されます。元の 509,000 BIO プールの残りの半分は、v5.41 で分割されます。
暗号化アジリティ基盤。アドレス導出と署名検証では、オプションのスキーム識別子を受け入れるようになりました。デフォルトは ML-DSA-44 で、元のアドレス式をバイトごとに再現します (実証済み、想定ではありません)。まだデフォルト以外は何も使用されていません。これは、新しい生成や 1 つの既存のアドレスを壊すことなく、後で 2 番目のスキームを追加できることを保証するだけです。 MATH_SPEC.md §0 を参照してください。
スケールに合わせてパフォーマンスを再調整しました (v5.43)。対象となる 8 つの修正 -- バリデーター選択キャッシュ、ブロックごとのスイープではなくヒープを介したイベント駆動型のノード停止、遅延エネルギー減衰、/verify 整合性キャッシュ、バッチ化されたバランス ロールバック、50,000 ブロック ホット ウィンドウ外のブロックの遅延コールド ストレージ、定期的な WAL チェックポイント、および新しく管理可能な定額転送料金 -- 2026 年 7 月 21 日に両方の運用ノードにデプロイされました。 8 つのうち 7 つは式を変更せず、のみ変更しました。計算戦略。 MATH_SPEC.md §14 に完全な詳細と修正ごとの正直な確認ステータス (実稼働ハードウェアでテスト済みかサンドボックスのみでテスト済み) が記載されています。
何も問題がないときに何が機能するかだけではなく、実際に何が壊れ、どのように修正されたかを文書化します。
コンポーネント
選択
署名スキーム
ML-DSA-44 (CRYSTALS-Dilithium3、NIST PQC 規格)
暗号バックエンド
リボクス (C) v

liboqs-python、大音量の必要なフォールバックとして dilithium_py を使用 - サイレントではありません
バックエンド
Python 3.14、FastAPI + Uvicorn
データベース
SQLite (WAL モード、RLock 下のアトミック トランザクション)
財布
React + Vite PWA、ブラウザ内 ML-DSA-44 署名用の liboqs-js
最大供給量
21,000,000 BIO、ハードキャップ、不変
ガバナンス
ステーク層に関係なく、1 ライブノード = 1 投票 — 資本は報酬に影響し、投票の重みはありません
完全な技術的な詳細は、このリポジトリの BioChain_Whitepaper_v5_40.docx にあります。
トケノミクス — 起源分布
ジェネシスで固定、不変、合計すると 21,000,000 BIO 上限になります:
創設者自身の開始残高（別のトップレベルの割り当てではなく、ジェネシスプールの残りから引き出された10,000 BIO）が最初のライブノードに資金を供給し、次に、これまでに登録された最初の100のウォレットにそれぞれ10 BIOを付与する専用の1,000 BIOプールが提供されました。同じジェネシス プールは 509,000 BIO 助成金プールにも資金を提供しており、v5.41 では開発者助成金 (254,500 BIO、助成金あたり最大 5,000 BIO) とサーバー オペレーター助成金 (254,500 BIO、助成金あたり最大 2,000 BIO) に均等に分割されます。どちらもガバナンス投票によってのみリリースされます。トークンセールは行われていません。 BIO は、いかなる時点でも誰かによって金銭で販売されたことはありません。
創設者の権利確定期間は 10 年間（6 か月のクリフ、その後はそれぞれ約 9,210 BIO の 114 回の月々の支払い）で、外部審査後の当初の 2 年間のスケジュールから延長されました。部分的な手数料燃焼メカニズムが存在し、完全にテストされていますが、0% (最大 50% まで制御可能) で開始されます。創設者は、ネットワークが成熟するまでデフレ圧力を保留することを選択しました。すべてのメカニズムの完全な式は MATH_SPEC.md にあります。各プールのわかりやすい説明は TOKENOMICS.md にあります。
./install.sh
システムの依存関係を処理し、liboqs を構築し (互換性を確認するためにバージョンが固定されています)、監視対象の systemd サービスをセットアップします。

e. ファイアウォールを設定し (生の API ポートを公共のインターネットに公開せずに - 最初のサーバーで一度犯した間違いであり、繰り返すつもりはありません)、自動データベース バックアップのスケジュールを設定します。
install.sh は、biochain.py がまだ配置されていない場合、つまりこのプロジェクトがパブリック パッケージ インデックスに載っていない場合に何をすべきかを正確に示します。 「アクセス」を参照してください。
意図的な手動の最初のステップが 1 つあり、その後は自動になります。ビットコイン (DNS シード、次に addr メッセージ) とイーサリアム (ブートノード、次に Kademlia) の両方が使用するのと同じパターンです。正確な式については、MATH_SPEC.md §12a を参照してください。
v5.41 の時点では、install.sh は対話的にピア アドレスを要求し、それらを systemd 環境変数 ( BIOCHAIN_PEER_URLS 、 BIOCHAIN_SELF_URL ) として保存します。 biochain.py 自体は決して編集されません。ピア プロンプトを空白のままにすると、 DEFAULT_BOOTSTRAP_PEERS にフォールバックします。 DEFAULT_BOOTSTRAP_PEERS は、コードに組み込まれた小規模で正常なシード リスト (現在は両方の運用ノード) です。これは、ビットコインの DNS シードとイーサリアムのブートノードが果たすのと同じ役割であり、これらと同様に、ネットワークの実際の信頼できるメンバーシップが変更されると、将来のリリースで更新が必要になる可能性があるリストです。
install.sh を実行します。現在の 2 つの運用ノードの場合、ピア プロンプトを空白のままにするだけで十分です。 DEFAULT_BOOTSTRAP_PEERS が自動的に使用されます。代わりに別のノードを指定するには (またはデフォルトが廃止される場合)、現在のアドレスを電子メールで問い合わせてください (下記を参照)。
新しいノードは、指定されているピアからチェーンの同期を直ちに開始します。
あなたのノードは、その既存のノード上で POST /peer/announce を呼び出します。基本的な活性チェックが実行され、それが表示される候補になります。これだけでは信頼は得られません。
既存の信頼セットの厳密な大多数が、お互いのゴシップを通じて (あなた自身についての発言を通じてではなく) あなたのノードを独立して確認すると、永続的に認証されます。

信頼できるピア リストにローモートされます。これ以上の手動手順は不要で、自動的に再起動しても存続します。
ノードの出現自体 (インパルスを送信した後、アドレスがライブの投票参加者になる) は常に完全に自動であり、誰からの紹介も必要ありません。その特定のメカニズムに追加されるシビル耐性タイミング要件については、MATH_SPEC.md の §6 を参照してください。
このリポジトリは公開されています。以前ここにあった段落の 2 つのことは今でも真実であり、静かに廃止するのではなく、はっきりと言う価値があります。それは、真の分散化 (複数の独立して運営されるノード) とプロジェクトの規制上の立場の法的見直しはどちらも遅れではなく、まだ進んでいるということです。現在公開することは、実際のエンジニアリングのフィードバックを得て、独立したノードオペレーターを見つけるための意図的な選択であり、どちらかが完了したという主張ではありません。
ウォレットをお試しください: biochainnetwork.com または node2.biochainnetwork.com — どちらの運用ノードも、同じライブ チェーンに支えられた同じウォレットにサービスを提供します。
暗号化またはコンセンサスロジックのレビュー
ポスト量子ブロックチェーン研究全般
連絡先: biochainnetwork@gmail.com
ポスト量子レイヤー 1 ブロックチェーン (ML-DSA-44/ダイリチウム 3)、有機ノードの出現、正直なトケノミクス
読み込み中にエラーが発生しました。このページをリロードしてください。
0
フォーク

[切り捨てられた]

## Original Extract

Post-quantum Layer-1 blockchain (ML-DSA-44/Dilithium3), organic node emergence, honest tokenomics - biochain-code/biochain

GitHub - biochain-code/biochain: Post-quantum Layer-1 blockchain (ML-DSA-44/Dilithium3), organic node emergence, honest tokenomics · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
biochain-code
/
biochain
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
24 Commits 24 Commits backend backend docs docs wallet wallet .gitignore .gitignore README.md README.md github_discussion_intro.md github_discussion_intro.md View all files Repository files navigation
Autonomous Adaptive Emergent Consensus Network
A post-quantum Layer-1 blockchain where nodes are not registered or deployed — they emerge organically from participant activity. An address that sends 21 impulses (transactions) automatically becomes a live network node; without continued activity its energy decays and it dies; new impulses revive it.
Status: early, working, production-deployed on two independent nodes. Not yet publicly announced. Access to this repository is by direct invitation while the project completes decentralization and legal review — see Access below.
Post-quantum from genesis. Every signature uses ML-DSA-44 (CRYSTALS-Dilithium3, NIST FIPS 204) — not retrofitted onto an ECDSA chain, built with it from block zero.
Organic node emergence. No staking-to-register, no permissioned validator set at launch. Activity itself creates nodes.
Integer money, no exceptions. All monetary values are satoshi-scale integers. The supply invariant ( wallets + pools + locked + staked == 21,000,000 BIO ) is checked with exact equality, not tolerance — any drift of even one satoshi is a bug, not a rounding error.
Custody-free atomic swaps. HTLC-based swaps ( SWAP_OFFER / SWAP_LOCK / SWAP_CLAIM / SWAP_REFUND ) between BIO and any external asset, with want_asset as free text — no hardcoded whitelist tying the protocol to any single external chain.
State checkpoints. New nodes can adopt a hash-verified snapshot instead of replaying the full chain from genesis, with automatic fallback to full replay on any hash mismatch.
liboqs (C) signature backend. ~228x faster ML-DSA-44 verification than the pure-Python reference implementation, measured on real production hardware — ~44,365 verifies/sec.
Real-world validation, not just a test suite
This isn't a whitepaper-only design. As of the current release:
194-test regression suite passes with genuine ML-DSA-44 cryptography (not mocked), on-device and in production. Covers consensus, the supply invariant across all five money-holding buckets, the wallet-registration grant, node-discovery/auto-promotion logic, Sybil-resistant node emergence, cryptographic peer self-recognition, and the developer and server-operator grants pools.
Two independently-hosted production nodes (different countries, different data centers) run live peer synchronization, including automatic fork resolution after a real network partition — the longer valid chain wins, with no operator intervention.
Automatic node discovery, once introduced. A brand-new node can announce itself ( POST /peer/announce , matching Bitcoin's addr messages / Ethereum's FINDNODE ) to become visible — this alone grants no trust. Actual promotion into a trusted peer still requires independent confirmation from a strict majority of the existing trust set through normal gossip, the same as any other candidate. A brand-new server can immediately pull the chain from any existing node once pointed at it; an operator on at least one existing trusted node still adds the new URL once, manually, for the reverse direction — from there, gossip and majority promotion spread that trust further automatically.
Sybil-resistant node emergence. Becoming a live, voting node has always required 21 impulses; it now additionally requires at least 7 real days between an address's first activity and that 21st impulse — turning mass node creation into something that costs real time, not just a script and spare change.
Developer grants. 254,500 BIO fund real-world builders (wallets, explorers, SDKs, integrations), released only via governance vote, capped at 5,000 BIO per grant.
Server-operator grants. 254,500 BIO fund independent node operators, released only via governance vote, capped at 2,000 BIO per grant — the other half of the original 509,000 BIO pool, split in v5.41.
Cryptographic agility foundation. Address derivation and signature verification now accept an optional scheme identifier, defaulting to ML-DSA-44 and reproducing the original address formula byte-for-byte (proven, not assumed). Nothing uses anything but the default yet — this only ensures a second scheme could be added later without a new genesis or breaking a single existing address. See MATH_SPEC.md §0.
Performance rework for scale (v5.43). Eight targeted fixes -- validator-selection caching, event-driven node death via a heap instead of a per-block sweep, lazy energy decay, /verify integrity caching, batched balance rollback, lazy cold storage for blocks outside a 50,000-block hot window, periodic WAL checkpointing, and a newly-governable flat transfer fee -- deployed to both production nodes July 21, 2026. Seven of the eight change no formula, only computation strategy. Full detail and an honest per-fix confirmation status (production-hardware-tested vs. sandbox-only) in MATH_SPEC.md §14.
We document what actually broke and how it got fixed, not just what works when nothing goes wrong.
Component
Choice
Signature scheme
ML-DSA-44 (CRYSTALS-Dilithium3, NIST PQC standard)
Crypto backend
liboqs (C) via liboqs-python, with dilithium_py as a loud, required fallback — never silent
Backend
Python 3.14, FastAPI + Uvicorn
Database
SQLite (WAL mode, atomic transactions under RLock)
Wallet
React + Vite PWA, liboqs-js for in-browser ML-DSA-44 signing
Max supply
21,000,000 BIO, hard cap, immutable
Governance
1 live node = 1 vote, regardless of stake tier — capital affects rewards, never voting weight
Full technical detail is in BioChain_Whitepaper_v5_40.docx in this repository.
Tokenomics — genesis distribution
Fixed at genesis, immutable, summing to the full 21,000,000 BIO cap:
The founder's own starting balance (10,000 BIO, drawn from the genesis pool's remainder — not a separate top-level allocation) funded the first live node and, in turn, a dedicated 1,000 BIO pool granting 10 BIO each to the first 100 wallets ever registered. The same genesis pool also funds a 509,000 BIO grants pool, split evenly in v5.41 into developer grants (254,500 BIO, up to 5,000 BIO per grant) and server-operator grants (254,500 BIO, up to 2,000 BIO per grant) — both released only via governance vote. No token sale has occurred; no BIO has been sold for money by anyone at any point.
Founder vesting spans 10 years (6-month cliff, then 114 monthly payments of ≈9,210 BIO each) — extended from an original 2-year schedule after external review. A partial fee-burning mechanism exists and is fully tested but launched at 0% (governable up to 50%) — the founder chose to hold off on deflationary pressure until the network has matured. Full formulas for every mechanism are in MATH_SPEC.md ; a plain-language walkthrough of every pool is in TOKENOMICS.md .
./install.sh
Handles system dependencies, builds liboqs (version-pinned for confirmed compatibility), sets up a supervised systemd service, configures the firewall (without exposing the raw API port to the public internet — a mistake we made once on our own first server and don't intend to repeat), and schedules automated database backups.
install.sh will tell you exactly what to do if biochain.py isn't in place yet — this project isn't on a public package index. See Access .
There's one manual, deliberate first step, then it's automatic — the same pattern Bitcoin (DNS seeds, then addr messages) and Ethereum (bootnodes, then Kademlia) both use. See MATH_SPEC.md §12a for the exact formulas.
As of v5.41, install.sh asks for peer addresses interactively and stores them as systemd environment variables ( BIOCHAIN_PEER_URLS , BIOCHAIN_SELF_URL ) — biochain.py itself is never edited. Leaving the peer prompt blank falls back to DEFAULT_BOOTSTRAP_PEERS , a small, known-good seed list baked into the code (currently both production nodes) — the same role Bitcoin's DNS seeds and Ethereum's bootnodes play, and, like those, a list that may need updating in a future release as the network's real, trusted membership changes.
Run install.sh — for the two current production nodes, leaving the peer prompt blank is enough; it uses DEFAULT_BOOTSTRAP_PEERS automatically. To point at a different node instead (or if the defaults are ever retired), email us (see below) for a current address.
Your new node immediately starts syncing the chain from whichever peer(s) it's pointed at.
Your node calls POST /peer/announce on that existing node — a basic liveness check runs, then it becomes a visible candidate. This alone grants no trust.
Once a strict majority of the existing trust set independently confirms your node through their own gossip with each other (not through anything you say about yourself), it's durably promoted into their trusted-peer list — automatically, no further manual step, and it survives restarts.
Node emergence itself (an address becoming a live, voting participant after sending impulses) has always been fully automatic and requires no introduction from anyone — see §6 in MATH_SPEC.md for the Sybil-resistance timing requirement added to that specific mechanism.
This repository is public. Two things from the paragraph that used to be here are still true, and still worth saying plainly rather than dropping quietly: real decentralization (multiple independently-operated nodes) and a legal review of the project's regulatory position are both still ahead, not behind. Going public now is a deliberate choice to get real engineering feedback and find independent node operators — not a claim that either of those is finished.
Try the wallet: biochainnetwork.com or node2.biochainnetwork.com — both production nodes serve the same wallet, backed by the same live chain.
reviewing the cryptography or consensus logic
post-quantum blockchain research generally
Reach out: biochainnetwork@gmail.com
Post-quantum Layer-1 blockchain (ML-DSA-44/Dilithium3), organic node emergence, honest tokenomics
There was an error while loading. Please reload this page .
0
forks

[truncated]
