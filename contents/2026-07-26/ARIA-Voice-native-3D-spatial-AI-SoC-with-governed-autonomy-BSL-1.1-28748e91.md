---
source: "https://github.com/sidevworks/aria-sec"
hn_url: "https://news.ycombinator.com/item?id=49057507"
title: "ARIA – Voice-native 3D spatial AI SoC with governed autonomy (BSL 1.1)"
article_title: "GitHub - sidevworks/aria-sec · GitHub"
author: "saryismail"
captured_at: "2026-07-26T12:58:38Z"
capture_tool: "hn-digest"
hn_id: 49057507
score: 1
comments: 0
posted_at: "2026-07-26T12:35:18Z"
tags:
  - hacker-news
  - translated
---

# ARIA – Voice-native 3D spatial AI SoC with governed autonomy (BSL 1.1)

- HN: [49057507](https://news.ycombinator.com/item?id=49057507)
- Source: [github.com](https://github.com/sidevworks/aria-sec)
- Score: 1
- Comments: 0
- Posted: 2026-07-26T12:35:18Z

## Translation

タイトル: ARIA – 自律性を備えた音声ネイティブ 3D 空間 AI SoC (BSL 1.1)
記事タイトル: GitHub - Sidevworks/aria-sec · GitHub
説明: GitHub でアカウントを作成して、sidevworks/aria-sec の開発に貢献します。

記事本文:
GitHub - Sidevworks/aria-sec · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 新しい外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
サイドワークス
/
アリア秒
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 Co

de 「その他のアクション」メニューを開く フォルダーとファイル
1 コミット 1 コミット .ebextensions .ebextensions aria-memory aria-memory build build docs docs electric electric infra/ トライアルフォーム infra/ トライアルフォーム public public scripts scripts サーバーサーバー src src test test .env.example .env.example .gitignore .gitignore DEPLOY.md DEPLOY.md Dockerfile Dockerfile LICENSEライセンス Procfile Procfile README.md README.md ROADMAP_AND_LIMITATIONS.md ROADMAP_AND_LIMITATIONS.md docker-compose.yml docker-compose.yml electric.vite.config.js electric.vite.config.js eslint.config.js eslint.config.js index.htmlindex.html package-lock.json package-lock.json package.json package.json pnpm-workspace.yaml pnpm-workspace.yaml vercel.json vercel.json vite.config.js vite.config.js すべてのファイルを表示 リポジトリ ファイルのナビゲーション
自律型レジリエンス インテリジェンス アーキテクチャ
自律性が管理された音声ネイティブの 3D 空間セキュリティ運用コックピット。必要に応じて、完全に独自のハードウェア上で実行できます。
評価と研究のためにソースが利用可能。オープンソースではありません — ライセンス を参照してください。
ほとんどの AI セキュリティ ツールは、機能してから信頼するよう求めます。 ARIA はそれを逆転させます。
自律的な能力は、測定された結果を通じてその権威を獲得する必要があります。
どこまで許可されているかを正確に確認して制限することができます。
これは、話しかけるシングル オペレーターの SOC コックピットです。機能セット全体は、
メニューのサイドバーではなく、ナビゲート可能な 3D 銀河です。あなたからテレメトリを取り込みます
クラウド、アイデンティティ、コード、ネットワーク サーフェスで何が重要かをわかりやすく説明します
言語を使用し、ポリシーによってゲートされ、
監査証跡。
正直なステータス: ARIA は、単独で積極的に開発中の実用的なプラットフォームであり、
硬化した市販品。本物のコネクタ、本物の機能を備えています。
ローカルネットワークとBLEディスク

実際の監査証跡、実際のローカル モデル パスはすべて必要ですが、
その一部はデモグレードであり、この README 全体および
製品自体。ここには、現状以上に完成したものとして示されているものはありません。
→ ROADMAP_AND_LIMITATIONS.md が完全版です
エンジニアリング監査、未編集で公開: ファイル パスとモジュールごとのステータスと
行番号。どのサブシステムが実稼働グレードで、どのサブシステムが実稼働グレードであるかを正確に指定します。
シミュレートした。この README と異なる場合は、代わりにそれを信じてください。短い要約
成熟期にあり、以下で行われていないこと。
🌌 空間銀河コマンドインターフェース
プラットフォーム全体が Three.js/WebGL の銀河です。セクターは能力ドメインです。
ノードは個々のパネルです。 3段階のカラーシステム（公称ブルー→アンバー）
脅威 → 赤クリティカル) はスキャンとテレメトリの状態からライブで伝播するため、ポスチャは
部屋の向こうからでも読みやすい。
交換可能な 4 つのナビゲーション方法: ノードをクリックする、目的地を話す、文字を入力する
コマンドを使用するか、重大なイベントが発生したときに ARIA に自動的にルーティングさせます。
⚖️ 統治された自治と信頼のはしご
核となるアイデアと残りの部分が存在する理由。自律性はグローバルなオン/オフではありません
スイッチ — それは能力ごとに獲得されます。
4 つの機能 (脅威分析、修復、封じ込め、
identity_actions ) はそれぞれ 4 段のはしごに独立して配置されます。
昇進には、測定された成果の実績が必要です。重要なのは、モデル
それ自体を昇格させることはできません。能力は、オペレーターがそれを昇格させた場合にのみ上昇します。
悪い結果に対する降格は即時です。あらゆる昇進、降格、承認、
アクションは、保持エンベロープを使用して追加専用の監査ログに書き込まれます。
🎙️ 音声ネイティブ&割り込み可能
ダッシュボードに固定されたチャットボットではなく、ライブデータを介した双方向の音声会話。
Speech-to-Text は常にローカルです (@xenova/transf 経由のウィスパー)

ormers ) — あなたの声
決してマシンから離れることはありません。テキスト読み上げは、残すことができるオプションのクラウド通話です
未構成。
🛡️ エアギャップ＆ソブリン対応
ARIA は、テレメトリをサードパーティに送信する規制された環境向けに構築されています
SaaS は単に望ましくないだけでなく、違法です。 LOCAL モードでは、推論が実行されます。
OpenAI 互換エンドポイント (Ollama、vLLM、LM Studio、
llama.cpp) と主権境界はコード内で強制されるものであり、コード内で約束されるものではありません。
ドキュメント:
server/localLlm.mjs は、ループバックまたは外部のホストへのプロンプトの送信を拒否します。
プライベート範囲。明示的に設定しない限り、パブリック アドレスは拒否されます。
ARIA_LOCAL_LLM_ALLOW_REMOTE=true 。
LOCAL モードは、クラウドが停止した場合でも、暗黙的にクラウド プロバイダーにフォールバックすることはありません。
API キーは環境内に存在します。ローカルエンジンが停止している場合
静かな出口ではなく、明確にラベル付けされた劣化概要とその理由が表示されます。
サーバーはデフォルトでループバックにバインドされており、インターネットに接続するように設計されていません。
GitHub、AWS、Okta、Snyk、Azure AD にわたる AI セキュリティ体制管理
VirusTotal および Elastic Security — AI 資産、シークレット、モデルのインベントリの作成
インフラストラクチャ、エンドポイント、ID リンク、その後、攻撃パスと爆発のマッピング
それらを横切る半径。コネクタの資格情報は保存時に AES-256-GCM で暗号化されます。
📡 ローカルネットワークと Bluetooth インテリジェンス
IEEE OUI ベンダー アトリビューションと Bluetooth を使用した実際の ARP テーブルと BLE 検出
割り当てられた番号のデコード、デバイス ID の主張、および近接推定。
ブロードキャストおよびマルチキャスト MAC は、レポートとして報告されるのではなく、解析時にフィルタリングされます。
デバイス。アクティブディスカバリは認可ゲート型です。受動的な可視性はありません。
5 分以内に API キーは不要 — ARIA はローカルのみの機能から開始します
そして、すべての AI プロバイダーは無効になっていますが、目に見えてそのようにラベル付けされています。
N

ode.js ≥ 22 (node:builtins およびネイティブ fetch を使用)
オプション: 主権ローカル推論のための Ollama
オプション: Docker、コンテナ化されたサーバー用
git clone https://github.com/ < your-org > /aria-guardian.git
CD アリアガーディアン
npmインストール
2. 設定する
cp .env.example .env.local
node scripts/generate-encryption-key.mjs --write # Credential Vault の AES-256-GCM キー
.env.example はすべての変数を文書化します。すべての AI キーはオプションです - 入力のみ
あなたが望むもの。 「Bring Your Own Key」を参照してください。
npm run start:local # API :5000 + Web UI :5173
次に、http://127.0.0.1:5173 を開き、開発オペレーターでログインします。
ユーザー名: オペレーター
パスワード: アリア
この組み込みアカウントは、 NODE_ENV がdevelopment/testである場合、または
ARIA_ENABLE_DEMO_LOGIN=true が運用環境外の場合。本番環境はフェールクローズされます。
実際のオペレーターの資格情報と資格情報がないと起動を拒否します。
暗号化キー。 ARIA_LOGIN_USERNAME / ARIA_LOGIN_PASSWORD を設定します (または
ARIA_OPERATORS ) 実際のデプロイメント用。
コマンド
何をするのか
npm 実行開始:ローカル
API と Web UI の併用 (最初の実行を推奨)
npm実行アリア
Electron デスクトップ アプリ
npm実行サーバー
API サーバーのみ、:5000
npm 実行開発
Vite フロントエンドのみ
npm ビルドを実行する
プロダクションフロントエンドバンドル
npm 実行距離
パッケージ化されたデスクトップ アプリ (macOS .dmg )
npmでlintを実行する
ESLint
npm 実行テスト:サーバー
サーバーテストスイート
ドッカー
export ARIA_CREDENTIAL_ENCRYPTION_KEY= $( ノードスクリプト/generate-encryption-key.mjs )
ドッカーの構成
そのキーが設定されていない場合、Compose はすぐに失敗し、説明メッセージが表示されます - イメージが実行されます
NODE_ENV=production を使用すると、資格情報が平文でサイレントに保存されません。
コンテナ内ではなくホスト上で実行されているモデル サーバーにアクセスするには、次のように設定します。
ARIA_LOCAL_LLM_URL=http://host.docker.internal:11434/v1 。
ARIA はキーなしで出荷され、テレメトリもなく、プロキシもありません

つまりあなたのトラフィック
他人のインフラストラクチャを介して。このリポジトリには分析 SDK はありません —
自分で確認してください:
grep -rniE --include= ' *.mjs ' --include= ' *.js ' --include= ' *.jsx ' \
" posthog|mixpanel|amplitude|google-analytics|gtag|datadog|sentry " src/server/
コードベース内のすべての送信ホストは、ユーザーが構成したホスト、つまり選択した LLM です。
プロバイダー、コネクタ独自の API、および音声を有効にしている場合は イレブンラボ。
キーは .env.local (gitignored) またはプロセス環境に存在します。コネクタ
UI を通じて入力された認証情報は、保存時に AES-256-GCM で暗号化されます。
AWS Secrets Manager に委任されます。
コックピットからライブで切り替え可能な 4 つのインテリジェンス モード:
セレクターは願望ではなく現実を反映します。各モードはグレー表示され、
実際に応答できない場合は説明をホバーするとLOCALに名前が表示されます
実際にロードしたモデル。次の場所で直接クエリします
/api/model/status を取得します。
オラマサーブ
オラマ プル qwen3:8b
それが全体の構成です。 ARIA はループバックでデフォルトで Ollama を使用します。
モデルに名前を付けていないため、サーバーが報告する最初のモデルが使用されます。そのため、モデルと連携します。
あなたがすでに引いたものは何でも。明確に言うと:
ARIA_LOCAL_LLM_URL=http://127.0.0.1:11434/v1
ARIA_LOCAL_LLM_MODEL=qwen3:8b
vLLM、LM Studio、llama.cpp のサーバー、および text-generate-webui はすべて、
同じパス — /v1/chat/completions を話すものすべて。
┌─────────────────────────┐
│ レンダラ — React 19 + Three.js (Electron ウィンドウまたはブラウザ) │
│ ギャラクシーシーン・パネル・ボイスループ・インテリジェンスモードセレクター │
━━━━━━━━

─────┬───────────────┘
│ HTTP + WebSocket (ループバック)
┌───────────────────┴─────────────────┐
│ Node.js サーバー —server/index.mjs │
│ │
│ authz、quotaGuard、rateLimiter、auditLog、securityHeaders │
│ ariaTrust（トラストラダー）・actionRunner・policyStore │
│ 推論エンジン・autonomousResponse・ariaMemory │
│ aiSpmInventory · aiRiskEngine · aiSpmBlastRadius │
│ NetworkIntelligence/ · Bluetooth/ · コネクタ/ │
━───┬───────────┬─────────────┬─────────┘
│ │ │
┌───┴───┐ ┌───┴─────┐ ┌───┴───────┐
│ 推論 │ │ コネクター │ │ 永続性 │
│ │ │ │ │ │
│ Anthropic │ │ GitHub・AWS │ │ JSON 配下 │
│ ジェミニ │ │ オクタ・スニック │ │ aria-memory/ │
│ ローカル │ │ Azure AD │ │ + オプションの KV ミラー │
│ (ソブリン)│ │ VirusTotal・Elastic │ │ AES-256-GCM ボールト │
━━━━━━━━━━━━━━━━━━━━━━┘ ━━━━━━━━━━━━━━━┘
フロントエンド — @react-three/fiber 、 drei 、およびを介した Three.js を使用した React 19
後処理。パネルは遅延コード分割されます。開発とビルドにVite

;
デスクトップターゲット用のelectron-vite。
サーバー - 単一の Node.js HTTP + WebSocket サーバー、フレームワークなし、約 4,500 行
74 の集中モジュールに委任する、server/index.mjs 内のルーティング。ループバックバウンド
デフォルト。不透明を使用したロールベースの認証 (所有者/管理者/アナリスト/ビューア)
インメモリセッショントークン。
推論 — 1 つのディスパッチャー ( callAI ) が Anthropic、Gemini、またはあなたのチームにファンアウトします。
ローカル エンジン、p 付き

[切り捨てられた]

## Original Extract

Contribute to sidevworks/aria-sec development by creating an account on GitHub.

GitHub - sidevworks/aria-sec · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry New Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
sidevworks
/
aria-sec
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
1 Commit 1 Commit .ebextensions .ebextensions aria-memory aria-memory build build docs docs electron electron infra/ trial-form infra/ trial-form public public scripts scripts server server src src test test .env.example .env.example .gitignore .gitignore DEPLOY.md DEPLOY.md Dockerfile Dockerfile LICENSE LICENSE Procfile Procfile README.md README.md ROADMAP_AND_LIMITATIONS.md ROADMAP_AND_LIMITATIONS.md docker-compose.yml docker-compose.yml electron.vite.config.js electron.vite.config.js eslint.config.js eslint.config.js index.html index.html package-lock.json package-lock.json package.json package.json pnpm-workspace.yaml pnpm-workspace.yaml vercel.json vercel.json vite.config.js vite.config.js View all files Repository files navigation
Autonomous Resilience Intelligence Architecture
A voice-native, 3D spatial security operations cockpit with governed autonomy — that runs entirely on your own hardware if you want it to.
Source-available for evaluation and research. Not open source — see License .
Most AI security tools act and then ask you to trust them. ARIA inverts that: every
autonomous capability has to earn its authority through measured outcomes, and
you can see and bound exactly how far it is allowed to go.
It is a single-operator SOC cockpit you talk to. The entire feature set is a
navigable 3D galaxy rather than a sidebar of menus. It ingests telemetry from your
cloud, identity, code, and network surfaces, explains what matters in plain
language, and proposes bounded actions that are gated by policy and written to an
audit trail.
Honest status: ARIA is a working platform under active solo development, not
a hardened commercial product. It is genuinely functional — real connectors, real
local network and BLE discovery, real audit trails, a real local-model path — but
parts of it are demo-grade and labelled as such throughout this README and in the
product itself. Nothing here is presented as more finished than it is.
→ ROADMAP_AND_LIMITATIONS.md is the full
engineering audit, published unedited: module-by-module status with file paths and
line numbers, naming exactly which subsystems are production-grade and which are
simulated. If it disagrees with this README, believe it instead. A shorter summary
is in Maturity & what is not done below.
🌌 Spatial Galaxy Command Interface
The whole platform is a Three.js/WebGL galaxy. Sectors are capability domains;
nodes are individual panels. A three-tier colour system (blue nominal → amber
threat → red critical) propagates live from scan and telemetry state, so posture is
legible from across the room.
Four interchangeable ways to navigate: click a node, speak a destination, type a
command, or let ARIA route you automatically when a critical event fires.
⚖️ Governed Autonomy & the Trust Ladder
The core idea, and the reason the rest exists. Autonomy is not a global on/off
switch — it is per-capability and earned .
Four capabilities ( threat_analysis , remediation , containment ,
identity_actions ) each sit independently on a four-rung ladder:
Promotion requires a track record of measured outcomes. Crucially, the model
cannot promote itself — a capability only climbs when an operator promotes it, and
demotion on a bad outcome is immediate. Every promotion, demotion, approval and
action is written to an append-only audit log with a retention envelope.
🎙️ Voice-Native & Interruptible
Two-way spoken conversation over live data, not a chatbot bolted to a dashboard.
Speech-to-text is always local (Whisper via @xenova/transformers ) — your voice
never leaves the machine. Text-to-speech is an optional cloud call you can leave
unconfigured.
🛡️ Air-Gapped & Sovereign-Ready
ARIA is built for regulated environments where sending telemetry to a third-party
SaaS is not merely undesirable but illegal. In LOCAL mode reasoning runs against
your own model over an OpenAI-compatible endpoint (Ollama, vLLM, LM Studio,
llama.cpp) and the sovereignty boundary is enforced in code, not promised in
docs :
server/localLlm.mjs refuses to send a prompt to any host outside loopback or a
private range. A public address is rejected unless you explicitly set
ARIA_LOCAL_LLM_ALLOW_REMOTE=true .
LOCAL mode never silently falls back to a cloud provider, even when a cloud
API key is sitting right there in your environment. If your local engine is down
you get a clearly-labelled degraded summary and the reason — not a quiet egress.
The server binds to loopback by default and is not designed to face the internet.
AI Security Posture Management across GitHub, AWS, Okta, Snyk, Azure AD,
VirusTotal, and Elastic Security — inventorying AI assets, secrets, model
infrastructure, endpoints, and identity links, then mapping attack paths and blast
radius across them. Connector credentials are encrypted at rest with AES-256-GCM.
📡 Local Network & Bluetooth Intelligence
Real ARP-table and BLE discovery with IEEE OUI vendor attribution and Bluetooth
assigned-number decoding, device identity claims, and proximity estimation.
Broadcast and multicast MACs are filtered at parse time rather than reported as
devices. Active discovery is authorization-gated; passive visibility is not.
Under five minutes, no API keys required — ARIA starts with local-only features
and every AI provider disabled but visibly labelled as such.
Node.js ≥ 22 (uses node: builtins and native fetch )
Optional: Ollama for sovereign local reasoning
Optional: Docker, for the containerised server
git clone https://github.com/ < your-org > /aria-guardian.git
cd aria-guardian
npm install
2. Configure
cp .env.example .env.local
node scripts/generate-encryption-key.mjs --write # AES-256-GCM key for the credential vault
.env.example documents every variable. All AI keys are optional — fill in only
what you want. See Bring Your Own Key .
npm run start:local # API on :5000 + web UI on :5173
Then open http://127.0.0.1:5173 and log in with the development operator:
username: operator
password: aria
This built-in account exists only when NODE_ENV is development / test , or
when ARIA_ENABLE_DEMO_LOGIN=true outside production. Production is fail-closed:
it refuses to boot without real operator credentials and a credential
encryption key. Set ARIA_LOGIN_USERNAME / ARIA_LOGIN_PASSWORD (or
ARIA_OPERATORS ) for any real deployment.
Command
What it does
npm run start:local
API + web UI together (recommended first run)
npm run aria
Electron desktop app
npm run server
API server only, on :5000
npm run dev
Vite frontend only
npm run build
Production frontend bundle
npm run dist
Packaged desktop app (macOS .dmg )
npm run lint
ESLint
npm run test:server
Server test suite
Docker
export ARIA_CREDENTIAL_ENCRYPTION_KEY= $( node scripts/generate-encryption-key.mjs )
docker compose up
Compose fails fast with an explanatory message if that key is unset — the image runs
with NODE_ENV=production , which will not silently store credentials in plaintext.
To reach a model server running on your host rather than in a container, set
ARIA_LOCAL_LLM_URL=http://host.docker.internal:11434/v1 .
ARIA ships with no keys , has no telemetry , and never proxies your traffic
through anyone else's infrastructure . There is no analytics SDK in this repo —
verify it yourself:
grep -rniE --include= ' *.mjs ' --include= ' *.js ' --include= ' *.jsx ' \
" posthog|mixpanel|amplitude|google-analytics|gtag|datadog|sentry " src/ server/
Every outbound host in the codebase is one you configured: your chosen LLM
provider, your connectors' own APIs, and ElevenLabs if you enable speech.
Keys live in .env.local (gitignored) or your process environment. Connector
credentials entered through the UI are encrypted at rest with AES-256-GCM, or can be
delegated to AWS Secrets Manager.
Four Intelligence Modes , switchable live from the cockpit:
The selector reflects reality, not aspiration : each mode is greyed out with a
hover explanation when it cannot actually answer, and LOCAL displays the name of
the model you actually have loaded. Query it directly at
GET /api/model/status .
ollama serve
ollama pull qwen3:8b
That is the entire configuration. ARIA defaults to Ollama on loopback and, if you
have not named a model, uses the first one the server reports — so it works with
whatever you have already pulled. To be explicit:
ARIA_LOCAL_LLM_URL=http://127.0.0.1:11434/v1
ARIA_LOCAL_LLM_MODEL=qwen3:8b
vLLM, LM Studio, llama.cpp's server and text-generation-webui all work through the
same path — anything that speaks /v1/chat/completions .
┌──────────────────────────────────────────────────────────────────┐
│ Renderer — React 19 + Three.js (Electron window or browser) │
│ Galaxy scene · panels · voice loop · Intelligence Mode selector │
└───────────────────────────────┬──────────────────────────────────┘
│ HTTP + WebSocket (loopback)
┌───────────────────────────────┴──────────────────────────────────┐
│ Node.js server — server/index.mjs │
│ │
│ authz · quotaGuard · rateLimiter · auditLog · securityHeaders │
│ ariaTrust (Trust Ladder) · actionRunner · policyStore │
│ reasoningEngine · autonomousResponse · ariaMemory │
│ aiSpmInventory · aiRiskEngine · aiSpmBlastRadius │
│ NetworkIntelligence/ · Bluetooth/ · connectors/ │
└───┬──────────────────────┬───────────────────────┬───────────────┘
│ │ │
┌───┴────────┐ ┌─────────┴──────────┐ ┌────────┴─────────────┐
│ Reasoning │ │ Connectors │ │ Persistence │
│ │ │ │ │ │
│ Anthropic │ │ GitHub · AWS │ │ JSON under │
│ Gemini │ │ Okta · Snyk │ │ aria-memory/ │
│ Local │ │ Azure AD │ │ + optional KV mirror │
│ (sovereign)│ │ VirusTotal·Elastic │ │ AES-256-GCM vault │
└────────────┘ └────────────────────┘ └──────────────────────┘
Frontend — React 19 with Three.js via @react-three/fiber , drei , and
postprocessing . Panels are lazily code-split. Vite for dev and build;
electron-vite for the desktop target.
Server — a single Node.js HTTP + WebSocket server, no framework, ~4,500 lines of
routing in server/index.mjs delegating to 74 focused modules. Loopback-bound by
default. Role-based authz ( owner / admin / analyst / viewer ) with opaque
in-memory session tokens.
Reasoning — one dispatcher ( callAI ) fans out to Anthropic, Gemini, or your
local engine, with p

[truncated]
