---
source: "https://underscoredone.com/"
hn_url: "https://news.ycombinator.com/item?id=49078096"
title: "Show HN: _done – Expanding AI Capabilities"
article_title: "Actions for AI. Real-world tasks your agent can't do alone - Made by _done"
author: "onescales"
captured_at: "2026-07-28T01:41:46Z"
capture_tool: "hn-digest"
hn_id: 49078096
score: 1
comments: 0
posted_at: "2026-07-28T01:24:44Z"
tags:
  - hacker-news
  - translated
---

# Show HN: _done – Expanding AI Capabilities

- HN: [49078096](https://news.ycombinator.com/item?id=49078096)
- Source: [underscoredone.com](https://underscoredone.com/)
- Score: 1
- Comments: 0
- Posted: 2026-07-28T01:24:44Z

## Translation

タイトル: HN: _done を表示 – AI 機能の拡張
記事のタイトル: AI のためのアクション。エージェントが単独で実行できない現実世界のタスク - _done によって作成されました
HN テキスト: AI のアクション。エージェントだけでは実行できない現実世界のタスク。 x402 では従量課金制 - アカウントは必要ありません。ログインはありません。鍵はありません。

記事本文:
AIに対するアクション。エージェントだけでは実行できない現実世界のタスク。
使用ごとに支払います - アカウントは必要ありません。ログインはありません。鍵はありません。
IP アドレスまたは ASN 番号の背後にあるネットワーク オペレーター、国、およびアドレス ブロックを検索します。
このツールは、インターネット アドレス (IP) またはネットワーク識別番号 (ASN) を指定すると、そのインターネットを誰が運営しているのか、つまりネットワーク番号、所有者の名前、登録されている国、それを管理する地域レジストリ、およびそのインターネットが属する正確なアドレス範囲を返します。トラフィックの送信元を確認したり、セキュリティ警告を強化したり、インターネット インフラストラクチャを計画したりするのに役立ちます。
URL とオリジンを送信すると、サーバーが付与するクロスオリジンのアクセス許可が正確に返されます。
実際のリクエストを指定した URL に送信し、Origin ヘッダーを添付して、サーバーが返す 6 つの標準のクロスオリジン許可ヘッダーをすべて読み取ります。クロスオリジンリクエストがそもそも有効になっているかどうか、特定のオリジンが許可されているかどうかを示し、サーバーが送信したとおりのすべての許可値を表示します。ブラウザーがリクエストをブロックしている理由を解明したり、新しくデプロイされたサービスが正しく構成されていることを確認したり、パブリック API が認証情報付きのクロスオリジン呼び出しを許可しているかどうかを監査したりするのに最適です。
米国CPIデータとインフレレポート
BLS からの米国の公式インフレ (CPI) 数値の最新、特定の月、または過去の範囲を返します。
政府の消費者物価指数 BLS から米国の公式インフレ数値を提供します。最新の測定値、過去の特定の月、または月全体の範囲を尋ねると、全体的なインフレと「コア」インフレ (食品とエネルギーの価格を無視する) の明確なパーセンテージと、平易な英語の要約文が返されます。
クラウドから実際の HTTP リクエストを作成し、ステータス コード、ヘッダー、本文を取得します。

即座に。
当社のサーバーから任意の URL に HTTP リクエストを送信し、ステータス コード、すべての応答ヘッダー、本文の内容を含む完全な応答を受け取ります。 GET、POST、PUT、PATCH、DELETE、および HEAD をサポートします。最大 3 つのリダイレクトを自動的に追跡します。エンドポイントのテスト、サーバーが正しく応答していることの確認、リダイレクト チェーンのデバッグ、またはローカル設定を必要としない生データのフェッチに役立ちます。
1 つのリクエストで 1 つまたは複数のドメインの電子メール セキュリティ (DMARC) 構成を確認します。
このツールにドメイン名のリストを与えると、各ドメインの DMARC 電子メール セキュリティ レコードが即座にチェックされます。 DMARC は、ドメインから送信されたふりをする偽メールまたはなりすましメールの処理方法を電子メール プロバイダーに指示する標準です。送信したドメインごとに、有効なレコードが存在するかどうか、ポリシーの内容 (なし、隔離、または拒否)、レポート アドレスが設定されているかどうか、および完全な生のレコード テキストが返されます。セットアップなしで一度に多くのドメインを監査するのに最適です。
1 回の呼び出しで、13 の DNS レコード タイプすべてと、最大 10 個のドメインの完全な WHOIS 登録データを検索します。
最大 10 個のドメイン名を送信し、すべての DNS レコード タイプ (A、AAAA、MX、CNAME、TXT、NS、PTR、SOA、SRV、CAA、NAPTR、DS、TLSA) と各ドメインの完全な WHOIS 登録プロファイル (レジストラ、日付、連絡先、ステータス、ネームサーバー) を返します。 URL は受け入れられ、自動的に裸のドメインに取り除かれます。重複したドメインは処理前に静かに削除されます。結果はドメインごとに 1 つのフラットな行として返され、スプレッドシート、データベース、またはパイプラインにドロップできるようになります。
ドメインのリストを入力すると、登録日、有効期限、経過時間、残り時間を即座に取得できます。
ドメイン名のリストを送信すると、このツールは、それぞれのドメイン名がどれくらい古いのか、最初に登録されたのはいつか、有効期限が切れるのはいつか、そしてどれくらいの期間がかかるのかを正確に教えてくれます。

e は期限切れになる前に残ります。まったく新しい疑わしいドメインを見つけたり、競合他社を調査したり、ドメインを購入したりリンクしたりする前にドメインを評価するのに最適です。世界中の 100 以上のドメイン拡張子で動作します。
公式の RDAP プロトコルを使用して、1 ～ 10 個のドメイン名にアクティブな登録レコードがあるかどうかを確認します。
この API に最大 10 個のドメイン名を指定すると、それぞれのドメイン名が公式レジストリに登録記録があるかどうかがわかります。 RDAP (WHOIS に代わる最新の構造化された代替手段) を使用しているため、面倒なフリーテキストの解析はありません。各ドメインは、「登録済み」、「未登録」、または「不定」というラベルが付けられて戻ります (レジストリがその末尾の RDAP をサポートしていない場合)。重要: 「未登録」はレコードが見つからなかったことを意味し、通常は名前が使用可能であることを意味しますが、予約名またはプレミアム名ではレコードが返されないこともあります。この API は登録ステータスのみを通知します。価格、プレミアムステータス、またはレジストラが実際に名前の購入を許可するかどうかはわかりません。
ENS 名を Ethereum アドレスに解決するか、アドレスを ENS 名に解決します。
ENS 名 (vitalik.eth など) を取得し、それが指す Ethereum アドレスを返します。逆の動作も行います。0x... アドレスを指定すると、そのアドレスに設定されたプライマリ ENS 名が存在する場合は、そのアドレスが返されます。送信内容から方向が自動的に判断されるため、入力する必要があるのは 1 つのフィールドだけです。これは、何かを行う前にフレンドリーなウォレット名を実際のアドレスに変換する必要がある、または長い 16 進文字列の代わりにフレンドリー名を表示する必要がある AI エージェントまたはアプリに役立ちます。
カテゴリ別に Hacker News 記事を取得したり、特定の日付のすべての記事/ジョブ/投票をスキャンしたり、オプションでネストされたコメントを追加したりできます。
このツールは、人々がテクノロジーを共有し議論する人気の Web サイトである Hacker News からライブ情報を読み取ります。

ニュースです。現在の記事のリスト (トップ、新規、ベスト、質問、番組、求人リストなど) を要求することも、過去の特定の暦日に投稿されたすべての記事、求人情報、投票を要求することもできます。また、各ストーリーの下に、数レベルの深さのネストされた返信などのディスカッション コメントを取り込むこともできます。
ハッシュ、HMAC、チェックサム スイート
暗号化ハッシュ、HMAC 署名、チェックサムを計算し、定数時間比較で Webhook 署名を検証します。
この API にテキスト データまたはバイナリ データを与えると、選択したハッシュ アルゴリズム (SHA-256、SHA-512、MD5、BLAKE2、CRC32 など) に基づいて即座にフィンガープリントが返されます。また、秘密鍵 (HMAC) を使用してデータに署名し、最も重要なこととして、秘密漏洩攻撃を防ぐタイミングセーフな比較を使用して、受信した署名が一致するかどうかを安全に検証します。言語モデルはハッシュを実行できません。言語モデルは、正しい長さと文字セットの文字列を生成しますが、これは正しく見えますが、単に間違っているだけです。 Webhook 検証、冪等キー、キャッシュ フィンガープリント、幻覚ハッシュに依存する整合性チェックはすべて、静かに破られます。この API が解決策です。
任意の Web アドレスを取得し、その応答ヘッダー、ステータス コード、およびリダイレクト パスを返します。
指定した Web アドレスにリクエストを送信し、サーバーが応答した内容すべてをレポートします。ステータス コード、すべての応答ヘッダー (コンテンツ タイプ、キャッシュ ルール、Cookie、セキュリティ設定など)、リダイレクトされた回数、全体にかかった時間などです。 Web サイトのキャッシュ、セキュリティ、コンテンツ設定が期待どおりに設定されているかどうかを確認するのに便利です。
最大 10 個の URL のライブ ステータスを一度に確認し、リダイレクトを即座に検出します。
この API に最大 10 個の Web アドレスのリストを与えると、各アドレスにアクセスして通知します。

機能しているか、壊れているか、訪問者を別の場所にリダイレクトしているか。正確なステータス番号 (OK の場合は 200、見つからない場合は 404、サーバーエラーの場合は 500 など)、アドレスがリダイレクトされるかどうか、およびリダイレクト先が返されます。コードを書かずに壊れたリンクを見つけたり、Web サイトの移行を検証したり、簡単な SEO 監査を実行したりするのに最適です。
JSON の検証、修復、変換、クエリ、差分
JSON の検証、修復、フォーマット、クエリ、差分、変換、正規化を 1 つのエンドポイントで実行できます。これは、すべてのエージェント ワークフローで最も一般的な操作です。
すべての AI エージェントと開発者は、壊れた JSON や混乱を招く JSON に常に遭遇します。このツールは、最も一般的な JSON の問題をすべて 1 か所で処理します。つまり、JSON が有効かどうかを確認し (正確なエラーの場所を確認して)、タイプミスや書式設定の間違いがある JSON に近いものを修正し、整形または縮小し、パス式を使用して特定の値を取り出し、2 つの JSON ドキュメントを比較して変更内容を正確に確認し、YAML または CSV との間で変換し、ハッシュまたは署名用のバイト安定正規形式を生成し、スキーマと照合して検証し、サイズと構造の統計を取得します。同じ入力からは常に同じ出力が得られます。完全に予測可能であり、副作用はありません。
通常のテキストを leetspeak に変換するか、leetspeak をデコードして通常の読み取り可能なテキストに戻します。
テキストの一部を取得し、それを leetspeak に変換する (「hello」が「h3110」になるなど、文字を数字や記号に置き換える) か、leetspeak を通常の読み取り可能な単語に変換します。変換ルールは固定されており一貫性があるため、同じテキストは常に同じ結果を生成します。
住宅ローンとローンの償却計算ツール
固定金利ローンの正確な月々の支払額と完全な期間ごとの内訳を取得します。その結果は、AI モデルだけでは確実に計算できません。
この API に融資額、金利、返済期間を与えると、

正確な月々の支払い額と、毎月の利息がいくらになるか、残高がいくら減るか、そしてまだ借りている金額を示す完全なスケジュールを返します。 「月に追加で 300 ドル支払ったらどうなりますか?」と尋ねることもできます。または「24 か月目に 10,000 ドルを一括支払いしたらどうなるでしょうか?」そして、何ヶ月貯蓄し、どれくらいの利息を回避できるかを正確に教えてくれます。すべての数値は銀行の四捨五入とピン留めされた算術規則を使用して計算されるため、同じ入力を実行する 2 つの正しいシステムがセント単位に至るまで同一の結果を生成します。レート検索や市場データは必要ありません。レートを指定すると、API が計算を行います。
Tesseract OCR を使用して任意の画像からテキストを抽出します。
画像をパブリック Web アドレスまたは Base64 でエンコードされた画像データとして送信し、その中にあるプレーン テキストを取得します。このサービスは、人間がスキャンした文書、領収書、サイン、またはスクリーンショットを読み取るのと同じ方法で画像を読み取り、認識した単語と信頼スコアを提供します。現時点では英語のテキストのみがサポートされています。
ホストとポート範囲を送信し、開いているポートとそのポートでどのサービスが実行されているかの完全なリストを返します。
この API にホスト名または IP アドレスを指定すると、要求された範囲内の各ポートへの接続が試行されます。応答するすべてのポートについて、そのポートが開いているかブロックされているか、そこで実行されている可能性のあるサービス (Web サーバーや SSH など)、および検出されたソフトウェア バージョンがわかります。これにより、ネットワーク ツールをインストールしたり、面倒なコマンドライン出力を解析したり、ソケット ループを自分で作成したりする必要がなくなります。自分が所有しているホスト、またはスキャン権限を持つホストのみをスキャンします。このツールはそれをチェックしません。
テキスト、URL、または任意の文字列のリストを QR コード画像に瞬時に変換します。1 回の呼び出しで、多くのコードを実行できます。
テキストのリストを送信します — ウェブサイトのアドレス、電話番号、平文

rds、WiFi の詳細など、あらゆる情報を取得し、それぞれについてすぐに使用できる QR コード画像を取得します。すべての QR コードは、base64 でエンコードされた PNG として提供されており、Web ページに直接ドロップしたり、ディスクに保存したりできます。マーケティング キャンペーン、イベント チケット、製品ラベル、メニューなど、一度に多くの QR コードが必要なあらゆる状況に最適です。空のエントリは静かにスキップされます。各入力は 700 文字に制限されています。空ではないエントリを少なくとも 1 つ送信する必要があります。
JavaScript を多用するページを含む、完全にレンダリングされた Web ページのスクリーンショットをキャプチャします。
すべてのスクリプトと動的コンテンツを含む Web ページを実際のブラウザとまったく同じようにレンダリングし、その写真を撮ります。表示領域だけまたはスクロール可能なページ全体をキャプチャしたり、画面サイズを選択したり、遅い画像の読み込みが完了するまで待機したり、写真を撮影する前に Cookie バナーやポップアップを非表示にしたりすることができます。
Web ページを取得し、タイトル、メタ タグ、見出し、正規 URL など、主要な SEO フィールドをすべて即座に抽出します。
この API に Web アドレスを与えると、そのページにアクセスし、SEO 専門家が知っておくべきすべての情報 (ページ タイトル、メタ ディスクリプション、メタ キーワード、ロボットの指示、正規リンク、H1/H2/H3 見出し、ページ上のリンクの数、HTTP ステータス コード) を返します。セットアップは必要ありません — jus

[切り捨てられた]

## Original Extract

Actions for AI. Real-world tasks your agent can't do alone. Pay per use with x402 - no accounts. no logins. no keys.

Actions for AI. Real-world tasks your agent can't do alone.
Pay per use - no accounts. no logins. no keys.
Look up the network operator, country, and address block behind any IP address or ASN number.
Given an internet address (IP) or a network identification number (ASN), this tool returns who operates that piece of the internet: the network number, the owner's name, the country it's registered in, the regional registry that manages it, and the exact address range it belongs to. Useful for checking where traffic is coming from, enriching security alerts, or mapping out internet infrastructure.
Send a URL and origin, get back exactly which cross-origin permissions that server grants.
Sends a real request to any URL you provide, attaches an Origin header, and reads back all six standard cross-origin permission headers the server returns. Tells you whether cross-origin requests are enabled at all, whether your specific origin is permitted, and shows you every permission value exactly as the server sent it. Perfect for figuring out why a browser is blocking a request, confirming a freshly deployed service is configured correctly, or auditing whether a public API allows credentialed cross-origin calls.
US CPI Data & Inflation Report
Returns the latest, a specific month's, or a historical range of official US inflation (CPI) figures from BLS .
Gives you official United States inflation numbers from the government's Consumer Price Index BLS. Ask for the newest reading, a specific month in the past, or a whole range of months, and get back clear percentages for overall inflation and 'core' inflation (which ignores food and energy prices), plus a plain-English summary sentence.
Make a real HTTP request from the cloud and get back the status code, headers, and body instantly.
Send an HTTP request to any URL from our servers and receive the full response — including the status code, all response headers, and the body content. Supports GET, POST, PUT, PATCH, DELETE, and HEAD. Automatically follows up to 3 redirects. Useful for testing endpoints, verifying servers are responding correctly, debugging redirect chains, or fetching raw data without needing a local setup.
Check email security (DMARC) configuration for one or many domains in a single request.
Give this tool a list of domain names and it will instantly check each one's DMARC email security record. DMARC is a standard that tells email providers how to handle fake or spoofed emails pretending to come from your domain. For each domain you submit, you get back whether a valid record exists, what the policy is (none, quarantine, or reject), whether reporting addresses are set up, and the full raw record text. Perfect for auditing many domains at once without any setup.
Look up all 13 DNS record types plus full WHOIS registration data for up to 10 domains in one call.
Send up to 10 domain names and get back every DNS record type (A, AAAA, MX, CNAME, TXT, NS, PTR, SOA, SRV, CAA, NAPTR, DS, TLSA) plus the full WHOIS registration profile (registrar, dates, contacts, status, nameservers) for each one. URLs are accepted and automatically stripped down to the bare domain. Duplicate domains are quietly removed before processing. Results come back as one flat row per domain, ready to drop into a spreadsheet, database, or pipeline.
Give it a list of domains and get back registration date, expiration date, age, and time remaining — instantly.
Send a list of domain names and this tool tells you exactly how old each one is, when it was first registered, when it expires, and how much time is left before expiration. Great for spotting brand-new suspicious domains, researching competitors, or evaluating domains before buying or linking to them. Works with over 100 domain extensions worldwide.
Check whether 1–10 domain names have an active registration record using the official RDAP protocol.
Give this API up to 10 domain names and it will tell you whether each one has a registration record on file with the official registry. It uses RDAP — the modern, structured replacement for WHOIS — so there is no messy free-text parsing. Each domain comes back labeled 'registered', 'unregistered', or 'indeterminate' (when the registry does not support RDAP for that ending). Important: 'unregistered' means no record was found, which usually means the name is available — but reserved or premium names can also return no record. This API tells you registration status only; it does not tell you price, premium status, or whether a registrar will actually let you buy the name.
Resolve an ENS name to an Ethereum address, or an address back to its ENS name.
Takes an ENS name (like vitalik.eth) and returns the Ethereum address it points to. Also works in reverse: give it a 0x... address and it returns the primary ENS name set for that address, if one exists. The direction is figured out automatically from what you send in, so you only ever need to fill in one field. This is useful for AI agents or apps that need to turn a friendly wallet name into a real address before doing anything, or to show a friendly name instead of a long hex string.
Fetch Hacker News stories by category, or scan all stories/jobs/polls from a specific date, with optional nested comments.
This tool reads live information from Hacker News, a popular website where people share and discuss technology news. You can either ask for a current list of stories (like the Top, New, Best, Ask, Show, or Job lists) or ask for every story, job posting, and poll that was posted on one specific calendar day in the past. You can also choose to pull in the discussion comments underneath each story, including nested replies several levels deep.
Hashing, HMAC & Checksum Suite
Compute cryptographic hashes, HMAC signatures, and checksums — and verify webhook signatures with constant-time comparison.
Give this API any text or binary data and it instantly returns its fingerprint under whichever hash algorithms you choose — SHA-256, SHA-512, MD5, BLAKE2, CRC32, and more. It also signs data with a secret key (HMAC) and, most importantly, safely verifies whether an incoming signature matches — using a timing-safe comparison that prevents secret-leaking attacks. Language models cannot perform hashing: they produce a string of the right length and character set that looks correct but is simply wrong. Every webhook verification, idempotency key, cache fingerprint, or integrity check that relies on a hallucinated hash is silently broken. This API is the cure.
Fetches any web address and returns its response headers, status code, and redirect path.
Sends a request to the web address you provide and reports back everything the server said in response: the status code, all the response headers (like content type, caching rules, cookies, and security settings), how many times it got redirected, and how long the whole thing took. Handy for checking if a website's caching, security, or content settings are set up the way you expect.
Check the live status of up to 10 URLs at once and detect redirects instantly.
Give this API a list of up to 10 web addresses and it will visit each one to tell you whether it is working, broken, or redirecting visitors somewhere else. You get back the exact status number (like 200 for OK, 404 for not found, 500 for server error), whether the address redirects, and where it redirects to. Great for finding broken links, verifying website migrations, and running quick SEO audits without writing any code.
JSON Validate, Repair, Convert, Query & Diff
One endpoint to validate, repair, format, query, diff, convert, and canonicalize JSON — the most common operation in every agent workflow.
Every AI agent and developer runs into broken or confusing JSON constantly. This tool handles all the most common JSON headaches in one place: check if JSON is valid (with exact error location), fix near-JSON that has typos or formatting mistakes, pretty-print or minify, pull out specific values using a path expression, compare two JSON documents and see exactly what changed, convert to and from YAML or CSV, produce a byte-stable canonical form for hashing or signing, validate against a schema, and get size and structure statistics. Same input always gives the same output — fully predictable, no side effects.
Converts regular text into leetspeak or decodes leetspeak back into normal readable text.
Takes a piece of text and either converts it into leetspeak (replacing letters with numbers and symbols, like 'hello' becoming 'h3110') or converts leetspeak back into normal readable words. The conversion rules are fixed and consistent, so the same text always produces the same result.
Mortgage & Loan Amortization Calculator
Get the exact monthly payment and full period-by-period breakdown for any fixed-rate loan — results an AI model cannot reliably compute on its own.
Give this API a loan amount, an interest rate, and a repayment term, and it hands back the precise monthly payment plus a complete schedule showing — for every single month — how much goes to interest, how much reduces the balance, and what you still owe. You can also ask 'what if I pay an extra $300 a month?' or 'what if I make a $10,000 lump payment in month 24?' and it will tell you exactly how many months you save and how much interest you avoid. Every number is computed with banker's rounding and a pinned arithmetic rule so that two correct systems running the same inputs produce identical results, down to the cent. No rate lookups, no market data — you supply the rate, the API does the math.
Extract text from any image using Tesseract OCR.
Send a picture, either as a public web address or as base64-encoded image data, and get back the plain text found inside it. The service reads the picture the same way a person would read a scanned document, receipt, sign, or screenshot, and gives you the words it recognized along with a confidence score. Only English text is supported right now.
Send a host and port range, get back a clean list of which ports are open and what services are running on them.
Give this API a hostname or IP address and it will try connecting to each port in your requested range. For every port that answers, it tells you whether it is open or blocked, which service is likely running there (like a web server or SSH), and what software version was detected. This saves you from installing network tools, parsing messy command-line output, or writing socket loops yourself. Only scan hosts you own or have permission to scan — this tool does not check that for you.
Turn a list of text, URLs, or any strings into QR code images instantly — one call, many codes.
Send a list of texts — website addresses, phone numbers, plain words, WiFi details, anything — and get back a ready-to-use QR code image for each one. Every QR code comes as a base64-encoded PNG you can drop straight into a webpage or save to disk. Great for marketing campaigns, event tickets, product labels, menus, or any situation where you need many QR codes in one go. Empty entries are quietly skipped. Each input is limited to 700 characters. You must send at least one non-empty entry.
Capture a screenshot of a fully rendered webpage, including JavaScript-heavy pages.
Renders a webpage exactly as a real browser would, including all its scripts and dynamic content, then takes a picture of it. You can capture just the visible area or the whole scrollable page, choose the screen size, wait for slow-loading images to finish, and hide cookie banners or popups before the picture is taken.
Fetch a webpage and instantly extract all key SEO fields: title, meta tags, headings, canonical URL, and more.
Give this API a web address and it will visit that page, then hand back everything an SEO professional needs to know: the page title, meta description, meta keywords, robots instructions, canonical link, H1/H2/H3 headings, how many links are on the page, and the HTTP status code. No setup required — jus

[truncated]
