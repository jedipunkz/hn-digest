---
source: "https://github.com/PrithviSeran/hunch-mcp"
hn_url: "https://news.ycombinator.com/item?id=49102017"
title: "Show HN: Hunch – A local MCP that lets your LLM use your Mac in the background"
article_title: "GitHub - PrithviSeran/hunch-mcp: Drive your Mac focus-free over MCP: OS APIs, AppleScript, CDP, and Accessibility · GitHub"
author: "prithviseran"
captured_at: "2026-07-29T20:09:27Z"
capture_tool: "hn-digest"
hn_id: 49102017
score: 1
comments: 0
posted_at: "2026-07-29T19:39:47Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Hunch – A local MCP that lets your LLM use your Mac in the background

- HN: [49102017](https://news.ycombinator.com/item?id=49102017)
- Source: [github.com](https://github.com/PrithviSeran/hunch-mcp)
- Score: 1
- Comments: 0
- Posted: 2026-07-29T19:39:47Z

## Translation

タイトル: Show HN: Hunch – LLM がバックグラウンドで Mac を使用できるようにするローカル MCP
記事のタイトル: GitHub - PrithviSeran/hunch-mcp: MCP 経由で Mac をフォーカスフリーで駆動する: OS API、AppleScript、CDP、およびアクセシビリティ · GitHub
説明: MCP 経由で Mac をフォーカスフリーで駆動する: OS API、AppleScript、CDP、およびアクセシビリティ - PrithviSeran/hunch-mcp
HN テキスト: こんにちは、HN、タイトルにあるように、LLM がユーザーの集中力を奪うことなくバックグラウンドで Mac を駆動できるローカル MCP を構築しました。 Hunch を Peekaboo (OpenClaw のコンピューター使用) と Cua Drivers に対してベンチマークしたところ、54 個の MacOS ローカル タスクのうち、Hunch は 6.03 ドルの費用をかけながら 53 個を完了しました。 Peekaboo は 41 のタスクを完了して 31.03 ドルを使用し、Cua Driver は 31 のタスクを完了して 37.42 ドルを使用しました。これらのタスクと方法論の詳細については、添付したリポジトリに記載されています。バックグラウンドでのコンピューター使用ツールは非常に便利で、ユーザーと同時にラップトップを使用するパーソナル アシスタントのように機能すると思います。しかし、世にあるツールの多くはトークンを大量に消費し、時間がかかります (信頼性も低いです)。また、ほとんどはスクリーンショット ベースであり、仮想マシンを使用したアプリケーションに重点を置いているため、ユーザーと同時に、または同じラップトップ上で操作することはできません。これらが私が Hunch を作成した理由です。また、Hunch の構築中に直面した問題についていくつかの技術ブログを書いています。このブログは、Hunch の Web サイト (tryhunch.ca) にあります。また、Hunch SDK (hunch-sdk) を利用してメニュー バーに配置される Swift アプリにも取り組んでいます。これにより、Hunch を利用した LLM に即座にアクセスできるようになります。ぜひHunchを試してみていただければ幸いです。ぜひご意見やご感想をお聞かせください。 Hunch は Homebrew または Pip を通じてインストールでき、セットアップには 1 行しかかかりません (詳細な手順はリポジトリと Web サイトにあります)

）。再度、感謝します！

記事本文:
GitHub - PrithviSeran/hunch-mcp: MCP 経由で Mac をフォーカスフリーで駆動する: OS API、AppleScript、CDP、およびアクセシビリティ · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を非常に真剣に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
プリスヴィセラン
/
予感-MCP
公共
通知
通知を変更するにはサインインする必要があります

アイケーション設定
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
57 コミット 57 コミットの例 例 サイト site src/ hunch src/ hunch testing testing .gitignore .gitignore AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md mcp.json mcp.json pyproject.toml pyproject.toml server.json server.json uv.lock uv.lock すべてのファイルを表示 リポジトリ ファイルのナビゲーション
任意の LLM で Mac を駆動します。フォーカスフリー、バックグラウンド、MCP 経由。
Hunch は、LLM エージェントに Mac 上での操作を提供する MCP サーバーです。インストールされているアプリや、
画面を占有することなく、ログインしたセッションやファイルを保存できます。で働き続けながら、
フォアグラウンドでは、エージェントはバックグラウンド アプリの UI を読み取ったり、そのボタンをクリックしたり、メールや音楽を操作したりできます。
AppleScript、Web フォームに入力、またはファイルを移動します。ブラウザだけでなく、実際のネイティブ アプリでも動作します。
最新の LLM で最適に動作します。 Hunch は、MCP サーバーの指示として詳細なプレイブックを同梱しています。
有能なツールを使用するモデル (Claude Sonnet/Opus クラス以上) はこれによく従います。小型モデルでは、
より不格好なパスを選択します (ツリーの読み取りとクリックの代わりにスクリーンショットとキーストローク)。
Hunch は常に最も直接的なレイヤーを好みます。より速く、より信頼性が高く、(最後を除く)
決して画面に触れないでください。
ゲートされた最後の手段 (スクリーンショット + 座標のクリック/キーストローク) は、次のようなアプリに対して存在します。
アクセシビリティ ツリーは本当に空です。それは集中力を奪うので、最初にあなたに尋ねます。
仕組み (サーバーもクラウドもなし)
「MCP サーバー」という言葉は、これがいかにローカルであるかをよく表しています。 Hunch Serve は、ユーザーが実行できるプレーンな Python プロセスです。
MCP ホスト (Claude Desktop、Cursor など) が子プロセスとして生成され、他のプロセスと通信します。
stdin/stdout 上の JSON-RPC (MCP の stdio トランスポート)。 HTTP エンドポイントもポートもありません
Hunch はリッスンしますが、デーモンもテレメーターもありません

やあ。ホストが辞めると、Hunch はいなくなります。
これらのツールは、プロセス内で直接 macOS API を呼び出すものです: pyobjc を介したアクセシビリティ フレームワーク、
AppleScript の場合は osascript、ファイル/クリップボードの場合は OS API、Web レイヤーの場合はローカル
WebSocket を Chrome の DevTools ポート 127.0.0.1 に接続します。唯一触れられるのは、
ネットワークは Chrome 自体であり、通常のブラウジングを実行します。モデルが見ているものはどんなツールであっても
ホスト経由で戻ります。マシンからは何も残りません。
Hunch は、ログインしている実際の Mac 上の他の macOS コンピュータ使用エージェントに対して測定されます。
mac-agent-bench — 同じ頭脳、異なる手 :
タスクごとの claude -p は同一ですが、MCP アダプターのみが異なります。プログラムでタスクの成功をスコアリングします
チェッカーと妨害: エージェントが動作中にカーソルとフォアグラウンドをどれだけハイジャックするか。
5 つの複雑な複数ステップのタスク (n=3):
完璧な信頼性、約 10 倍のコスト、約 5 ～ 10 倍の速度、そして基本的に画面に触れることはありません ( 0
カーソルが移動し、15 回のトライアルにわたって 1 つのフォーカス スイッチが行われます)。完全な方法論とタスクごとのテーブルは、
ベンチマークリポジトリ 。
macOS 13+。 PyPI から (ディストリビューションは hanch-sdk 、インポートと CLI はhunch です):
pipx install hanch-sdk # または: pip install hanch-sdk
pip install 'hunch-sdk[agent]' # + オプションの LLM エージェント ループ (両方のバックエンド;
# [api] または [subscription] は 1 つだけ選択します)
または Homebrew 経由 — Python 環境を管理しない場合に最適です。それは孤立したものをバンドルします
安定したパスにある Python。これにより、macOS の権限付与が最も予測可能になります。
brew install プリスヴィセラン/予感/予感
次に、一度:
予感セットアップ # macOS 権限付与を実行する
予感ドクター # すべてのレイヤーを検証します。フラグが付いているものはすべて修正します
予感接続 claude-desktop # または: クロードコード、カーソル
MCP ホストを再起動し、「ハンチを使用して…」するように依頼します。
macOS の信頼は、サーバーを実行するアプリに接続されます

r , meaning your MCP host (Claude Desktop,
Cursor, your terminal), not "hunch" itself. hunch setup walks you through it:
アクセシビリティ (必須): Hunch がアプリの UI を読み取って、フォーカスフリーでクリックできるようにします。 MCP に付与します
「システム設定」→「プライバシーとセキュリティ」→「アクセシビリティ」でホストアプリを実行します。
自動化 (アプリごと、自動): Hunch がアプリを初めてスクリプト化すると、macOS は 1 回限りのスクリプトを表示します。
「制御を許可する」プロンプト。
画面録画 (オプション): スクリーンショット/ビジョン フォールバックのみ。
hunch doctor reports what's granted.注: そのアクセシビリティ行には、実行したターミナルが反映されています
それはからです。 the server inherits the host's grant.
Hunch もインポート可能なライブラリです。MCP ツールと同じフォーカスフリーのプリミティブで、駆動されます。
独自の Python (cron ジョブ、テスト ハーネス、独自のエージェント ループ) から決定的に、LLM なし
必須です。 The distribution is hunch-sdk ;インポートは予感です:
ハンチからインポート ハンチ
mac = Hunch () # あなたのマシン、ログインしているアプリ
print ( mac . snapshot ( "Mail" )) # アクセシビリティ ツリー、フォーカスフリー
マック。 act ([{ "action" : "click" , "ref" : "e12" }])
マック。ウェブ。 open ( url = "https://github.com" ) # CDP を介した実際の永続的な Chrome プロファイル
print ( mac . web . snapshot ())
マック。ファイル 。ゴミ箱 ([ "~/Downloads/old.zip" ]) # 元に戻せる削除、Finder なし
マック。 applescript ( 'アプリケーション「音楽」に再生するように指示します' )
コンストラクター ノブ: app (最初のスナップショット ターゲット)、confirm="dialog"|"off" (以下を参照)、
check_permissions (アクセシビリティを事前にチェック)、同時 (決して触れないでください)
foreground/cursor/keyboard), cdp_port .
権限: ライブラリの使用の場合、スクリプトを実行するもの (ターミナルまたは IDE) がそれです。
アクセシビリティが必要です (MCP サーバーは代わりにホスト アプリの許可を使用します)。コンストラクターはチェックします
そして、命令で AccessibilityNotGranted を発生させます。スクリーンショット()追加

必要があります
画面録画。
安全ゲートのデフォルトは ON : 同じワンクリックの「先に進む」ダイアログと ~/.hunch/config.json
ゲートを MCP サーバーとして使用します。 Hunch(confirm="off") はそのインスタンスのみを自動承認します。
無人スクリプト。 auto_approve_all と同じ注意事項があります。
エラー: メソッドはステータス文字列を返します ( REFUSED を確認してください)。 SDK はのみを発生させます
ApprovalDenied (ユーザーがダイアログを拒否)、AccessibilityNotGranted、WebNotOpen
( .web.open() の前の .web )、StaleRef (再スナップショット)、および CDP 時の HunchError
ブラウザを開けません (web.restart() は古いインスタンスを回復します)。
資格情報: mac.web.fill_login(service) / fill_secret(service, ref) タイプのキーチェーン
値をページに直接入力し、決して返しません。ドメインバインディングが強制されます。
共存: SDK と MCP サーバーは CDP ポート (9337) と永続的な Hunch を共有します。
ブラウザプロファイル - 最初に開いたものが再利用されますが、web.restart() / web.login() は強制終了します
ポートを保持しているものは何でも。
実行可能なスクリプトは、examples/ にあります。
インスタンス SDK は決定論的なプリミティブを提供します。エージェント ループは LLM を運転席に置きます。
あなたが平易な英語でタスクを渡すと、クロードは同じプリミティブで Mac を操作します。
Scrapybara の act() ですが、ログインしたアプリがインストールされたマシン上にあります。追加オプションです
(基本インストールにモデル SDK が含まれないようにします):
pip インストール「hunch-sdk[エージェント]」
python -c ' 予感をインポートします。 huch.login() ' # クロードのサブスクリプション サインイン (ブラウザー OAuth) — いいえ
# APIキー。すでに Claude Code にサインインしていますか?スキップしてください。
ハンチからインポート ハンチ
マック = 勘 ()
結果 = マック 。エージェント 。 run (「サラの最新のメールに返信しますが、送信はしません」)
print ( result . text ) # クロードの最終的な要約
print (結果を回転、結果を使用)
サインインする
認証は明示的で目に見える表面です。何も黙って清掃されることはありません

。ループを実行するには 2 つの方法があります。
コードに適したバックエンドを、構築時または呼び出しごとに選択します。
mac = Hunch (agent_backend = "subscription" ) # このインスタンスのエージェント = サブスクリプション
マック。エージェント 。 run ( task ) # ...呼び出しごとのセレモニーなし
マック。エージェント 。 run ( other_task , backend = "api" ) # 呼び出しごとのオーバーライドが依然として優先されます
デフォルトの backend="auto" は、文書化された固定順序 (API キー →
CLAUDE_CODE_OAUTH_TOKEN env → Claude Code サインイン → huch.login(token=...) token) — 検査
それはいつでも、hunch.auth.status() または Hunch Doctor を使用して実行できます。資格情報がまったくない場合、run()
両方の修正を指定する HunchError が発生します。決して推測しません。
認証はパブリック Python API です (ログイン CLI はありません。MCP サーバーはログイン CLI を必要としません。
アプリは独自のオンボーディングを所有します):
輸入の予感
st = 予感。認証。 status () # AuthStatus: .source / .email / .plan / .subscription_ready
そうでない場合は st 。サブスクリプション準備完了:
予感。ログイン () # ブラウザ OAuth;または huch.login(token="sk-ant-oat-...") ヘッドレス
予感。 logout() #login() が保存したものだけを削除します
on_event(kind, data) コールバックで動作するのを見てください — kind はテキストの 1 つです (Claude の
推論)、ツール ( {name, input} )、tool_result (プレビュー)、done (最終テキスト)、error 。
継続: フォローアップ run() 呼び出しにより会話が維持されます (クロードはまだどのメールを知っているかを知っています)
サラのものです); mac.agent.reset() は新しいタスクを開始します。
レイヤーを自由に混合: mac.snapshot(...) / mac.clipboard.get() を決定的に呼び出します。
mac.agent.run(...) — 実マシンではクラウド サンドボックスでは実行できないこと。
ノブ: run(task, model=None, max_turns=40,effort=None, on_event=None, system_suffix="", backend=None) — model=None は API バックエンドの claude-opus-4-8 を意味し、
それ以外の場合は、サブスクリプションのデフォルト モデル。 AgentResult には text 、turn 、s が含まれます

トップ理由 、
使用、中止されました。
安全性 : インスタンスのゲート構成がループを制御します。デフォルトのconfirm="dialog"では、
本当の「どうぞ？」集中力を奪う、または危険なステップの前にダイアログを表示します。
ただし、ゲートされたアクションにより、ダイアログのタイムアウトの間無人実行が停止する可能性があります。 cron ジョブの場合
Hunch(confirm="off") を使用し、リスクを受け入れます。クロードはまだあなたに尋ねます (notify_user 経由)
メッセージの送信など、元に戻せないまたは外向きのアクションの前に。拒否されたゲートが再び戻ってくる
REFUSED の結果としてモデルを作成するため、ループはクラッシュせずに適応します。
コスト: サブスクリプションのバックエンドで、実行はクロード プランの使用制限に基づいて行われます - いいえ
トークンごとの請求。 API バックエンドでは、各ターンでツリーを多く含む履歴が再送信されます。プロンプトキャッシュは
デフォルトでオンになっているため、キャッシュされた入力は最大 10 倍安くなります。ただし、長時間の自律実行は依然として加算されます。
max_turns はどちらの方法でも上限を設定します。
その他のモデル: エージェント ループはクロードのみですが、インスタンス SDK プリミティブはプロバイダーに依存しません。
mac.snapshot() / mac.act() を独自の OpenAI/Gemini/etc に接続します。ツールとしてのエージェント ループ。
SDK は開発者第一です。単一の均一なセマンティクス、すべてがインスタンス所有であり、アンビエントなものは何もありません
オプトインしない限り。上記の MCP サーバー自体は、その上に構築された最初のアプリにすぎません。
「個人的な」動作 (~/.hunch/config

[切り捨てられた]

## Original Extract

Drive your Mac focus-free over MCP: OS APIs, AppleScript, CDP, and Accessibility - PrithviSeran/hunch-mcp

Hey HN, like the title says, I built a local MCP that lets your LLM drive your Mac in the background, without stealing your focus. I benchmarked Hunch against Peekaboo (OpenClaw's computer use) and Cua Drivers, and out of the 54 MacOS local tasks, Hunch completed 53 while costing $6.03. Peekaboo completed 41 tasks and used $31.03, and Cua Driver completed 31 tasks and used $37.42. More details on these tasks and methodologies are in the repo I have attached. I think background computer use tools are very useful, and can act like personal assistants that use your laptop as the same time as you. But a lot of the tools out there are very token intensive and takes a lot of time (also unreliable). Most are also screenshot based and more focused on applications with virtual machines, so cannot operate at the same time as you or on the same laptop. These were the reasons I created Hunch. I have also written some technical blogs about the problems I faced while building Hunch, which can be found on Hunch's website (tryhunch.ca). I am also working on a Swift app that sits on the menu bar powered by the Hunch SDK (hunch-sdk), so that you have instant access to an LLM powered by Hunch. It would be really appreciated if you guys can give Hunch a try! I would love to hear your thoughts and feedback. Hunch can be installed through Homebrew or Pip, and takes only one line to set up (more detailed instructions are in the repo and website). Thanks again!

GitHub - PrithviSeran/hunch-mcp: Drive your Mac focus-free over MCP: OS APIs, AppleScript, CDP, and Accessibility · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
PrithviSeran
/
hunch-mcp
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
57 Commits 57 Commits examples examples site site src/ hunch src/ hunch tests tests .gitignore .gitignore AGENTS.md AGENTS.md CLAUDE.md CLAUDE.md LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md mcp.json mcp.json pyproject.toml pyproject.toml server.json server.json uv.lock uv.lock View all files Repository files navigation
Drive your Mac with any LLM: focus-free, in the background, over MCP .
Hunch is an MCP server that gives an LLM agent hands on your Mac: your installed apps, your
logged-in sessions, your files, without taking over your screen. While you keep working in the
foreground, an agent can read a background app's UI, click its buttons, drive Mail or Music by
AppleScript, fill a web form, or move files. It works on real native apps, not just a browser.
Works best with modern LLMs. Hunch ships a detailed playbook as MCP server instructions;
capable tool-using models (Claude Sonnet/Opus-class and up) follow it well. Smaller models may
pick clumsier paths (screenshots and keystrokes instead of tree reads and clicks).
Hunch always prefers the most direct layer. It's faster, more reliable, and (except the last)
never touches your screen:
A gated last resort ( screenshot + coordinate clicks/keystrokes) exists for apps whose
accessibility tree is truly empty. It steals focus, so it asks you first.
How it works (no server, no cloud)
"MCP server" undersells how local this is. hunch serve is a plain Python process that your
MCP host (Claude Desktop, Cursor, …) spawns as a child process and talks to over
JSON-RPC on stdin/stdout (MCP's stdio transport). There is no HTTP endpoint, no port
Hunch listens on, no daemon, and no telemetry. When your host quits, Hunch is gone.
The tools are direct macOS API calls in-process: the Accessibility framework via pyobjc,
osascript for AppleScript, OS APIs for files/clipboard, and, for the web layer, a local
WebSocket to Chrome's DevTools port on 127.0.0.1 . The only thing that ever touches the
network is Chrome itself, doing ordinary browsing. What the model sees is whatever the tools
return through your host; nothing else leaves the machine.
Hunch is measured against other macOS computer-use agents on a real, logged-in Mac in
mac-agent-bench — same brain, different hands :
identical claude -p per task, only the MCP adapter differs. It scores task success with programmatic
checkers and disturbance: how much the agent hijacks your cursor and foreground while it works.
Across 5 complex multi-step tasks (n=3):
Perfect reliability, ~10x cheaper, ~5–10x faster, and it essentially never touches your screen ( 0
cursor moves, 1 focus switch across 15 trials). Full methodology and per-task tables are in the
benchmark repo .
macOS 13+. From PyPI (the distribution is hunch-sdk ; the import and CLI are hunch ):
pipx install hunch-sdk # or: pip install hunch-sdk
pip install 'hunch-sdk[agent]' # + the optional LLM agent loop (both backends;
# [api] or [subscription] picks just one)
Or via Homebrew — best if you don't manage Python environments; it bundles an isolated
Python at a stable path, which makes the macOS permission grants the most predictable:
brew install prithviseran/hunch/hunch
Then, one time:
hunch setup # walk the macOS permission grants
hunch doctor # verify every layer; fix anything it flags
hunch connect claude-desktop # or: claude-code, cursor
Restart your MCP host and ask it to "use hunch to …" .
macOS trust attaches to the app that runs the server , meaning your MCP host (Claude Desktop,
Cursor, your terminal), not "hunch" itself. hunch setup walks you through it:
Accessibility (required): lets Hunch read app UIs and click focus-free. Grant it to your MCP
host app in System Settings → Privacy & Security → Accessibility.
Automation (per-app, automatic): the first time Hunch scripts an app, macOS shows a one-time
"allow control" prompt.
Screen Recording (optional): only for the screenshot/vision fallback.
hunch doctor reports what's granted. Note: its Accessibility line reflects the terminal you ran
it from; the server inherits the host's grant.
Hunch is also an importable library — the same focus-free primitives as the MCP tools, driven
deterministically from your own Python (a cron job, a test harness, your own agent loop), no LLM
required. The distribution is hunch-sdk ; the import is hunch :
from hunch import Hunch
mac = Hunch () # your machine, your logged-in apps
print ( mac . snapshot ( "Mail" )) # accessibility tree, focus-free
mac . act ([{ "action" : "click" , "ref" : "e12" }])
mac . web . open ( url = "https://github.com" ) # real persistent Chrome profile over CDP
print ( mac . web . snapshot ())
mac . files . trash ([ "~/Downloads/old.zip" ]) # reversible delete, no Finder
mac . applescript ( 'tell application "Music" to play' )
Constructor knobs: app (initial snapshot target), confirm="dialog"|"off" (see below),
check_permissions (Accessibility check up front), simultaneous (never touch the
foreground/cursor/keyboard), cdp_port .
Permissions : for library use it's whatever runs your script — your terminal or IDE — that
needs Accessibility (the MCP server instead uses the host app's grant). The constructor checks
and raises AccessibilityNotGranted with instructions. screenshot() additionally needs
Screen Recording.
Safety gates default ON : the same one-click "Go ahead" dialogs and ~/.hunch/config.json
gates as the MCP server. Hunch(confirm="off") auto-approves for that instance only — for
unattended scripts, with the same caveats as auto_approve_all .
Errors : methods return status strings (check for REFUSED ); the SDK raises only
ApprovalDenied (user declined a dialog), AccessibilityNotGranted , WebNotOpen
( .web before .web.open() ), StaleRef (re-snapshot), and HunchError when a CDP
browser can't be opened ( web.restart() recovers a stale instance).
Credentials : mac.web.fill_login(service) / fill_secret(service, ref) type Keychain
values straight into the page and never return them; domain binding is enforced.
Coexistence : the SDK and the MCP server share the CDP port (9337) and the persistent Hunch
browser profile — whichever opened it first is reused, but web.restart() / web.login() kill
whatever holds the port.
Runnable scripts live in examples/ .
The instance SDK gives you deterministic primitives. The agent loop puts an LLM in the driver's seat:
you hand it a task in plain English and Claude drives the Mac through those same primitives —
Scrapybara's act() , but on your machine with your logged-in apps. It's an optional extra
(keeps the base install free of the model SDKs):
pip install ' hunch-sdk[agent] '
python -c ' import hunch; hunch.login() ' # Claude subscription sign-in (browser OAuth) — no
# API key. Already signed into Claude Code? Skip it.
from hunch import Hunch
mac = Hunch ()
result = mac . agent . run ( "reply to Sarah's latest email, but don't send it" )
print ( result . text ) # Claude's final summary
print ( result . turns , result . usage )
Signing in
Auth is an explicit, visible surface — nothing is scavenged silently. Two ways to run the loop:
You choose the backend where it suits your code — at construction, or per call:
mac = Hunch ( agent_backend = "subscription" ) # this instance's agent = subscription
mac . agent . run ( task ) # ...no per-call ceremony
mac . agent . run ( other_task , backend = "api" ) # per-call override still wins
The default backend="auto" picks by credentials, in a fixed documented order (API key →
CLAUDE_CODE_OAUTH_TOKEN env → Claude Code sign-in → hunch.login(token=...) token) — inspect
it any time with hunch.auth.status() or hunch doctor . With no credentials at all, run()
raises a HunchError naming both fixes — it never guesses.
Auth is public Python API (there is no login CLI — the MCP server never needs one, and your
app owns its own onboarding):
import hunch
st = hunch . auth . status () # AuthStatus: .source / .email / .plan / .subscription_ready
if not st . subscription_ready :
hunch . login () # browser OAuth; or hunch.login(token="sk-ant-oat-...") headless
hunch . logout () # removes what login() stored, and only that
Watch it work with an on_event(kind, data) callback — kind is one of text (Claude's
reasoning), tool ( {name, input} ), tool_result (preview), done (final text), error .
Continuation : follow-up run() calls keep the conversation (Claude still knows which email
is Sarah's); mac.agent.reset() starts a fresh task.
Mix layers freely : call mac.snapshot(...) / mac.clipboard.get() deterministically around
mac.agent.run(...) — the thing a cloud sandbox can't do on your real machine.
Knobs : run(task, model=None, max_turns=40, effort=None, on_event=None, system_suffix="", backend=None) — model=None means claude-opus-4-8 on the api backend and
your subscription's default model otherwise. AgentResult has text , turns , stop_reason ,
usage , aborted .
Safety : the instance's gate config governs the loop. The default confirm="dialog" pops a
real "Go ahead?" dialog before any focus-stealing or risky step — good when you're at the
machine, but a gated action can stall an unattended run for the dialog's timeout. For cron jobs
use Hunch(confirm="off") and accept the risk; Claude still asks you (via notify_user )
before irreversible or outward actions like sending a message. A declined gate comes back to the
model as a REFUSED result, so the loop adapts instead of crashing.
Cost : on the subscription backend, runs draw on your Claude plan's usage limits — no
per-token bill. On the api backend each turn resends the tree-heavy history; prompt caching is
on by default, so cached input is ~10× cheaper — but long autonomous runs still add up.
max_turns caps it either way.
Other models: the agent loop is Claude-only, but the instance-SDK primitives are provider-agnostic —
wire mac.snapshot() / mac.act() into your own OpenAI/Gemini/etc. agent loop as tools.
The SDK is developer-first: one uniform semantics, everything instance-owned, nothing ambient
unless you opt in. The MCP server above is itself just the first app built on it — its
"personal" behavior (the ~/.hunch/config

[truncated]
