---
source: "https://github.com/gowtham-sai-yadav/entangle"
hn_url: "https://news.ycombinator.com/item?id=49168465"
title: "Show HN: Entangle, hand a live AI coding session to anyone with three words"
article_title: "GitHub - gowtham-sai-yadav/entangle: Hand a live Claude Code, Codex, or opencode session to anyone with three spoken words - encrypted, no account. Or carry your whole Claude Code history to a new laptop, every path rewritten. · GitHub"
author: "gowthamsaiyadav"
captured_at: "2026-08-04T13:51:39Z"
capture_tool: "hn-digest"
hn_id: 49168465
score: 4
comments: 0
posted_at: "2026-08-04T13:12:22Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Entangle, hand a live AI coding session to anyone with three words

- HN: [49168465](https://news.ycombinator.com/item?id=49168465)
- Source: [github.com](https://github.com/gowtham-sai-yadav/entangle)
- Score: 4
- Comments: 0
- Posted: 2026-08-04T13:12:22Z

## Translation

タイトル: Show HN: Entangle、3 つの単語を持っている人にライブ AI コーディング セッションを提供します
記事のタイトル: GitHub - gowtham-sai-yadav/entangle: ライブのクロード コード、コーデックス、またはオープンコード セッションを、暗号化され、アカウントなしで 3 つの音声を持っている人に渡します。または、クロード コードの履歴全体を新しいラップトップに持ち込んで、すべてのパスを書き換えることもできます。 · GitHub
説明: ライブのクロード コード、コーデックス、またはオープンコード セッションを、暗号化され、アカウントなしで 3 つの音声を持っている人に渡します。または、クロード コードの履歴全体を新しいラップトップに持ち込んで、すべてのパスを書き換えることもできます。 - gowtham-sai-yadav/entangle

記事本文:
GitHub - gowtham-sai-yadav/entangle: ライブ クロード コード、コーデックス、またはオープンコード セッションを、暗号化され、アカウントなしで 3 つの音声を持っている人に渡します。または、クロード コードの履歴全体を新しいラップトップに持ち込んで、すべてのパスを書き換えることもできます。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン
外観設定
プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索またはジャンプ...
コード、リポジトリ、ユーザー、問題、プル リクエストを検索します...
クリア
検索構文のヒント
フィードバックを提供する
-->
私たちはフィードバックをすべて読み、ご意見を真摯に受け止めます。
保存された検索を使用して結果をより迅速にフィルタリングします
-->
名前
クエリ
利用可能なすべての修飾子を確認するには、ドキュメントを参照してください。
外観設定
フォーカスをリセットする
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します

。
アラートを閉じる
{{ メッセージ }}
ゴーサム・サイ・ヤダブ
/
絡まる
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
122 コミット 122 コミット .github .github docs docs 内部 内部 vscode vscode .gitignore .gitignore .goreleaser.yaml .goreleaser.yaml COTRIBUTING.md CONTRIBUTING.md DESIGN.md DESIGN.md ライセンス ライセンス README.md README.md SECURITY.md SECURITY.md go.mod go.mod go.sum go.sum install.ps1 install.ps1 install.sh install.sh main.go main.go すべてのファイルを表示 リポジトリ ファイルのナビゲーション
誰にでもライブ コーディング セッションを提供します。
クロード コード、コーデックス、オープンコード。
暗号化、アカウントなし、3 つの話し言葉。
コーディング セッションは、それを作成したマシン上でのみ存在します。だから、あなたが望む日は、
チームメイトに会話を渡したり、新しいラップトップに移ったりすると、行き詰まってしまい、コピーすることはできません。
すべてのセッションが正確なパスに固定されているため、手動でフォルダーを作成することはできません。
プロジェクトは着々と進んでおり、その道は他の場所では異なります。
entangle を使用すると 1 つのコマンドになります。短いメッセージで誰かにライブ セッションを直接送信する
入力したコードを保存したり、履歴全体をパックして新しいマシンに持ち込んだりできます。
パスが書き換えられたため、中断したところから会話が開始されます。
これは、1 つのコーディング エージェントだけでなく、複数のコーディング エージェントにわたって機能します。クロード コード セッションはクロード コードに進み、
Codex から Codex への Codex セッション、opencode への opencode セッション - 一緒にリストされ、共有された
同じように。 (あるツールのセッションを別のツール内で開くことは別の問題であり、
1つはまだ解決しています。）
2 つのスコープ、どちらが必要かが重要です。 1 つのセッションを共有すると、3 つのツールすべてがカバーされます。
マシン全体をパッキングすると、現時点ではクロード コードのみがカバーされます - Codex または opencode を移動します
代わりにエンタングル共有を使用したセッション。エンタングル エクスポート 電話

むしろ何を残したのですか？
新しいラップトップでそれを見つけるよりも。
建設により私有地となっております。何もアップロードされておらず、アカウントもサーバーもありません。
真ん中。直接転送はエンドツーエンドで暗号化され、おそらく秘密は事前にスクラブされます。
何もかもマシンから離れるため、ログイン情報がまったく転送されることはありません。唯一の例外は、
新しいリリースを匿名で 1 日 1 回チェックします。これにより、あなたに関する情報は何も送信されません。
オフにすることができます。
名前を変更しました。このプロジェクトは、単一のベンダーを超えてしまうまで、クロード テレポートでした。
古いコマンドは引き続き機能し、自身を更新します。切り替える方法を教えてくれます。
カール -fsSL https://gowthamsai.in/install.sh |しー
自作の場合:
brew install gowtham-sai-yadav/tap/entangle
Windows (PowerShell):
irm https://gowthamsai.in/install.ps1 |アイエックス
それぞれが適切な事前構築バイナリをフェッチし、そのチェックサムを検証して、それを PATH に置きます。エンタングルバージョンで確認します。
github.com/gowtham-sai-yadav/entangle@latest をインストールしてください
これは $(go env GOPATH)/bin (通常は ~/go/bin ) にインストールされます。その後コマンドが見つからない場合は、そのフォルダーはまだ PATH 上にありません: echo 'export PATH="$HOME/go/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc 。
直接ダウンロード: 最新リリース (Apple Silicon の場合は ...-darwin-arm64、Intel Mac の場合は -darwin-amd64、-linux-amd64 または -windows-amd64.exe ) からマシン用のファイルを取得し、macOS/Linux では chmod +x を実行して PATH に移動します。
git clone https://github.com/gowtham-sai-yadav/entangle
cd entangle && go build -o entangle 。
ここから始めましょう: インタラクティブなコックピット
最も簡単な方法は、単に実行することです:
ターミナルを接続すると、全画面のコックピットが開き、キーを押すだけでセッションが検索可能なリストに表示されます。暗号化されたコードを介してチームメイトに送信し、ファイルに共有し、受信して経験値を取得します

フラグを 1 つも覚えずに、バックアップの ort やインポート、あるいはその場での更新を行うことができます。プレス ？各キーの機能について説明します。 entangle tui を実行して明示的に開きます。パイプまたはスクリプトのどちらでも、同じバイナリは通常の CLI のように動作します。
自分でコマンドを入力したいですか?以下のすべては単独でも動作します。
チームメイトとセッションを共有する
ある会話をすべてのコンテキストをそのままにして他の人に渡し、他の人が会話を進めることができるようにします。まずセッションを見つけます。
エンタングルセッション
次に、2 つの方法のいずれかで送信します。
直接、コードによって (移動するファイルはなく、どこにもアップロードされません):
エンタングル送信 < id >
出力されるショートコードを読み上げます。彼らは entangle を実行し、プロジェクトのコピーから <code> を受け取ります。転送はエンドツーエンドで暗号化されるため、サーバーはそれを読み取ることができません。
エンタングルがない場合は、インストールについて話し合う必要はありません。 send キーとコックピットの c キーの両方を押すと、チャットに貼り付ける準備ができているメッセージが表示されます。インストール行と受信コマンドがすでに含まれています。
プロジェクトのコピー内からこれらを実行します
(すでにエンタングルがある場合は、最初の行をスキップしてください):
カール -fsSL https://gowthamsai.in/install.sh |しー
絡み受け6-開拓村
Windows PowerShell: irm https://gowthamsai.in/install.ps1 |アイエックス
受信したセッションは、セッションが実行されているディレクトリに接続され、会話内のすべてのファイル パスが一致するように書き換えられます。そのため、プロジェクトのコピー内から実行するように指示されています。間違ったフォルダーからはインポートされますが、そのフォルダー内のパスはそこに存在しないファイルを指しています。
これが彼らが見ているものです。これは、実行しようとしている書き換えを実行前に表示し、ユーザーが調べなくてもセッションが実際に再開できることを確認します。
パスの再マッピング:
/Users/alex/work/dashboard -> /Users/sam/projects/dashboard
/ユーザー/sam -> /ユーザー/sam
/ユーザー/アレックス -

> /ユーザー/サム
プロジェクト:
古い新しいセッション
+ /Users/alex/work/dashboard /Users/sam/projects/dashboard 1
完了しました。 1 個のファイルが書き込まれ、0 個がスキップされました (すでに存在します)。 0 個のプロジェクト エントリが .claude.json にマージされました。
確認: 1/1 の移行済みプロジェクトは再開可能であるように見えます。
重要: ログイン情報は転送されませんでした。資格情報は決して転送されません。
Claude Code を開いて一度ログインし、プロジェクト内で `claude --resume` を実行します。
ファイルとして、現在存在しない場合:
エンタングルシェア < id >
後で entangle import <file> を使用してインポートします。
いずれにせよ、機密情報 (キー、トークン、パスワード) の可能性があるものは、何かがマシンから流出する前にスクラブされ、ログイン情報は決して含まれません。 --last は最新のセッションを選択し、 --with-context にはプロジェクトのメモリ ファイルも含まれます。
すべての作業内容を新しいコンピュータに移動するには、2 つのコマンドを 1 つずつ実行します。
古いマシンでは、すべてをファイルにパックします。
エンタングルエクスポート
作成したファイルを、AirDrop、USB スティック、 scp などの任意の方法で新しいマシンにコピーします。
新しいマシンに Claude Code をインストールし、一度サインインしてから復元します。
entangle import entangle-backup- * .tgz
すべてのプロジェクトとその着地点が表示され、確認を求められ、パスが修正され、セッションを再開できるかどうかが確認されます。何も書かずにプレビューするには --dry-run を追加し、場所が間違っていると推測した場合は --map /old/path=/new/path を追加します。
絡み合うGUI
ブラウザでも同じ復元を行います。バンドルを指定し、すべてのプロジェクトを from → to の行として表示し、不要な項目のチェックを外します。確認するまで何も書き込まれません。バンドル パスを追加してレビューに直接スキップするか、--port N を追加してポートを固定します。 127.0.0.1 でのみ機能し、ネットワークは必要ありません。
次に、プロジェクトを開いて次の作業を続けます。
cd ~ /path/to/your/project
クロード --再開
ログイン情報は意図的に転送されません。資格情報は各マッハにロックされます

新しいものに一度サインインするだけです。
VS Code を使い続けたいですか? entangle 拡張機能は、コマンド パレット、ステータス バー ボタン、サイドバーに同じアクションを追加します。コードによるセッションの送信、受信、ファイルへの共有、セッションの参照などです。これは内部で CLI を駆動し、最初の使用時に CLI のインストールを提案します。
これはまだ VS Code マーケットプレイスに掲載されていません ( #41 )。それまでは、このリポジトリから実行します。VS Code で vscode/ を開き、 F5 を押します。上記の CLI はサポートされている方法です。
エンタングルアップデート
新しいリリースをチェックし、バイナリを所定の場所に交換します。 Homebrew を使用してインストールした場合は、代わりに brew upgrade entangle を実行してください。 Keg 内のファイルを置き換えることは機能しますが、brew は依然として古いバージョンがインストールされていると認識したままになり、後の brew アップグレードによって古いビルドが上書きに戻される可能性があります。 entangle はこれを実行する前に警告します。
リリースが存在する場合も通知されるため、古いコピーは偶然ではなく選択となります。
注: entangle 0.7.0 が利用可能です (あなたは 0.6.0 を持っています)。更新方法: エンタングル更新
コックピットのヘッダーにも同じことが表示されます。その背後にあるルール:
決してお待たせすることはありません。チェックはコマンドの終了後に実行され、検出された内容のみが記録されます。次回何かを実行するときにメモが表示されます。セッション リストがネットワークの往復に遅れることはありません。
要求は多くても 1 日に 1 回行われ、失敗後も 1 日は沈黙を保つため、オフライン マシンはすべてのコマンドを再試行しません。
標準出力に書き込むことはありません。このメモは、端末が接続されている場合にのみ、stderr に保存されます。セッションのもつれ --json は機械可読なままであり、スクリプトや CI がそれを認識することはありません。
失敗しても何も言われません。要求されていないネットワーク エラーはありません。
ソース (開発バージョン) からのビルドではオフになります。そうでないと、遅れていることが永久に通知されることになります。
これは私

■ entangle が独自にネットワークに問い合わせるのは、匿名で認証されていない GET api.github.com/repos/.../releases/latest です。アカウントも識別子もなく、セッションについても何もありません。メッセージとリクエストの両方をオフにするには:
エクスポート ENTANGLE_NO_UPDATE_CHECK=1
何が動くのか、何が動かないのか
移動: クロード コードのセッション、プロジェクト メモリ、設定、プロンプト履歴、 ~/.claude.json の移植可能な部分はすべて新しいマシン用に再パスされます。 Codex セッションと opencode セッションは、エンタングル シェアを使用して一度に 1 つずつ移動します。
決して移動しないでください: あなたのログイン。資格情報は機械的にロックされており、意図的に残されています。
スキップ: キャッシュ、テレメトリ、および自動的に再構築されるその他の使い捨てファイル。
インポートは宛先で実行されるため、OS が検出され、Windows のドライブ文字やバックスラッシュを含むすべてが変換されます。 Linux および macOS のパスは /home/you または /Users/you のようになります。 Windows は C:\Users\you を使用します。特別なことをする必要はなく、任意の方向に機能するだけです。
既存のファイルを上書きすることはありません。マージして、何がスキップされたかを示します。 --overwrite は、置換された各ファイルを最初にバックアップします。
--dryrun は、何かが書き込まれる前に何が起こるかを正確に示します。
ファイルベースのフローは完全にオフラインです。シャリン

[切り捨てられた]

## Original Extract

Hand a live Claude Code, Codex, or opencode session to anyone with three spoken words - encrypted, no account. Or carry your whole Claude Code history to a new laptop, every path rewritten. - gowtham-sai-yadav/entangle

GitHub - gowtham-sai-yadav/entangle: Hand a live Claude Code, Codex, or opencode session to anyone with three spoken words - encrypted, no account. Or carry your whole Claude Code history to a new laptop, every path rewritten. · GitHub
Skip to content
Navigation Menu
Sign in
Appearance settings
Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search or jump to...
Search code, repositories, users, issues, pull requests...
Clear
Search syntax tips
Provide feedback
-->
We read every piece of feedback, and take your input very seriously.
Use saved searches to filter your results more quickly
-->
Name
Query
To see all available qualifiers, see our documentation .
Appearance settings
Resetting focus
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
gowtham-sai-yadav
/
entangle
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
122 Commits 122 Commits .github .github docs docs internal internal vscode vscode .gitignore .gitignore .goreleaser.yaml .goreleaser.yaml CONTRIBUTING.md CONTRIBUTING.md DESIGN.md DESIGN.md LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md go.mod go.mod go.sum go.sum install.ps1 install.ps1 install.sh install.sh main.go main.go View all files Repository files navigation
Hand a live coding session to anyone.
Claude Code, Codex, and opencode.
Encrypted, no account, three spoken words.
Your coding sessions live only on the machine that made them. So the day you want to
hand a conversation to a teammate, or move to a new laptop, you are stuck: copying the
folder by hand does not work, because every session is pinned to the exact path your
project sat at, and that path is different everywhere else.
entangle makes it one command. Send a live session straight to someone with a short
code they type in, or pack your whole history and carry it to a new machine, with every
path rewritten so the conversation opens where it left off.
It works across coding agents, not just one. A Claude Code session goes to Claude Code,
a Codex session to Codex, an opencode session to opencode - listed together, shared the
same way. (Opening one tool's session inside another is a different problem, and not
one this solves yet.)
Two scopes, and it matters which you need. Sharing one session covers all three tools.
Packing a whole machine covers Claude Code only so far - move a Codex or opencode
session with entangle share instead. entangle export tells you what it left out rather
than letting you discover it on the new laptop.
It is private by construction. Nothing is uploaded, there is no account and no server in
the middle; a direct transfer is end-to-end encrypted, likely secrets are scrubbed before
anything leaves your machine, and your login never travels at all. The one exception is an
anonymous once-a-day check for a new release, which sends nothing about you and
can be turned off .
Renamed. This project was claude-teleport until it outgrew a single vendor.
The old command still works and still updates itself; it will tell you how to switch.
curl -fsSL https://gowthamsai.in/install.sh | sh
With Homebrew:
brew install gowtham-sai-yadav/tap/entangle
Windows (PowerShell):
irm https: // gowthamsai.in / install.ps1 | iex
Each one fetches the right prebuilt binary, verifies its checksum, and puts it on your PATH. Confirm with entangle version .
go install github.com/gowtham-sai-yadav/entangle@latest
This installs into $(go env GOPATH)/bin (usually ~/go/bin ). If the command is not found afterward, that folder is not on your PATH yet: echo 'export PATH="$HOME/go/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc .
Direct download: grab the file for your machine from the latest release ( ...-darwin-arm64 for Apple Silicon, -darwin-amd64 for Intel Macs, -linux-amd64 , or -windows-amd64.exe ), then on macOS/Linux chmod +x it and move it onto your PATH.
git clone https://github.com/gowtham-sai-yadav/entangle
cd entangle && go build -o entangle .
Start here: the interactive cockpit
The quickest way in is to just run:
With a terminal attached, that opens a full-screen cockpit: your sessions in a searchable list, with every action a keypress away. Send one to a teammate over an encrypted code, share it to a file, receive one, export or import a backup, or update in place, all without remembering a single flag. Press ? for what each key does. Run entangle tui to open it explicitly; piped or scripted, the same binary behaves like a normal CLI.
Prefer typing the commands yourself? Everything below works on its own too.
Share a session with a teammate
Hand one conversation to someone else, with all its context intact, so they can carry it forward. Find the session first:
entangle sessions
Then send it one of two ways.
Straight across, by code (no file to move, nothing uploaded anywhere):
entangle send < id >
You read out the short code it prints; they run entangle receive <code> from their copy of the project. The transfer is end-to-end encrypted, so no server can read it.
If they do not have entangle, you do not have to talk them through installing it. Both send and the cockpit's c key give you a message that is ready to paste into chat, with the install line and the receive command already in it:
Run these from inside your copy of the project
(skip the first line if you already have entangle):
curl -fsSL https://gowthamsai.in/install.sh | sh
entangle receive 6-pioneer-village
Windows PowerShell: irm https://gowthamsai.in/install.ps1 | iex
Received sessions attach to the directory they are run in, and every file path in the conversation is rewritten to match. That is why it says to run it from inside their copy of the project. From the wrong folder it still imports, but the paths in it point at files that are not there.
This is what they see. It shows the rewrite it is about to do before it does it, then confirms the session can actually be resumed rather than leaving you to find out:
Path remapping:
/Users/alex/work/dashboard -> /Users/sam/projects/dashboard
/Users/sam -> /Users/sam
/Users/alex -> /Users/sam
Projects:
OLD NEW SESSIONS
+ /Users/alex/work/dashboard /Users/sam/projects/dashboard 1
Done. 1 file(s) written, 0 skipped (already present); 0 project entries merged into .claude.json.
Verify: 1/1 migrated project(s) look resume-ready.
IMPORTANT: your login was NOT transferred - credentials never are.
Open Claude Code, log in once, then run `claude --resume` inside a project.
As a file , if they are not around right now:
entangle share < id >
They import it later with entangle import <file> .
Either way, likely secrets (keys, tokens, passwords) are scrubbed before anything leaves your machine, and your login is never included. --last picks your most recent session, and --with-context also includes the project's memory files.
Moving all of your work to a new computer is two commands, one on each.
On the old machine , pack everything into a file:
entangle export
Copy the file it creates to the new machine any way you like: AirDrop, a USB stick, scp .
On the new machine , install Claude Code and sign in once, then restore:
entangle import entangle-backup- * .tgz
It shows you every project and where it will land, asks you to confirm, fixes the paths, and checks your sessions are resume-ready. Add --dry-run to preview without writing anything, and --map /old/path=/new/path if it guesses a location wrong.
entangle gui
does the same restore in a browser: point it at a bundle, see every project as a from → to row, untick what you do not want, and nothing is written until you confirm. Add a bundle path to skip straight to the review, or --port N to pin the port. It serves on 127.0.0.1 only and needs no network.
Then open a project and carry on:
cd ~ /path/to/your/project
claude --resume
Your login does not transfer, on purpose. Credentials are locked to each machine, so just sign in once on the new one.
Prefer to stay in VS Code? The entangle extension adds the same actions to the Command Palette, a status-bar button, and a sidebar: send a session by code, receive one, share to a file, or browse your sessions. It drives the CLI under the hood and offers to install it for you on first use.
It is not on the VS Code marketplace yet ( #41 ). Until it is, run it from this repo: open vscode/ in VS Code and press F5 . The CLI above is the supported way in.
entangle update
checks for a newer release and swaps the binary in place. If you installed with Homebrew, run brew upgrade entangle instead. Replacing the file inside a keg works, but leaves brew still believing the old version is installed, and a later brew upgrade can then put an older build back over the top. entangle warns you before doing this.
You will also be told when a release exists, so a stale copy is a choice rather than an accident:
note: entangle 0.7.0 is available (you have 0.6.0). Update with: entangle update
The cockpit shows the same thing in its header. The rules behind it:
It never makes you wait. The check runs after your command has finished and only writes down what it found; the note appears the next time you run something. Your session list is never behind a network round trip.
It asks at most once a day , and stays quiet for a day after a failure too, so an offline machine does not retry on every command.
It never writes to stdout. The note goes to stderr, and only when a terminal is attached. entangle sessions --json stays machine-readable, and scripts and CI never see it.
It says nothing when it fails. No network errors you did not ask for.
It is off for builds from source ( dev versions), which would otherwise be told forever that they are behind.
This is the one thing entangle asks the network about on its own: an anonymous, unauthenticated GET api.github.com/repos/.../releases/latest . No account, no identifier, and nothing about your sessions. To turn off both the message and the request:
export ENTANGLE_NO_UPDATE_CHECK=1
What moves, and what doesn't
Moves: your Claude Code sessions, project memory, settings, prompt history, and the portable parts of ~/.claude.json , all re-pathed for the new machine. Codex and opencode sessions move one at a time with entangle share .
Never moves: your login. Credentials are machine-locked and deliberately left out.
Skipped: caches, telemetry, and other throwaway files that rebuild themselves.
Import runs on the destination, so it detects the OS and translates everything, including Windows drive letters and backslashes. Linux and macOS paths look like /home/you or /Users/you ; Windows uses C:\Users\you . You do not have to do anything special, it just works in any direction.
It never overwrites an existing file. It merges and tells you what it skipped; --overwrite backs up each replaced file first.
--dry-run shows exactly what will happen before anything is written.
The file-based flow is fully offline. Sharin

[truncated]
