---
source: "https://github.com/h4ckf0r0day/obscura"
hn_url: "https://news.ycombinator.com/item?id=49218432"
title: "Obscura: Headless browser engine. For web scraping and AI agent automation"
article_title: "GitHub - h4ckf0r0day/obscura: The headless browser for AI agents and web scraping · GitHub"
author: "thunderbong"
captured_at: "2026-08-08T02:50:28Z"
capture_tool: "hn-digest"
hn_id: 49218432
score: 1
comments: 0
posted_at: "2026-08-08T02:34:00Z"
tags:
  - hacker-news
  - translated
---

# Obscura: Headless browser engine. For web scraping and AI agent automation

- HN: [49218432](https://news.ycombinator.com/item?id=49218432)
- Source: [github.com](https://github.com/h4ckf0r0day/obscura)
- Score: 1
- Comments: 0
- Posted: 2026-08-08T02:34:00Z

## Translation

タイトル: Obscura: ヘッドレス ブラウザ エンジン。 WebスクレイピングとAIエージェント自動化用
記事タイトル: GitHub - h4ckf0r0day/obscura: AI エージェントと Web スクレイピング用のヘッドレス ブラウザ · GitHub
説明: AI エージェントと Web スクレイピング用のヘッドレス ブラウザ - h4ckf0r0day/obscura

記事本文:
GitHub - h4ckf0r0day/obscura: AI エージェントと Web スクレイピング用のヘッドレス ブラウザ · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン 外観設定 プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
/ と入力して検索します。 サインイン サインアップ 外観設定
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
h4ckf0r0day
/
オブスキュラ
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
814 コミット 814 コミット .cargo .cargo .github .github アセット アセット クレート クレート ドキュメント ドキュメント render-repros render-repros スキル/ オブスキュラ スキル/ オブスキュラ ベンダー ベンダー .gitattributes .gitattributes .gitbook.yaml .g

itbook.yaml .gitignore .gitignore AGENTS.md AGENTS.md CONTRIBUTING.md CONTRIBUTING.md Cargo.lock Cargo.lock Cargo.toml Cargo.toml Dockerfile Dockerfile LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md build.log build.log すべてのファイルを表示 リポジトリ ファイルのナビゲーション
AI エージェントと Web スクレイピング用のオープンソースのヘッドレス ブラウザー。
軽量でステルス性が高く、Rust が組み込まれています。
ネイティブレンダリングはここにあります。クロムは必要ありません。 🎉
Obscura を使用して、スクリーンショットをキャプチャし、ライブ ページをスクリーンキャストし、PDF を直接エクスポートします。
Obscura は、Rust で書かれたヘッドレス ブラウザ エンジンで、Web スクレイピングと AI エージェントの自動化のために構築されています。 V8 経由で実際の JavaScript を実行し、Chrome DevTools プロトコルをサポートし、Puppeteer と Playwright を備えたヘッドレス Chrome のドロップイン代替として機能します。
なぜヘッドレス Chrome ではなく Obscura を使うのか?
デスクトップの閲覧ではなく、大規模な自動化を目的として設計されています。
🎉 10,000 個のスターとその先
私たちは、管理されたインフラストラクチャ、住宅用プロキシ、専用サポートを備えたホスト型バージョンの Obscura Cloud に取り組んでいます。自分で操作せずにエンジンが欲しい人向け。
オープンソース エンジンは Apache-2.0 のままで、フル機能を備えています。機能ゲーティングは一切ありません。
順番待ちリストに登録する →
📅 デモを予約する →
Obscura は、開発の独立性を維持するスポンサーによってサポートされています。
スポンサーになりたいですか? hello@obscura.sh に電子メールを送信します。
Releases から最新のバイナリを取得します。
# Linux x86_64
カール -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-x86_64-linux.tar.gz
tar xzf obscura-x86_64-linux.tar.gz
./obscura fetch https://example.com --eval " document.title "
# Linux ARM64 (aarch64)
カール -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-aarch64-linux.tar.gz
tar xzf obscura-aarch64-linux.tar.gz
# Arch Linux (AUR)
やったーS obscura-browser
#ニクスOS
ニックス-

env -iA nixpkgs.obscura
# macOS アップルシリコン
カール -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-aarch64-macos.tar.gz
tar xzf obscura-aarch64-macos.tar.gz
# macOS インテル
カール -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-x86_64-macos.tar.gz
tar xzf obscura-x86_64-macos.tar.gz
# Windows
リリース ページから「 .zip 」をダウンロードし、手動で解凍します。
Chrome も Node.js も依存関係もありません。リリース アーカイブには両方が含まれます
obscura と obscura-worker ;それらを同じディレクトリに保存してください
パラレルスクレイピングコマンド。
Linux リリースはターゲット Ubuntu 22.04 をビルドするため、ダウンロードされたバイナリは残ります
glibc 2.35+ を搭載した一般的な LTS サーバーで使用できます。
docker run -d --name obscura -p 127.0.0.1:9222:9222 h4ckf0r0day/obscura
Docker Hub 上のイメージ。 distroless/cc でのマルチステージ ビルド、シェルなし、パッケージ マネージャーなし、最大 57 MB 圧縮。
git クローン https://github.com/h4ckf0r0day/obscura.git
CDオブスキュラ
# レンダリング
カーゴビルド --release -p obscura-cli --bins --features render
# レンダリングとステルス
カーゴ ビルド --release -p obscura-cli --bins --features レンダリング、ステルス
# レンダリングなし
カーゴビルド --release -p obscura-cli --bins --no-default-features
# レンダリングなし、ステルスあり
カーゴ ビルド --release -p obscura-cli --bins --no-default-features --features ステルス
Rust 1.75+ (rustup.rs) が必要です。最初のビルドには約 5 分かかります (V8 はソースからコンパイルされ、後でキャッシュされます)。
ステルス ビルドは BoringSSL もコンパイルし、バインディングを生成するため、
CMake、Clang、libclang/LLVM 開発ライブラリ。 Ubuntu/Debian の場合:
sudo apt-get install build-essential cmake clang libclang-dev llvm-dev
レンダリング ビルドでは Rustle を使用します。レンダリングとステルス ビルドでは次を使用します。
wrq/BoringSSL のため、上記の追加のビルド ツールが必要です。
# ページタイトルを取得する
あいまいな

ra fetch https://example.com --eval " document.title "
# すべてのリンクを抽出する
obscura fetch https://example.com --dump リンク
# JavaScript をレンダリングして HTML をダンプする
オブスクラフェッチ https://news.ycombinator.com --dump html
# ダンプまたは評価出力をファイルに書き込む
obscura fetch https://example.com --dump text --output page.txt
# 生の応答本文をそのままストリーミングします (バイナリセーフ、JS/DOM レイヤーをバイパスします)。
# 画像、JSON、JS、CSS、または HTML 以外のリソースにこれを使用します。
obscura fetch https://picsum.photos/200/300 --dump オリジナル > photo.jpg
# ページが取得するすべてのサブリソース URL をリストします (NDJSON、アセットごとに 1 レコード)
obscura fetch https://example.com --dump アセット
# HTTP または SOCKS プロキシ経由で取得する
obscura --proxy sock5://127.0.0.1:1080 fetch https://example.com --dump text
# 動的コンテンツを待つ
obscura fetch https://example.com --wait-until networkidle0
# 遅いページまたは壊れたページのバインドされたナビゲーション時間
オブスクラフェッチ https://example.com --timeout 10
# 決済されたページをPNGとしてキャプチャします
obscura fetch https://example.com --screenshot page.png
# スクリーンショットフラグには短い形式もあります
obscura fetch https://example.com -s page.png
レンダリング
公式リリース アーカイブと Docker イメージには、レンダリング エンジンが含まれています。
CSS レイアウトとペイント、ビューポートと全ページのスクリーンショットを提供します。
スクロール対応の固定ジオメトリとスティッキー ジオメトリ、アクティビティ駆動型の CDP スクリーンキャスト、
Chromium を起動せずにラスター PDF をエクスポートします。
ページを待ちます。 setViewport({幅:1440、高さ:1000});
ページを待ちます。 goto ( 'https://example.com' , { waitUntil : 'load' } ) ;
ページを待ちます。スクリーンショット ({ パス : 'page.png' 、fullPage : true } ) ;
ページを待ちます。 pdf ( { パス : 'page.pdf' 、形式 : 'A4' 、printBackground : true } ) ;
現在の実装は、ブロック、インライン、フレックス、グリッド、テーブル、フロート、
ポジショニング、

オーバーフロー、変換、テキスト、画像、SVG、キャンバス、背景、境界線、
そしてアニメーションパス。進化する独立系企業であり続けます
エンジン: ロングテール CSS、一部の Web API、メディア再生、コンポジター エフェクト、
プラットフォームのフォントのラスタライズは Chromium とは異なる場合があります。既存の
人形遣い ,
劇作家、そして
MCP ガイドでは、キャプチャ API と制限について説明します。
オブスキュラ サーブ --ポート 9222
# ステルスモードあり (検出防止 + トラッカーブロック)
obscuraserve --port 9222 --stealth
並行して削る
オブスキュラスクレイプ url1 url2 url3 ... \
--同時実行 25 \
--eval " document.querySelector('h1').textContent " \
--json の形式
# スクリプトに適した出力のために標準エラー出力でのスクレイピングの進行を抑制します
オブスキュラスクレイプ https://example.com --quiet --format json
# スクレイピングワーカーはグローバルプロキシを継承します
obscura --proxy http://127.0.0.1:8080 スクレイピング https://example.com https://news.ycombinator.com
人形遣い / 劇作家
npm インストール puppeteer-core
'puppeteer-core' から puppeteer をインポートします。
const browser = await puppeteer 。接続 ( {
ブラウザWSEndpoint : 'ws://127.0.0.1:9222/devtools/browser' ,
} ) ;
const page = ブラウザを待ちます。 newPage() ;
ページを待ちます。 goto ( 'https://news.ycombinator.com' ) ;
const stories = ページを待ちます。評価 ( ( ) =>
配列 。 from ( document . querySelectorAll ( '.titleline > a' ) )
。 map ( a => ( { title : a . textContent , url : a . href } ) )
) ;
コンソール。ログ (ストーリー) ;
ブラウザを待ちます。切断 ( ) ;
劇作家
npm インストール playwright-core
'playwright-core' から { chrome } をインポートします。
const ブラウザ = クロムを待ちます。 connectOverCDP ( {
エンドポイントURL : 'ws://127.0.0.1:9222' 、
} ) ;
const page = ブラウザを待ちます。 newContext() 。 then ( ctx => ctx .newPage() ) ;
ページを待ちます。 goto ( 'https://en.wikipedia.org/wiki/Web_scraping' ) ;
コンソール。ログ (ページ待ち。タイトル () ) ;
ブラウザを待ちます。クロ

せ();
フォーム送信とログイン
ページを待ちます。 goto ( 'https://quotes.toscrape.com/login' ) ;
ページを待ちます。評価 ( ( ) => {
書類をquerySelector ( '#username' ) 。値 = '管理者' ;
書類をquerySelector ( '#password' ) 。値 = '管理者' ;
書類をquerySelector ( 'form' ) 。送信() ;
} ) ;
// Obscura は POST を処理し、302 リダイレクトに従い、Cookie を維持します
ベンチマーク
完全なベンチマーク スイート (WPT 準拠、障害物コース、現実世界のコーパス、および vs-Chrome の速度) は別のリポジトリにあります: https://github.com/h4ckf0r0day/obscura-benchmark
--features render,stealth を使用してビルドし、実行時にステルスを有効にします。
グローバル --ステルス フラグ。ステルス ビルドには完全なレンダリングが含まれています
エンジン。ステルスを有効にしても、スクリーンショット、スクリーンキャスト、PDF、CDP、または
MCP機能。
セッションごとの指紋のランダム化 (GPU、画面、キャンバス、オーディオ、バッテリー)
現実的な navigator.userAgentData (Chrome 145、高エントロピー値)
ディスパッチされたイベントの場合は、event.isTrusted = true
非表示の内部プロパティ (Object.keys(window) セーフ)
ネイティブ関数のマスキング ( Function.prototype.toString() → [ネイティブ コード] )
navigator.webdriver = 未定義 (実際の Chrome と一致します)
分析、広告、テレメトリー、フィンガープリンティング スクリプトをブロックします。
トラッカーの読み込みを完全に阻止します
--stealth で自動的に有効になります
Obscura は、Puppeteer/Playwright の互換性のために Chrome DevTools プロトコルを実装しています。
1 つの巨大な Network.getResponseBody BLOB を持たずに大きなリソースをダウンロードするには、 Fetch.takeResponseBodyAsStream を呼び出してから、 IO.read / IO.close を使用してチャンクで読み取ります。キャッシュ制限 ( OBSCURA_NETWORK_BODY_BUFFER_BYTES 、デフォルトは 2 MiB) を超える応答本文は保持されないため、大規模なダウンロードをストリーミングする場合は制限を上げてください。
Obscura は V8 を直接埋め込みます。 --v8-flags を使用して raw を渡します

flags から V8 まで、Chromium の --js-flags および Node のコマンドライン フラグと同じ構文。最も一般的な使用法は、JS の多いページで JavaScript ヒープのメモリ不足を修正するためにヒープ キャップを増やすことです。
obscura --v8-flags " --max-old-space-size=4096 " fetch < url >
大量の SPA (スクリプト実行バジェット)
Obscura はページのスクリプト実行フェーズに上限を設けているため、1 つの遅いページやハングしたページによってワーカーが停止することはありません。デフォルトの予算は 30 秒です。より早く終了したページはすぐに返されるため、上限は実行を続けるページにのみ影響します。低速ネットワーク上の非常に重い React/Vue/Angular SPA は、データ リクエストを発行するまでに起動にさらに時間がかかる場合があります。 OBSCURA_SCRIPT_DEADLINE_MS (ミリ秒) で予算を増やし、CDP クライアントで一致するナビゲーション タイムアウトと組み合わせます。
OBSCURA_SCRIPT_DEADLINE_MS=60000 オブスキュラ サーブ --ポート 9222
オブスキュラサーブ
単一のページを取得してレンダリングします。
ワーカー プロセスと並行して複数の URL をスクレイピングします。
Obscura は、AI エージェント (Claude Desktop、Cursor など) にブラウザ自動化ツールを公開する MCP サーバーを同梱しています。
stdio (デフォルト) — サブプロセスを起動する Claude デスクトップおよび MCP クライアントの場合:
オブスキュラMCP
HTTP — ネットワーク経由で接続するクライアントの場合:
obscura mcp --http --port 8080
# エンドポイント: http://127.0.0.1:8080/mcp

[切り捨てられた]

## Original Extract

The headless browser for AI agents and web scraping - h4ckf0r0day/obscura

GitHub - h4ckf0r0day/obscura: The headless browser for AI agents and web scraping · GitHub
Skip to content
Navigation Menu
Sign in Appearance settings Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Type / to search Sign in Sign up Appearance settings
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
h4ckf0r0day
/
obscura
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
814 Commits 814 Commits .cargo .cargo .github .github assets assets crates crates docs docs render-repros render-repros skills/ obscura skills/ obscura vendor vendor .gitattributes .gitattributes .gitbook.yaml .gitbook.yaml .gitignore .gitignore AGENTS.md AGENTS.md CONTRIBUTING.md CONTRIBUTING.md Cargo.lock Cargo.lock Cargo.toml Cargo.toml Dockerfile Dockerfile LICENSE LICENSE README.md README.md SECURITY.md SECURITY.md build.log build.log View all files Repository files navigation
The open-source headless browser for AI agents and web scraping.
Lightweight, stealthy, and built in Rust.
Native rendering is here. No Chromium required. 🎉
Capture screenshots, screencast live pages, and export PDFs directly with Obscura.
Obscura is a headless browser engine written in Rust, built for web scraping and AI agent automation. It runs real JavaScript via V8, supports the Chrome DevTools Protocol, and acts as a drop-in replacement for headless Chrome with Puppeteer and Playwright.
Why Obscura over headless Chrome?
Designed for automation at scale, not desktop browsing.
🎉 10,000 stars and what's next
We are working on Obscura Cloud the hosted version, with managed infrastructure, residential proxies, and dedicated support. For people who want the engine without operating it themselves.
The open-source engine stays Apache-2.0, fully featured. No feature gating, ever.
Get on the waitlist →
📅 Book a demo →
Obscura is supported by sponsors who help keep development independent.
Want to sponsor? Email hello@obscura.sh .
Grab the latest binary from Releases :
# Linux x86_64
curl -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-x86_64-linux.tar.gz
tar xzf obscura-x86_64-linux.tar.gz
./obscura fetch https://example.com --eval " document.title "
# Linux ARM64 (aarch64)
curl -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-aarch64-linux.tar.gz
tar xzf obscura-aarch64-linux.tar.gz
# Arch Linux (AUR)
yay -S obscura-browser
# NixOS
nix-env -iA nixpkgs.obscura
# macOS Apple Silicon
curl -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-aarch64-macos.tar.gz
tar xzf obscura-aarch64-macos.tar.gz
# macOS Intel
curl -LO https://github.com/h4ckf0r0day/obscura/releases/latest/download/obscura-x86_64-macos.tar.gz
tar xzf obscura-x86_64-macos.tar.gz
# Windows
Download the ` .zip ` from the releases page and extract it manually.
No Chrome, no Node.js, no dependencies. Release archives include both
obscura and obscura-worker ; keep them in the same directory for the
parallel scrape command.
Linux release builds target Ubuntu 22.04 so the downloaded binary remains
usable on common LTS servers with glibc 2.35+.
docker run -d --name obscura -p 127.0.0.1:9222:9222 h4ckf0r0day/obscura
Image on Docker Hub . Multi-stage build on distroless/cc , no shell, no package manager, ~57 MB compressed.
git clone https://github.com/h4ckf0r0day/obscura.git
cd obscura
# Rendering
cargo build --release -p obscura-cli --bins --features render
# Rendering and stealth
cargo build --release -p obscura-cli --bins --features render,stealth
# No rendering
cargo build --release -p obscura-cli --bins --no-default-features
# No rendering, with stealth
cargo build --release -p obscura-cli --bins --no-default-features --features stealth
Requires Rust 1.75+ ( rustup.rs ). First build takes ~5 min (V8 compiles from source, cached after).
The stealth build also compiles BoringSSL and generates bindings, so it needs
CMake, Clang, and the libclang/LLVM development libraries. On Ubuntu/Debian:
sudo apt-get install build-essential cmake clang libclang-dev llvm-dev
The rendering build uses rustls. The rendering-and-stealth build uses
wreq/BoringSSL and therefore needs the additional build tools above.
# Get the page title
obscura fetch https://example.com --eval " document.title "
# Extract all links
obscura fetch https://example.com --dump links
# Render JavaScript and dump HTML
obscura fetch https://news.ycombinator.com --dump html
# Write dump or eval output to a file
obscura fetch https://example.com --dump text --output page.txt
# Stream the raw response body verbatim (binary-safe; bypasses the JS/DOM layer).
# Use this for images, JSON, JS, CSS, or any non-HTML resource.
obscura fetch https://picsum.photos/200/300 --dump original > photo.jpg
# List every sub-resource URL the page would fetch (NDJSON; one record per asset)
obscura fetch https://example.com --dump assets
# Fetch through an HTTP or SOCKS proxy
obscura --proxy socks5://127.0.0.1:1080 fetch https://example.com --dump text
# Wait for dynamic content
obscura fetch https://example.com --wait-until networkidle0
# Bound navigation time for slow or broken pages
obscura fetch https://example.com --timeout 10
# Capture the settled page as PNG
obscura fetch https://example.com --screenshot page.png
# The screenshot flag also has a short form
obscura fetch https://example.com -s page.png
Rendering
Official release archives and the Docker image include the rendering engine.
It provides CSS layout and paint, viewport and full-page screenshots,
scroll-aware fixed and sticky geometry, activity-driven CDP screencasting, and
raster PDF export without starting Chromium.
await page . setViewport ( { width : 1440 , height : 1000 } ) ;
await page . goto ( 'https://example.com' , { waitUntil : 'load' } ) ;
await page . screenshot ( { path : 'page.png' , fullPage : true } ) ;
await page . pdf ( { path : 'page.pdf' , format : 'A4' , printBackground : true } ) ;
The current implementation covers block, inline, flex, grid, table, float,
positioning, overflow, transform, text, image, SVG, canvas, background, border,
and animation paths. It remains an evolving independent
engine: long-tail CSS, some Web APIs, media playback, compositor effects, and
platform font rasterization may differ from Chromium. The existing
Puppeteer ,
Playwright , and
MCP guides cover their capture APIs and limits.
obscura serve --port 9222
# With stealth mode (anti-detection + tracker blocking)
obscura serve --port 9222 --stealth
Scrape in parallel
obscura scrape url1 url2 url3 ... \
--concurrency 25 \
--eval " document.querySelector('h1').textContent " \
--format json
# Suppress scrape progress on stderr for script-friendly output
obscura scrape https://example.com --quiet --format json
# Scrape workers inherit the global proxy
obscura --proxy http://127.0.0.1:8080 scrape https://example.com https://news.ycombinator.com
Puppeteer / Playwright
npm install puppeteer-core
import puppeteer from 'puppeteer-core' ;
const browser = await puppeteer . connect ( {
browserWSEndpoint : 'ws://127.0.0.1:9222/devtools/browser' ,
} ) ;
const page = await browser . newPage ( ) ;
await page . goto ( 'https://news.ycombinator.com' ) ;
const stories = await page . evaluate ( ( ) =>
Array . from ( document . querySelectorAll ( '.titleline > a' ) )
. map ( a => ( { title : a . textContent , url : a . href } ) )
) ;
console . log ( stories ) ;
await browser . disconnect ( ) ;
Playwright
npm install playwright-core
import { chromium } from 'playwright-core' ;
const browser = await chromium . connectOverCDP ( {
endpointURL : 'ws://127.0.0.1:9222' ,
} ) ;
const page = await browser . newContext ( ) . then ( ctx => ctx . newPage ( ) ) ;
await page . goto ( 'https://en.wikipedia.org/wiki/Web_scraping' ) ;
console . log ( await page . title ( ) ) ;
await browser . close ( ) ;
Form submission & login
await page . goto ( 'https://quotes.toscrape.com/login' ) ;
await page . evaluate ( ( ) => {
document . querySelector ( '#username' ) . value = 'admin' ;
document . querySelector ( '#password' ) . value = 'admin' ;
document . querySelector ( 'form' ) . submit ( ) ;
} ) ;
// Obscura handles the POST, follows the 302 redirect, maintains cookies
Benchmarks
The full benchmark suite (WPT conformance, obstacle course, real-world corpus, and vs-Chrome speed) lives in a separate repo: https://github.com/h4ckf0r0day/obscura-benchmark
Build with --features render,stealth , then enable stealth at runtime with the
global --stealth flag. The stealth build includes the complete rendering
engine; enabling stealth does not remove screenshot, screencast, PDF, CDP, or
MCP functionality.
Per-session fingerprint randomization (GPU, screen, canvas, audio, battery)
Realistic navigator.userAgentData (Chrome 145, high-entropy values)
event.isTrusted = true for dispatched events
Hidden internal properties ( Object.keys(window) safe)
Native function masking ( Function.prototype.toString() → [native code] )
navigator.webdriver = undefined (matches real Chrome)
Blocks analytics, ads, telemetry, and fingerprinting scripts
Prevents trackers from loading entirely
Enabled automatically with --stealth
Obscura implements the Chrome DevTools Protocol for Puppeteer/Playwright compatibility.
To download a large resource without one giant Network.getResponseBody blob, call Fetch.takeResponseBodyAsStream then read it in chunks with IO.read / IO.close . Response bodies over the cache limit ( OBSCURA_NETWORK_BODY_BUFFER_BYTES , default 2 MiB) are not retained, so raise that limit when you intend to stream large downloads.
Obscura embeds V8 directly. Use --v8-flags to pass raw flags through to V8, same syntax as Chromium's --js-flags and Node's command-line flags. Most common use is raising the heap cap to fix JavaScript heap out of memory on JS-heavy pages:
obscura --v8-flags " --max-old-space-size=4096 " fetch < url >
Heavy SPAs (script execution budget)
Obscura caps the page's script-execution phase so one slow or hung page cannot stall a worker. The default budget is 30s; pages that finish sooner return immediately, so the cap only affects pages that keep running. A very heavy React/Vue/Angular SPA on a slow network can need more time to boot before it fires its data requests. Raise the budget with OBSCURA_SCRIPT_DEADLINE_MS (milliseconds), and pair it with a matching navigation timeout in your CDP client:
OBSCURA_SCRIPT_DEADLINE_MS=60000 obscura serve --port 9222
obscura serve
Fetch and render a single page.
Scrape multiple URLs in parallel with worker processes.
Obscura ships an MCP server that exposes browser automation tools to AI agents (Claude Desktop, Cursor, etc.).
stdio (default) — for Claude Desktop and MCP clients that launch a subprocess:
obscura mcp
HTTP — for clients that connect over the network:
obscura mcp --http --port 8080
# endpoint: http://127.0.0.1:8080/mcp

[truncated]
