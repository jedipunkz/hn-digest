---
source: "https://cactuscompute.com/needle"
hn_url: "https://news.ycombinator.com/item?id=49246804"
title: "Show HN: Needle2: 14MB agentic LLM for phones, wearables, smart home and robots"
article_title: "Needle 2 - The 14 MB Agentic LLM for Tiny Devices | Cactus"
author: "HenryNdubuaku"
captured_at: "2026-08-10T17:43:53Z"
capture_tool: "hn-digest"
hn_id: 49246804
score: 1
comments: 0
posted_at: "2026-08-10T17:22:07Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Needle2: 14MB agentic LLM for phones, wearables, smart home and robots

- HN: [49246804](https://news.ycombinator.com/item?id=49246804)
- Source: [cactuscompute.com](https://cactuscompute.com/needle)
- Score: 1
- Comments: 0
- Posted: 2026-08-10T17:22:07Z

## Translation

タイトル: HN を表示: Needle2: 電話、ウェアラブル、スマート ホーム、ロボット用の 14MB エージェント LLM
記事のタイトル: Needle 2 - 小型デバイス用の 14 MB Agentic LLM |サボテン
説明: ツールの呼び出し、デバイスの使用、構造化された抽出のためのオープンな 45M パラメーター モデル。 Needle 2 は、28 MB のセッション RAM で 14 MB バイナリとして実行されます。
HN テキスト: こんにちは、HN、カクタスのヘンリーです!当社は以前、ツール呼び出し、デバイスの使用、電話、ウェアラブル、スマート ホーム、小型ロボット、マイクロコントローラーの構造化抽出用の 14 MB エージェント LLM である Cactus Needle をリリースしました。ここで本当に素晴らしいフィードバックをいただき、その提案を反映して Needle 2 をリリースすることにしました。モデル全体は、28MB の RAM で完全なセッションを実行する単一の 14MB バイナリです。 2bit圧縮時45mパラメータ。 Needle のデコード速度は、Raspberry Pi 5 で 500 トークン/秒に達し、Meta Quest 3S や Apple Vision Pro などの VR デバイスでは 400 ～ 1,500 トークン/秒、Samsung A シリーズなどの 200 ドル以下の携帯電話では 300 ～ 700 トークンの範囲です。ツール呼び出しとモバイルデバイスの使用ベンチマークでは、Needle 2 は、LFM2.5 230M や Apple Foundation Model などの最も近い小型モデルと比べて、5 倍から 70 倍小さく、どちらも f16 と 2 ビットの Needle 2 に勝っています。 Needle は、論文 ( https://arxiv.org/abs/2607.18363 ) の Simple Attendance Networks に基づいています。エッジ AI は最近では Mac や PC を意味しますが、それは現在世界で 210 億を超える接続された IoT デバイスのうちの 15 億台にすぎません。新興市場では、ほとんどの携帯電話は 200 ドル以下で出荷され、NPU や安価な GPU はありません。これらには、格安携帯電話、Raspberry Pi、マイクロコントローラー、ウェアラブル、Reachy Mini などの小型ロボット、コネクテッド ホーム デバイスが含まれます。 Needle の幅と深さの従来のトランスフォーマーはトークンごとに 164 MFLOP を費やし、Needle のパラメータ数に絞り込んだトランスフォーマーでも 87 を費やしますが、Needle では 70 を費やします。ハイエンドの電話機であっても、

Ways-on アシスタントは電力バジェットの範囲内で動作します。すべての MFLOP はミリワット時であり、Needle がトークンあたりに費やすコストは、最も小さいパフォーマンスの LLM よりも 7 分の 1 から 85 分の 1 です。アーキテクチャの詳細については、リンクを参照してください。消費者向けデバイスのインテリジェンスを、型指定されたパラメーターを持つ関数として構造化する場合、唯一の難しい部分は、それらに乱雑な文章をマッピングすることです。どの関数とどの値を使用するか。私たちの研究では、そのように構成すると、問題には世界の知識も自由な散文も必要ないことがわかりました。そのため、4,500 万のパラメーターで十分です。 Needle 2 は構造化抽出に拡張され、ツールの代わりにスキーマを渡すことができ、モデルは構造化出力を返します。 Needle は、フリーレンジ デコード以外のすべてのキー フィールドを抽出するスキーマを提供することで、列挙型フィールドを備えたテキスト分類モデルとして使用したり、要約モデルとして使用したりできます。すべての製品には独自のツール語彙があり、ニードルの微調整はカスタム タスクでフロンティア レベルのパフォーマンスを達成するのに役立ちます。そのため、Python パッケージ ( https://github.com/cactus-compute/needle ) を使用すると、自動データ生成パイプラインを使用して、いくつかのサンプルを渡すだけで、Mac/PC 上でニードルを数分から数時間で微調整できます。それにもかかわらず、すべての応答には、Cactus Hybrid 技術に基づいて学習された信頼度スコアが含まれます。しきい値を超えている場合は行動し、しきい値を下回っている場合はクラウドまたはより大きなモデルにエスカレーションします。 Needle 2 とプライベート DeepSeek-v4-Flash 導入を組み合わせると、ほとんどコストをかけずにエンタープライズ レベルのタスクに特に効果的に機能します。当社はこのセットアップをお手伝いします。私たちは Needle 2 に多くの考えを注ぎ込みましたが、まだかなりの部分が欠けている可能性があります。提供されたリンクのプレイグラウンドを使用して Needle をテストし、意見を共有してください。いつも感謝しています。

記事本文:
Needle 2 - 小型デバイス用の 14 MB Agentic LLM | Cactus Cactus ハイブリッド ニードル エンジン ドキュメント ブログ ⌘K 私たちに話してください Cactus Needle
小型デバイス用のエージェント LLM
ツールの呼び出し、デバイスの使用、構造化された抽出のためのオープン 14MB モデル。
モデルの読み込み... 定義されたツール 定義されたツール · 3 つのツール クエリ実行 例を試す スマート ホーム · マルチステップ ロボット · 3 つの動作 ギャラリー · チェーンされたデバイス制御 · 3 つのアクション 抽出 → 電子メール 通貨 · ライブ API ドキュメント · 抽出 センチメント · 分類 12 ツール · ルーティング 繰り返しの呼び出し 配列引数 フライト · フォーム入力 トピック外 · 拒否 結果 ブラウザ内で WebAssembly を直接実行 本日、Needle 2 をリリースします: オープンツールの呼び出し、デバイスの使用、構造化された抽出のための 45M パラメータ モデル。モデル全体は、28MB の RAM で完全なセッションを実行する単一の 14MB バイナリです。これは、Simple Attendant Network の調査結果に基づいて構築されており、 Cactus Quants を使用して CQ2 ビットに圧縮され、独自のエンジンに組み込まれています。
ツール呼び出しとモバイルデバイス使用ベンチマークでは、Needle 2 は FunctionGemma 270M、LFM2.5 230M、Apple FM などの他の小型モデルと比較して、5 倍から 70 倍小さく、f16 に対して 2 ビットで勝っています。 Needle のデコード速度は、Raspberry Pi 5 では 500 トークン/秒、Meta Quest 3S や Apple Vision Pro などの VR デバイスでは 400 ～ 1,500 トークン/秒、Samsung A シリーズなどの 200 ドル未満の携帯電話では 300 ～ 700 トークンに達します。ピーク時のセッション RAM は約 28MB で、Needle は ESP32-S3 などの新しいマイクロコントローラーで動作します。
Playground を使用すると、ウェアラブル、ロボット、スマート ホーム、電話、自動車用の Needle をテストできます。 Needle は Apache 2.0 に基づいてライセンスされており、Hugging Face に重みが付けられています。リポジトリがあればすぐに実行できます。
サイズと品質のフロンティア: モバイルクラス以下
オンデバイス AI を 200 ドル未満のデバイスに導入 : 最近、エッジ AI は Mac と PC を意味します

しかし、エッジのほとんどは安価なハードウェアです。約 15 億台の PC に対して 210 億台を超える IoT デバイスが接続されており、新興市場ではほとんどの携帯電話が 200 ドル未満で出荷されます。格安携帯電話、Raspberry Pi、マイクロコントローラー、ウェアラブル、Reachy Mini のような小型ロボット、コネクテッド ホーム デバイスを数えてみると、エッジ デバイスのおよそ 5 つ中 4 つは 200 ドル未満です。それが Needle のターゲットとなるハードウェアです。GPU も NPU もなし、数百 MB の RAM です。
関数呼び出しとデバイスの使用: ライトをオンにするのにフロンティア モデルは必要ありません。時計、家、ロボット。それぞれの機能は型指定されたパラメーターを持つ関数としてすでに公開されているため、唯一の難しい部分は、どの関数にどの値を使用するかという乱雑な文をそれらにマッピングすることです。このように構成すると、この問題には世界の知識も自由な散文も必要ありません。だからこそ、チャットには数十億のパラメータが必要ですが、4,500 万のパラメータで十分です。その小さな定式化は、他のすべてがそこから導かれる賭けです。
抽出と構造化出力: スキーマはインターフェイスであり、同じ定式化がドキュメントをカバーします。スキーマと段落は型付きフィールドを返し、列挙フィールドは分類子で、配列フィールドは 1 回の呼び出しでリストを収集します。これを規約ではなく規約で強制します。すべてのターンはコール エンベロープで応答され、空のコールは拒否であり、宣言されたスキーマからコンパイルされたバイトレベルの文法がすべてのトークンを制約します。文法には構文が含まれるため、4,500 万個のパラメーターすべてが関数の選択とユーザーの言葉の根拠となる引数に使用されます。
エッジとクラウドのコラボレーション : すべてをカバーする小さなモデルはないため、Needle 氏は推測ではなくそう言います。すべての応答には学習された信頼スコアが含まれており、トピックから外れたリクエストは空の呼び出しを返します。閾値を超えて、行動してください。その下で、再質問するか、クラウドにエスカレーションします。ほとんどのデバイスリクエストは日常的な制御であるため、エスカレーションはまれです

デフォルトのパスはプライベート、インスタント、無料のままです。
ロスレス 2 ビット量子化: 小さなモデルはポストホック量子化に該当しないため、ポストホック量子化は行いません。Needle 2 は、事前トレーニングからポストトレーニング、重み、アクティベーション、KV キャッシュまで、Cactus Quants に対してトレーニングを行います。デプロイする 2 ビット モデルは、トレーニングされたモデルです。これにより、バッテリーを無駄にすることなく、4,500 万のパラメータを 14 MB に収めることができます。
共同設計されたモデルと推論: すべてのアーキテクチャ上の選択は、パラメーターを取得する前にターゲット ハードウェアでベンチマークされ、成果物は重みではなくペアです。依存関係のない単一の C++ バイナリで、起動時に CPU を調査し、モデル、トークナイザー、および文法コンパイラーが内部に封入されたカーネルを選択します。 1 つのアーティファクトは、Cortex-M から x86、そして WebAssembly まで実行されます。インストールするものやダウンロードするものは何もありません。
Mac/PC 上で微調整: 各製品には独自のツールボキャブラリがあり、45M モデルは実行場所を再トレーニングするのに十分な大きさです。リポジトリと Python パッケージは、数分から数時間で自分のコンピュータ上で調整してテストできます。一般的なアシスタントではなく、デバイスのツールを話す Needle を出荷してください。
Needle は、最小限の RAM フットプリント、低レイテンシー、プライバシー、オフラインの信頼性を必要とする製品向けに生産準備が整っています。現代のウェアラブル業界のパイオニアである Pebble は、Index 01 アプリ内でローカルに実行し、ネットワーク接続に依存せずに音声によるリクエストをアクションに変換します。
「 Pebble Index Ring には画面がありません。そのため、インターネット接続の有無にかかわらず、話しかけるとアクションが発生するだけです。私たちは Cactus Needle をクラウドに依存するのではなく、アプリ内でローカルに実行します。モデルの設置面積は小さく、パフォーマンスは決して私たちを失望させません。」
Needle 2 は独自の 115B トークン コーパスとポストトラで事前トレーニングされています。

コンパクトな推論トレースと慎重なデータセット配布設計を備えた 38B トークンに基づいています。規模の場合: LFM2.5-230M は、Needle の合計の約 120 倍に相当する 19 兆トークンで事前トレーニングされました。以下の評価は 2 つのトレード勝利を示しています。各コンポーネントは、帯域幅を購入せずに機能を購入するために存在します。アダマール MLP は、通常の高密度の上下投影を固定ウォルシュ変換と学習された対角線に置き換えるため、小さなモデルの重み読み取りを支配するチャネル ミキシングにはパラメータがほとんどかかりません。エングラムは、世界の知識をスタックからハッシュ化された N-gram テーブルに移動し、トークンごとに数行読み取られます。デコード時にはほぼ無料の容量ですが、フラッシュから読み取られるすべてのメガバイトが遅延とバッテリーとなるデバイスではこれが重要になります。マルチレーンの残留ストリームは、27 層、512 幅のネットワークに、より多くの注意や MLP ボリュームではなく、層ごとに数ドットの積を犠牲にして、はるかに広いネットワークと同様のルーティングの柔軟性を与えます。
メモリ システムは、固定 RAM デバイスから逆に設計されています。アテンションは 256 トークンのスライディング ウィンドウを使用するため、セッションの実行時間がどれだけ長くても KV キャッシュは制限され、システム プロンプトとツール宣言は永続的なシンクとして固定されるため、ツール呼び出しモデルが決して忘れてはいけない 1 つのもの (ツール) は構造的に削除できません。キャッシュ自体は QAT でトレーニングされ、重みは平均 2 ビットの混合ビット/重みで Cactus Quants に保存されます。その結果、品質の決定と展開の決定は切り離されたままになります。つまり、ターゲット デバイスが許容できる精度とウィンドウに特化した 1 つのトレーニング済みモデルです。
エンジンは、計算を拒否したものから速度を獲得します。重みが RAM に解凍されることはありません。2 ビット コードはベクトル レジスタ内で展開され、整数のドット積に融合されるため、メモリに常駐します。

BLOB サイズのままで、算術パスは int8 エンドツーエンドです (アクティベーション、KV キャッシュ、レーン ルーティング テーブルも同様)。文法は単なる保証ではなく最適化です。マッチャーはロジットが存在する前にどのトークンが正当であるかを知っているため、エンジンは候補行のみの出力スコアを計算し、構造トークンの語彙射影の最大 98% をスキップし、出力がすでに強制されているステップでは完全にスキップします。 1 つのユニバーサル バイナリが起動時に CPU をプローブし、そのカーネル層 (SDOT、NEON、AVX2、RISC-V ベクトル、wasm SIMD、またはスカラー) を自己選択します。また、スレッド プールはスリープせずにトークンの短いシリアル セクションをスピンし、これだけでデコード速度がほぼ 2 倍になります。これによって単一の出力が変更されることはありません。すべてのトリックは正確であるか、参照パスに対してトークンごとに検証されるかのいずれかです。
それはすべて、最終的にはエネルギーに関する議論です。デバイスのシリコンでは、フラッシュまたは DRAM からバイトを移動する場合、積和演算よりも桁違いにコストがかかるため、重要な予算はトークンあたりの FLOP とトークンあたりのバイト数を合わせたものになります。このアーキテクチャは最初の部分をカットします。Needle の幅と深さの従来のトランスフォーマーはトークンごとに 164 MFLOP を費やし、Needle のパラメーター数に絞り込んだものでも 87 MFLOP を費やします。これは、それが所有するすべてのパラメーターが matmul を通じて実行される必要があるためです。 Needle は 70 を費やし、そのパラメータの 5 分の 1 を演算コストがまったくかからない収集されたメモリとして保持します。エンジン セクションで示したように、バイナリは 2 番目をカットします。何も再実体化せず、算術演算は端から端まで int8 のままで、文法プルーニングは完全に計算するため、トークンのデコードでは最大 14MB の BLOB を 1 回読み込むだけで、構造トークンの場合は大幅に少なくなります。これがバッテリーの寿命です。ハイエンドの携帯電話であっても、常時オンのアシスタントは電力バジェットの範囲内で動作します。イブ

MFLOP はミリワット時であり、Needle がトークンあたりに費やすコストは、ベンチマーク対象のモデルよりも 7 分の 1 から 85 分の 1 です。
境界のあるセッション メモリにより、マイクロコントローラーがアクセスできるようになります。スライディング ウィンドウ キャップの状態により、Needle 2 の RAM は決定的な 28MB の上限であり、会話の長さに応じて増加する曲線ではありません。これは、32MB の PSRAM を搭載した ESP32-P4、または SDRAM を搭載した STM32H7 および NXP i.MX RT ボードなどの外部 RAM を搭載した MCU クラスの部品に適合します。このエンジンはベアメタル用にシングルスレッドでコンパイルされ、Cortex-M4、M7、および M55 用の静的ライブラリとして出荷されます。
Google の Mobile Actions、DroidCall、Seal-Tools のドメイン内およびドメイン外テスト、BFCL v4 シングルターンの 5 つのパブリック関数呼び出しベンチマークで評価します。スコアリングは厳密な完全一致で順序付けされます。行は、関数名、呼び出し順序、およびすべての引数値が一致する場合にのみ合格します。すべての Needle 2 数値は、製品構成で出荷された C++ エンジンを通じてエンドツーエンドで測定されます。CQ2 ビットの重み、ツール取得、および 256 トークンのスライディング KV ウィンドウです。ベンチマークのために何も緩和されていません。数値は、ウィンドウのエビクションを含む、デバイスが実行する正確なエンジンを反映しています。ベースラインはリリースされたチェックポイントを vLLM の下でフルコンテキストで実行し、Apple FM はデバイス上で実行されます。
2 つの非対称性がこの比較を困難にしているため、両方のことを前もって述べておきます。精度: ベースラインは意図的に f16 に留まります。これは、従来のトレーニング後の 2 ビットへの量子化では、積極的な圧縮のためにトレーニングされていないモデルが崩壊するためです。一方、Cactus Quants は Needle のトレーニングに最初から組み込まれています。この偏りはベースラインに有利です。範囲: Needle は、エージェント ツールの呼び出しのみに特化してトレーニングされていますが、すべてのベースラインは、ツールの呼び出しとともにチャット、散文、世界の知識を運ぶ一般的な言語モデルです。

。その偏りはニードルに有利です。両方を同時にレベルアップするきれいな方法はないので、試しません。この表は、どのモデルがオンデバイスの予算内でツール呼び出しを正しく実行するかという 1 つの狭い質問に答えています。私たちは偏りを受け入れます。それはまだ私たちが意図した絵を描いています。
DroidCall テストの分割 (200 行)
ドメイン内の Seal-Tools (700 行)
ドメイン外の Seal-Tools (654 行)
Needle は一般的な関数呼び出しについてはトレーニングされていません。そのコーパスは消費者向けデバイスのアクション (スマート ホーム、モバイル、ウェアラブル、テレビ、自動車) と構造化抽出であり、Java および JavaScript SDK カテゴリを含む BFCL の汎用およびエンタープライズ API サーフェスは、完全にそのディストリビューションの外側にあります。それにもかかわらず、それは外挿されます。Python の単純な呼び出しでは、まさにこのタスク用にトレーニングされた 6 倍大きいモデルである FunctionGemma のポイント内に到達し、3,641 行すべてにわたって 93.4 の整形式レートを維持します。ギャップは、トレーニング データがこれまで存在しなかった場所、つまり Java、JavaScript、および並列マルチコール カテゴリに集中します。
BFCL v4 シングルターン (3,641 行)
ハグフェイス GitHub 研究論文 オンデバイスアクションをハードウェアに組み込む？
Needle は Apache 2.0 ライセンスを取得しています。カスタム ツール スキーマ、ハードウェア チューニング、トレーニング後のことについては、ぜひご相談ください。
最新のアプリケーション向けのハイブリッド推論

[切り捨てられた]

## Original Extract

An open 45M-parameter model for tool calling, device use, and structured extraction. Needle 2 runs as a 14 MB binary in 28 MB of session RAM.

Hey HN, Henry from Cactus here! We previously released Cactus Needle, a 14MB agentic LLM for tool call, device use, and structured extraction for phones, wearables, smart homes, small robots and microcontrollers. We got really great feedback here, and have now incorporated the suggestions to release Needle 2. The whole model is a single 14MB binary that runs a full session in 28MB of RAM; 45m parameters at 2bit compression. Needle hits 500 tokens/sec decode speed on a Raspberry Pi 5, sits between 400-1,500 tokens/sec on VR devices like Meta Quest 3S and Apple Vision Pro, and ranges 300-700 on sub-$200 phones such as the Samsung A-Series. On the tool call and mobile device use benchmarks, Needle 2 trades wins with closest small models like LFM2.5 230M and Apple Foundation Model, at 5x to 70x smaller, both at f16 vs Needle 2 at 2bit. Needle is based on Simple Attention Networks from our paper ( https://arxiv.org/abs/2607.18363 ). Edge AI has lately meant Macs and PCs, but that is just 1.5 billion of over 21 billion connected IoT devices in the world today, and in emerging markets most phones ship under $200, no NPU, cheap GPUs. These include budget phones, Raspberry Pis, microcontrollers, wearables, small robots like Reachy Mini, and connected home devices. A conventional transformer of Needle's width and depth spends 164 MFLOPs per token, and even one squeezed down to Needle's parameter count spends 87, Needle spends 70. Even on a high-end phone, an always-on assistant lives inside a power budget; every MFLOP is milliwatt-hours, and Needle spends 7x to 85x fewer of them per token than the smallest performant LLMs. More about the architecture in the link. When we structure intelligence for consumer devices as functions with typed parameters, the only hard part is mapping a messy sentence onto them; which function, with which values. Our research found that when framed that way, the problem needs no world knowledge and no open-ended prose, which is why 45M parameters suffice. Needle 2 expands to structured extraction where the schema can be passed in-place of tools and the model returns structured output. You can use Needle as a text-classification model with an enum field, as a summarization model by providing a schema that extracts key fields, everything but free-range decode. Every product has its own tool vocabulary and fine-tuning needle helps it achieve frontier-level performance on custom tasks, so using the python package ( https://github.com/cactus-compute/needle ), Needle can be fine-tuned Needle on a Mac/PC in minutes to a few hours, with automated data-generation pipeline, just pass a couple samples. Nonetheless, every response carries a learned confidence score based our Cactus Hybrid technique. If above your threshold, act, below it, escalate to the cloud or bigger model. Combining Needle 2 with a private DeepSeek-v4-Flash deployment works particularly well for enterprise-level tasks at barely any cost, we can help with this setup. We have put a lot of thoughts into Needle 2 but might still be missing quite a lot, please use the playground in the provided link to test Needle and share your thoughts, always appreciated!

Needle 2 - The 14 MB Agentic LLM for Tiny Devices | Cactus Cactus Hybrid Needle Engine Docs Blog ⌘K Talk to us Cactus Needle
Agentic LLM for tiny devices
An open 14MB model for tool calling, device use, and structured extraction.
loading model... Defined tools Defined tools · 3 tools Query Run Try an example Smart home · multi-step Robot · 3 moves Gallery · chained Device control · 3 actions Extract → email Currency · live API Document · extraction Sentiment · classification 12 tools · routing Repeated calls Array argument Flight · form filling Off-topic · refusal Result Running in WebAssembly directly in your browser Today we release Needle 2: an open 45M-parameter model for tool calling, device use and structured extraction. The whole model is a single 14MB binary that runs a full session in 28MB of RAM. It is built on our Simple Attention Network findings, compressed to CQ2-bit with Cactus Quants , and baked into its own engine.
On the tool call and mobile device use benchmarks, Needle 2 trades wins with other small models like FunctionGemma 270M, LFM2.5 230M and Apple FM, at 5× to 70× smaller, and 2 bits against their f16. Needle hits 500 tokens/sec decode speed on a Raspberry Pi 5, between 400–1,500 tokens/sec on VR devices like Meta Quest 3S and Apple Vision Pro, and ranges 300–700 on sub-$200 phones such as the Samsung A-Series. With a peak session RAM around 28MB, Needle runs on newer microcontrollers like ESP32-S3.
The Playground lets you test Needle for wearables, robots, smart homes, phones, and automotive. Needle is licensed under Apache 2.0, with weights on Hugging Face ; the repo gets you running.
Size–quality frontier: mobile-class and below
Bringing On-Device AI to <$200 Devices : Edge AI has lately meant Macs and PCs, but the edge is mostly cheap hardware: over 21 billion connected IoT devices against roughly 1.5 billion PCs, and in emerging markets most phones ship under $200. Count budget phones, Raspberry Pis, microcontrollers, wearables, small robots like Reachy Mini, and connected home devices, and roughly four in five edge devices cost under $200. That is the hardware Needle targets: no GPU, no NPU, a few hundred MB of RAM.
Function Call & Device Use : Turning on a light does not need a frontier model. A watch, a home, a robot: each already exposes its abilities as functions with typed parameters, so the only hard part is mapping a messy sentence onto them: which function, with which values. Framed that way, the problem needs no world knowledge and no open-ended prose, which is why 45M parameters suffice where chat needs billions. That smaller formulation is the bet everything else follows from.
Extraction & Structured Outputs : The schema is the interface, and the same formulation covers documents: a schema plus a paragraph returns typed fields, an enum field is a classifier, an array field collects a list in one call. We enforce this with a contract, not a convention: every turn is answered with a call envelope, the empty call is the refusal, and a byte-level grammar compiled from the declared schemas constrains every token. The grammar carries the syntax, so all 45M parameters go to choosing functions and grounding arguments in the user's words.
Edge-Cloud Collaboration : No small model covers everything, so Needle says so instead of guessing: every response carries a learned confidence score, and off-topic requests return the empty call. Above your threshold, act; below it, re-ask or escalate to the cloud. Most device requests are routine control, so escalation stays rare and the default path stays private, instant, and free.
Lossless 2bit Quantization : Small models break under post-hoc quantization, so we never quantize post-hoc: Needle 2 trains against Cactus Quants from pretrain through post-train, weights, activations, and KV cache alike. The 2bit model you deploy is the model that was trained. That is what fits 45M parameters into 14MB with nothing lost on our battery.
Co-designed Model & Inference : Every architectural choice was benchmarked on the target hardware before it earned its parameters, and the deliverable is the pair, not the weights: a single dependency-free C++ binary that probes the CPU at startup and picks its kernels, with the model, tokenizer, and grammar compiler sealed inside. One artifact runs from Cortex-M to x86 to WebAssembly. There is nothing to install and nothing to download.
Fine-tune on your Mac/PC : Every product has its own tool vocabulary, and a 45M model is small enough to retrain where it runs: the repo and python package tune and test on your own computer in minutes to a few hours. Ship a Needle that speaks your device's tools, not a generic assistant.
Needle is production-ready for products that require a minimal RAM footprint, low latency, privacy, and offline reliability. Pebble - the pioneer of the modern wearable industry - runs it locally in the Index 01 app to turn spoken requests into actions without depending on a network connection.
“ The Pebble Index Ring has no screen. So when you speak to it, the action just has to happen, every time, with or without internet connection. We run Cactus Needle locally in the app, instead of relying on the cloud. The model's footprint is tiny and the performance never lets us down.
Needle 2 is pretrained on a proprietary 115B-token corpus and post-trained on 38B tokens with compact reasoning traces and careful dataset distribution design. For scale: LFM2.5-230M was pretrained on 19 trillion tokens, roughly 120× Needle's total, and the evaluation below shows the two trading wins. Each component exists to buy capability without buying bandwidth. The Hadamard MLP replaces the usual dense up-and-down projections with a fixed Walsh transform and learned diagonals, so the channel mixing that dominates a small model's weight reads costs almost no parameters at all. The engram moves world knowledge out of the stack into hashed n-gram tables that are read a few rows per token: capacity that is nearly free at decode time, which matters on devices where every megabyte read from flash is latency and battery. The multi-lane residual streams give a 27-layer, 512-wide network the routing flexibility of a much wider one, at the cost of a few dot products per layer rather than more attention or MLP volume.
The memory system is designed backwards from fixed-RAM devices. Attention uses a 256-token sliding window so the KV cache is bounded no matter how long a session runs, and the system prompt and tool declarations are pinned as permanent sinks so the one thing a tool-calling model must never forget—its tools—is structurally unable to be evicted. The cache itself is trained with QAT, and weights are stored in Cactus Quants at a mixed bits per weight averaging 2bit. The result is that quality decisions and deployment decisions stay decoupled: one trained model, specialized to whatever precision and window a target device can afford.
The engine earns its speed from what it refuses to compute. Weights never decompress into RAM: the 2-bit codes are expanded inside vector registers, fused into integer dot products, so resident memory stays at blob size and the arithmetic path is int8 end to end—activations, KV cache, and the lane routing tables alike. The grammar is an optimization, not just a guarantee: because the matcher knows which tokens are legal before the logits exist, the engine computes output scores only for candidate rows, skipping up to 98% of the vocabulary projection on structural tokens, and skips it entirely on steps whose output is already forced. One universal binary probes the CPU at startup and self-selects its kernel tier—SDOT, NEON, AVX2, RISC-V vectors, wasm SIMD, or scalar—and the thread pool spins through the short serial sections of a token instead of sleeping, which alone nearly doubled decode. None of this changes a single output: every trick is either exact or validated token-for-token against the reference path.
All of it is ultimately an energy argument. On device silicon, moving a byte out of flash or DRAM costs orders of magnitude more than a multiply-accumulate, so the budget that matters is FLOPs per token and bytes per token together. The architecture cuts the first: a conventional transformer of Needle's width and depth spends 164 MFLOPs per token, and even one squeezed down to Needle's parameter count spends 87, because every parameter it owns must be exercised through a matmul. Needle spends 70, and keeps a fifth of its parameters as gathered memory that costs no arithmetic at all. The binary cuts the second, as the engine section showed: nothing rematerializes, the arithmetic stays int8 end to end, and the grammar prunes compute outright, so decoding a token reads at most the 14MB blob once, and on structural tokens meaningfully less. This is what battery life is made of. Even on a high-end phone, an always-on assistant lives inside a power budget; every MFLOP is milliwatt-hours, and Needle spends 7× to 85× fewer of them per token than the models it is benchmarked against.
Bounded session memory is what puts microcontrollers in reach. Because the sliding window caps state, Needle 2's RAM is a deterministic 28MB ceiling, not a curve that grows with conversation length. That fits MCU-class parts with external RAM, such as ESP32-P4 with 32MB of PSRAM, or STM32H7 and NXP i.MX RT boards with SDRAM. The engine compiles single-threaded for bare metal and ships as a static library for Cortex-M4, M7, and M55.
We evaluate on five public function-calling benchmarks: Google's Mobile Actions, DroidCall, the Seal-Tools in-domain and out-of-domain tests, and BFCL v4 single-turn. Scoring is ordered strict exact match: a row passes only if the function names, the call order, and every argument value match. All Needle 2 numbers are measured end-to-end through the shipped C++ engine in its production configuration: CQ2-bit weights, tool retrieval on, and the 256-token sliding KV window. Nothing is relaxed for benchmarking; the numbers reflect the exact engine a device runs, window eviction included. Baselines run the released checkpoints under vLLM at full context, and Apple FM runs on-device.
Two asymmetries make this comparison hard, and we state both upfront. Precision: the baselines stay at f16 deliberately, because conventional post-training quantization to 2 bits collapses models that were never trained for aggressive compression, while Cactus Quants is baked into Needle's training from the ground up. That skew favors the baselines. Scope: Needle is trained specifically for agentic tool calling and nothing else, while every baseline is a general language model carrying chat, prose, and world knowledge alongside its tool calling. That skew favors Needle. There is no clean way to level both at once, so we do not try. The tables answer one narrow question: which model executes tool calls correctly within an on-device budget. We accept the skew; it still paints the picture we intend.
DroidCall test split (200 rows)
Seal-Tools in-domain (700 rows)
Seal-Tools out-of-domain (654 rows)
Needle was not trained for general function calling: its corpus is consumer device actions—smart home, mobile, wearables, TV, car—plus structured extraction, and BFCL's general-purpose and enterprise API surfaces, including the Java and JavaScript SDK categories, sit entirely outside that distribution. It extrapolates nonetheless: on Python simple calls it lands within a point of FunctionGemma, a model six times larger trained for exactly this task, and it keeps a 93.4 well-formed rate across all 3,641 rows. The gap concentrates where its training data has never been: Java, JavaScript, and the parallel multi-call categories.
BFCL v4 single-turn (3,641 rows)
Hugging Face GitHub Research paper Building on-device actions into hardware?
Needle is Apache 2.0-licensed. For custom tool schemas, hardware tuning, and post-training, we'd love to talk.
Hybrid inference for modern applications

[truncated]
