---
source: "https://github.com/Bevel-Software/Hexis"
hn_url: "https://news.ycombinator.com/item?id=49260145"
title: "Hexis: Git-backed skills, tools and context for AI agents"
article_title: "GitHub - Bevel-Software/Hexis: Git-backed skills, tools & context for AI agents · GitHub"
author: "juanviera23"
captured_at: "2026-08-11T15:50:40Z"
capture_tool: "hn-digest"
hn_id: 49260145
score: 1
comments: 0
posted_at: "2026-08-11T15:46:35Z"
tags:
  - hacker-news
  - translated
---

# Hexis: Git-backed skills, tools and context for AI agents

- HN: [49260145](https://news.ycombinator.com/item?id=49260145)
- Source: [github.com](https://github.com/Bevel-Software/Hexis)
- Score: 1
- Comments: 0
- Posted: 2026-08-11T15:46:35Z

## Translation

タイトル: Hexis: AI エージェント向けの Git ベースのスキル、ツール、コンテキスト
記事のタイトル: GitHub - Bevel-Software/Hexis: AI エージェント向けの Git に裏付けられたスキル、ツール、コンテキスト · GitHub
説明: AI エージェント向けの Git ベースのスキル、ツール、コンテキスト。 GitHub でアカウントを作成して、Bevel-Software/Hexis の開発に貢献してください。

記事本文:
GitHub - Bevel-Software/Hexis: AI エージェント向けの Git に裏付けられたスキル、ツール、コンテキスト · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン 外観設定 プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
/ と入力して検索します。 サインイン サインアップ 外観設定
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
ああ、ああ！
読み込み中にエラーが発生しました。このページをリロードしてください。
ベベルソフトウェア
/
ヘクシス
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
383 コミット 383 コミット .changeset .changeset .github .github アプリ アプリ ドキュメント ドキュメント パッケージ パッケージ スクリプト スクリプト .coderabbit.yaml .coderabbit.y

aml .dockerignore .dockerignore .env.example .env.example .gitattributes .gitattributes .gitignore .gitignore .nvmrc .nvmrc Dockerfile Dockerfile ライセンス ライセンス README.md README.md docker-compose.override.yml docker-compose.override.yml docker-compose.yml docker-compose.yml eslint.config.js eslint.config.js Glama.json Glama.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yaml tsconfig.base.json tsconfig.base.json すべてのファイルを表示 リポジトリ ファイルのナビゲーション
あなたの会社の AI スキル、ツール、知識が存在する 1 つの場所:
一元的に管理、レビュー、アクセス制御され、あらゆる AI から使用可能
代理店。 Bevel プラットフォームのオープンソース コア。
すべての従業員が 1 回、数分で接続します。ワークスペースを追加する
Claude Code、ChatGPT、Cursor、または単一の MCP 対応エージェント
接続キーを使用すると、エージェントはスキル、ツール、および
自分の役割が許す知識。ツールごとに認証情報が渡されることはありません。
エージェントごとのセットアップ プロジェクト。
会社は主導権を握り続けます。スキル、ツールへのアクセス、知識は
1 か所で管理およびレビュー: すべての変更には作成者と方法があります
変更リクエストを通じて提案されたものはすべてその所有者に届きます
着陸前に確認するために。ルールはエージェントにそのまま適用されます。
人に当てはまります。
いかなるエージェントベンダーからも独立しています。ワークスペースはオープンに話すため
プロトコルに応じて、価格とパフォーマンスに応じてエージェント ベンダーを切り替えたり、組み合わせたりすることができます。
エージェントが知っていること、実行できることを再構築することなく、タスクと役割別にそれらを管理します。
投資は、特定のベンダーの壁の内側ではなく、あなたとともに生きます。それが何です
エンタープライズ エージェントのロールアウトを迅速化します。次のチームまたは次のチームをオンボーディングします。
エージェントを最初からやり直すのではなく。
内部では、すべてが任意の Git 上で所有する Git リポジトリに存在します。
ホスト: スキル ( SKILL.md

フォルダ）、暗号化されたツールマニュアル（UTCP）
秘密の保管庫と知識。ブランチを取得し、所有者にリクエストを変更します
承認、ロールベースのアクセス、組み込みのリモート MCP サーバー (OAuth 2.1)
エージェントが接続する接続先。
完全なチュートリアルをご覧ください: エージェントの接続、会社コンテキストの使用、レビュー
提案された変更を確認し、チームのアクセスを管理します。
スキル変更の提案と承認
誰でも新しいスキルを提案したり、既存のスキルを改善したりできます。保護中
支店、所有者は正確な変更を確認し、変更される前に承認します。
チームのエージェントが利用できます。
クロードでチームのスキルを活用しましょう
MCP 経由でクロードを Hexis に接続し、通常どおりに要求します。クロードは発見することができ、
承認されたスキルの指示と、自分の役割がアクセスできる会社のコンテキストをロードします。
ツール間でプロンプトをコピーする必要はありません。
Cline は、リモート Streamable HTTP MCP サーバーとして Hexis に直接接続できます。
Cline CLI からパブリック デモ接続をインストールします。
cline mcp install hexis --transport http https://demo.bevel.software/api/mcp --yes
プロンプトが表示されたら、ブラウザーで OAuth サインインを完了します。クラインはその後発見する
Hexis ロールがアクセスできるスキル、ツール、コンテキスト。あなただけのヘクシスのために
デプロイメントでは、demo.bevel.software をデプロイメントのホストに置き換えます。
適切な人とスキルを共有する
必要に応じて、チームメイトを役割に追加するか、直接アクセスを許可します。みんながつながる
同じワークスペースにアクセスできますが、各個人とそのエージェントには自分が見たいものだけが表示されます。
読むことが許可されています。
デモ.ベベル.ソフトウェア
Google アカウントでサインインできるパブリック インスタンスです。
架空の会社の知識、スキル、ツールを使用して。 「ここからスタート」ページ
ループ全体を説明します。MCP 経由で独自のエージェントを接続し、それを使用します。
スキルからセールスデッキを構築し、提案された改善が成果として反映されるのを観察します。
変更リクエスト。デモは共有され、ほとんどが読み取ります

（来場者提案、オーナー様
承認します）;以下のすべては、制限なしで同じ結果をもたらします。
弊社がお客様に代わって実行し (ホスティング、アップグレード、バックアップ、SSO)、お客様のチームは署名するだけです。
ali.raza@bevel.software に書き込みます。
5 分でデプロイ (Docker)
必要なもの: Compose を備えた Docker
サーバー (またはラップトップ。下の 1 行が異なります)、および
任意のホスト上の空の git リポジトリ (GitHub、GitLab、Bitbucket、Azure DevOps、
自己ホスト型) ナレッジ ベースを保持します。アプリはスターターをシードします
初回実行時のテンプレート。
git clone https://github.com/Bevel-Software/Hexis.git
cd ヘクシス
cp .env.example .env
.env を開き、4 つの必要な値を入力します (他の値はすべて待機できます)。
ADMIN_EMAIL=you@example.com # デプロイメント所有者、常に管理者
ADMIN_PASSWORD=何かを選択 # サインイン パスワード;パスワードによるログインのみ (SSO のみの展開では削除されます。以下を参照)
JWT_SECRET=… # 以下のコマンドで生成
SECRETS_ENC_KEY=… # 以下のコマンドで生成
2 つのシークレットを生成します (2 回実行し、それぞれに 1 つの結果を貼り付けます)。
ノード -e " console.log(require('crypto').randomBytes(32).toString('base64')) "
# ノードがインストールされていませんか? docker run --rm ノード:22-スリム ノード -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
パブリック デプロイメントの場合は、元の値も設定します。
PUBLIC_BACKEND_URL=https://bevel.your-domain.com # パブリックオリジン; OAuth リダイレクトはそれから構築されます
PUBLIC_FRONTEND_URL=https://bevel.your-domain.com # 同じオリジン: バックエンドが SPA にサービスを提供します
次に、すべて (Postgres + アプリ) を開始します。リバースプロキシの背後で
(Coolify、Traefik、nginx; 推奨):
docker compose -f docker-compose.yml up -d
また、TRUST_PROXY をプロキシ ホップ カウント (単一プロキシの場合は 1) に設定します。
レート制限では、プロキシの代わりに実際のクライアント IP が参照されます。
明示的な -f は docker-compose.override をスキップします

.yml 、つまりアプリが公開する
ホスト ポートがありません: プロキシは構成経由でポート 3001 に到達します
ネットワーク。これは意図的なものです。固定された公開ポートにより、すべての再デプロイが失敗します。
代替コンテナが開始されるため、ポートはすでに割り当てられています
一方、送信側はまだそれを保持しています。
直接公開 (プロキシなし): プレーンな docker compose up -d公開
:3001 ; APP_PORT=8080 docker compose up -d を別のホスト ポートに使用します。
ここでは TRUST_PROXY を未設定のままにしておきます。前にプロキシがない場合、転送を信頼する
ヘッダーを使用すると、クライアントは自分のアドレスを偽装できるようになります。
ドメインを開き、 ADMIN_EMAIL / ADMIN_PASSWORD でサインインします。
ラップトップで試してみるだけですか?同じ手順で元の値を除いたもの: プレーン
docker compose up -d を実行し、 http://localhost:3001 を開きます。
最初のサインイン: セットアップ画面
アプリは推測できなかったことを尋ね、それらをテストします。
保存する前の実際のホスト:
ナレッジベース リポジトリ : 空のリポジトリの https クローン URL。
Git 認証情報 : 読み取り/書き込みアクセス権を持つトークン (GitHub の場合:
コンテンツを含むきめ細かい個人アクセス トークン: 読み取りと書き込み
レポで十分です）。
ブランチ モデル: どのブランチがデフォルトで、どのブランチが保護されているか
(保護されたブランチへの変更は、承認された変更リクエストによってのみ適用されます)。
リポジトリの実際のブランチは提案として提供されます。空のリポジトリの場合
デフォルト ( main ) で問題ありません。
リポジトリは空であるため、アプリはバンドルされたテンプレートからリポジトリを初期化し、
最初の管理者があなたであるroles.yamlを書き込みます。それだけです：あなたはその中にいます
ワークスペース。 「スキルとツール」に移動して最初のグループとスキルを作成し、
(アプリ メニューで) 接続して、MCP 経由でエージェントを接続します。
セットアップ画面ではなく環境ごとに構成したいですか?それぞれの
これらの値には環境変数 ( KB_REPO_URL 、 GIT_TOKEN 、 DEFAULT_BRANCH 、…) があります。
何でも

環境に設定されているものがセットアップ画面よりも優先されます。参照
.env.example 。
再デプロイ後に生き残る状態: Postgres データと 3 つのアプリ ボリューム
(ワークスペース クローン、差分レビュー バックアップ、ツール チェーン スピル ファイル) に名前が付けられます
したがって、再デプロイまたはイメージの再構築によって何も失われません。 pgdataをバックアップする
ボリュームとナレッジベースの git リポジトリ。他のすべては導出可能です。
ヘルス: /api/health を取得します。最初の起動には 1 ～ 2 分かかることがありますが、
移行を実行し、ナレッジベース リポジトリをシードします。
シングル サインオン: OIDC_ISSUER_URL / OIDC_CLIENT_ID / を設定します。
OIDC_CLIENT_SECRET (仕様に準拠した任意のプロバイダー)、または
設定画面には、登録するリダイレクト URI が表示されます。 SSO のみの場合
デプロイメントでは LOGIN_PASSWORD=false を設定し、 ADMIN_PASSWORD を削除します。もしあなたの
発行者がマルチテナント (Google、Entra common )、設定されている
ALLOWED_EMAIL_DOMAINS 。 SSO はアカウントを自動プロビジョニングします。そのリストは
サインアップ境界のみ。
ローカル開発 (ソースから実行)
必要なもの: ノード 22 ( .nvmrc ; エンジン範囲は >=22 <23 )、
pnpm 10 、 git ≥ 2.41 、および Postgres 17 (バンドルされているもので問題ありません):
docker compose up -d db # データベースのみ
pnpmインストール
pnpm build # アプリがインポートするパッケージをビルドします
cp .env.example .env # 同じ 4 つの必須値を入力します。
# デフォルトの DATABASE_URL はすでにバンドルされたデータベースを指しています
pnpm dev # バックエンド:3001、Vite 開発サーバー:5173
http://localhost:5173 を開きます (開発サーバーはバックエンドにプロキシします)。役に立つ
コマンド: pnpm test 、 pnpm typecheck 、 pnpm lint 。
移行は起動時に自動的に実行されます。個別の移行手順はありません。
開発中または運用中。
必要な 4 つの値、次に残りの値。セットアップ画面でマークされているものすべて
未設定のままにし、最初のサインイン時にアプリで構成することができます (env が常に優先されます)。
.env.example では、すべての変数が完全に文書化されています。
ポートはすでに割り当てられています

再デプロイ時: プロキシの背後にいますが、実行されました
-f docker-compose.yml を指定せずに作成したため、オーバーライドによりホストが公開されました
ポート。明示的に -f を指定してデプロイします (上記を参照)。
POSTGRES_PASSWORD を変更しましたが接続できません: Postgres はそれらを適用します
データ ボリュームが最初に作成されたときの値のみ。開発では、
docker compose down -v はリセットします。運用環境では、パスワードを変更します
データベース自体。
ノードのバージョンで pnpm インストールが失敗する: エンジンの範囲が厳密です
( >=22 <23 ) ネイティブ依存関係の ABI のため。 nvm はピックアップを使用します
.nvmrc 。
セットアップ画面が git トークンを拒否します: トークンには読み取りと書き込みが必要です
KB リポジトリへの (プッシュ) アクセス。セットアップ画面のテストでどれがわかるか
半分失敗しました。 GitHub では、きめの細かいトークンにも明示的にリポジトリが必要です
選択されました。
ADMIN_PASSWORD を変更しましたが、何も起こりませんでした。一度読み取られました。
スタートアップ。アプリコンテナを再起動します。
最初の起動直後にアプリが異常になる: start_period を指定する
(〜90年代);最初のブートでは移行が実行され、応答する前に KB リポジトリがシードされます。
パス
それは何ですか
パッケージ/共有
@bevel-software/platform-shared : 共有タイプ + 純粋なドメイン ユーティリティ
パッケージ/コアバックエンド
@bevel-software/platform-core-backend : コア バックエンド (migrations/ + kb-template/ を出荷)
パッケージ/c

[切り捨てられた]

## Original Extract

Git-backed skills, tools & context for AI agents. Contribute to Bevel-Software/Hexis development by creating an account on GitHub.

GitHub - Bevel-Software/Hexis: Git-backed skills, tools & context for AI agents · GitHub
Skip to content
Navigation Menu
Sign in Appearance settings Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Type / to search Sign in Sign up Appearance settings
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Uh oh!
There was an error while loading. Please reload this page .
Bevel-Software
/
Hexis
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
383 Commits 383 Commits .changeset .changeset .github .github apps apps docs docs packages packages scripts scripts .coderabbit.yaml .coderabbit.yaml .dockerignore .dockerignore .env.example .env.example .gitattributes .gitattributes .gitignore .gitignore .nvmrc .nvmrc Dockerfile Dockerfile LICENSE LICENSE README.md README.md docker-compose.override.yml docker-compose.override.yml docker-compose.yml docker-compose.yml eslint.config.js eslint.config.js glama.json glama.json package.json package.json pnpm-lock.yaml pnpm-lock.yaml pnpm-workspace.yaml pnpm-workspace.yaml tsconfig.base.json tsconfig.base.json View all files Repository files navigation
One place where your company's AI skills, tools and knowledge live:
centrally managed, reviewed and access-controlled, and usable from any AI
agent . The open-source core of the Bevel platform.
Every employee connects once, in minutes. They add the workspace to
Claude Code, ChatGPT, Cursor or any MCP-capable agent with a single
connection key, and their agent can use exactly the skills, tools and
knowledge their role allows. No per-tool credentials handed around, no
per-agent setup projects.
The company stays in control. Skills, tool access and knowledge are
managed and reviewed in one place: every change has an author and a way
back, and anything proposed through a change request reaches its owners
for review before it lands. The rules apply to agents exactly as they
apply to people.
Independent of any agent vendor. Because the workspace speaks open
protocols, you can switch agent vendors on price and performance, or mix
them by task and role, without rebuilding what your agents know and can do.
The investment lives with you, not inside one vendor's walls. That is what
makes enterprise agent rollouts fast: onboard the next team, or the next
agent, instead of starting over.
Under the hood, everything lives in a git repository you own , on any git
host: skills ( SKILL.md folders), tool manuals (UTCP) with an encrypted
secrets vault, and knowledge. You get branches, change requests with owner
approval, role-based access, and a built-in remote MCP server (OAuth 2.1)
that agents connect to.
Watch the full walkthrough: connect an agent, use company context, review
proposed changes, and manage team access.
Propose and approve skill changes
Anyone can propose a new skill or improve an existing one. On protected
branches, owners review the exact change and approve it before it becomes
available to the team's agents.
Use your team's skills in Claude
Connect Claude to Hexis over MCP, then ask normally. Claude can discover and
load the approved skill instructions and company context your role can access,
without copying prompts between tools.
Cline can connect directly to Hexis as a remote Streamable HTTP MCP server.
Install the public demo connection from the Cline CLI:
cline mcp install hexis --transport http https://demo.bevel.software/api/mcp --yes
Complete the OAuth sign-in in your browser when prompted. Cline then discovers
the skills, tools and context your Hexis role can access. For your own Hexis
deployment, replace demo.bevel.software with your deployment's host.
Share skills with the right people
Add teammates to roles or grant access directly when needed. Everyone connects
to the same workspace, while each person and their agent only sees what they
are allowed to read.
demo.bevel.software
is a public instance you can sign into with your Google account, populated
with a fictional company's knowledge, skills and tools. The Start here page
walks you through the whole loop: connect your own agent over MCP, have it
build a sales deck from a skill, watch its proposed improvement arrive as a
change request. The demo is shared and read-mostly (visitors propose, owners
approve); everything below gets you the same thing with none of the limits.
We run it for you (hosting, upgrades, backups, SSO) and your team just signs
in. Write to ali.raza@bevel.software .
Deploy it in 5 minutes (Docker)
You need: Docker with Compose on a
server (or your laptop; one line below differs), and an
empty git repository on any host (GitHub, GitLab, Bitbucket, Azure DevOps,
self-hosted) to hold your knowledge base. The app seeds it with a starter
template on first run.
git clone https://github.com/Bevel-Software/Hexis.git
cd Hexis
cp .env.example .env
Open .env and fill in the four required values (everything else can wait):
ADMIN_EMAIL=you@example.com # the deployment owner, always an admin
ADMIN_PASSWORD=pick-something # sign-in password; only with password login (SSO-only deployments drop it, see below)
JWT_SECRET=… # generate with the command below
SECRETS_ENC_KEY=… # generate with the command below
Generate the two secrets (run twice, paste one result into each):
node -e " console.log(require('crypto').randomBytes(32).toString('base64')) "
# no Node installed? docker run --rm node:22-slim node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
For a public deployment, also set the origin values:
PUBLIC_BACKEND_URL=https://bevel.your-domain.com # public origin; OAuth redirects are built from it
PUBLIC_FRONTEND_URL=https://bevel.your-domain.com # same origin: the backend serves the SPA
Then start everything (Postgres + the app). Behind a reverse proxy
(Coolify, Traefik, nginx; recommended):
docker compose -f docker-compose.yml up -d
Also set TRUST_PROXY to your proxy hop count ( 1 for a single proxy), so
rate limits see real client IPs instead of the proxy's.
The explicit -f skips docker-compose.override.yml , so the app publishes
no host port : your proxy reaches it on port 3001 over the compose
network. This is deliberate: a fixed published port makes every redeploy fail
with port is already allocated , because the replacement container starts
while the outgoing one still holds it.
Directly exposed (no proxy): plain docker compose up -d publishes
:3001 ; use APP_PORT=8080 docker compose up -d for a different host port.
Leave TRUST_PROXY unset here. With no proxy in front, trusting forwarded
headers would let clients spoof their own address.
Open your domain and sign in with ADMIN_EMAIL / ADMIN_PASSWORD .
Just trying it on your laptop? Same steps, minus the origin values: plain
docker compose up -d , then open http://localhost:3001 .
First sign-in: the setup screen
The app asks for the things it could not guess, and tests them against the
real host before saving :
Knowledge-base repo : the https clone URL of that empty repository.
Git credential : a token with read/write access to it (for GitHub: a
fine-grained personal access token with Contents: read & write on that one
repo is enough).
Branch model : which branch is the default and which are protected
(changes to protected branches only land through approved change requests).
The repository's real branches are offered as suggestions; for an empty repo
the default ( main ) is fine.
Since the repo is empty, the app initialises it from the bundled template and
writes a roles.yaml whose first Admin is you. That's it: you're in the
workspace. Head to Skills & Tools to make your first group and skill, and to
Connect (in the app menu) to hook up an agent over MCP.
Prefer configuring by environment instead of the setup screen? Every one of
those values has an env var ( KB_REPO_URL , GIT_TOKEN , DEFAULT_BRANCH , …);
anything set in the environment wins over the setup screen. See
.env.example .
State that survives redeploys : Postgres data plus three app volumes
(workspace clones, diff-review backups, tool-chain spill files) are named
volumes, so a redeploy or image rebuild loses nothing. Back up the pgdata
volume and your knowledge-base git repo; everything else is derivable.
Health : GET /api/health . First boot can take a minute or two while it
runs migrations and seeds the knowledge-base repo.
Single sign-on : set OIDC_ISSUER_URL / OIDC_CLIENT_ID /
OIDC_CLIENT_SECRET (any spec-compliant provider) or configure it on the
setup screen, which shows you the redirect URI to register. For SSO-only
deployments set LOGIN_PASSWORD=false and drop ADMIN_PASSWORD . If your
issuer is multi-tenant (Google, Entra common ), set
ALLOWED_EMAIL_DOMAINS . SSO auto-provisions accounts, and that list is the
only signup boundary.
Local development (run from source)
You need: Node 22 ( .nvmrc ; the engine range is >=22 <23 ),
pnpm 10 , git ≥ 2.41 , and a Postgres 17 (the bundled one is fine):
docker compose up -d db # just the database
pnpm install
pnpm build # builds the packages the apps import
cp .env.example .env # fill the same four required values;
# the default DATABASE_URL already points at the bundled db
pnpm dev # backend on :3001, Vite dev server on :5173
Open http://localhost:5173 (the dev server proxies to the backend). Useful
commands: pnpm test , pnpm typecheck , pnpm lint .
Migrations run automatically on boot; there is no separate migrate step, in
dev or in production.
The four required values, then the rest. Everything marked setup screen
can be left unset and configured in the app at first sign-in (env always wins).
.env.example documents every variable in full.
port is already allocated on redeploy : you're behind a proxy but ran
compose without -f docker-compose.yml , so the override published a host
port. Deploy with the explicit -f (see above).
Changed POSTGRES_PASSWORD but can't connect : Postgres applies those
values only when its data volume is first created. In dev,
docker compose down -v resets it; in production, change the password in
the database itself.
pnpm install fails on Node version : the engine range is strict
( >=22 <23 ) because of a native dependency's ABI. nvm use picks up
.nvmrc .
Setup screen rejects the git token : the token needs read and write
(push) access to the KB repository; the setup screen's test tells you which
half failed. On GitHub, fine-grained tokens also need the repo explicitly
selected.
Changed ADMIN_PASSWORD and nothing happened : it's read once at
startup; restart the app container.
App unhealthy right after first start : give it the start_period
(~90s); first boot runs migrations and seeds the KB repo before answering.
Path
What it is
packages/shared
@bevel-software/platform-shared : shared types + pure domain utilities
packages/core-backend
@bevel-software/platform-core-backend : the core backend (ships migrations/ + kb-template/ )
packages/c

[truncated]
