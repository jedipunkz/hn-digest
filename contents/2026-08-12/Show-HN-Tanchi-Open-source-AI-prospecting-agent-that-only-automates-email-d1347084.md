---
source: "https://github.com/tanchihq/tanchi"
hn_url: "https://news.ycombinator.com/item?id=49274973"
title: "Show HN: Tanchi – Open-source AI prospecting agent that only automates email"
article_title: "GitHub - tanchihq/tanchi: Autonomous B2B prospecting engine. The AI sources, researches and writes your outreach every evening, you review your queue in ten minutes. Open source, self-hostable. · GitHub"
author: "saysb"
captured_at: "2026-08-12T16:46:35Z"
capture_tool: "hn-digest"
hn_id: 49274973
score: 1
comments: 0
posted_at: "2026-08-12T16:28:30Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Tanchi – Open-source AI prospecting agent that only automates email

- HN: [49274973](https://news.ycombinator.com/item?id=49274973)
- Source: [github.com](https://github.com/tanchihq/tanchi)
- Score: 1
- Comments: 0
- Posted: 2026-08-12T16:28:30Z

## Translation

タイトル: Show HN: Tanchi – メールのみを自動化するオープンソース AI 見込み客エージェント
記事タイトル: GitHub - tanchihq/tanchi: 自律型 B2B 探査エンジン。 AI が毎晩アウトリーチを情報源、調査、書き込みし、キューを 10 分で確認します。オープンソース、自己ホスト可能。 · GitHub
説明: 自律型 B2B 探査エンジン。 AI が毎晩アウトリーチを情報源、調査、書き込みし、キューを 10 分で確認します。オープンソース、自己ホスト可能。 - たんちhq/たんち

記事本文:
GitHub - Tanchihq/tanchi: 自律型 B2B 探査エンジン。 AI が毎晩アウトリーチを情報源、調査、書き込みし、キューを 10 分で確認します。オープンソース、自己ホスト可能。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン 外観設定 プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
/ と入力して検索します。 サインイン サインアップ 外観設定
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
ああ、ああ！
読み込み中にエラーが発生しました。このページをリロードしてください。
タンチク
/
たんち
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く フォルダーとファイル
134 コミット 134 コミット .github .gith

ub アプリ アプリ docker/ aio docker/ aio パッケージ/ 共有パッケージ/ 共有 .dockerignore .dockerignore .env.example .env.example .gitignore .gitignore .gitleaks.toml .gitleaks.toml CLAUDE.md CLAUDE.md COTRIBUTING.md COTRIBUTING.md ライセンス ライセンスREADME.md README.md bun.lock bun.lock bunfig.toml bunfig.toml docker-compose.yml docker-compose.yml package.json package.json すべてのファイルを表示 リポジトリ ファイルのナビゲーション
探査は夜間に行われます。午前中に承認します。
自律型 B2B 探査エンジン — オープンソース、自己ホスト型。
AI は毎晩情報源、調査、執筆、フォローアップを行います。送信される内容を常に制御できます。
システムは、見栄えの良いものからではなく、実際にコンバージョンにつながるものから学習します。
Tanchi は、ほとんどの AI 探査ツールとは反対の立場をとります。
まずはメール。本当に安全に自動化できる唯一のチャネルです。それ以外はすべて支援されます (AI が草案を作成し、人間が送信します)。
検証された知性。見込み客関係書類内のすべての事実は、見込み客自身の Web サイトまたは LinkedIn から取得されます。発明されたことはなく、ギャップが埋められたこともありません。
まずは質的な学習。統計の前に、ICP ごとに何が変換されるかを平易な言葉で抽出します。解釈可能、修正可能で、1 か月目から有効になります。
私たちは返信と会議を測定しますが、決して開かれません。
探知（たんち）とは日本語で「探知」を意味し、暗闇で信号を探すレーダーのことです。
プロジェクトの中心である調達、インテリジェンス パイプライン、エージェント、学習ループについては、 apps/api/README-moteur.md に説明されています。
最速 — 1 つのコンテナー (トライアル / シンプルなセルフホスト)
すべて (PostgreSQL、Redis、API、Web) を 1 つのイメージにまとめます。必要なのは Docker と Anthropic API キーのみです。
docker run -it --name たんち \
-p 8080:8080 \
-v 丹地データ:/var/lib/postgresql/data \
たんちhq/たんち
ターミナルを接続して最初の実行時 ( -i

t )、短いセットアップ ウィザードは、オプションの Hunter.io キーとオプションの SMTP に対して、クロード (API キーまたはクロード CLI) をどのように使用するかを尋ね、すべてをデータ ボリュームに保存してから開始します。次回の実行ではウィザードをスキップします。
完全に非対話型の開始 (CI、ワンライナー) を好みますか?代わりにキーを環境変数として渡します。ターミナルが接続されていない場合、ウィザードはスキップされます。
docker run -d --name たんち \
-p 8080:8080 \
-e ANTHROPIC_API_KEY=sk-ant-... \
-v 丹地データ:/var/lib/postgresql/data \
たんちhq/たんち
後で再構成するには: docker exec -it Tanchientrypoint.sh setup 、コンテナーを再起動します。 claude CLI プロバイダーを選択した場合は、docker exec -it Tanchi claude で 1 回認証します (セッションを維持するには、mount -v Tanchi-claude:/root/.claude)。
http://localhost:8080 を開きます。データベースの移行は自動的に実行されます。セッション シークレットが生成され、最初の起動時にデータ ボリュームに保存されます。 Web アプリと API は 1 つのポートの背後で同じオリジンで提供されるため、Cookie 認証は http://localhost を介してすぐに機能します。
電子メール認証はデフォルトではオフになっています。サインアップすると、アプリに直接ログインします。確認メールは送信されず、リンクをクリックするよう求められることもありません。メーラーがある場合にのみこれをオンにします。 -e REQUIRE_EMAIL_VERIFICATION=true と MAIL_SMTP_HOST (または RESEND_API_KEY ) を組み合わせます。検証をオンにしても、サインアップするとログインできますが、アプリには「メールの検証」画面が表示され、リンクがクリックされるまで API はどこでも 403 を返します。アカウントを検証できないため、API は検証をオンにしてメーラーなしで起動することを拒否します。
フル — docker compose (サービス分割、実際のデプロイメントに推奨)
git clone git@github.com:tanchihq/tanchi.git
CDたんち
cp .env.example .env
# .env を編集: ANTHROPIC_API_KEY、AUTH_SECRET、ENCRYPTION_KEY、POSTGRES_PASSWORD
docker 構成 -d -

-ビルド
ウェブ: http://localhost:8080
ドメインの背後にある認証。実際のデプロイでは、リバース プロキシ (Caddy、Traefik、nginx) 経由で共有親ドメイン (例: app.tanchi.io / api.tanchi.io ) 上の HTTPS 経由で Web アプリと API を提供し、それに応じて .env で APP_URL 、 AUTH_BASE_URL 、 VITE_API_URL を設定します。
スタック: どこでもバン (ランタイム、パッケージ マネージャー、テスト)。 npm/pnpm/yarn はありません。
bun install # ワークスペース全体をインストールします
cp .env.example .env # リポジトリのルートにある単一の .env、両方のアプリによって読み取られます
bun run dev # API + Web を並行して実行します
bun run dev:api # API のみ (http://localhost:3000)
bun run dev:web # Web のみ (http://localhost:5173)
bun run typecheck # typecheck API + Web
bun run test # API テスト
API には PostgreSQL と Redis が必要です。開発で最も単純なもの:
docker compose up -d postgres redis
バンラン移行
リポジトリのルートに 1 つの .env。どちらのアプリもルート .env を読み取ります (API は --env-file 経由、Web は Vite envDir 経由)。必要なキー: DATABASE_URL 、 AUTH_SECRET (≥32 文字)、 ENCRYPTION_KEY (≥44 文字、 openssl rand -base64 32 )。
API テスト スイートはエンドツーエンドです。実際の PostgreSQL および Redis に対して実際の Hono アプリを駆動し、外部境界 (LLM、ソーシング、メールボックス、アウトバウンド Web フェッチ) のみを模擬します。専用のtanchi_testデータベースを使用するため、開発データには決して触れません。すべてのテストで、ケース間のすべてのテーブルが切り捨てられます。
docker compose up -d postgres redis
bun run test # API スイートを実行します (tanchi_test 自体を作成して移行します)
スイートのデフォルトは postgres://postgres:postgres@localhost:5432/tanchi_test および redis://localhost:6379 です。 DATABASE_URL / REDIS_URL をオーバーライドして別の場所を指すようにします。セーフティネットとして、名前に test が含まれていないデータベースは拒否され、 DATABASE_URL がテスト以外のデータベースを指している場合はデータベース名を Tanchi_test に書き換えます。すべてのモジュールのすべてのルートがカバーされています

マルチテナント分離、認証、入力検証、秘密漏洩防止アサーションを備えたものです。
たんち/
アプリ/
api/ # Hono + Bun + PostgreSQL バックエンド (生の SQL、ORM なし) + BullMQ
web/ # React + Vite フロントエンド
パッケージ/
共有/ # 語彙とタイプを前/後ろで共有 (@app/shared)
docker/aio/ # オールインワンイメージ (postgres + redis + api + web)
docker-compose.yml
.env.example
ライセンス番号 AGPL-3.0
各アプリは独自の README.md と CLAUDE.md を保持します (バックエンドとフロントエンドの規則は異なります)。バックエンドは厳密なルールに従います。ORM のない生の SQL、値としてのエラー、コメントなし、すべて不変です。 apps/api/CLAUDE.md を参照してください。
貢献は大歓迎です — COTRIBUTING.md を参照してください。つまり、Bun のみ、各アプリの規則に従い、PR の前に bun run typecheck と bun run test green を実行してください。
AGPL-3.0 。 Tanchi を自由にホストおよび変更できます。変更されたバージョンをネットワーク サービスとして提供する場合は、そのソースを公開する必要があります。
ステータス:初期。当社独自の探査用に構築され、実戦テストされています。荒削りな部分も予想されますが、問題や PR は歓迎です。
自律型 B2B 探査エンジン。 AI が毎晩アウトリーチを情報源、調査、書き込みし、キューを 10 分で確認します。オープンソース、自己ホスト可能。
Readme AGPL-3.0 ライセンス
貢献活動 カスタム プロパティ スター
0 フォーク レポート リポジトリ リリース
© 2026 GitHub, Inc.
フッターナビゲーション
私の個人情報を共有しないでください

## Original Extract

Autonomous B2B prospecting engine. The AI sources, researches and writes your outreach every evening, you review your queue in ten minutes. Open source, self-hostable. - tanchihq/tanchi

GitHub - tanchihq/tanchi: Autonomous B2B prospecting engine. The AI sources, researches and writes your outreach every evening, you review your queue in ten minutes. Open source, self-hostable. · GitHub
Skip to content
Navigation Menu
Sign in Appearance settings Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Type / to search Sign in Sign up Appearance settings
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
Uh oh!
There was an error while loading. Please reload this page .
tanchihq
/
tanchi
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Folders and files
134 Commits 134 Commits .github .github apps apps docker/ aio docker/ aio packages/ shared packages/ shared .dockerignore .dockerignore .env.example .env.example .gitignore .gitignore .gitleaks.toml .gitleaks.toml CLAUDE.md CLAUDE.md CONTRIBUTING.md CONTRIBUTING.md LICENSE LICENSE README.md README.md bun.lock bun.lock bunfig.toml bunfig.toml docker-compose.yml docker-compose.yml package.json package.json View all files Repository files navigation
Your prospecting runs at night. You approve in the morning.
Autonomous B2B prospecting engine — open source, self-hostable.
The AI sources, researches, writes and follows up every night. You stay in control of what gets sent.
The system learns from what actually converts, not from what looks nice.
Tanchi takes the opposite stance to most AI prospecting tools:
Email-first. The only channel that is genuinely safe to automate. Everything else is assisted (the AI drafts, a human sends).
Verified intelligence. Every fact in a prospect dossier is sourced from the prospect's own website or LinkedIn. Never invented, never a filled-in gap.
Qualitative learning first. We distill, in plain language, what converts per ICP before any statistics. Interpretable, correctable, effective from month one.
We measure replies and meetings, never opens.
tanchi (探知) means "detection" in Japanese — the radar sweeping the dark for signals.
The heart of the project — sourcing, the intelligence pipeline, the agents, the learning loop — is described in apps/api/README-moteur.md .
Fastest — one container (trial / simple self-host)
Everything (PostgreSQL, Redis, API, web) in a single image. You only need Docker and an Anthropic API key.
docker run -it --name tanchi \
-p 8080:8080 \
-v tanchi-data:/var/lib/postgresql/data \
tanchihq/tanchi
On first run with a terminal attached ( -it ), a short setup wizard asks how you want to use Claude (API key or the claude CLI), for an optional Hunter.io key and optional SMTP, saves everything to the data volume, then starts. Next runs skip the wizard.
Prefer a fully non-interactive start (CI, one-liner)? Pass the key as an env var instead — the wizard is skipped when no terminal is attached:
docker run -d --name tanchi \
-p 8080:8080 \
-e ANTHROPIC_API_KEY=sk-ant-... \
-v tanchi-data:/var/lib/postgresql/data \
tanchihq/tanchi
To reconfigure later: docker exec -it tanchi entrypoint.sh setup , then restart the container. If you picked the claude CLI provider, authenticate once with docker exec -it tanchi claude (mount -v tanchi-claude:/root/.claude to keep the session).
Open http://localhost:8080 . Database migrations run automatically; session secrets are generated and persisted to the data volume on first boot. Because the web app and API are served same-origin behind one port, cookie auth works out of the box over http://localhost .
Email verification is off by default. Signing up logs you straight into the app: no confirmation email is sent and nothing asks you to click a link. Turn it on only if you have a mailer — -e REQUIRE_EMAIL_VERIFICATION=true together with MAIL_SMTP_HOST (or RESEND_API_KEY ). With verification on, sign-up still logs you in, but the app shows a "verify your email" screen and the API answers 403 everywhere until the link is clicked. The API refuses to boot with verification on and no mailer, since no account could ever be verified.
Full — docker compose (services split, recommended for real deployments)
git clone git@github.com:tanchihq/tanchi.git
cd tanchi
cp .env.example .env
# edit .env: ANTHROPIC_API_KEY, AUTH_SECRET, ENCRYPTION_KEY, POSTGRES_PASSWORD
docker compose up -d --build
Web: http://localhost:8080
Auth behind a domain. For a real deployment, serve the web app and API over HTTPS on a shared parent domain (e.g. app.tanchi.io / api.tanchi.io ) via a reverse proxy (Caddy, Traefik, nginx) and set APP_URL , AUTH_BASE_URL , VITE_API_URL accordingly in .env .
Stack: Bun everywhere (runtime, package manager, tests). No npm/pnpm/yarn.
bun install # install the whole workspace
cp .env.example .env # single .env at the repo root, read by both apps
bun run dev # api + web in parallel
bun run dev:api # api only (http://localhost:3000)
bun run dev:web # web only (http://localhost:5173)
bun run typecheck # typecheck api + web
bun run test # api tests
The API needs PostgreSQL and Redis. Simplest in dev:
docker compose up -d postgres redis
bun run migrate
One .env at the repo root. Both apps read the root .env (the API via --env-file , the web via Vite envDir ). Required keys: DATABASE_URL , AUTH_SECRET (≥32 chars), ENCRYPTION_KEY (≥44 chars, openssl rand -base64 32 ).
The API test suite is end-to-end: it drives the real Hono app against a real PostgreSQL and Redis, and mocks only the external boundaries (LLM, sourcing, mailbox, outbound web fetch). It uses a dedicated tanchi_test database so it never touches your dev data — every test truncates all tables between cases.
docker compose up -d postgres redis
bun run test # runs the API suite (creates and migrates tanchi_test itself)
The suite defaults to postgres://postgres:postgres@localhost:5432/tanchi_test and redis://localhost:6379 ; override DATABASE_URL / REDIS_URL to point elsewhere. As a safety net it refuses any database whose name does not contain test , and rewrites the database name to tanchi_test when DATABASE_URL points at a non-test database. Every route of every module is covered, with multi-tenant isolation, auth, input-validation and secret-non-leakage assertions.
tanchi/
apps/
api/ # Hono + Bun + PostgreSQL backend (raw SQL, no ORM) + BullMQ
web/ # React + Vite frontend
packages/
shared/ # vocabulary and types shared front/back (@app/shared)
docker/aio/ # all-in-one image (postgres + redis + api + web)
docker-compose.yml
.env.example
LICENSE # AGPL-3.0
Each app keeps its own README.md and CLAUDE.md (the backend and frontend conventions differ). The backend follows strict rules: raw SQL without an ORM, errors as values, zero comments, everything immutable — see apps/api/CLAUDE.md .
Contributions are welcome — see CONTRIBUTING.md . In short: Bun only, follow each app's conventions, bun run typecheck and bun run test green before any PR.
AGPL-3.0 . You may host and modify Tanchi freely; if you offer a modified version as a network service, you must publish its source.
Status: early. Built for and battle-tested on our own company's prospecting. Expect rough edges — issues and PRs welcome.
Autonomous B2B prospecting engine. The AI sources, researches and writes your outreach every evening, you review your queue in ten minutes. Open source, self-hostable.
Readme AGPL-3.0 license Contributing
Contributing Activity Custom properties Stars
0 forks Report repository Releases
© 2026 GitHub, Inc.
Footer navigation
Do not share my personal information
