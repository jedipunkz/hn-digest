---
source: "https://ronreiter.github.io/aidr/"
hn_url: "https://news.ycombinator.com/item?id=49351138"
title: "AI;dr – AI wrote it, you shouldn't have to read it"
article_title: "ai;dr — AI; didn't read"
image: ""
author: "ronreiter"
captured_at: "2026-08-18T19:21:15Z"
capture_tool: "hn-digest"
hn_id: 49351138
score: 1
comments: 0
posted_at: "2026-08-18T19:17:59Z"
tags:
  - hacker-news
  - translated
---

# AI;dr – AI wrote it, you shouldn't have to read it

- HN: [49351138](https://news.ycombinator.com/item?id=49351138)
- Source: [ronreiter.github.io](https://ronreiter.github.io/aidr/)
- Score: 1
- Comments: 0
- Posted: 2026-08-18T19:17:59Z

## Translation

タイトル: AI;dr – AI が書いたものなので、読む必要はありません
記事タイトル: ai;dr — AI;読みませんでした
説明: AI が書いたものなので、あなたがそうすべきです

記事本文:
">
ファインダーファイル編集ビュー
月 9:41
あい;博士
他人の AI が 1 つの文を 6 つの段落に詰め込むと、
ai;dr が要約してくれるので、全部読む必要はありません
もの — 小さなモデルを使用する
アプリ内、Mac 上で。
ファインダーファイル編集
月 9:41
コピーしたもの
アイ博士
ローカル モデルは、コピーしたものを要約します。
llama.cpp を使用してインプロセスで実行されるため、Mac からは何も残りません。
10 秒間、またはクリックするまで表示されます。フォーカスが盗まれることはありません。
ai;dr はメニュー バーに常駐し、クリップボードを監視します。コピーするとき
100 文字以上 - 電子メール スレッド、変更履歴、利用規約
ページ、このようなドキュメント — テキストを小さな言語モデルに渡します
(Qwen3-0.6B) llama.cpp を通じてインプロセスで実行されます。サーバーはありません、いいえ
Ollama、API キーもネットワーク呼び出しもありません。
その後、カードがメニューバーのアイコンから太字の一行でドロップされます。
見出しと短い概要が 10 秒間、またはクリックされるまで表示されます。
そして、あなたがしていた事から決して集中力を奪うことはありません。メニューを見逃した場合は、
最後のクリップボードを戻すための「クリップボードの概要を表示」と「最後のコピー」があります。
概要」を参照して、別の場所に貼り付けます。
行き場がないため、何もアップロードされません。の
モデル ファイルはアプリ バンドル内に同梱されており、推論はマシン自体で実行されます
シリコンで動作し、アプリはネットワーク リクエストをまったく行わないため、
飛行機、ロックダウンされたラップトップ、または Wi-Fi が完全にオフになっているとき。
クリップボードは決して変更されず、概要はディスクに書き込まれません。
100 文字フロアが存在するため、その間アプリは静かなままになります。
通常の作業: ファイル パス、変数名、URL、または 1 行のコピー
コマンドは何も生成しません。あなたが実際に起きていることのために目覚めます
スキム — リリースノート、Slack のバックスクロールの壁、法的な文章、スタック
トレース、その段落を収集

「考え？」を貼り付けたague添付。
ダウンロードは約 680 MB で、そのほぼすべてがモデルです
アプリではなくそれ自体。開発者 ID で署名され、公証されています。
Apple なので、インストールする他のものと同様に、最初の起動時に開きます。アップル
推論は Metal バックエンドに対して構築されるため、シリコンのみ。
選択 · ⌘C · それが商品全体です
工程中
llama.cpp はアプリバイナリ内で Qwen3-0.6B を実行します。サーバーを起動したり、デーモンを子守したり、API キーを貼り付けたりする必要はありません。
建設により私有
クリップボードがマシンから離れることはありません。クリップボードが送信される先は何もありません。オフラインでも動作します。
ゼロの儀式
ウィンドウも入力もボタンもありません。すでに行っているようにテキストをコピーします。 4 段落ではなく 1 行を読んでください。
macOS 用のダウンロード
Apple Silicon · ~620 MB、モデル付属 · ディスク イメージを開き、ai;dr をアプリケーションにドラッグします。

## Original Extract

AI wrote it, you shouldn

">
Finder File Edit View
Mon 9:41
ai ; dr
When someone else's AI pads one sentence into six paragraphs,
ai;dr summarizes it for you, so you don't have to read the whole
thing — using a tiny model
inside the app, on your Mac.
Finder File Edit
Mon 9:41
What you copied
ai;dr
A local model summarizes anything you copy.
Runs in-process with llama.cpp, so nothing leaves the Mac.
Shows for ten seconds, or until you click it. No focus stolen.
ai;dr lives in the menu bar and watches the clipboard. When you copy
a hundred characters or more — an email thread, a changelog, a terms-of-service
page, a document like this one — it hands the text to a small language model
(Qwen3-0.6B) running in-process through llama.cpp. There is no server, no
Ollama, no API key, and no network call.
The card then drops from the menu-bar icon with a bold one-line
headline and a short summary, stays for ten seconds or until you click it,
and never steals focus from what you were doing. If you miss one, the menu
has "Show clipboard summary" to bring the last one back, and "Copy last
summary" to paste it somewhere else.
Nothing is uploaded, because there is nowhere for it to go. The
model file ships inside the app bundle, inference runs on your machine's own
silicon, and the app makes no network requests at all — so it works on a
plane, on a locked-down laptop, or with the Wi-Fi switched off entirely.
Your clipboard is never modified, and no summary is written to disk.
The hundred-character floor exists so the app stays quiet during
ordinary work: copying a file path, a variable name, a URL, or a one-line
command produces nothing at all. It wakes up for the things you actually
skim — release notes, a wall of Slack backscroll, a legal paragraph, a stack
trace, the paragraph a colleague pasted with "thoughts?" attached.
The download is around 680 MB, and almost all of that is the model
itself rather than the app. It is signed with a Developer ID and notarized by
Apple, so it opens on first launch like anything else you install. Apple
Silicon only, because the inference is built against the Metal backend.
select · ⌘C · that's the whole product
In-process
llama.cpp runs Qwen3-0.6B inside the app binary. No server to start, no daemon to babysit, no API key to paste.
Private by construction
Your clipboard never leaves the machine — there is nothing it could be sent to. Works offline.
Zero ritual
No window, no typing, no buttons. Copy text like you already do; read one line instead of four paragraphs.
Download for macOS
Apple Silicon · ~620 MB, model included · open the disk image and drag ai;dr to Applications.
