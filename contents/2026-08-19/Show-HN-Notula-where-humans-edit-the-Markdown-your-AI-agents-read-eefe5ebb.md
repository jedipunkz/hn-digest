---
source: "https://notula.org/"
hn_url: "https://news.ycombinator.com/item?id=49365980"
title: "Show HN: Notula – where humans edit the Markdown your AI agents read"
article_title: "Notula - WYSIWYG Markdown editor for docs as code"
image: "https://notula.org/og.png"
author: "anetrebskii"
captured_at: "2026-08-19T20:16:51Z"
capture_tool: "hn-digest"
hn_id: 49365980
score: 1
comments: 1
posted_at: "2026-08-19T19:18:54Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Notula – where humans edit the Markdown your AI agents read

- HN: [49365980](https://news.ycombinator.com/item?id=49365980)
- Source: [notula.org](https://notula.org/)
- Score: 1
- Comments: 1
- Posted: 2026-08-19T19:18:54Z

## Translation

タイトル: Show HN: Notula – AI エージェントが読み取ったマークダウンを人間が編集する場所
記事のタイトル: Notula - コードとしてドキュメント用の WYSIWYG マークダウン エディター
説明: Notula は、コードとしてのドキュメント用の WYSIWYG Markdown エディターです。 git を使用しない人も含め、チーム全体が git リポジトリ内の Markdown に取り組んでいます。

記事本文:
NOTULA 製品の仕組み 安全性ステータスのフィードバック 製品のダウンロード 仕組み 安全性ステータスのフィードバック
早いですが実行中です
WYSIWYG マークダウン エディター。 git リポジトリ内のドキュメント用。
プログラマーでなくても、コードとしてドキュメントを作成できます。いいね
Google ドキュメント、リポジトリから離れることのないファイル:
パッセージにコメントし、返信し、解決し、ツリー内のドキュメントを参照し、
リベースを誰にも見せずに公開します。
Windows 用のダウンロード
何をするか見てみましょう
無料、そして無料のままです。アカウントもサーバーもありません。 macOS と Windows。
ドキュメントはすでに移動されています。
あなたのツールはそうではありませんでした。コードとしてのドキュメントにチームのコストがかかること。
エージェントは、プレーンな .md をネイティブに読み取り、書き込み、レビューします。リポジトリ内のファイルは最も強力な場所であり、エンジニアがすでに変更をレビューしている場所です。
仕様、PRD、RFC、および Runbook は Wiki から離れ、コードの隣に配置されました。それは正しい判断であり、誰も彼らを後退させません。
そして他の人は皆道具を失った
コードエディター、差分、リベースを最初に学ばなければ、コメントもスレッドもブラウズする方法もなく、段落を編集する方法もありません。
Google ドキュメントで使用するものはすべて、
リポジトリから離れることのないファイル。
誰かがコメントした瞬間や名前を知らせる あなたが文書を開いてから誰が変更したかを確認できる 構文を見ずに書く ファイルリストではなくツリーで文書を閲覧する リンクされた文書間を行き来できる ターミナルを開いたことがない人でも使用できる ファイルは自分のリポジトリ内のプレーンな .md のまま AI エージェントは同じファイルを読み取って編集できる 通常のプルリクエストとしてレビューできる ネットワークやベンダーアカウントなしで動作する AI エージェントにワークスペースの構成を伝える git 以外は何も話さないので機能するあなたが持っているもので
ドキュメントのみでコードはありません
数千のソース ファイルを含むリポジトリを開き、クリーンなツリーを取得します。
マークダウン。フォルダー

その中の文書は決して表示されません。
Markdown 構文は一切ありません。ハッシュもアスタリスクもテーブルパイプもありません。
カーソルが置かれている行でも同様です。ディスク上のファイルは通常のままです
マークダウン。
人魚の図は自ら描く
人魚のブロックは文書内の絵であり、矢や矢印の壁ではありません。
括弧。ディスク上では同じフェンスで囲まれたブロックであるため、
ホストも。
実際に見逃しているコメント
文章を選択し、コメントを残し、返信し、解決します。
ドキュメントのすべてのバージョンは、読んだり持ち帰ったりできるようになっています。誰も
リベース、スタッシュ、または切り離された HEAD という単語が表示されることはありません。
あなたが最後に知ることは決してありません
そしてカーソルの下では何も変化しません。
あなたの機械は、彼らが書いた文章とともにあなたに告げます。
本当の問題は順序ですか、それともフィールドの数ですか?
checkout-redesign.md · platform-docs
これをクリックすると、文書の先頭ではなく、その段落が開きます。
ウィンドウが閉じられた状態で、開いていないリポジトリを監視し続けます。
一度に2人が単独で到着します。それ以上のものは一緒に届くので、読まれたままになります。
そして、あなたは行って尋ねる必要はありませんでした。
チームの残りのメンバーが移動したすべてのドキュメントは、誰がいつ移動したかがツリー内にマークされます。
あなた自身の未発表の作品にも同じマークが付けられるので、忘れてしまった引き出しの中に何も残っていません。
仕事中は 30 秒ごとに、仕事をしていないときは数分ごとに表示されます。
すべてはフェッチされたブランチから読み取られるため、画面上のドキュメントは、変更を加えるまではそのままの状態のままです。
リポジトリ内に存在するスレッド
会話はその内容のドキュメントの隣でコミットされるため、会話が到着します
クローンを使用すると、すべてのアカウントよりも存続します。
書類はきれいなままです。目に見えないアンカー、ブロック ID、HTML コメントはありません。
W3C Web アノタに従って、引用された一節とそのコンテキストによってアンカーされます。

ションモデル。
Union マージ ドライバーを使用した追加専用のログなので、2 人が同時にコメントしても競合することはありません。
リンクをたどると、同じペインでドキュメントが開きます。前後に
スクロール位置も含めて、元の場所に正確に戻ります。
すべてのペインは独自の軌跡を保持するため、分割内で開かれた参照が妨げられることはありません。
ドキュメント間の相対リンクは、ホスト上でのリンクの方法を解決します。
非 Markdown リンク ターゲットは、別のアプリに渡されるのではなく、読み取り専用で開きます。
1 文字の編集は 1 文字の差分のままです
Notula は変更された内容のみを書き戻します。 6,000 件の実際のドキュメント全体で、96.4%
のブロックが編集後もそのまま残ります。
エージェントは推測ではなく説明を受けます
Notula はワークスペースがどのように機能するかを書き留め、Claude Code にそれを指示します。
ドキュメントを説明する notula-agents.md を書き込みます
タイプ、フィールド、レイアウト、コマンドの 3 つを借用します。
CLAUDE.md の行を参照してください。それは決してありません
そこに書いた内容を上書きします。
ブリーフィングには、一度破ると見えなくなるルールが定められています。
ドキュメントの名前変更は notula docs move です。
コメント ログはドキュメントのパスの下にファイルされます。
mv のすべてのスレッドが孤児になります。
その背後には、ドキュメント、タイプ、フィールドなどのコマンドラインがあります。
ステータス、すべて --json 付き - したがって、エージェントは
ファイルを推測するのではなく、ワークスペースを使用します。
あなたが開いている文書をエージェントが書き直すと、変更は
破壊されるのではなく、あなたの前に着陸します。
CLAUDE.md 内の 3 行
<!-- notula:begin -->
このリポジトリは Notula で編集されています。
ここに保存される内容: @notula-agents.md
<!-- notula:end --> 3 つのことが起こりますが、どれもあなたの問題ではありません
エンジニアがすでに推進しているものです。 Notula は git ls-files でそれを読み取るため、数千のソース ファイルを含むリポジトリが即座に開き、.gitignore が尊重されます。

無料でd。
見出し、表、リンク、画像。画面上に構文は表示されません。ディスクに配置されるのは、通常のプル リクエストでレビューされる通常の Markdown です。
Notula はバックグラウンドで取得して、何が到着したかを通知します。公開とは、変更内容を 1 つの文で説明するものであり、2 人が同じ段落を編集した場合は、競合マーカーの間ではなく、2 つのバージョンの間で選択することになります。
他の人が作業しているリポジトリ用に構築
Notula は他の人のワークツリーのゲストであり、そのワークツリーのように動作します。
すべての git コマンドには明示的なパス仕様が含まれます。 Notula はワークツリー全体をステージングすることはありません。
同僚の仕事はそのままです
他の人のコミットされていない変更が含まれているリポジトリを開きます。 Notula は決して隠したり、歴史を書き換えたりすることはありません。
Notula は、現在いるブランチにコミットし、ブランチを作成したり切り替えたりすることはありません。保護されている場合は、編集する前にその旨が表示されます。
Notula サーバーもアカウントもありません。文書やコメントなど、
履歴はリポジトリ内のファイルとして存在し、アプリに保存されるのは唯一のものです
対話先は独自の git リモートとそれが更新するリリース ページです
それ自体から。
早いですが、走っています。これが今日の各部の正直な状態です。
画面上に構文を使用せずに WYSIWYG を記述します。表、図、画像、脚注、コード マークダウンのラウンドトリップ 6,000 の実際のドキュメント上でバイトが同一で、編集後も 96.4% のブロックがそのまま存続 ワークスペースとナビゲーション ツリー、クイックオープン、全文検索、分割ペイン、ペインごとの前後送り、履歴 コメントとレビュー スレッド、返信、メンション、提案された編集、レビューの受信箱とデスクトップ通知 Git バックグラウンドの取得、一文での公開、2 つの選択肢の競合段落 macOS は Apple Silicon と Intel をダウンロードし、それ自体を更新します。まだ署名されていないため、開くには 1 つのコマンドが必要です。 Windows ダウンロード x64 および arm64 用のインストーラーで、自動的に更新されます。いいえ

まだ署名されていないため、SmartScreen が一度尋ねます Linux まだビルドされていません
重要な決定は、コードの前に次のように書き留められます。
GitHub 仕様キット 。ソースは
private while the app is this young.の
ダウンロードは行われず、すべてのビルドにリリースが含まれます
何が変わったのかメモ。
If you already have a Markdown editor
Obsidian A better personal knowledge base. A vault is not a repository other people review. Notion、Confluence にはレビュー レイヤーがあり、ドキュメントを独自のサーバーに保存します。 HackMD Has both, and asks somebody to host it. GitBook は美しく公開され、ソースを所有します。 VS Code にはすでにリポジトリがあり、段落のコメントについては何も言う必要はありません。 Google ドキュメントは依然としてドキュメントのレビューがどのようなものであるかを示すリファレンスであり、ドキュメントはリポジトリではない場所に保存されます。作業リポジトリを指定する前に人々が尋ねる質問
What is a WYSIWYG Markdown editor?
入力中に、それを生成する構文ではなく、書式設定された文書が表示されるもの。見出しの前にハッシュ、太字の周りにアスタリスク、表にパイプはありません。これは、生のテキストを 1 つのペインに配置し、レンダリングを別のペインに配置するライブ プレビューとは異なります。 Notula には生のペインがありません。画面上のドキュメントがドキュメントであり、ディスク上に置かれるのは通常の Markdown です。
ドキュメントをソフトウェアと同じリポジトリにプレーンテキストで保存し、コードと同じようにレビューしてバージョン管理します。 It solved the problem of documentation drifting away from what actually shipped, and it created a new one: only the people fluent in git could take part. Notula は 2 番目の問題に対する答えであり、最初の問題に代わるものではありません。
編集したファイルとその横のコメント ログのみ。すべての git コマンドには明示的なパス仕様が含まれます。 It never stages the whole w

orktree は決して隠したり、履歴を書き換えたり、ブランチを作成したり切り替えたりすることはありません。
いいえ、フォルダーを開いて、何が変わったかを 1 文で書いて公開します。リベース、スタッシュ、または競合マーカーは誰にも表示されません。
プライベートリポジトリでも動作しますか?
はい。 Notula はマシンにすでにインストールされている git を駆動するため、すでに使用している SSH キーまたは資格情報ヘルパーが使用されます。パスワードを要求されることはなく、独自のトークンも保存しません。
ユニオン マージ ドライバーを使用した追加専用ログとして、ドキュメントの隣のリポジトリにコミットされます。それらはクローンとともに到着し、すべてのアカウントよりも存続し、文書自体はクリーンな状態を保ちます。
大規模なリポジトリでは速度が低下しますか?
いいえ。ディスクを移動するのではなく git にファイル リストを要求し、Markdown を保持し、無料の .gitignore を尊重します。数千のソース ファイルは、ドキュメントのツリーとして開かれます。
私が見ているものはどれくらい最新のものですか?
Notula は、作業中は 30 秒ごと、作業していないときは数分ごとにチェックを行います。届いたものにはすべて、それを書いた人の名前がマークされ、それを受け取るまでマークされたままになります。画面上の文書が自動的に変更されることはありません。
はい、無料のままです。アカウントもサーバーも、料金を支払うシートもありません。有料のサービスが登場するとしたら、それはドキュメントを公開ページに公開するなど、背後にサーバーを備えたサービスとなるでしょう。あなたのマシン上のアプリはそうではありません。
同じリポジトリでクロード コードを使用し続けることはできますか?
それがポイントです。 Notula はワークスペースについて説明するブリーフィングを作成し、それを CLAUDE.md からリンクするので、エージェントは規則を理解できます。あなたが開いている文書を書き換えると、その変更があなたの目の前に現れます。
Windows 用の WYSIWYG Markdown エディタはありますか?
はい。 Notula は、x64 および arm64 上の Windows 用と、Apple Silico 上の macOS 用に出荷されます。

n and Intel, from the same page. Both update themselves from then on. Linux が続きます。
GitHub リポジトリ内の Markdown ファイルにコメントできますか?
プルリクエストを開かない限り、GitHub 自体の中にはありません。 Notula は、複製されたリポジトリ (GitHub、GitLab、Bitbucket、Gitea、またはリモートがまったくないリポジトリ) 内の任意の Markdown ファイルにコメント スレッドを追加し、追加専用ログとしてドキュメントの横にコミットします。彼らはクローンとともに到着しますが、Markdown には何も書き込まれません。
Does it render Mermaid diagrams?
はい、フェンスとして示されるのではなく、ドキュメント内に描かれます。ディスク上では通常の人魚のコード ブロックのままであるため、GitHub と Obsidian もそれをレンダリングします。
いいえ、Notula は、既に所有しているファイル、Markdown、リポジトリ、git 上のスタジオであり、独自のものは何も保持しません。コメント ログはドキュメントの横にコミットされたプレーン ファイルであり、ワークスペース スキーマは公開され、notula コマンドはウィンドウのすべての処理を実行します。アプリを削除しても、すべてのドキュメントとすべてのコメントはまだ存在しており、アプリを削除しなくても読むことができます。
Your docs are already Markdown.使えるようにしてください。
Free, and free of accounts. macOS on Apple Silicon and Intel, Windows on
x64 と arm64 は、両方ともそれ以来自動的に更新されます。 Linux が続きます。
ダウンロード

[切り捨てられた]

## Original Extract

Notula is a WYSIWYG Markdown editor for docs as code. The whole team works on the Markdown in your git repo, including the people who do not use git.

NOTULA Product How it works Safety Status Feedback Download Product How it works Safety Status Feedback
Early, but running
WYSIWYG Markdown editor. For the docs in your git repo.
Docs as code , without having to be a coder. Like
Google Docs , on files that never leave the repository :
comment on a passage, reply, resolve, browse the docs in a tree, and
publish without anybody being shown a rebase .
Download for Windows
See what it does
Free, and it stays free. No account, no server. macOS and Windows.
Your documentation already moved.
Your tools did not. What docs as code costs a team .
Agents read, write and review plain .md natively. Files in the repo are where they are strongest, and where your engineers already review changes.
Specs, PRDs, RFCs and runbooks left the wiki and landed next to the code. That was the right call, and nobody is moving them back.
And everyone else lost their tools
No comments, no threads, no way to browse, no way to edit a paragraph without learning a code editor, a diff and a rebase first.
Everything you use in Google Docs,
on files that never leave your repository.
Told the moment somebody comments or names you See who has changed a document since you opened it Write without seeing any syntax Browse documents in a tree, not a file list Back and forward between linked docs Usable by someone who has never opened a terminal Files stay plain .md in your own repository AI agents can read and edit the same files Reviews as a normal pull request Works with no network and no vendor account Tells your AI agent how the workspace is organised Speaks git and nothing else, so it works with what you have
Only the documentation, none of the code
Open a repository with thousands of source files and get a clean tree of
Markdown. Folders with no documents in them never appear.
No Markdown syntax, ever. No hashes, no asterisks, no table pipes, not
even on the line your cursor is on. The file on disk stays ordinary
Markdown.
Mermaid diagrams draw themselves
A mermaid block is a picture in the document, not a wall of arrows and
brackets. On disk it is still the same fenced block, so it draws on the
host too.
The comments you actually miss
Select a passage, leave a comment, reply, resolve.
Every version of the document is there to read and to bring back. Nobody
is ever shown a rebase, a stash, or the words detached HEAD.
You are never the last to know
and nothing ever changes under your cursor.
Your machine tells you, with the sentence they wrote.
Is the order the real problem, or the number of fields?
checkout-redesign.md · platform-docs
Clicking it opens the document at that paragraph, not at the top.
It keeps watching repositories you do not have open, with the window closed.
Two at a time arrive on their own; more than that arrive together, so they stay read.
And you did not have to go and ask.
Every document the rest of the team has moved on is marked in the tree, with who moved it and when.
Your own unpublished work is marked the same way, so nothing waits in a drawer you forgot about.
It looks every thirty seconds while you are working, and every few minutes while you are not.
All of it is read from the fetched branch, so the document on screen is still exactly what you left until you take the change.
Threads that live in the repository
A conversation is committed next to the document it is about, so it arrives
with the clone and outlives every account.
Documents stay clean. No invisible anchors, no block IDs, no HTML comments.
Anchored by the quoted passage and its context, following the W3C Web Annotation model.
An append-only log with a union merge driver, so two people commenting at once cannot conflict.
Follow a link and the document opens in the same pane. Back and forward
return you to exactly where you were, scroll position included.
Every pane keeps its own trail, so a reference held open in a split is never disturbed.
Relative links between documents resolve the way they do on the host.
Non-Markdown link targets open read-only instead of being handed to another app.
A one-character edit stays a one-character diff
Notula writes back only what changed. Across 6,000 real documents, 96.4%
of blocks survive an edit untouched.
Your agent gets a briefing, not a guess
Notula writes down how the workspace works, and points Claude Code at it.
It writes notula-agents.md describing the document
types, the fields, the layout and the commands, and borrows three
lines of your CLAUDE.md to point at it. It never
overwrites what you wrote there.
The briefing carries the rules that are invisible once broken.
Renaming a document is notula docs move , because the
comment log is filed under the document's path and a plain
mv orphans every thread on it.
There is a command line behind that - documents, types, fields,
status, all with --json - so an agent operates the
workspace instead of guessing at files.
When an agent rewrites the document you have open, the change
lands in front of you instead of being clobbered.
The three lines in your CLAUDE.md
<!-- notula:begin -->
This repository is edited with Notula.
What it keeps here: @notula-agents.md
<!-- notula:end --> Three things happen, and none of them are your problem
The one your engineers already push to. Notula reads it with git ls-files, so a repository with thousands of source files opens instantly and .gitignore is respected for free.
Headings, tables, links and images, with no syntax on screen. What lands on disk is ordinary Markdown that reviews in a normal pull request.
Notula fetches in the background and tells you what arrived. Publishing is one sentence about what changed, and if two people edited the same paragraph you pick between two versions, not between conflict markers.
Built for repositories other people are working in
Notula is a guest in someone else's worktree and behaves like one.
Every git command carries an explicit pathspec. Notula never stages the whole worktree.
Your colleague's work is untouched
Open a repository that has someone else's uncommitted changes in it. Notula never stashes and never rewrites history.
Notula commits to the branch you are on and never creates or switches one. If it is protected, it says so before you edit.
There is no Notula server and no account. Documents, comments and
history live as files in the repository, and the only things the app
talks to are your own git remote and the releases page it updates
itself from.
Early, but running. This is the honest state of each part today.
Writing WYSIWYG with no syntax on screen. Tables, diagrams, images, footnotes, code Markdown round trip Byte-identical on 6,000 real documents, and 96.4% of blocks survive an edit untouched Workspace and navigation Tree, quick open, full-text search, split panes, per-pane back and forward, history Comments and review Threads, replies, mentions, suggested edits, a review inbox and desktop notifications Git Background fetch, publishing in one sentence, conflicts as a choice between two paragraphs macOS download Apple Silicon and Intel, updating itself. Not signed yet, so opening it takes one command Windows download An installer for x64 and arm64, updating itself. Not signed yet, so SmartScreen asks once Linux Not built yet
The decisions that matter are written down before the code, using
GitHub Spec Kit . The source is
private while the app is this young. The
downloads are not , and every build carries the release
notes for what changed.
If you already have a Markdown editor
Obsidian A better personal knowledge base. A vault is not a repository other people review. Notion, Confluence They have the review layer, and they keep the document on their own server. HackMD Has both, and asks somebody to host it. GitBook Publishes beautifully, and owns the source. VS Code Has the repository already, and nothing to say about a comment on a paragraph. Google Docs Still the reference for what reviewing a document should feel like, and the document ends up somewhere your repository is not. Questions people ask before they point it at a work repository
What is a WYSIWYG Markdown editor?
One that shows you the formatted document while you type, instead of the syntax that produces it. No hashes in front of headings, no asterisks around bold, no pipes in tables. It is not the same as live preview, which puts the raw text in one pane and the rendering in another. Notula has no raw pane: the document on screen is the document, and what lands on disk is ordinary Markdown.
Keeping the documentation in the same repository as the software, in plain text, reviewed and versioned the way the code is. It solved the problem of documentation drifting away from what actually shipped, and it created a new one: only the people fluent in git could take part. Notula is an answer to the second problem, not a replacement for the first.
Only the files you edit and the comment log beside them. Every git command carries an explicit pathspec. It never stages the whole worktree, never stashes, never rewrites history, and never creates or switches a branch.
No. You open a folder, write, and publish with one sentence about what changed. Nobody is shown a rebase, a stash, or a conflict marker.
Does it work with private repositories?
Yes. Notula drives the git already installed on your machine, so the SSH key or credential helper you already use is the one it uses. It never asks you for a password and stores no token of its own.
Committed in the repository next to the document, as an append-only log with a union merge driver. They arrive with a clone and outlive every account, and the documents themselves stay clean.
Will it slow down on a large repository?
No. It asks git for the file list rather than walking the disk, keeps the Markdown, and respects .gitignore for free. Thousands of source files open as a tree of documents and nothing else.
How current is what I am looking at?
Notula checks every thirty seconds while you are working, and every few minutes when you are not. Anything that arrived is marked with the name of whoever wrote it, and stays marked until you take it - the document on your screen never changes on its own.
Yes, and it stays free. No account, no server, no seat to pay for. If something paid ever appears it will be a service with a server behind it, like publishing a document to a public page. The app on your machine is not that.
Can I keep using Claude Code in the same repository?
That is the point. Notula writes a briefing describing the workspace and links it from your CLAUDE.md, so the agent knows the conventions. When it rewrites a document you have open, the change lands in front of you.
Is there a WYSIWYG Markdown editor for Windows?
Yes. Notula ships for Windows on x64 and arm64, and for macOS on Apple Silicon and Intel, from the same page. Both update themselves from then on. Linux follows.
Can I comment on a Markdown file in a GitHub repo?
Not inside GitHub itself, unless you open a pull request for it. Notula adds comment threads to any Markdown file in a cloned repository - GitHub, GitLab, Bitbucket, Gitea, or no remote at all - and commits them beside the document as an append-only log. They arrive with a clone, and nothing is written into the Markdown.
Does it render Mermaid diagrams?
Yes, drawn in the document rather than shown as a fence. On disk it stays an ordinary mermaid code block, so GitHub and Obsidian render it too.
No. Notula is a studio over files you already own, your Markdown and your repository and your git, and it keeps nothing of its own. The comment log is a plain file committed beside the documents, the workspace schema is published, and the notula command does everything the window does. Delete the app and every document and every comment is still there, readable without it.
Your docs are already Markdown. Make them usable.
Free, and free of accounts. macOS on Apple Silicon and Intel, Windows on
x64 and arm64, both updating themselves from then on. Linux follows.
Download fo

[truncated]
