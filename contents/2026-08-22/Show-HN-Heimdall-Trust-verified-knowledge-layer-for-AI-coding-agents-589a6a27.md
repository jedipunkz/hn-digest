---
source: "https://github.com/ArihantDeva/heimdall"
hn_url: "https://news.ycombinator.com/item?id=49395883"
title: "Show HN: Heimdall – Trust-verified knowledge layer for AI coding agents"
article_title: "GitHub - ArihantDeva/heimdall: Verified, self-healing knowledge layer for AI coding agents — ranked search with trust verdicts (STRONG/WEAK/STALE/REBUILT), session auto-sync, grep guard. Orchestration over Graft-style semantic memory backends. · GitHub"
image: "https://opengraph.githubassets.com/5028bb017e46d4217370e0d97dcd9d308088d9831fd779062086fbbf1d76032f/ArihantDeva/heimdall"
author: "arihantdeva"
captured_at: "2026-08-22T02:09:18Z"
capture_tool: "hn-digest"
hn_id: 49395883
score: 1
comments: 0
posted_at: "2026-08-22T02:02:14Z"
tags:
  - hacker-news
  - translated
---

# Show HN: Heimdall – Trust-verified knowledge layer for AI coding agents

- HN: [49395883](https://news.ycombinator.com/item?id=49395883)
- Source: [github.com](https://github.com/ArihantDeva/heimdall)
- Score: 1
- Comments: 0
- Posted: 2026-08-22T02:02:14Z

## Translation

タイトル: Show HN: Heimdall – AI コーディング エージェントのための信頼性が検証された知識レイヤー
記事のタイトル: GitHub - ArihantDeva/heimdall: AI コーディング エージェント向けの検証済みの自己修復ナレッジ レイヤー — 信頼判定 (STRONG/WEAK/STALE/REBUILT) によるランク付け検索、セッション自動同期、grep ガード。 Graft スタイルのセマンティック メモリ バックエンド上のオーケストレーション。 · GitHub
説明: AI コーディング エージェント用の検証済みの自己修復ナレッジ レイヤー — 信頼判定 (STRONG/WEAK/STALE/REBUILT) によるランク付け検索、セッション自動同期、grep ガード。 Graft スタイルのセマンティック メモリ バックエンド上のオーケストレーション。 - アリハントデヴァ/ヘイムダル

記事本文:
GitHub - ArihantDeva/heimdall: AI コーディング エージェント用の検証済みの自己修復ナレッジ レイヤー — 信頼判定 (STRONG/WEAK/STALE/REBUILT) によるランク付け検索、セッション自動同期、grep ガード。 Graft スタイルのセマンティック メモリ バックエンド上のオーケストレーション。 · GitHub
コンテンツにスキップ
ナビゲーションメニュー
サインイン 外観設定 プラットフォーム AI コード作成 GitHub Copilot AI を使用してより良いコードを作成する
GitHub Copilot アプリ エージェントが発行からマージまで直接担当
MCP レジストリ 外部ツールの統合
開発者のワークフロー アクション あらゆるワークフローを自動化します
コードスペース インスタント開発環境
コードレビュー コードの変更を管理する
コードの品質 マージ時に品質を強制する
アプリケーションセキュリティ GitHub Advanced Security 脆弱性を見つけて修正する
コードのセキュリティ 構築時にコードを保護する
機密保護 漏洩が始まる前に阻止
企業規模別のソリューション
タイプごとに詳しく見る お客様の事例
サポートとサービスのドキュメント
オープンソース コミュニティ GitHub スポンサー オープンソース開発者に資金を提供する
エンタープライズ エンタープライズ ソリューション エンタープライズ プラットフォーム AI を活用した開発者プラットフォーム
利用可能なアドオン GitHub Advanced Security エンタープライズ グレードのセキュリティ機能
Copilot for Business エンタープライズ グレードの AI 機能
プレミアム サポート エンタープライズ レベルの 24 時間年中無休のサポート
検索 / サインイン サインアップ 外観設定
別のタブまたはウィンドウでサインインしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでサインアウトしました。リロードしてセッションを更新します。
別のタブまたはウィンドウでアカウントを切り替えました。リロードしてセッションを更新します。
アラートを閉じる
{{ メッセージ }}
アリハントデヴァ
/
ヘイムダル
公共
通知
通知設定を変更するにはサインインする必要があります
追加のナビゲーション オプション
コード
main ブランチ タグ ファイルに移動 コード その他のアクション メニューを開く 最新のコミット
17 コミット 17 コミット フォルダーとファイル
ビンビン構成設定

ig docs docs extensions extensions launchd launchd testing テスト タイプ タイプ ベンダー ベンダー .gitignore .gitignore ライセンス ライセンス README.md README.md package-lock.json package-lock.json package.json package.json tsconfig.json tsconfig.json すべてのファイルを表示 リポジトリ ファイル ナビゲーション
エージェントは、すでに行った作業を再構築し続けます。ヘイムダルがそれを止める。
すべての AI コーディング セッションはコールドで始まります。リポジトリ全体の grep は「did」に応答できません
この問題は別のプロジェクトですでに解決していますか?」 — 答えは別の場所にあります
散文で説明されているディレクトリは、開いたことのないパスの下にあります。結果:
同じ作品を3回作り直しました。
Heimdall は、AI コーディングのための自己修復型で信頼性が検証された知識レイヤーです
エージェント。エージェントの動作を監視し、セマンティック メモリ グラフを最新の状態に保ちます
あなたが触れるすべてのプロジェクト、そして他の誰も構築しない部分にラベルが付けられます
すべての検索に信頼性の判定がヒットするため、エージェントが死者に対して行動することはありません
パスまたは幻覚マッチ。
信頼性の判定 (差別化要因)
すべての検索結果は、エージェントが確認する前に現実と照合して検証されます。
STRONG — パスがディスク上に存在し、強力な語彙範囲、およびファイルの
実際のコンテンツがクエリに答える (コンテンツに応じたスコアリング)
WEAK — セマンティック一致のみ。もっともらしいが未検証
再構築 — ファイルは移動されました。ヘイムダルはそれを見つけて自動的に再固定した
STALE / REMOVED — デッドパス。ログに記録され、削除されたため、ランク付けが停止されます。
エージェントが行き止まりで行動するのは、応答がないより悪いです。評決こそが
グラフに基づいて行動するのに十分な信頼性があること。
同時エージェント下での自己修復
40 人のエージェントが一度に 1 つのファイルを編集できます。グラフは依然として正確に収束します
ディスク上にあるものは、Heimdall が何が変更されたかを決して推論しようとしないためです。
イベント駆動ではなく、レベルによってトリガーされます。フック、ウォッチャー、またはスクリプトは、
「この道を見てください」と言うだけです。それは私です

何が起こったのか決して信じていませんでした。
リコンサイラーはディスクからファイルを読み取り、グラフをそれと一致させます。あ
見逃したヒント、重複したヒント、または完全に間違ったヒントは 1 ステータスを消費します。
構成によるシングルライター。すべてのグラフの突然変異は 1 つを通過します。
O_EXCL ロック。 2 人のライターが存在することはできないため、インターリーブは行われません。
レース — ファイルが一時的に消えた古い削除してから挿入するウィンドウ
グラフが消えてしまいました。
正確な所有権。すべてのノードとエッジは 1 つのパスにのみ属します。あ
commit は、1 つのトランザクションでそのパスのすべての行を削除して再挿入します。
削除したシンボルはゴーストとして存続できません。
コンテンツのハッシュはオラクルです。同じバイト、同じ深さ ⇒ 同じグラフ。つまり
2 回和解することが 1 回和解することと同じである理由と理由
同時編集は単に衝突する可能性が低いだけでなく、無害です。
注文の独立性。まだインデックスが作成されていないシンボルへのファイル間エッジ
保留エッジとして保留され、どちらかの側から解決されるため、最終的なグラフは
どのファイルが最初にリコンサイルされたかには依存しません。
バックストップとしての監査。 heimdall verify はジャーナルを
ファイルシステムとドリフト時に非ゼロで終了します。これは、としての精度の主張です。
CI に追加できるコマンド。 --深く再ハッシュし、次のような書き換えもキャッチします。
保存されたサイズと mtime。
正直な保証は、瞬間的なものではなく、限界のある古さの収束です。
正確さ: 編集と次の調整パスの間では、グラフは遅れています。
峠を越えて生き残るという点では決して間違ってはいない。
heimdall デーモン # 単一のライター: タイマーで監視、調整、監査
heimdall reconcile # 今すぐ収束します (ワンショット; 同じロックを取得します)
heimdall reconcile --all # 詳細な監査 + すべてを修復する
heimdall verify --deep # 読み取り専用ドリフト レポート; 1 番出口がある場合は出口 1 を出ます。 CIセーフ
深さレベル
ノードは深さでインデックス付けされます。デフォルトの

d 推奨値は最大です
— グラフはファイルが存在することだけでなく、どの関数やクラスが存在するかを認識します。
そこに住んでいて、どの行にいて、何が何を呼んでいるのか。
max は、マシンが実際に実行できるものすべてに解決されます。シンボルとグラフの深さ
ツリーシッターが必要です。これがないと、パスはファイルの深さに応じて劣化します。
ツリーシッターが消えると、次の監査で自動的にアップグレードされます。
が表示されます。マシンの能力を超える深さの要求は、次の点に限り尊重されます。
可能であり、クランプされたと報告されます。
heimdall Depth # このマシンでできることとその理由
heimdall の深さ src/server.ts # 1 つのパスの要求された深さと有効な深さ
抽出は LLM 呼び出しではなく、ツリーシッター AST 解析です。深さは CPU を消費しますが、決して必要ありません。
トークン。
npm i -g ヘイムダル
# ワンタイム: グラフト バックエンド (ローカル セマンティック メモリ デーモン) をインストールします
# https://github.com/tinygrad/graft を参照 — `graft` バイナリを PATH に置きます
# (例: ~/.local/bin/graft) そして ~/.graft/config.yaml を作成します。
# cp "$(npm root -g)/heimdall/config/heimdall.yaml.example" ~/.graft/config.yaml
# ハーネスに配線します (pi | claude-code | codex |cursor | Windsurf | all)
heimdall init --harness claude-code
# バックエンドが正常であることを確認する
ヘイムダル博士
# あなたが関わったすべてのプロジェクトを検索します
Heimdall 検索「Excel トラッカー ポートフォリオ最適化」
# エージェントが何かを完了したときに再利用可能な作業を記録する
heimdall insert --title " poker jam_opt オプティマイザー " \
--body " ~/Repos/poker-bot/tools — ヘッズアップ ジャム/フォールド EV オプティマイザー " \
--キーワード ポーカー、最適化
init 、挿入すると、ハーネス配線はすぐに機能します。検索して
医師は移植片バックエンドを別途インストールする必要があります (ソースから構築 —
埋め込みモデルのダウンロードが含まれているため、すぐにはダウンロードできません)。ベンダー/グラフト/
ソース + 帰属表示であり、事前に構築されたバイナリではありません。
ハーネス
コマンド
統合
円周率
ヘイムダル

init --ハーネスパイ
ネイティブ拡張機能: kb_search / kb_insert / kb_sync ツール、編集自動同期、セッション指向
クロード・コード
heimdall init --harness claude-code
PostToolUse フックはパス ヒントとメモリ スニペットを出力します
コーデックス CLI
heimdall init --ハーネス コーデックス
AGENTS.md の検索/挿入命令
カーソル
heimdall init --ハーネス カーソル
検索優先ワークフローを使用したルール ファイル
ウィンドサーフィン
heimdall init --ハーネス ウィンドサーフィン
検索優先ワークフローを使用したルール ファイル
各アダプターがインストールするものについては、docs/adapters.md を参照してください。
コンポーネント
ファイル
仕事
CLI
bin/heimdall.js
init / 検索 / 挿入 / ドクター / デーモン / 調整 / 検証 / 深度 / ヒント
ランキング検索
bin/kb-search.sh
上位 k 位の候補者 + グラフ ウォーク、 --scope フィルター
信頼性の検証
bin/kb_search_verify.py
コンテンツを意識した強い / 弱い / 古い / 再構築の判定
調停者
bin/lib/reconcile.mjs
レベルトリガー収束: ディスクを読み取り、グラフを一致させます
日記
bin/lib/journal.mjs
権威インデックス: 所有権、ハッシュ、世代、重複排除キュー
シングルライターロック
bin/lib/lock.mjs
O_EXCL は、グラフの変更が通過するたびにロックします。
深さラダー
bin/lib/ Depth.mjs 、 bin/lib/extract.mjs
パス/ファイル/シンボル/グラフ;ツリーシッターの抽出
デーモン
bin/heimdall-reconciler.mjs
監視 + ドレイン + 定期監査、ロックの保持
編集ログの再生
bin/sync-edits.sh
ワンショットブートストラップ: セッション編集ログ→ヒント→調整
ヒントエミッター
拡張機能/kb-autosync.ts
ハーネスフック。 「このパスを見てください」を追加し、グラフを書きません
サーチガード
拡張機能/kb-search-guard.ts
メモリを参照せずに grep スタイルの検索を 3 回実行した後、エージェントに警告します
セッションのオリエンテーション
拡張機能/kb-orient.ts
関連する以前の作業をセッションの最初のプロンプトに挿入します
健康状態と遠隔測定
bin/kb-health.sh 、 bin/telemetry.sh
デーモンの健全性、インデックスの鮮度、使用状況の統計
古い剪定
bin/kb-st

ale-scan.py 、bin/kb-rehome.sh
死んだアンカーはリホーミングまたは除去される
バックエンド
ベンダー/グラフト/
Graft (Apache 2.0) — ローカルファーストのセマンティックメモリデーモン
建築
エージェント ハーネス (Pi / クロード コード / コーデックス / カーソル / ウィンドサーフィン)
│ フック + ツール
▼
ヘイムダルオーケストレーション ── 信頼検証 ── 自己修復同期
│ シン CLI コントラクト (挿入/取得/探索/取得/削除/統計)
▼
セマンティック メモリ バックエンド (Graft、ベンダー)
バックエンドはプラグ可能です。 CLI コントラクトを使用するすべてのストアが機能します。グラフトは
販売されているリファレンス。
ノード ≥ 22.5 (ジャーナルは組み込みのノード:sqlite を使用します)、 bash 、 python3
今日の macOS (launchd デーモン管理)。 Linux は手動デーモンで動作します
オプション、シンボル/グラフの深さ: ツリーシッターとその文法
気になる言語。インポート可能な Python 3 では次のことが実行されます。
python3 -m venv ~ /.heimdall/venv
~ /.heimdall/venv/bin/pip ツリーシッター ツリーシッター Python をインストール \
ツリーシッター-JavaScript ツリーシッター-タイプスクリプト ツリーシッター-ゴー ツリーシッター-Rust
heimdall Depth を実行して、マシンの最大解像度が何であるかを確認します。彼らなしでは
ファイルの深さではすべてがインデックスされます。
npm test # 41 テスト: CLI、アダプター、信頼判定、ガード、および
# 調整不変条件 — 40 個のインターリーブ ライターと 6 個の実数
# 1 つのファイルに対する子プロセス、冪等性、ABA/古い
コミット数、所有権の撤回、注文の独立性、
# 深度解像度、ドリフト検出+修復、ロック排他性
npx tsc --noEmit # タイプチェック
同時実行性テストがポイントです: シングルライターか冪等性か
プロパティが壊れることはありません、それらは赤信号になるテストです。
v0.2.0 — リコンサイラーを追加します: シングルライター、レベルトリガーコンバージェンス
コンテンツハッシュオラクル、正確なパスごとの所有権、および深さラダーを備えたループ
シンボルのインデックスを作成し、ラインごとにエッジを呼び出します。以前のデザインを置き換え、

ここで、フックはコマンドからの変更を推測し、そこから直接グラフを書き込みます。
複数のプロセスを一度に。
v0.1.0 には、パッケージ化された CLI、5 つのハーネス アダプタ、コンテンツ認識型信頼が同梱されています
評決、および新規インストール検証済みのクイックスタート。バックエンド (グラフト) は、
個別にインストールします。「クイックスタート」を参照してください。
マサチューセッツ工科大学独立したプロジェクト — Graft またはその作者とは提携していません。
Graft (Apache 2.0) — としてベンダー化されています。
デフォルトのセマンティックメモリバックエンド (vendor/graft/)。
グラフィファイ (MIT、© Safi Shamsi) —
ベンダー化されたリポジトリごとの AST+ セマンティック コード グラフ (vendor/graphify/)。
AI コーディング エージェント用の検証済みの自己修復ナレッジ レイヤー — 信頼判定 (STRONG/WEAK/STALE/REBUILT) によるランク付け検索、セッション自動同期、grep ガード。 Graft スタイルのセマンティック メモリ バックエンド上のオーケストレーション。
Readme MIT ライセンス アクティビティ スター
0 フォーク レポート リポジトリ リリース
© 2026 GitHub, Inc.
フッターナビゲーション
私の個人情報を共有しないでください

## Original Extract

Verified, self-healing knowledge layer for AI coding agents — ranked search with trust verdicts (STRONG/WEAK/STALE/REBUILT), session auto-sync, grep guard. Orchestration over Graft-style semantic memory backends. - ArihantDeva/heimdall

GitHub - ArihantDeva/heimdall: Verified, self-healing knowledge layer for AI coding agents — ranked search with trust verdicts (STRONG/WEAK/STALE/REBUILT), session auto-sync, grep guard. Orchestration over Graft-style semantic memory backends. · GitHub
Skip to content
Navigation Menu
Sign in Appearance settings Platform AI CODE CREATION GitHub Copilot Write better code with AI
GitHub Copilot app Direct agents from issue to merge
MCP Registry Integrate external tools
DEVELOPER WORKFLOWS Actions Automate any workflow
Codespaces Instant dev environments
Code Review Manage code changes
Code Quality Enforce quality at merge
APPLICATION SECURITY GitHub Advanced Security Find and fix vulnerabilities
Code security Secure your code as you build
Secret protection Stop leaks before they start
Solutions BY COMPANY SIZE Enterprises
EXPLORE BY TYPE Customer stories
SUPPORT & SERVICES Documentation
Open Source COMMUNITY GitHub Sponsors Fund open source developers
Enterprise ENTERPRISE SOLUTIONS Enterprise platform AI-powered developer platform
AVAILABLE ADD-ONS GitHub Advanced Security Enterprise-grade security features
Copilot for Business Enterprise-grade AI features
Premium Support Enterprise-grade 24/7 support
Search / Sign in Sign up Appearance settings
You signed in with another tab or window. Reload to refresh your session.
You signed out in another tab or window. Reload to refresh your session.
You switched accounts on another tab or window. Reload to refresh your session.
Dismiss alert
{{ message }}
ArihantDeva
/
heimdall
Public
Notifications
You must be signed in to change notification settings
Additional navigation options
Code
main Branches Tags Go to file Code Open more actions menu Latest commit
17 Commits 17 Commits Folders and files
bin bin config config docs docs extensions extensions launchd launchd tests tests types types vendor vendor .gitignore .gitignore LICENSE LICENSE README.md README.md package-lock.json package-lock.json package.json package.json tsconfig.json tsconfig.json View all files Repository files navigation
Your agent keeps rebuilding work you already did. Heimdall makes it stop.
Every AI coding session starts cold. Grep across your repos can't answer "did
I already solve this in another project?" — the answer lives in a different
directory, described in prose, under a path you've never opened. The result:
the same work rebuilt three times.
Heimdall is a self-healing, trust-verified knowledge layer for AI coding
agents. It watches what your agent does, keeps a semantic memory graph fresh
across every project you touch, and — the part nobody else builds — labels
every search hit with a trust verdict so your agent never acts on a dead
path or a hallucinated match.
The trust verdict (the differentiator)
Every search result is verified against reality before your agent sees it:
STRONG — path exists on disk, strong lexical coverage, and the file's
actual content answers the query (content-aware scoring)
WEAK — semantic match only; plausible but unverified
REBUILT — file moved; Heimdall found it and re-anchored automatically
STALE / REMOVED — dead path, logged and pruned so it stops ranking
An agent acting on a dead path is worse than no answer. Verdicts are what make
the graph trustworthy enough to act on.
Self-healing under concurrent agents
Forty agents can edit one file at once. The graph still converges to exactly
what is on disk, because Heimdall never tries to infer what changed .
Level-triggered, not event-driven. A hook, a watcher, or a script can
only say "look at this path". It is never believed about what happened.
The reconciler reads the file from disk and makes the graph match it. A
missed hint, a duplicated hint, or a flatly wrong hint costs one stat .
Single writer by construction. Every graph mutation goes through one
O_EXCL lock. Two writers cannot exist, so there is no interleaving to
race — the old delete-then-insert window where a file briefly vanished from
the graph is gone.
Exact ownership. Every node and edge belongs to exactly one path. A
commit deletes and re-inserts all of that path's rows in one transaction, so
a symbol you deleted cannot survive as a ghost.
Content hash is the oracle. Same bytes, same depth ⇒ same graph. That is
what makes reconciling twice identical to reconciling once, and it is why
concurrent edits are harmless rather than merely unlikely to collide.
Order independence. A cross-file edge to a symbol that is not indexed yet
parks as a pending edge and resolves from either side, so the final graph
does not depend on which file was reconciled first.
Audit as backstop. heimdall verify compares the journal against the
filesystem and exits non-zero on drift — it is the accuracy claim as a
command you can put in CI. --deep re-hashes and catches even a rewrite that
preserved size and mtime.
The honest guarantee is bounded-staleness convergence , not instantaneous
correctness: between an edit and the next reconcile pass, the graph is behind.
It is never wrong in a way that survives a pass.
heimdall daemon # the single writer: watch, reconcile, audit on a timer
heimdall reconcile # converge now (one-shot; takes the same lock)
heimdall reconcile --all # deep audit + repair everything
heimdall verify --deep # read-only drift report; exit 1 if any. CI-safe
Depth levels
Nodes are indexed at a depth. The default and the recommendation is maximum
— the graph knows not just that a file exists but which functions and classes
live in it, at which lines, and what calls what.
max resolves to whatever the machine can actually do. Symbol and graph depth
need tree-sitter; without it a path degrades to file depth rather than
disappearing, and the next audit upgrades it automatically once tree-sitter
appears. Asking for a depth above the machine's capability is honored as far as
possible and reported as clamped .
heimdall depth # what this machine can do, and why
heimdall depth src/server.ts # requested vs effective depth for one path
Extraction is tree-sitter AST parsing, not an LLM call: depth costs CPU, never
tokens.
npm i -g heimdall
# one-time: install the graft backend (local semantic-memory daemon)
# see https://github.com/tinygrad/graft — put the `graft` binary on PATH
# (e.g. ~/.local/bin/graft) and create ~/.graft/config.yaml:
# cp "$(npm root -g)/heimdall/config/heimdall.yaml.example" ~/.graft/config.yaml
# wire it into your harness (pi | claude-code | codex | cursor | windsurf | all)
heimdall init --harness claude-code
# verify the backend is healthy
heimdall doctor
# search across every project you've worked in
heimdall search " excel tracker portfolio optimization "
# record reusable work when your agent finishes something
heimdall insert --title " poker jam_opt optimizer " \
--body " ~/Repos/poker-bot/tools — heads-up jam/fold EV optimizer " \
--keywords poker,optimize
init , insert and the harness wiring work immediately; search and
doctor need the graft backend installed separately (build from source —
embedding model download included, so not instant). The vendored vendor/graft/
is source + attribution, not a prebuilt binary.
Harness
Command
Integration
Pi
heimdall init --harness pi
native extensions: kb_search / kb_insert / kb_sync tools, edit autosync, session orientation
Claude Code
heimdall init --harness claude-code
PostToolUse hook emits path hints + memory snippet
Codex CLI
heimdall init --harness codex
AGENTS.md search/insert instructions
Cursor
heimdall init --harness cursor
rules file with search-first workflow
Windsurf
heimdall init --harness windsurf
rules file with search-first workflow
See docs/adapters.md for what each adapter installs.
Component
File
Job
CLI
bin/heimdall.js
init / search / insert / doctor / daemon / reconcile / verify / depth / hint
Ranked search
bin/kb-search.sh
top-k ranked candidates + graph walk, --scope filter
Trust verification
bin/kb_search_verify.py
content-aware STRONG / WEAK / STALE / REBUILT verdicts
Reconciler
bin/lib/reconcile.mjs
level-triggered convergence: read disk, make the graph match
Journal
bin/lib/journal.mjs
authoritative index: ownership, hashes, generations, dedup queue
Single-writer lock
bin/lib/lock.mjs
O_EXCL lock every graph mutation passes through
Depth ladder
bin/lib/depth.mjs , bin/lib/extract.mjs
path / file / symbol / graph ; tree-sitter extraction
Daemon
bin/heimdall-reconciler.mjs
watch + drain + periodic audit, holding the lock
Edit-log replay
bin/sync-edits.sh
one-shot bootstrap: session edit logs → hints → reconcile
Hint emitter
extensions/kb-autosync.ts
harness hook; appends "look at this path", never writes the graph
Search guard
extensions/kb-search-guard.ts
warns the agent after 3 grep-style searches without consulting memory
Session orientation
extensions/kb-orient.ts
injects relevant prior work into the first prompt of a session
Health & telemetry
bin/kb-health.sh , bin/telemetry.sh
daemon health, index freshness, usage stats
Stale pruning
bin/kb-stale-scan.py , bin/kb-rehome.sh
dead anchors get rehomed or removed
Backend
vendor/graft/
Graft (Apache 2.0) — local-first semantic memory daemon
Architecture
agent harness (Pi / Claude Code / Codex / Cursor / Windsurf)
│ hooks + tools
▼
Heimdall orchestration ── trust verification ── self-healing sync
│ thin CLI contract (insert/retrieve/explore/get/delete/stats)
▼
semantic memory backend (Graft, vendored)
Backends are pluggable. Any store speaking the CLI contract works; Graft is
the vendored reference.
Node ≥ 22.5 (the journal uses the built-in node:sqlite ), bash , python3
macOS today (launchd daemon management); Linux works with a manual daemon
Optional, for symbol / graph depth: tree-sitter and the grammars for the
languages you care about. Any Python 3 with them importable will do:
python3 -m venv ~ /.heimdall/venv
~ /.heimdall/venv/bin/pip install tree-sitter tree-sitter-python \
tree-sitter-javascript tree-sitter-typescript tree-sitter-go tree-sitter-rust
Run heimdall depth to see what the machine resolved max to. Without them
everything still indexes, at file depth.
npm test # 41 tests: CLI, adapters, trust verdicts, guard, plus the
# reconciler invariants — 40 interleaved writers and 6 real
# child processes against one file, idempotency, ABA/stale
# commits, ownership retraction, order independence,
# depth resolution, drift detect+repair, lock exclusivity
npx tsc --noEmit # typecheck
The concurrency tests are the point: if the single-writer or idempotency
properties ever break, those are the tests that go red.
v0.2.0 — adds the reconciler: a single-writer, level-triggered convergence
loop with a content-hash oracle, exact per-path ownership, and a depth ladder
that indexes symbols and call edges by line. Replaces the previous design,
where hooks inferred changes from commands and wrote the graph directly from
several processes at once.
v0.1.0 shipped the packaged CLI, five harness adapters, content-aware trust
verdicts, and a fresh-install-verified quickstart. Backend (Graft) is a
separate install — see Quickstart.
MIT. Independent project — not affiliated with Graft or its authors.
Graft (Apache 2.0) — vendored as the
default semantic-memory backend ( vendor/graft/ ).
Graphify (MIT, © Safi Shamsi) —
vendored per-repo AST+semantic code graph ( vendor/graphify/ ).
Verified, self-healing knowledge layer for AI coding agents — ranked search with trust verdicts (STRONG/WEAK/STALE/REBUILT), session auto-sync, grep guard. Orchestration over Graft-style semantic memory backends.
Readme MIT license Activity Stars
0 forks Report repository Releases
© 2026 GitHub, Inc.
Footer navigation
Do not share my personal information
