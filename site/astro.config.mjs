import { defineConfig } from 'astro/config';

export default defineConfig({
  // Update this to your actual Vercel URL after deployment
  site: 'https://hn-digest-psi.vercel.app',
  output: 'static',
});
